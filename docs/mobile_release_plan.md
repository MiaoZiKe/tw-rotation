# 手機 App 上架計畫：三條路的比較、費用、送審會卡在哪，以及今天已經做掉的第一步

> 撰寫：`mobile-release` Agent｜日期：2026-09-24（台北時間）｜對象：Andy
>
> Andy 的原話：「**手機如何上架 ios 和 安卓**」「**並且手機 app 先著手開始執行**」。
> 所以這份文件**不只是計畫**：PWA 的第一步已經做掉了，做了什麼寫在第 ⑨ 節，
> 要合進 `site/index.html` 的那幾行寫在第 ⑩ 節。

---

## 怎麼讀這份文件

每一條結論都標兩件事（沿用 `docs/product_strategy.md` 的體例）：

- **〔事實〕**＝ 我查到的或在 repo 裡讀到的，附來源。
- **〔判斷〕**＝ 我的推論，可以被推翻。
- **信心度**：**高**（兩個以上獨立來源，或直接讀到原始條文／原始碼）／
  **中**（單一可靠來源）／**低**（只有二手摘要）／**未確認**（查不到，**不准當成可以**）。

⚠ **這個容器只放行 `WebSearch`，而且只拿得到搜尋摘要，點不進原文。**
App Store、Google Play、Apple 開發者文件的網址我**一個都打不開**
（`WebFetch` 與 `curl` 回 `EGRESS_BLOCKED` / 403）。
所以下面凡是引用條文的地方，**我引的是搜尋摘要轉述的條文，不是我親眼讀到的原文**。
⚠ `WebSearch` 的摘要會自己「補」出原文沒有的東西（這個專案踩過兩次，見 `CLAUDE.md`），
所以金額與條號我都盡量找第二個獨立來源；找不到的一律標低信心。

---

# 摘要（先看這 18 行）

1. **今天就能用的東西已經做好了**：`site/manifest.webmanifest` 換新、新增 `site/sw.js`、
   新增 `site/icons/`（轉針圖示，192／512／maskable）。
   合上第 ⑩ 節那幾行之後，**手機瀏覽器打開網站 → 分享 → 加入主畫面**，
   就會變成一個全螢幕、有自己圖示、離線也打得開的 App。**不用審查、不用花錢、今天就有。**
2. **★ 最重要的一句話：`site/data/**` 一個位元組都沒有進快取。**
   那是每天盤後重算的資料，快取住等於他打開 App 看到昨天的數字 ——
   **那比沒有 PWA 還糟**。這條我用 Playwright 真的翻過 Cache Storage 驗證（第 ⑨ 節有數字）。
3. **三條路我的建議是「分階段走 1 → 2」，不是三選一**：
   **先 PWA（已完成）→ 再 Google Play（TWA 包裝，幾乎零額外開發）→ iOS 最後做（Capacitor ＋ 原生層）**。
   原生重寫**不要做**，理由在 2-4。
4. **兩個商店的難度差距非常大，這是這份文件最重要的一個發現。**
   **Google 官方支援直接把 PWA 包成 App 上架**（Trusted Web Activity ＋ Bubblewrap，信心：高）——
   我們幾乎不用多寫程式。
   **Apple 明文禁止「只是把網站包起來」**（設計指南 4.2 最低功能性，信心：高）——
   要上 iOS，一定得加一層真的原生東西，否則會被退件。
5. **〔事實〕費用很便宜，難的不是錢**：Apple 開發者 **US$99／年**（約 NT$3,200），
   Google Play **US$25 一次付清、不續費**（信心：高）。第一年合計約 NT$4,000。
6. **〔事實・新帳號的隱藏門檻〕Google Play 個人開發者帳號**（2023-11-13 之後開的）
   **必須先跑封閉測試：至少 12 個測試者、連續 14 天，才能申請正式發佈**，
   申請審核再約 7 天（信心：高）。
   **這是整條路上最長的一段等待，而且花錢也買不到。Andy 要先湊得出 12 個人。**
7. **★★ 最可能讓我們上不了 iOS 的，不是 4.2，是 3.2.1(viii)。**
   〔事實〕Apple：「用於金融交易、投資或資金管理的 App，**應由提供該服務的金融機構提交**，
   並且在上架地區持有必要的執照與許可」（信心：高）。
   **而且舊版允許的「用金融機構的公開 API」這個替代路徑已經被拿掉了**（信心：中）。
   我們不是金融機構、沒有投顧執照。
   **〔判斷〕我們的定位必須寫成「行情與產業資訊工具」，不能寫成「投資建議／選股推薦」**——
   這同時也是台灣《證券投資信託及顧問法》那條紅線的同一件事（見 `docs/product_strategy.md` 摘要第 1 條）。
8. **〔事實〕Google Play 那邊也要填「財務功能聲明」**（Financial features declaration，2023-08-31 起強制），
   而且要遵守目標國家的在地法規與揭露要求（信心：高）。
9. **★ 收訂閱費會連鎖引爆三件事**，不是只有抽成：
   ① 蘋果抽 **30%**（小型企業方案 **15%**，年收入 100 萬美元以下可申請，信心：高）；
   ② **GitHub Pages 使用條款禁止拿它經營線上生意**（信心：高，見 `product_strategy.md`），
   所以收錢就得搬家，而 App 包的就是這個網站；
   ③ **台灣投顧法的「報酬」要件會成立**。
   **〔判斷〕結論：先免費上架，訂閱制留到法遵與搬家做完再談。**
10. **399／799 的實收試算**（第 ⑤ 節有完整表）：
    399 走 App 內購，蘋果標準抽成實收 **NT$279**，小型企業方案 **NT$339**；
    Google Play 訂閱 15%，實收 **NT$339**。**走網頁自己收款實收約 NT$387，但那條路現在走不了**（見第 9 條）。
11. **〔事實〕Apple 審查很快**：平均 24～48 小時，2026 年多數追蹤資料落在「排隊 5～14 小時 ＋ 審查約 1 小時」（信心：中）。
    **慢的是退件重送的輪次，不是單次審查。**
12. **時程**：**里程碑 A（第一版能上架）＝ 實作 5～7 個工作天，日曆時間 5～7 週**，
    瓶頸是 Google 的 12 人 × 14 天與 Apple 的退件輪次。
    **里程碑 B（完整體驗：帳號、推播、跨裝置同步、訂閱）＝ 再 6～10 週，而且要先有後端。**
13. **★ 一個非做不可的前提：Andy 不跑本機指令。**
    所以**所有打包都必須在 GitHub Actions 裡跑**：
    Android（Bubblewrap）跑 Linux runner，iOS（Capacitor）要 **macOS runner**。
    兩個都做得到，簽章金鑰放 GitHub Secrets（**不進 repo**）。
    這一條不解決，後面全部是空談。
14. **〔判斷・要先確認的坑〕TWA 需要 Digital Asset Links**：
    要在**網域根目錄**放 `/.well-known/assetlinks.json` 來證明 App 與網站是同一個人的。
    我們是 `miaozike.github.io/tw-rotation/`（專案頁），根目錄屬於 `miaozike.github.io`，
    **必須另外開一個名為 `miaozike.github.io` 的 repo 才放得進去**（信心：中，這是我從機制推的）。
    **〔判斷〕與其繞，不如直接買一個自己的網域**（約 NT$500～1,000／年）——
    它同時解決品牌、Digital Asset Links、以及未來搬家的問題。
15. **〔判斷〕命名還沒定案，所以這批的 App 名稱先沿用「台股資金輪動儀表板」。**
    `docs/product_strategy.md` 列了 8 個候選、最推薦〈剖面〉，Andy 還沒挑。
    **商店上架之後改名字是可以的，但 App Store 的 Bundle ID 與 Google 的 package name 一旦上架就永遠不能改** ——
    **所以命名要在第一次送審之前決定**，這是硬期限。
16. **〔事實〕iOS 的 Web Push 只在「已加入主畫面」的 PWA 裡才有**（iOS 16.4+，信心：高），
    而且**可靠度有已知問題**（重開機後 SW 事件不一定觸發、使用者會莫名被取消訂閱，信心：中）。
    **〔判斷〕推播這件事，iOS 上要做得可靠就只能走原生，這是 PWA 真正的天花板。**
17. **〔事實〕iOS 對 PWA 的儲存很兇**：不常開的 PWA 快取與 IndexedDB 可能在 **7 天**內被清掉（信心：中高）。
    **對我們反而幾乎無害** —— 我們刻意不快取資料，被清掉只是下次多抓一次殼。
18. **我沒有驗到的部分寫在第 ⑫ 節，請直接看那一節**，不要從上面推。

---

# ① 起點：我們現在有什麼、缺什麼

**〔事實，信心：高，直接讀 repo〕**

| 項目 | 現況 |
|---|---|
| 產品型態 | **響應式靜態網站**，GitHub Pages 託管，<https://miaozike.github.io/tw-rotation/> |
| 前端 | `site/index.html` ＋ 約 40 支自家 JS（約 2.7MB）＋ `vendor/` 第三方圖表庫（約 1.9MB） |
| 資料 | `site/data/**` 共 31 個 JSON ＋ 個股／歷史目錄，**每天盤後由 GitHub Actions 重算、重新部署** |
| 後端 | **沒有**。唯一的伺服器端是 `workers/quote-proxy/`（Cloudflare Worker，只做即時報價的 CORS 代理） |
| 帳號 | **沒有**。全站設定存在瀏覽器 `localStorage`（22 個鍵） |
| 金流 | **沒有** |
| 推播 | **沒有** |
| 維護人力 | **一個人** |

**這個起點決定了三件事**（〔判斷〕）：

1. **路徑一定是「網站 → 可安裝 → 上架」**，不是從零寫 App。這一點三條路都同意。
2. **「跨裝置同步」「推播」「訂閱」三個商店最看重的 App 特徵，我們一個都沒有** ——
   而缺這三個，正好就是 Apple 4.2 退件的典型畫像。
3. **沒有後端 ＝ 里程碑 B 的每一件事都要先補後端**，那是這條路上最大的一筆工。

---

# ② 三條路的比較

## 2-0 一眼看完

| | **路線 1：PWA** | **路線 2：WebView 包裝** | **路線 3：原生重寫** |
|---|---|---|---|
| 進得了商店嗎 | ❌ **不在商店裡** | ✅ 兩家都可以 | ✅ 兩家都可以 |
| 要開發者帳號嗎 | ❌ 不用 | ✅ 要（Apple US$99/年、Google US$25 一次） |✅ 同左 |
| 要審查嗎 | ❌ 不用 | ✅ 要（Apple 是主要風險） | ✅ 要 |
| 額外開發量 | **已完成（今天）** | Android 約 0.5 天；**iOS 約 3～5 天**（要加原生層） | **3～6 個月** |
| 原生推播 | iOS 勉強、Android 可以 | ✅ 兩邊都可靠 | ✅ |
| 每次改東西要重新送審嗎 | ❌ 不用，改網站就好 | ❌ **內容改動不用**（殼不動就不用送審） | ✅ 每次都要 |
| 一個人維護得動嗎 | ✅ | ✅ | ❌ **維護三份程式碼** |
| **我的建議** | **✅ 先做（已做）** | **✅ 接著做，Android 先、iOS 後** | **❌ 不要做** |

## 2-1 路線 1：PWA（加到主畫面）

**做法**：`manifest.webmanifest` ＋ Service Worker ＋ 圖示，使用者用瀏覽器的「加入主畫面」安裝。

**優點**
- **成本 0、審查 0、等待 0。** 今天做完今天就有。
- 改版即時生效，不用送審，這對「每天換資料」的產品特別重要。
- **它是另外兩條路的共同底座**：Capacitor 包的是這個網站，TWA 包的也是這個網站。
  所以這一步永遠不會白做。

**缺點（要誠實講）**
- **〔事實〕不在商店裡。** 沒有 App Store／Google Play 的搜尋曝光，也沒有商店評價這個信任背書。
  對「沒有品牌、沒有流量」的我們，**這正是最痛的一點**（`product_strategy.md` 摘要第 6 條）。
- **〔事實〕安裝路徑在 iOS 上又長又難教**：Safari → 分享 → 加入主畫面。
  **而且 iOS 只有 Safari 能裝**（其他瀏覽器裝不了，信心：中高）。轉換率必然很低。
- **〔事實〕iOS 的 Web Push 只在「已加入主畫面」的 PWA 裡有效**（iOS 16.4 起，信心：高），
  且**可靠度有已知問題**：SW 事件在裝置重開後不一定觸發、使用者會莫名被取消訂閱、
  點通知有時開錯網址（信心：中）。
- **〔事實〕iOS 儲存會被回收**：不常開的 PWA，快取／IndexedDB 可能在約 7 天內被清掉（信心：中高）；
  Safari 17 之後有 Persistent Storage API 可以請求豁免，**但那要先拿到通知權限**（信心：中）。
  → **對我們影響很小**，因為我們本來就不快取資料。
- **〔事實〕iOS 對 PWA 的快取上限約 50MB**（信心：中）。我們的殼約 4MB，**不會頂到**。

**〔判斷〕結論**：PWA 解決「手機上用起來像 App」，**解決不了「被找到」與「可靠的推播」**。

## 2-2 路線 2：WebView 包裝

這條路其實**是兩條不一樣的路**，兩個商店的機制完全不同，不能混為一談。

### 2-2-A Android：Trusted Web Activity（TWA）＋ Bubblewrap —— **這是官方支援的路**

**〔事實，信心：高〕** Google 官方提供 **Bubblewrap** 這個命令列工具，
把 PWA 包成 Android App Bundle（AAB）上架 Google Play；它是 Google Chrome Labs 的專案，
會處理 JDK／Android SDK，並支援推播通知委派與定位。
需要 **Digital Asset Links** 驗證網域所有權，並建議 Lighthouse 分數 ≥ 80（信心：中）。

**〔判斷〕為什麼這條路對我們幾乎是白送的**：
TWA 不是 WebView，是「把 Chrome 全螢幕嵌進 App」，所以**網站長什麼樣，App 就長什麼樣**，
不用重排版、不用改一行前端。額外工作只有：打包、簽章、assetlinks、商店素材。

**要注意的**
- **〔判斷，信心：中〕`assetlinks.json` 的位置問題**（摘要第 14 條）：
  必須在 `https://miaozike.github.io/.well-known/assetlinks.json`，
  也就是 **user site 的根目錄**，不是我們的專案目錄。要另開一個 `miaozike.github.io` repo，
  或者買自己的網域。**這一條一定要先驗證，卡住的話整條路會停在這裡。**
- **〔事實，信心：高〕個人帳號的 12 人 × 14 天封閉測試**（摘要第 6 條）。2026 年起 Google 還會檢查
  「這些測試者是不是真的有在用」（信心：中）。
- **〔事實，信心：高〕目標 API 等級**：2026-08-31 起新上架與更新要 **Android 16（API 36）**。
  Bubblewrap 產出的專案要記得跟上，否則會被擋。

### 2-2-B iOS：Capacitor（或同類）—— **這條的關卡是 Apple 4.2**

**〔事實，信心：高〕設計指南 4.2 最低功能性**的原文轉述：
> 「你的 App 應該包含**超越『重新包裝的網站』**的功能、內容與介面。
> 如果你的 App 不是特別有用、獨特或『像 App』，它就不屬於 App Store。」

實務上被退件的畫像（信心：中高，多個來源一致）：
> 「只用 WebView 載入你的網站、什麼都沒加：**沒有原生導覽、沒有離線行為、沒有推播、
> 沒有裝置整合**」→ 被判定為「與用手機瀏覽器逛網站沒有顯著差異」。

**〔事實，信心：中〕怎麼避開**：多個來源一致指出，
「加了原生推播、原生導覽、離線處理、原生購買的 Capacitor App **例行地會過**」。
Apple 退的不是 WebView 本身，退的是**沒有加值的殼**。

**〔判斷〕我們具體要加的原生層（這是 iOS 那 3～5 天工作的內容）**：

| # | 要加什麼 | 為什麼這一條擋得住 4.2 | 工作量 |
|---|---|---|---|
| 1 | **原生底部分頁列**（總覽／資金流向／產業／個股／設定），點了用原生轉場切網頁路由 | 直接對應「沒有原生導覽」這條退件理由；也是審查員第一眼看到的東西 | 1 天 |
| 2 | **原生推播**（APNs）：盤後資料更新完、自選族群進入「漲潮區」時推一則 | 這是 PWA 做不好、原生做得好的那一件事，**最有說服力的加值** | 1～1.5 天（**需要後端派送**） |
| 3 | **原生離線畫面**：斷線時顯示「上次更新時間 ＋ 最後一次看到的結論」，**不是白畫面** | 「網路一斷就白畫面」被明確點名為退件特徵 | 0.5 天 |
| 4 | **原生分享**：把某張剖析圖／某個族群結論分享出去（Share Sheet） | 裝置整合，成本低效益高 | 0.5 天 |
| 5 | **Face ID／Touch ID 鎖住 App**（自選股清單） | 裝置整合；但**要先有自選股功能**，所以排在里程碑 B | 0.5 天 |
| 6 | **Home Screen Widget**（顯示今天四問的結論一行） | 〔判斷〕**最強的一張牌** —— Widget 是網頁在物理上做不到的東西 | 1.5～2 天 |

**〔判斷〕我會在第一次送審就放 1、2、3、4，並在「給審查員的備註」欄裡寫清楚
「本 App 的原生功能有 A/B/C/D，請以這幾個畫面評估」。**
審查員看不到的加值等於不存在，這一欄不要留白。

⚠ **但 4.2 不是 iOS 最大的風險，3.2.1(viii) 才是**（摘要第 7 條、第 ④ 節）。

## 2-3 路線 3：原生重寫（Swift ＋ Kotlin）

**〔判斷〕不要做。** 三個理由，每一個單獨就足以否決：

1. **我們最值錢的東西沒辦法搬過去。** 27 張 2D 剖析圖、19 個 3D 場景、18 張題材圖，
   全部是 ECharts／three.js 的網頁程式碼（`site/dg/`、`site/three3d.js`，約 1.3MB 原始碼）。
   原生重寫等於**這些全部重畫**，而它們正是我們唯一難以被複製的資產。
2. **一個人要維護三份**（網站 ＋ iOS ＋ Android）。
   現況是每天盤後自動重算資料、常常一天改好幾次前端；
   原生的每一次改動都要重新送審，**產品節奏會直接死掉**。
3. **投入完全不成比例**：3～6 個月的工，換到的只有「原生的滑順感」——
   而我們真正缺的是使用者與法遵，不是幀率。

**唯一會讓我改口的情況**：產品變成以**即時交易**為核心（毫秒級報價、下單），
那時 WebView 的延遲與背景執行限制會變成硬傷。**我們離那裡很遠。**

## 2-4 ★ 我的建議

**分階段走，不要三選一：**

```
第 0 階段（已完成，今天）  PWA 底座：manifest + Service Worker + 圖示
        ↓ 合第 ⑩ 節那幾行，部署完就有
第 1 階段（1～2 週工，5～6 週等）  Google Play（TWA）
        ↓ 幾乎零額外開發；卡在 12 人 × 14 天
第 2 階段（3～5 天工 + 送審輪次）  iOS App Store（Capacitor + 原生層）
        ↓ 卡在 4.2 與 3.2.1(viii)
第 3 階段（里程碑 B）  後端 + 帳號 + 推播派送 + 訂閱
```

**為什麼 Android 先、iOS 後**（〔判斷〕）：
1. Android 那條幾乎不用寫程式，**先拿到「我們有 App」這件事**；
2. Google 的 14 天等待可以**跟 iOS 的開發平行跑**，不浪費日曆時間；
3. iOS 的原生層（推播）**需要後端**，而後端還不存在 —— 先做 Android 可以把這個依賴往後推。

---

# ③ 帳號與費用（〔事實〕，匯率以 US$1 ≈ NT$32 估）

| 項目 | 金額 | 性質 | 信心 |
|---|---|---|---|
| **Apple Developer Program** | **US$99／年**（約 NT$3,200／年） | **每年續費**，不繳就下架 | 高（兩個以上來源一致） |
| **Google Play 開發者帳號** | **US$25 一次付清**（約 NT$800） | **只付一次**，沒有年費 | 高 |
| 第一年合計 | **US$124**（約 NT$4,000） | | 高 |
| 自有網域（建議） | 約 NT$500～1,000／年 | 〔判斷〕Digital Asset Links ＋ 品牌 ＋ 未來搬家都要 | — |
| 打包用的 CI | **US$0** | GitHub Actions 對 **public repo** 的 Linux 與 macOS runner 免費（信心：中，額度政策會變） | 中 |
| 推播派送 | **US$0** | Firebase Cloud Messaging 免費；APNs 含在開發者帳號裡 | 中 |
| 後端（里程碑 B 才需要） | 約 NT$150～500／月 | Cloudflare Workers 付費方案或同級 | 中 |

**〔判斷〕錢不是問題，NT$4,000 是一頓飯。真正的成本是時間與法遵。**

**★ 帳號類型的選擇（信心：中，但影響很大）**
- **Apple**：個人（Individual）用本人姓名當開發者名稱；公司（Organization）要 D-U-N-S 編號、審核更久。
  〔判斷〕**先開個人**。但注意：**個人帳號的開發者名稱會顯示成 Andy 的本名**，
  這對「金融資訊 App」是不是想要的曝光，Andy 自己決定。
- **Google**：**個人帳號就是被 12 人 × 14 天綁住的那一種**；
  組織帳號（需要公司登記與 D-U-N-S）**可以直接上正式版**。
  〔判斷〕如果 Andy 名下剛好有公司，開組織帳號可以**直接省掉 3～5 週**，這是這份文件裡最划算的一個選項。

---

# ④ ★ 送審會卡在哪（這一節是整份文件的重點）

我把風險由高到低排。**前兩條是會讓整件事做不成的，不是「要注意一下」的等級。**

## 4-1 【最高】Apple 3.2.1(viii)：金融類 App 應由金融機構提交

**〔事實，信心：高〕** 條文轉述：
> 「用於**金融交易、投資或資金管理**的 App，應由提供該服務的**金融機構**提交，
> 並且在你上架的地區**持有必要的執照與許可**。」

**〔事實，信心：中〕** 舊版條文曾允許一個替代路徑（「或使用該機構的公開 API 且遵守其條款」），
**這個替代路徑在較新的版本裡被刪掉了**。

**我們踩不踩得到？**
- 我們**不執行交易、不代客操作、不碰錢**，所以嚴格說不是「金融交易或資金管理」。
- **但我們有「今日候選」「技術評分」「進場時機」** ——
  〔判斷〕**這幾個字就是踩線的地方**。審查員只看畫面，不看我們的內部定義。

**〔判斷〕緩解（這幾條要當成產品需求做，不是文案潤飾）**
1. **商店文案與 App 內文案，全部改成「資訊工具」的語氣**：
   「呈現公開資料計算出的指標」，**不要出現「建議」「推薦」「該買」「進場點」**。
2. **「今日候選」改名**（`product_strategy.md` ⑦-1 已經提過同一件事，Andy 還沒答）——
   例如「今日符合條件的標的」，並在卡片上固定一行「這是條件篩選結果，不是推薦」。
3. **首次啟動的免責聲明頁**（必須按「我知道了」才進得去），
   以及**每一頁頁尾固定的免責聲明版位**。
   ⚠ 目前**全站 grep 不到免責聲明**（`product_strategy.md` ⑧-10），**這是上架前的必做項**。
4. **在「給審查員的備註」直接寫明**：本 App 不執行任何交易、不持有資金、不提供個人化投資建議，
   資料全部來自證交所／櫃買／集保的公開資料集。

⚠ **這條同時是台灣法規的問題**：《證券投資信託及顧問法》§4＋§107，
「以收取報酬、對不特定人經常性提供證券分析意見或推介建議」未經許可即屬非法經營證顧業務
（`product_strategy.md` 摘要第 1 條，信心：高）。
**免費時缺「報酬」要件，風險低；一旦收訂閱費，性質就變了。**
**〔判斷〕所以第一版一定要免費上架。**

## 4-2 【高】Apple 4.2：最低功能性（「重新包裝的網站」）

條文與避開的做法寫在 2-2-B。**〔判斷〕機率評估：不加原生層 ≈ 90% 會被退；加了 1～4 項之後 ≈ 25%。**
（這個數字是〔判斷〕，來自多個二手來源的一致敘述，**不是統計數據**。）

**⚠ 一個常被忽略的細節**：4.2 也退「**只是一本目錄／一個網站書籤**」型的 App。
我們的畫面資訊量很大，這一點反而是優勢 —— **要在截圖裡把資訊密度秀出來**。

## 4-3 【高】Google Play：財務功能聲明 ＋ 在地法規

**〔事實，信心：高〕** 任何含財務功能的 App 都必須在 Play Console 填寫
**財務功能聲明表（Financial features declaration）**，2023-08-31 起強制。
政策定義的「金融產品與服務」包含**與資金投資管理相關者，含個人化建議**。
若 App 含有或推廣金融產品與服務，**必須遵守目標國家的在地法規**，例如依當地法律做必要揭露。

**〔判斷〕對我們的意思**：
- 要勾「投資／理財資訊」而**不是**「證券交易」「投資顧問」。
- 台灣的在地揭露要求＝ 免責聲明、資料來源與更新時間、**明確寫出「非投資建議」**。
- ⚠ **未確認**：台灣是否要求上傳投顧執照才能過這個聲明。我查不到 Google 針對台灣的清單。
  **這一條 Andy 或我要在真的填表時才會知道**，屬於「做了才知道」的風險。

## 4-4 【中】Google Play：個人帳號的 12 人 × 14 天

**〔事實，信心：高〕** 2023-11-13 之後建立的**個人**開發者帳號，
必須先完成**至少 12 位測試者、連續 14 天**的封閉測試，才能申請正式發佈；
「opted in」指測試者**真的接受邀請並用對應的 Google 帳號安裝**，只邀請沒安裝不算。
申請正式發佈的審核通常 **7 天以內**。2026 年起 Google 還會檢查測試者是否真的有使用。

**〔判斷〕緩解**：
- **最好的解**：如果有公司，開**組織帳號**，這條直接不適用。
- 次好的解：**現在就開始湊 12 個人**（朋友、同事、社群）。
  這件事不用等程式做完，**可以今天就開始，是整條路上最值得提前做的一件事**。
- ⚠ 網路上有賣「12 個測試者」的服務。**〔判斷〕不要用** ——
  Google 現在會查真實使用行為，被判定造假會影響帳號。

## 4-5 【中】GitHub Pages 的使用條款

**〔事實，信心：高，來自 `product_strategy.md`〕** GitHub Pages 使用條款明文禁止拿它當免費主機
經營線上生意、電商或商業 SaaS；但**明確允許捐款按鈕與群眾募資連結**。

**〔判斷〕App 包的就是這個網站，所以網站的條款就是 App 的條款。**
免費 App ＋ 贊助連結 → 沒問題。收訂閱費 → **必須先搬家**（Cloudflare Pages／Netlify／自架）。

## 4-6 【中】資料授權會跟著 App 一起被檢視

**〔事實，信心：中高，來自 `product_strategy.md`〕** 證交所／櫃買／集保 OpenAPI ＝ 政府資料開放授權
（允許商業利用）；**Yahoo 明文禁止商業用途**，而我們用它抓分 K。

**〔判斷〕免費 App 大概率不會被問；一旦收費，Yahoo 那條必須先拔掉。**
⚠ `mis.twse.com.tw` 即時報價與 FinMind 的商用條款仍是**未確認**。

## 4-7 【低】技術性的必過項

| 項目 | 要求 | 信心 |
|---|---|---|
| Android 目標 API 等級 | 2026-08-31 起，新上架與更新需 **Android 16（API 36）**；既有 App 需 ≥ Android 15（API 35）。可申請延期到 2026-11-01 | 高 |
| Google Play 資料安全表單 | 必填，且**內容要與隱私權政策一致**（不一致會被退） | 高 |
| Apple 隱私權「營養標示」 | 必填，所有 App 都要 | 高 |
| 隱私權政策網址 | 兩家都要求一個**公開可讀的網址**。〔判斷〕我們可以直接放一頁在網站上 | 高 |

---

# ⑤ 訂閱與內購：399／799 的實收試算

## 5-1 規則（〔事實〕）

| 規則 | 內容 | 信心 |
|---|---|---|
| Apple 標準抽成 | **30%** | 高 |
| Apple 小型企業方案 | **15%**，條件是前一年淨收入 < US$1,000,000（新開發者直接符合），**要主動申請** | 高 |
| Apple 訂閱滿一年 | 同一位訂閱者連續訂滿 12 個月後，**第二年起降到 15%** | 中 |
| Google Play 訂閱 | **15%**（訂閱自第一天起就是 15%） | 中 |
| 美國以外能不能導到網頁付款 | **〔判斷〕現階段：不行。** 美國因 Epic 案的禁制令，2025-04 起 Apple 在**美國商店**對外部連結購買收 **0%**；但那是**美國專屬**，而且案子還在最高法院（Apple 2026-09-14 遞出主要訴狀）。**台灣商店沒有這個豁免** | 中 |

⚠ **我第一次查的時候，搜尋摘要給了「美國小型企業 12%／其他 27%」這個數字。
那是禁制令生效前的舊安排，跟同一批結果裡的「2025-04 起零抽成」互相矛盾。**
我用第二組關鍵字重查才發現。**這一格的數字請當成「方向對、細節要在真的要做時重查」。**

## 5-2 實收試算（NT$，單月，未計所得稅）

| 收款方式 | 月費 399 實收 | 月費 799 實收 | 備註 |
|---|---|---|---|
| **App 內購・Apple 標準 30%** | **279** | **559** | 沒申請小型企業方案就是這個 |
| **App 內購・Apple 小型企業 15%** | **339** | **679** | 一定要申請 |
| **Google Play 訂閱 15%** | **339** | **679** | |
| **網頁自己收款**（綠界／藍新，手續費約 3%） | **387** | **775** | ⚠ **但這條路現在走不了**：GitHub Pages 條款 ＋ 投顧法（見 4-5、4-1） |

**〔判斷〕三個結論**
1. **上架前一定要申請 Apple 小型企業方案**，399 的方案差 **NT$60／人／月**，
   100 個訂閱戶就是一年 **NT$72,000**。這是最便宜的一筆「錢」。
2. **抽成不是最大的問題，法遵才是。** 15% 換到的是商店曝光與信任背書，
   以我們 0 品牌 0 流量的起點，**這個交換是划算的**。
3. **〔判斷・我可能跟 Andy 不同意〕399／799 的價格帶，比 `product_strategy.md` 建議的 149～249 高一截，
   也高過對標 Tide 的 199／1,990。**
   沒有品牌背書時定在 399 以上，我認為撐不住（理由在 `product_strategy.md` 摘要第 9 條）。
   這一條請 Andy 直接裁決，我不再問第二次。

---

# ⑥ 送審素材清單（兩家分開列）

## 6-1 Apple（〔事實〕，信心：高）

| 素材 | 規格 |
|---|---|
| **App 圖示** | **1024×1024 PNG**，sRGB 或 Display P3，**不可有透明、不可自己畫圓角** |
| **iPhone 截圖** | **6.9 吋**：直式 **1290×2796**（或 1320×2868 / 1260×2736），至少 1 張、最多 10 張 |
| **iPad 截圖**（若支援 iPad） | **13 吋**：直式 **2064×2752** 或 **2048×2732** |
| **App 名稱** | 最長 30 字元 |
| **副標題** | 最長 30 字元 |
| **關鍵字** | 最長 100 字元，逗號分隔 |
| **說明** | 最長 4000 字元 |
| **隱私權政策網址** | 必填，公開可讀 |
| **隱私權營養標示** | 必填：蒐集哪些資料、用途、是否連結到使用者、是否用於追蹤 |
| **年齡分級問卷** | 必填。〔判斷〕金融資訊類通常會落在 17+（含「頻繁／強烈的模擬賭博」選否） |
| **給審查員的備註 ＋ 測試帳號** | 我們沒有帳號，**但這一欄一定要寫原生功能清單**（見 2-2-B） |

**〔判斷〕我們的隱私權標示應該可以全部勾「不蒐集」** ——
我們沒有帳號、沒有分析工具、資料只存在使用者自己的瀏覽器 `localStorage`。
⚠ **但如果之後掛了 GoatCounter／Cloudflare Web Analytics（`product_strategy.md` ⑧-9 建議的），
這一欄就要改成「蒐集使用資料、未連結到使用者」** —— **標示跟實際不符是會被下架的**。

## 6-2 Google Play（〔事實〕，信心：中高）

| 素材 | 規格 |
|---|---|
| **App 圖示** | **512×512 PNG**（32 位元、含 alpha） |
| **主題圖片（Feature graphic）** | **1024×500 PNG／JPG**，**必填** |
| **手機截圖** | 至少 **2 張**（多數指引建議 4～8 張），16:9 或 9:16，短邊 ≥ 320px |
| **簡短說明** | 最長 80 字元 |
| **完整說明** | 最長 4000 字元 |
| **隱私權政策網址** | 必填 |
| **資料安全表單** | 必填，**要與隱私權政策一致** |
| **財務功能聲明** | **必填（我們一定要填）**，見 4-3 |
| **內容分級問卷** | 必填 |
| **AAB（Android App Bundle）** | 不再接受 APK；目標 API 見 4-7 |

**〔判斷〕截圖要拍哪幾張**（兩家共用，各 6 張）：
1. 總覽（四問的結論一次看完）2. 資金流向的桑基圖 3. 輪動時鐘
4. 一張產業鏈剖析圖（**這是我們最獨特的東西，一定要放**）5. 個股頁的多週期 SMC 6. 題材熱度

---

# ⑦ 時程與成本

## 里程碑 A：「第一版能上架」

**目標**：Google Play 與 App Store 各有一個**免費**的 App，內容就是現在的網站。

| # | 工作 | 人天 | 日曆時間 | 誰做 | 相依 |
|---|---|---|---|---|---|
| A0 | **PWA 底座** | **已完成** | — | Claude | — |
| A1 | 合入 `index.html` 註冊片段 ＋ 部署 ＋ 手機實測安裝 | 0.5 | 1 天 | Claude ＋ Andy | A0 |
| A2 | **免責聲明**（首次啟動頁 ＋ 頁尾固定版位）、「今日候選」改名 | 1 | 2 天 | Claude | — |
| A3 | 隱私權政策頁（`site/#privacy`）＋ 商店文案（中英各一份） | 0.5 | 1 天 | Claude | — |
| A4 | **決定 App 名稱**（Bundle ID／package name 一旦上架不能改） | — | — | **Andy（硬期限）** | — |
| A5 | 商店素材：圖示 1024／512、主題圖 1024×500、12 張截圖 | 1 | 2 天 | Claude | A2、A4 |
| A6 | **Google 帳號 US$25 ＋ 自有網域 ＋ assetlinks 驗證** | 0.5 | 3 天 | Andy ＋ Claude | A4 |
| A7 | **Bubblewrap 打包 ＋ GitHub Actions 自動出 AAB** | 1 | 2 天 | Claude | A6 |
| A8 | **Google 封閉測試：湊 12 人 × 連續 14 天** | 0.5 | **14 天（不可壓縮）** | **Andy 找人** | A7 |
| A9 | Google 申請正式發佈 ＋ 審核 | — | **≤ 7 天** | — | A8 |
| A10 | **Apple 帳號 US$99 ＋ 憑證 ＋ App Store Connect 建檔** | 0.5 | 3 天 | Andy ＋ Claude | A4 |
| A11 | **Capacitor 專案 ＋ 原生層 1～4**（分頁列、離線畫面、分享；推播先留介面） | **3～4** | 6 天 | Claude | A10 |
| A12 | **macOS runner 在 GitHub Actions 出 IPA ＋ 上傳 TestFlight** | 1 | 3 天 | Claude | A11 |
| A13 | Apple 送審（**預留 2～3 輪退件**） | 0.5 | **7～14 天** | Claude | A12、A2、A3 |
| | **合計** | **約 10～11 人天** | **5～7 週** | | |

⚠ **A8 的 14 天可以跟 A10～A13 完全平行跑**，所以日曆時間的瓶頸是
「Google 那條 ≈ 5 週」與「Apple 那條 ≈ 5～6 週」取大者，不是相加。

**里程碑 A 的現金成本：約 NT$4,000 ＋ 網域 NT$1,000 ＝ NT$5,000。**

## 里程碑 B：「完整體驗」

**目標**：有帳號、有自選股跨裝置同步、有可靠推播、可以收訂閱費。

| # | 工作 | 人天 | 備註 |
|---|---|---|---|
| B0 | **法遵先決**：投顧執照，或把產品重新定位成純資訊工具 | — | **不是工程問題。這一項沒解決，B1 以後全部不要開始** |
| B1 | **搬離 GitHub Pages**（Cloudflare Pages 或同級） | 3 | 4-5 的條款要求；順便解決 assetlinks |
| B2 | **拔掉 Yahoo 分 K**，換成合規來源或自己從既有資料合成 | 3～5 | 4-6；⚠ 可能要改口徑，會動到 `pipeline/` |
| B3 | **後端**：帳號、自選股、推播訂閱表、收據驗證 | 10～15 | **目前完全沒有，這是最大一筆** |
| B4 | **推播派送**（APNs ＋ FCM）：盤後更新、族群進入漲潮區 | 4 | 依賴 B3 |
| B5 | **IAP／Google Play Billing ＋ 伺服器端收據驗證** | 5～8 | 依賴 B3 |
| B6 | **Home Screen Widget**（iOS ＋ Android） | 3～4 | 〔判斷〕對 4.2 最有說服力的一張牌 |
| B7 | 即時報價的可用度（目前掛在免費方案 Worker 上，`product_strategy.md` 摘要第 7 條說「今天壞了五次」） | 3 | **收費產品不可接受現況** |
| | **合計** | **約 31～42 人天** | **6～10 週**，且 B0 是前提 |

**里程碑 B 的經常性成本：後端 NT$150～500／月 ＋ Apple NT$3,200／年。**

---

# ⑧ 風險表（每一條附機率與緩解）

| # | 風險 | 機率 | 衝擊 | 緩解 |
|---|---|---|---|---|
| **1** | **Apple 以 3.2.1(viii)（金融類需由金融機構提交／需執照）退件** | **中高（〔判斷〕40～60%）** | **致命** —— 可能永遠上不了 iOS | ① 全站改成「資訊工具」語氣、拿掉「建議／推薦／進場」用語 ② 首啟免責頁 ③ 審查備註明講不執行交易、不持有資金、不做個人化建議 ④ **先送 Google，拿 Android 上架當談判與參照** |
| **2** | **Apple 以 4.2（重新包裝的網站）退件** | 加原生層前 **高（〔判斷〕≈90%）**；加了之後 **中低（≈25%）** | 高，但可修 | 做齊 2-2-B 的 1～4 項；審查備註列出原生功能；截圖秀資訊密度 |
| **3** | **Google 12 人 × 14 天湊不到人** | **中（〔判斷〕30%）** | 中 —— 整條 Android 卡住 | ① **今天就開始找人**，不要等程式 ② 有公司就開組織帳號，直接不適用 ③ ⚠ 不要買代測服務 |
| **4** | **`assetlinks.json` 放不到 `miaozike.github.io` 根目錄** | **中（〔判斷〕40%，機制推論）** | 中 —— TWA 會退成普通瀏覽器分頁（有網址列） | **買自有網域**，一次解決；或另開 `miaozike.github.io` repo |
| **5** | **台灣投顧法**：收費後被認定經營證顧業務 | 免費時**低**；**收費後高** | **致命（刑責）** | **第一版一律免費**；收費前先取得法律意見或執照（`product_strategy.md` 紅線一） |
| **6** | **GitHub Pages 條款**：App 被視為在上面經營生意 | 免費時**低**；收費後**高** | 高 | 收費前搬家（B1） |
| **7** | **Yahoo 條款**：商業用途 | 免費時**低**；收費後**中高** | 中 | 收費前拔掉（B2） |
| **8** | **Andy 不跑本機指令，打包卡住** | **高（如果不先處理）** | 高 | **所有打包都寫成 GitHub Actions**；金鑰放 Secrets。⚠ **這一條要在 A7／A12 一次做對**，不要留「請你在電腦上跑一下」的步驟 |
| **9** | **iOS 打包要 macOS runner**，額度或設定出問題 | 中（〔判斷〕30%） | 中 | public repo 的 macOS runner 目前免費（信心：中）；真的不行就改用 Xcode Cloud（US$ 另計）或請人代跑一次 |
| **10** | **命名沒定案就送審**，Bundle ID 永久固定 | **中（如果現在不決定）** | 中 —— 只能重新上架一個新 App | **A4 設為硬期限**；Bundle ID 用中性的 `io.github.miaozike.twrotation` 之類，**不要把產品名寫死進去** |
| **11** | **資料每天變，商店截圖很快就跟畫面對不上** | 高 | 低 | 〔判斷〕截圖挑**版面**不挑**數字**；不要在截圖上標具體股價 |
| **12** | **PWA 的 Service Worker 快取住舊版，使用者看到舊畫面** | **已處理（低）** | 高（如果發生） | 導覽一律網路優先 ＋ 每次部署換一個新的快取名稱（做法與驗證在第 ⑨ 節） |
| **13** | 即時報價 Worker 的可用度（現況「今天壞了五次」） | 高 | 中（免費時）／高（收費後） | B7；上架前至少把錯誤處理與「上次更新時間」做明確 |

---

# ⑨ PWA 第一步：已經做掉的東西

**這一批只新增檔案，一支既有檔案都沒有動**（`site/index.html` 一行未改，那支在別人手上）。

## 9-1 新增了什麼

| 檔案 | 內容 |
|---|---|
| `site/manifest.webmanifest` | ⚠ **這支是「改」不是「新增」**（它本來就存在）。補上 `id`／`lang`／`display_override`／`categories`／三個捷徑，圖示補齊 192／512／maskable，底色對齊網站真正的 `#070b16`，`orientation` 由 `portrait` 改成 `any` |
| `site/sw.js` | Service Worker，162 行，整份策略寫在檔頭。**核心規則：`data/**` 與任何 `.json` 一律不進快取** |
| `site/icons/icon.svg` | 圖示母檔（圓角底板）。手寫 SVG，沒有用任何設計工具 |
| `site/icons/icon-maskable.svg` | 圖示母檔（滿版底板，給 Android maskable 與 iOS 用） |
| `site/icons/icon-192.png`／`icon-512.png` | `purpose: any`，圓角外透明 |
| `site/icons/icon-maskable-192.png`／`icon-maskable-512.png` | `purpose: maskable`，不透明 |
| `site/icons/apple-touch-icon-180.png` | iOS 主畫面用，**不透明**（iOS 會把透明填成黑） |

## 9-2 Service Worker 的四條規則（照順序判斷）

| 順序 | 什麼樣的請求 | 怎麼處理 | 為什麼 |
|---|---|---|---|
| 0 | 非 GET、或**跨網域** | **完全不介入**（不呼叫 `respondWith`） | 即時報價走 Cloudflare Worker 代理與 `mis.twse`，每分鐘就過期，而且快取 opaque 回應只會浪費配額 |
| ① | **`data/**` 與任何 `.json`** | **★ 完全不介入，一個位元組都不存** | **這是整支 SW 最重要的一條。**那是每天盤後重算的資料，快取住等於他看到昨天的數字 |
| ② | **導覽／`index.html`** | **網路優先**，斷線才退回快取 | 兩個理由：① 他永遠看到最新的畫面 ② **這是「版本能不能換掉」的關鍵** —— HTML 是新的，裡面的版號才是新的，SW 才換得掉（見 9-3） |
| ③ | **`vendor/**`** | 快取優先，放在**不跟版號跑**的 `tw-vendor-v1` | ECharts ＋ Lightweight Charts 約 1.2MB，而且 `stamp_assets.py` 刻意不給 vendor 加版本戳。跟著版號換名字＝每天叫他重抓 1.2MB。**換函式庫時要把 `v1` 加一** |
| ④ | 其餘 app shell（自家 JS／CSS／圖示） | 快取優先，放在 `tw-shell-<版號>` | 這些的網址都被 `stamp_assets.py` 加過 `?v=<版號>`，**內容一變網址就變**，所以快取優先拿不到過期的東西 |

**安裝時只預先抓 6 個小檔**（`./`、`index.html`、manifest、三個圖示，合計約 45KB）。
**刻意不預抓那 40 支自家 JS（2.7MB）與 vendor（1.9MB）** —— 安裝時就灌 4MB 在手機網路上是不禮貌的，
那些檔案第一次用到時自然會被快取起來。

## 9-3 版本化：新版怎麼「真的」換掉舊的

```
部署 → stamp_assets.py 換掉 <meta name="tw:build"> 的值
     → 使用者重新整理，拿到新的 index.html（因為導覽是網路優先）
     → 頁面讀 meta，註冊 sw.js?v=<新版號>
     → 網址變了 ＝ 一支新的 SW → install（skipWaiting）→ activate
     → activate 把所有 tw- 開頭、不在白名單裡的快取全部刪掉
     → 舊的 tw-shell-<舊版號> 消失，clients.claim() 接手
```

**為什麼不用「在 sw.js 裡寫死一個常數」**：寫死的常數要有人記得改，
而「有人記得改」在這個專案裡已經被證明不成立。
`?v=` 讓它**跟著現有的版號機制自動跑**，不必多做任何事。

⚠ **這套會成立的唯一前提是「導覽網路優先」。**
如果 `index.html` 是從快取拿的，版號永遠是舊的，就會**鎖死在舊版再也更新不了** ——
這是 PWA 最經典的一個坑，所以我把它寫在 `sw.js` 的檔頭當警告。

## 9-4 我實際量到的驗證結果

**工具**：Playwright ＋ 真的 Chromium，真的註冊一次 Service Worker，
**真的去翻 Cache Storage 的內容**（不是看有沒有 render）。
**環境**：本機 HTTP 伺服器，掛的是 `site/` 底下**真實的檔案**（symlink），
載的是**真正的 `index.html`**（暫存區的複本，加上第 ⑩ 節那段註冊片段），
所以整個 app 真的被啟動、41 支自家 JS 真的被載入。**跑了三次，最後兩次結果一致。**

```
✅ ① Service Worker 註冊成功：scope=http://127.0.0.1:8791/ script=sw.js?v=A1111111
✅ ① 註冊網址帶到版號 A1111111
✅ ① SW 已接手這個頁面（controller 存在）
✅ ② index.html 已在快取中
✅ ② app.js 已在快取中
✅ ② manifest.webmanifest 已在快取中
✅ ② 圖示已在快取中
✅ ② vendor/echarts.min.js 在 tw-vendor-v1 快取中（不跟版號跑）
✅ ② vendor 沒有被重複存進 shell 快取
   shell 快取筆數 = 43 ／ vendor 快取筆數 = 2
       └ 43 筆的內容：index.html ＋ `/`（導覽）＋ app.js／industry.js／chart.js／diagrams.js／
         market3.js／three3d.js／themes3d.js／live.js／livek.js／tasks.js ＋ **27 支 dg/*.js**
         ＋ manifest ＋ 3 個圖示。**一筆 data 都沒有。**
       └ vendor 2 筆：echarts.min.js、lightweight-charts.js（放在不跟版號跑的快取裡）
✅ ③ ★ data/** 與 .json 完全沒有被快取（違規 0 筆）
✅ ④ 新版快取 tw-shell-B2222222 已建立
✅ ④ ★ 舊版快取 tw-shell-A1111111 已被刪掉
✅ ④ vendor 快取沒有被換版誤殺（不必重抓 1.2MB）
✅ ④ 接手的是新版 SW：sw.js?v=B2222222
✅ ④ 換版之後 data/** 仍然沒被快取（違規 0 筆）
✅ ⑤ 離線仍打得開，標題＝「台股資金輪動儀表板」

通過 16 項，失敗 0 項
```

**③ 這一條的做法要講清楚**：我不是「檢查有沒有 `data/xxx.json` 這一筆」，
而是**把每一個快取裡的每一個網址全部撈出來**，
凡是路徑含 `/data/` 或副檔名是 `.json` 的就算違規 —— **結果是 0**。
測試前我還刻意讓頁面去抓 `data/meta.json`、`data/candidates.json`、`data/hist/index.json`，
確定「真的有發生過 data 請求」，不是因為沒請求所以沒快取。

**其他驗證**
- `python3 -c "json.load(...)"` → `manifest.webmanifest` 是合法 JSON，16 個鍵。
- `node --check site/sw.js` → 通過。
- 圖示：程式量過 **maskable 的安全區**（Android 規定內容要落在中央 80% 圓內）——
  內容最遠半徑 **192.3**，上限 **204.8**，**通過**；安全區外的非底板像素 **0**。
- 圖示：在 **29／48／60／120／192／512** 六個尺寸 × **深底／淺底／圓形遮罩** 三種情境合成一張對照表看過。
  **29px 下指針仍然認得出來**（這正是選「轉針」而不是「四格關卡」的原因）。

## 9-5 圖示的設計說明（**這是暫時版**）

照 `docs/product_strategy.md` ⑥ 的結論做 **方向 D「單一轉針」**：

- **為什麼是轉針**：29×29 只放得下一個形狀，指針是唯一在該尺寸仍然高對比的元素。
- **為什麼弧是斷成四段**：原本的方向 B（齒輪咬合）在小尺寸會糊成鋸齒雜訊。
  斷開的四段粗弧既讀得出來，又**把「四問」的四道關卡編進圖形裡**（長度與粗細遞增）。
- **配色**：底板 `#16203c`（比網站底色亮一階，深色桌布上才有輪廓）、
  弧 `#93a2c4`（中性灰藍，深淺底都活得下來）、指針 `#ffb454`（琥珀）。
  **刻意避開紅綠** —— 台股紅漲綠跌有既定語意，拿來當品牌色會製造誤讀。

⚠⚠ **這是暫時版，命名定案後整組要重做。**
`docs/product_strategy.md` ⑤ 列了 8 個候選名字、最推薦〈剖面〉，Andy 還沒挑。
名字換了，圖示的方向可能要跟著換（例如選〈剖面〉就該用方向 A 剖面線）。
`manifest.webmanifest` 的 `name`／`short_name` 與 `site/icons/*` 裡都留了註解指到這一段。

---

# ⑩ ★ 要合進 `site/index.html` 的那幾行（我沒有動這支檔案）

⚠ **`site/index.html` 目前有另一個 agent 在改（手機版面重整），所以我一行都沒碰。**
下面三段請由 CEO 合入。三段都是**純新增**，不會動到既有的任何一行。

## 10-1 【必要】註冊 Service Worker —— 貼在 `</body>` 之前

```html
<!-- PWA：註冊 Service Worker（2026-09-24 新增）。
     ?v= 帶的是 <meta name="tw:build"> 的版號，每次部署都會變 ——
     版號一變就等於換了一支新的 SW，舊快取才會在 activate 時被真的刪掉。
     快取策略與「為什麼 data/** 一律不快取」全部寫在 site/sw.js 的檔頭。 -->
<script>
if ('serviceWorker' in navigator && location.protocol.indexOf('http') === 0) {
  addEventListener('load', function () {
    var m = document.querySelector('meta[name="tw:build"]');
    var v = (m && m.content) || 'dev';
    navigator.serviceWorker.register('sw.js?v=' + encodeURIComponent(v))
      .catch(function (e) { console.warn('Service Worker 註冊失敗：', e); });
  });
}
</script>
```

**為什麼是 `?v=` 而不是在 `sw.js` 裡寫死一個常數**：寫死的常數要有人記得改，
而「有人記得改」在這個專案裡已經被證明不成立。`?v=` 讓它**跟著現有的版號機制自動跑**，
`scripts/stamp_assets.py` 每次部署都會換掉那個 meta，不必多做任何事。

**為什麼要判 `location.protocol`**：`scripts/_show.py` 與直接開檔（`file://`）都不該註冊 SW，
Service Worker 在 `file://` 下本來就會丟例外。

## 10-2 【建議】換掉 apple-touch-icon，加上 favicon —— 在 `<head>` 內

現在第 20 行是 `<link rel="apple-touch-icon" href="icon.png">`，
指向的 `site/icon.png` 是 2026-09-18 的 2KB 舊圖。建議換成新的（**`site/icon.png` 我沒有刪，留著當退路**）：

```html
<link rel="apple-touch-icon" href="icons/apple-touch-icon-180.png">
<link rel="icon" type="image/png" sizes="192x192" href="icons/icon-192.png">
<link rel="icon" type="image/svg+xml" href="icons/icon.svg">
```

⚠ `apple-touch-icon` **不可以有透明**（iOS 會把透明填成黑），所以我特地為它出了一張不透明的滿版圖；
`icons/icon-192.png`／`icon-512.png` 則是圓角＋圓角外透明，給 Android 與桌面瀏覽器用。

## 10-3 【建議】把 `theme-color` 拆成深／淺兩個 —— 在 `<head>` 內

現在第 7 行只有 `<meta name="theme-color" content="#070b16">`，
所以**明亮主題的人裝成 App 之後，狀態列仍然是深色的**。建議改成：

```html
<meta name="theme-color" content="#070b16" media="(prefers-color-scheme: dark)">
<meta name="theme-color" content="#f6f7fb" media="(prefers-color-scheme: light)">
```

⚠ **這一段我標「建議」不是「必要」**，因為它會動到既有的那一行，
而且網站的主題是用 `localStorage` 的 `tw.theme` 決定的，不完全等於系統偏好 ——
真正對齊要在 `applyTheme` 裡同步改那個 meta，那是別人手上的檔案，**我不越界**。

---

# ⑪ 我判斷過、但 Andy 可能不同意的地方（請一次否決，不要一項一項問我）

| # | 我的判斷 | 你可能的反對 | 如果你反對，我的退路 |
|---|---|---|---|
| **1** | **`manifest.webmanifest` 我是「改」不是「新增」** —— 那支檔案已經存在（2026-09-11 的版本，只有一張 512 圖示、沒有 192、沒有真正的 maskable）。任務說「新增」，但再開一支 `manifest2` 只會更糟 | 「不是說不准改既有檔案嗎」 | 舊內容完整保留在 git 裡，`git checkout HEAD -- site/manifest.webmanifest` 一行就退回去。**這支檔案沒有別的 agent 在動**（git log 顯示上次變動是 2026-09-11 的資料 commit） |
| **2** | **`orientation` 從 `portrait` 改成 `any`** | 「我本來就要直式」 | 這是一個有 K 線圖、桑基圖、3D 場景的儀表板，**鎖直式等於不准他橫過來看 K 線**。要鎖回去改一個字就好 |
| **3** | **`background_color`／`theme_color` 從 `#0e1014` 改成 `#070b16`** | 「差那麼一點有差嗎」 | 有 —— 舊值跟網站真正的底色不一樣，開啟時的啟動畫面會先閃一下不同的黑。改回去改兩個字 |
| **4** | **第一版一定要免費上架，不要帶訂閱** | 「那我什麼時候才收得到錢」 | 理由是 4-1 與 4-5：收費會同時引爆投顧法與 GitHub Pages 條款，而且會讓 Apple 3.2.1(viii) 的檢視變嚴。**先上架、先累積使用者**，收費放里程碑 B |
| **5** | **399／799 這個價格帶我認為偏高**（`product_strategy.md` 建議 149～249，對標 Tide 是 199） | 「我就是要 399」 | 我不再問第二次，你說了算。但請看 5-2 的實收：399 扣蘋果 15% 是 **NT$339** |
| **6** | **Android 先、iOS 後** | 「iOS 才是我要的」 | 可以倒過來，但 iOS 那條要多 3～5 天開發（原生層），而且有 4-1 的致命風險。**先做 Android 的好處是「先有一個 App 存在」** |
| **7** | **建議買一個自有網域** | 「github.io 不是好好的嗎」 | 自有網域一次解決三件事：TWA 的 assetlinks、品牌、未來搬家不用換網址。NT$1,000／年 |
| **8** | **「今日候選」要改名、全站要加免責聲明** | 「這是我要的詞」 | 保留詞，但卡片上加一行「這是條件篩選結果，不是推薦」。⚠ **免責聲明本身沒有退路** —— 那是 4-1 與 4-3 的硬要求 |
| **9** | **圖示我自己決定用「轉針」而不是「剖面線」** | 「我比較喜歡剖面」 | `product_strategy.md` 說這兩個在 29×29 都活得下來，轉針更安全。**SVG 是手寫的，換一個方向就是改幾行**。而且反正命名定案後整組要重做 |
| **10** | **Bundle ID 建議用中性的 `io.github.miaozike.twrotation`**，不要把產品名寫進去 | 「那不是很醜」 | 使用者看不到 Bundle ID，但它**一旦上架就永遠不能改**。把還沒定案的名字寫進去，是把一個可逆的決定變成不可逆的 |

---

# ⑫ ★ 我沒有查到／沒有驗到的（不准用模糊的話帶過）

## 12-1 沒查到的

| # | 沒查到的東西 | 為什麼重要 | 怎麼才查得到 |
|---|---|---|---|
| 1 | **台灣是否要求投顧執照才能通過 Google 的「財務功能聲明」** | 直接決定 Android 能不能上 | 只有真的去 Play Console 填那張表才會知道要求上傳什麼。**這是「做了才知道」** |
| 2 | **Apple 3.2.1(viii) 對「純資訊、不下單」的 App 實際上怎麼判** | 這是最高風險那一條 | 我查到的全是條文轉述，沒有判例。**最快的驗證是先送一次 TestFlight 審查** |
| 3 | **台灣商店能不能用「導到網頁付款」** | 影響抽成 15% 還是 0% | 我查到的是**美國專屬**的禁制令，且案子在最高法院進行中。台灣的部分**未確認** |
| 4 | **GitHub Actions 的 macOS runner 對 public repo 是否仍免費、額度多少** | A12 能不能自動化 | 這個政策改過好幾次，**要在真的要用時看當下的計費頁** |
| 5 | **`miaozike.github.io` 根目錄能不能放 `.well-known/assetlinks.json`** | TWA 成敗 | 我是從機制推的（信心：中）。**開一個 `miaozike.github.io` repo 試一次就知道，10 分鐘** |
| 6 | **StockIntelli／Tide 是不是已經上架、用哪一種包裝** | 對標 | 這個容器打不開 App Store。**Andy 自己開商店搜一次就有答案** |
| 7 | **Apple 對「未上市公司的個人開發者」送金融類 App 的額外文件要求** | 影響 A10 的時間 | 未確認 |

## 12-2 ★ 我沒有驗到的（重要）

1. **我沒有在真的手機上裝過這個 PWA。** 這個容器沒有 iPhone 也沒有 Android，
   我驗的是 **headless Chromium**。iOS Safari 的行為**已知跟 Chromium 不一樣**
   （安裝流程、`display: standalone` 的安全區、狀態列顏色、Web Push）。
   **→ Andy 一定要自己用手機裝一次，這一步我取代不了。**
2. **我沒有打開 <https://miaozike.github.io/tw-rotation/> 確認過任何事。**
   這個容器打不開那個網址（`CLAUDE.md` 已載明）。
   **我也沒有 commit、沒有 push**（任務明令）。所以線上目前還沒有這些檔案。
3. **我沒有跑 `pytest`、`_preview.py`、`_uitest.py`。**
   理由：這批**只新增檔案、沒有改任何既有檔案**，`site/index.html` 一行未動，
   所以現有的任何一段驗收在物理上都不會碰到我新增的東西。
   **⚠ 但合入第 ⑩ 節那幾行之後，就要跑一次前端關卡** ——
   那時 `git diff --name-only` 會出現 `site/index.html`，
   按 `CLAUDE.md` 的對照表至少要跑 `總覽`、`縮放掃描`、`手機` 三段，並跑 `_preview.py`。
4. **我沒有驗「Service Worker 上線之後，`live.js` 的每分鐘即時報價還正常」。**
   理論上不會受影響（SW 對跨網域請求完全不介入，程式碼裡是 `url.origin !== self.location.origin` 就 return），
   **但這是理論，不是量測。** 合入之後請把 `盤中即時` 那一段一起跑。
5. **我沒有量 Lighthouse 的 PWA 分數。** TWA 上架建議 ≥ 80（信心：中），
   這個容器沒有 Lighthouse CLI。**上架前要補量。**
6. **圖示只在「深底／淺底／圓形遮罩」三種情境下用合成圖看過**，
   沒有在真的 iOS 主畫面與 Android 啟動器上看過。

---

# 附：來源清單

⚠ **以下每一條我都只讀得到搜尋摘要，沒有讀到原文。** 信心度標在括號裡。

**Apple**
- App Review Guidelines（4.2 最低功能性、3.2.1(viii) 金融類）：<https://developer.apple.com/app-store/review/guidelines/>（高）
- Guideline 4.2 與網站包裝的實務討論：<https://www.mobiloud.com/blog/app-store-review-guidelines-webview-wrapper/>、<https://appcompliance.io/blog/apple-guideline-4-2-minimum-functionality/>（中）
- 3.2.1(viii) 的開發者論壇討論：<https://developer.apple.com/forums/thread/704470>、<https://developer.apple.com/forums/thread/775803>（中）
- Small Business Program：<https://developer.apple.com/app-store/small-business-program/>、<https://www.revenuecat.com/docs/platform-resources/apple-platform-resources/app-store-small-business-program>（高）
- 截圖規格：<https://developer.apple.com/help/app-store-connect/reference/screenshot-specifications/>（高）
- 隱私權營養標示：<https://developer.apple.com/app-store/app-privacy-details/>（高）
- 審查時間：<https://appcompliance.io/blog/app-store-review-time-2026/>、<https://patchrelease.com/app-store-review-times>（中）

**Google**
- 新個人帳號的測試要求（12 人 × 14 天）：<https://support.google.com/googleplay/android-developer/answer/14151465>（高）
- Financial Services 政策：<https://support.google.com/googleplay/android-developer/answer/9876821>（高）
- 財務功能聲明表：<https://support.google.com/googleplay/android-developer/answer/13849271>（高）
- 目標 API 等級：<https://support.google.com/googleplay/android-developer/answer/11926878>（高）
- Trusted Web Activity 快速上手：<https://developer.android.com/develop/ui/views/layout/webapps/guide-trusted-web-activities-version2>（高）
- Bubblewrap：<https://github.com/GoogleChromeLabs/bubblewrap>（高）
- TWA 的新功能：<https://developer.chrome.com/docs/android/trusted-web-activity/whats-new>（中）

**費用**
- <https://splitmetrics.com/blog/google-play-apple-app-store-fees/>、<https://axonbuild.com/blog/cost-to-publish-an-app-to-the-app-stores>（高，兩個來源一致）

**iOS 的 PWA 現況**
- <https://www.mobiloud.com/blog/progressive-web-apps-ios/>、<https://www.magicbell.com/blog/pwa-ios-limitations-safari-support-complete-guide>（中）
- Apple 開發者論壇「iOS PWA 資料保存超過 7 天」：<https://developer.apple.com/forums/thread/710157>（中）

**Epic v. Apple（外部連結付款）**
- 第九巡迴上訴法院 2025-12 判決：<https://law.justia.com/cases/federal/appellate-courts/ca9/25-2935/25-2935-2025-12-11.html>（中）
- 最高法院進度（Apple 2026-09-14 遞出主要訴狀）：<https://www.techtimes.com/articles/327527/20260915/app-store-commission-limbo-enters-new-phase-apples-epic-merits-brief-opens-scotus-fight.htm>（低，單一二手來源）

**本專案**
- `docs/product_strategy.md`（法規紅線、GitHub Pages 條款、資料授權、命名候選、圖示方向）
- `CLAUDE.md`（Andy 不跑本機指令、容器打不開自家網站、`WebSearch` 摘要會湊關係）
- `site/`、`scripts/stamp_assets.py`（版號機制）、`workers/quote-proxy/`
