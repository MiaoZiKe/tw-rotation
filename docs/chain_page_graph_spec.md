# 產業鏈頁下半部重構：關聯圖改成 Obsidian 式、拿掉重複的區塊

> Andy 2026-09-23 交辦四件事（原話節錄）：
> 1. 「我看到『環節清單』與『關聯圖』長得很像，先幫我確認是否重複，如果是只保留一個」
> 2. 「圖一紅框處這兩個拿掉，然後以後圖二下方的族群的關聯圖改成圖三那樣的形式，像 Obsidian 關聯圖那樣展開，
>    並且新增幾種風格我要看看（科技、電路、星際、泡泡），且置中，會依據當前螢幕大小調整比例，
>    另外它 Default 只顯示族群名稱，且下面是標籤式列出個股，直到點擊才是個股清單，並且旁邊會附註關聯圖」
> 3. 「圖四族群卡片幫我移除，漲跌幅 & 占比等基本資訊可以放在上方關聯圖顯示」
>    ＋「這點整個確認，**所有介紹資訊需附上連結**」
> 4. 「圖五下方成分股清單可以暫時覆蓋旁邊事件頁面」

## 〇、先回答第 1 題：重複嗎？**是同一份資料的兩種畫法，但不是同時出現**

機器查到的事實（`site/industry.js`）：

| | DOM | 現在怎麼出現 |
|---|---|---|
| 環節清單 | `#chainList`（`.seglist`） | 預設顯示 |
| 關聯圖 | `#chainMap`（`.chainmap`） | `hidden`，按 `#chainView`（「看關聯圖 →」）才切出來 |

- 第 1168 行的註解自己寫著：**「環節卡清單跟關聯圖是同一份資料的兩種畫法，所以高亮規則也要同一套」**。
- 第 537–538 行寫了當初為什麼留兩個：
  「清單＝這條鏈有哪些格、每格有誰（預設，因為高度只有關聯圖的三分之一）；
   關聯圖＝誰連到誰、線多粗（140 條邊本身，只有圖畫得出來）」。

**結論**：資料同一份、兩個畫面互斥（同一顆按鈕切換），所以**畫面上不重複**，但**維護上重複**（同一份資料兩套繪圖、兩套高亮）。
第 2 題的新關聯圖既要顯示族群名稱、又要標籤式列個股 —— **那正是清單在做的事**。
所以：**留關聯圖、移除環節清單**（`#chainList` 與 `#chainView` 切換鈕一起拿掉）。

## 一、要拿掉的（圖一紅框 ＋ 圖四）
| 拿掉 | DOM | 理由 |
|---|---|---|
| 環節色標晶片列 | `#segChips` | Andy 指名 |
| 環節詳情面板（含跨鏈對照 `#xChains`） | `#segBox` | Andy 指名 |
| 環節清單 | `#chainList` ＋ `#chainView` 鈕 | 第 0 節的結論 |
| 族群卡片 | `#groupCards` | Andy 指名；漲跌幅／占比搬進關聯圖的節點 |

⚠ 這四塊都有既有驗收在量（`產業`、`族群頁`、`產業關係面板`、`批次20-產業鏈版面`…）。
**改掉的斷言要逐條寫理由，不准直接刪掉整段。** 被它們承載的功能（環節篩選、跨鏈對照、族群篩選）
**要在新關聯圖裡找到新家**，不可以連功能一起消失。

## 二、新的關聯圖（力導向，Obsidian 式）
- **預設只顯示族群名稱**：一個族群＝一個節點，半徑依「占本鏈成交值」，顏色＝族群色。
- **節點上直接顯示基本資訊**：族群名 ＋ 今日漲跌幅（紅漲綠跌）＋ 占比。這是第 3 題要的。
- **下面是標籤式個股**：節點底下用小標籤列出成分股代號（最多 N 個，多的收成 `+12`）。
- **點一下節點才展開成個股清單**（節點展開成子節點，或右側面板出個股清單 —— 兩種都要試，截圖給 Andy 挑）。
- **旁邊附註關聯圖**：選起來的節點旁顯示「它連到誰、為什麼」的小卡（沿用現在 `產業關係面板` 的內容）。
- **置中**、**依螢幕大小調整比例**（`ResizeObserver` 重算佈局與縮放，不是固定 viewBox）。
- 互動：拖曳節點、滾輪縮放、雙擊回到置中、點背景取消選取（跟剖析圖同一套手勢語言）。

### 四種風格（**第一個交付就是這四張截圖，給 Andy 挑**）
| 風格 | 長相 |
|---|---|
| **科技** | 深底、節點是帶邊框的圓角方塊、連線細直線帶發光端點、等寬字 |
| **電路** | 連線走直角折線（像 PCB 走線）、節點是方形焊盤、轉角有圓點、金屬色 |
| **星際** | 深藍黑底、節點是發光星球（大小差距拉大）、連線是淡淡的星塵、有輕微的漂浮感 |
| **泡泡** | 淺底、節點是半透明彩色泡泡、有柔和陰影與高光、連線是細柔曲線 |
四種共用同一份佈局與互動，**只換視覺**（走 `--dg-*` 風格的 token，深淺兩主題都要成立）。

## 三、所有介紹資訊都要附連結（Andy 特別強調）
畫面上每一段「介紹／說明」文字，只要講到**可以連過去的東西**，就要是連結：
- 族群 → 族群頁；個股 → 個股頁；環節 → 該環節的剖析圖；產業鏈 → 該鏈頁。
- 外部資料來源（媒體、公司法說會）→ 附原始網址。
- 查不到、沒有對應的，照舊明講「查不到」，**不要放一個死連結**。

## 四、成分股可以覆蓋事件面板
`#memberTable` 那一區在需要時可以往右延伸、蓋住右側「今日事件」面板（Andy：「可以暫時覆蓋」）。
做法：成分股區加一顆「放寬」鈕或捲到它時自動加寬；事件面板不是被刪掉，是被蓋住，關掉就回來。

## 五、驗收（新段落 `批次25-關聯圖`）
1. 四種風格各切一次，節點／連線／底色真的變（量 computed style，不是只看 class）。
2. 預設只顯示族群名稱與標籤式個股；點節點才出現個股清單。
3. 節點上有漲跌幅與占比，紅漲綠跌方向正確。
4. 置中：容器中心與圖的外接盒中心差 ≤ 容器寬的 2%；改變視窗寬度後仍然成立（1440／1100／800／390）。
5. 拖曳節點真的移動、滾輪真的縮放、點背景真的取消選取。
6. **所有「介紹資訊」裡該有連結的都有**：逐條檢查族群名、個股代號、環節名是不是 `<a>` 且 href 正確。
7. 被拿掉的四塊確實不在 DOM 裡，而它們承載的功能在新圖裡找得到對應操作。
8. 成分股覆蓋事件面板：開啟後成分股寬度變大、事件面板被蓋住；關掉回復。

---

# 第二版（Andy 2026-09-23 給了兩張目標參考圖，**蓋掉上面第二節的節點設計**）

參考圖存在 `docs/diagram_refs/graph_ref_layout.webp`（版面與右側面板）與
`docs/diagram_refs/graph_ref_4styles.webp`（四種風格並排）。**這兩張是驗收的基準。**

> Andy 原話：「關聯圖風格改成這樣試試看，**大圓點為族群、周遭的小點為個股**，
> **預設狀態只顯示大族群，並且周圍依據個股數量顯示對應數量的小點**，
> **當游標移動過去該族群大點上，才會顯示對應個股的名稱、漲幅、占比等資訊**，
> 點擊該個股一樣保留旁邊會出現資訊欄位，**資訊欄內容有相關連結請附上**。
> 版面需置中，且依據當前螢幕調整適當大小及範圍，**不可留太多空白**。」

## 一、節點：大點＝族群，小點＝個股（取代第一版的「標籤式個股」）
- **族群＝大圓點**，直徑依成交占比；顏色依分類（參考圖左下的圖例：設計／材料設備／製造／封裝測試／記憶體…）。
- **個股＝小點**，**繞著所屬族群的大點散佈**，**數量＝該族群的成分股檔數**（不是固定幾顆）。
  檔數很多時（例如 20 檔以上）仍要畫得出來：縮小半徑、排成兩圈，**不准用「+N」省略**（Andy 要的就是「看得出有幾檔」）。
- **預設（沒有 hover、沒有選取）只顯示族群名稱**；小點只是點，不帶文字。
- **游標移到族群大點上** → 該族群的小點浮出**個股名稱、漲幅、占比**（小卡或標籤，不要蓋住別的族群）；
  移開就收回。**hover 是唯一的展開條件**，不要一進來就全部攤開。
- **點個股小點** → 右側資訊欄顯示那一檔（見第三節）。
- 族群之間的連線＝上下游／同環節／設備與材料（見第四節的篩選）。

## 二、版面（照參考圖一比一）
| 位置 | 內容 |
|---|---|
| 左上 | 標題「產業關聯圖」＋副標「探索產業之間的連結」；下一行灰字「拖曳節點・滾輪縮放・點選查看關聯」 |
| 標題下 | 目前範圍的晶片，例如「半導體 ×」（按 × 回到全市場） |
| 右上 | 搜尋框（搜產業或公司）、「篩選」鈕、「重設視角」鈕 |
| 左下 | 分類圖例（彩色小圓點 ＋ 分類名） |
| 右下 | 縮放控制：−／＋／置中三顆 |
| 右側 | 資訊欄（見第三節），可關（右上角 ×） |
- **置中**：圖的外接盒中心對齊畫布中心，誤差 ≤ 容器寬的 2%。
- **依螢幕調整大小與範圍、不可留太多空白**：
  節點佈局的外接盒要**撐到畫布的 ≥ 80%**（寬與高各自算），不足就整體放大、超出就縮小；
  視窗改變時重算（`ResizeObserver`）。**這條要寫成機器斷言**（第六節）。
- 右側資訊欄在窄畫面（< 1100）改成蓋在圖上的浮層或底部抽屜，不要把圖擠扁。

## 三、右側資訊欄（照參考圖）
選到**族群**時：
- 彩色圓點 ＋ 族群名；一行說明。
- 數據盒：**族群漲跌**（紅漲綠跌）與**成交占比**兩個大數字，底下小字標資料日期／「盤中暫定值」。
- **代表公司**清單（公司名 ＋ 代號 ＋ `›`），**每一列都是連到個股頁的連結**。
- **顯示範圍**：直接關聯／兩層關聯 兩顆切換。
- **關係類型**：上下游／同環節／設備與材料 三個勾選框，勾了才畫那種線。
- **「以此節點為中心」**按鈕：以它重新佈局。
選到**個股**時：名稱、代號、今日漲跌、所屬族群（連結）、所屬環節（連結到剖析圖）、以及「它供給誰／誰供給它」。
★ **資訊欄裡所有講得到的東西都要是連結**（族群→族群頁、個股→個股頁、環節→剖析圖、產業鏈→鏈頁；
外部來源附原始網址）。查不到的照舊明講，**不要放死連結**。

## 四、四種風格（照 `graph_ref_4styles.webp`，連標題那一行的字都照做）
| # | 風格 | 標語（放在風格切換的說明） | 視覺 |
|---|---|---|---|
| 01 | **星際** | 深邃宇宙・星辰連結・探索無限產業可能 | 深紫藍星空底、節點是發光星球、連線細亮、背景有星點 |
| 02 | **科技** | 精準科技・網格秩序・數據驅動產業洞察 | 深藍底＋細網格、節點是帶光環的圓、連線青藍 |
| 03 | **泡泡** | 輕盈柔和・繽紛氣泡・親切易讀的產業地圖 | 淺藍白底、半透明彩色泡泡、柔和陰影 |
| 04 | **電子電路** | 晶片脈絡・電路連結・驅動產業未來 | 深綠底＋PCB 紋路、節點是方形晶片（帶接腳）、金色直角走線 |
- 四種**全部做、全部保留**，右上角或篩選面板裡可切換、記進 `localStorage`（DECISIONS #249：選項全上，不拿來問）。
- 每種風格**深淺兩主題都要成立**（泡泡本來就是亮底，深色主題下字要跟著換）。

## 五、不變的
- 第一版第一節的結論仍然有效：**移除環節清單**（`#chainList` ＋ `#chainView`）。
- 要拿掉的四塊不變：`#segChips`、`#segBox`、`#chainList`＋`#chainView`、`#groupCards`；功能要在新圖裡找到新家。
- 成分股可暫時覆蓋事件面板。

## 六、驗收（`批次25-關聯圖`，在第一版那 8 條之外再加）
9. **小點數量＝該族群的成分股檔數**（抽三個族群逐一比對，不准用「+N」省略）。
10. **預設不顯示個股文字**；`hover` 族群大點之後，該族群的個股名稱／漲幅／占比**真的出現**，移開**真的收回**。
11. **佈局撐滿**：節點外接盒的寬與高各自 ≥ 畫布的 80%，四個寬度（1440／1100／800／390）都成立。
12. **置中誤差 ≤ 2%**，改變視窗寬度後重算仍成立。
13. 右側資訊欄：族群模式有漲跌／占比／代表公司／顯示範圍／關係類型／以此節點為中心；個股模式有所屬族群與環節。
14. **資訊欄裡每一個該有連結的都有**（逐條檢查 `<a>` 與 href 正確）。
15. 四種風格各切一次，底色／節點／連線的 computed style 真的變；深淺兩主題各驗一次。
16. 關係類型三個勾選框：取消勾選，那一類的線**真的從 DOM／畫布上消失**。
