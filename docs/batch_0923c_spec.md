# 批次 0923-C 規格書（Andy 2026-09-23 第十批，五件）

Andy 原話（逐字）：
> 幫我將 產品剖析圖 、族群預覽 、配色拿掉、另外收合圖 移動到上方同一排
>
> 傳產與內需 & 基礎建設與能源 2D 圖並沒有調整適當大小，請去調整，調整適當範圍，別浪費空白
>
> 金融麻煩也生成2D圖
>
> IC 封裝剖析 這頁似乎沒做好，請確認
>
> 另外圖五 下方的關聯圖，幫我退版 回到之前的格式，但是需要將關聯圖置中，並且，
> Default 顯示族群相連標籤個股，點擊後才會跳初下拉清單，並且點擊股票 右側會顯示對應訊息

---

## C1 — 剖析圖那一排：拿掉三個、收合圖併上去

他紅框圈的是剖析圖標題列：
- **拿掉**「產品剖析圖」這個標題（`#dgSec` 的 `<h4>`；圖名 `#dgTitle` 要保留）
- **拿掉**「← 族群總覽」（`#dgBack`）
- **拿掉**「配色：科技」（`#dgPal`）
- **把「收合圖 ▴」（`#dgFold`）移到上方同一排** —— 它現在被擠到第二行。

⚠ `#dgPal` 拿掉之後，剖析圖配色**只剩「跟著全站主題走」**（上一批 W3-10 剛做的自動切換）。
那正好是他要的：他不想要一顆手動切的鈕，他要它跟著主題。
所以 `#dgPal` 是**整顆移除**，不是藏起來；但 `themePal()` 那條自動切換的路徑**一行都不准動**。

⚠ `#dgBack` 拿掉之後，回族群總覽只剩上方分頁列的「族群總覽」那一格 —— 它本來就在，
所以導覽不會斷（跟上一批移除「← 返回」同一個理由）。

## C2 — 傳產與內需、基礎建設與能源的 2D 圖再調一次

上一批（W8）已經補過一輪，**他看過之後說還是沒調好**。以他的為準，再做一次：

| 檔位 | 檔案 |
|---|---|
| `petrochemical`（輕油裂解廠：一進多出與裂解價差） | `site/dg/petrochemical.js` |
| `heavy_electric`（電力路徑：變壓器與 GIS） | `site/dg/heavy_electric.js` |

從他的截圖看，**真正的問題不是「框裡有空白」，是整張圖的尺度**：
這兩張還是 `native: 980`，而這一批其餘 12 張已經收到 **660／700**，
所以它們在同樣的欄寬下被壓得比別人小、四周留白比別人多。
**做法跟那 12 張一致**：`native` 收窄、說明改用 `rs` ＋ `extRow` 外掛成 HTML 卡片、
畫布只留「畫」的部分。不要再用「在框裡補小圖」的方式塞空白。

## C3 — 金融鏈補 2D 圖

`financial` 鏈（`pipeline/groups/groups.yaml`：`order: [bank, life_fhc, securities_fhc]`）
目前一張剖析圖都沒有。三個族群各補一張，**`scene: null`，不做 3D**
（比照軟體鏈那四張，他當時也只要 2D）。

金融沒有實體零件，跟軟體鏈同一個道理：**每一張先寫 `q`（回答哪一個具體問題），再照問題長出骨架**。

## C4 — IC 封裝剖析那一頁沒做好

`ai_adv_packaging`（`site/dg/ai_adv_packaging.js`）。他的截圖顯示：
左欄五張卡片正常，**主剖面被切掉右半邊**，右側章節列（①～④）擠在畫布右緣。
這張在 W5 被改成 v2 版面時，主剖面整組套了 `translate(-175,0)`；
高度嫌疑是**那個位移與新的 660 畫布寬對不起來**，或 `externalize()` 之後 viewBox 沒跟著收。
**先重現、再修**，不要憑推論。

## C5 — 產業關聯圖退版

他要的四件事：
1. **退回之前的格式**。判讀：他同一句話裡接著講的三件事，
   跟 2026-09-23 第一批他自己下的規格**完全一致**
   （「Default 只顯示族群名稱，且下面是標籤式列出個股，直到點擊才是個股清單」），
   所以「之前的格式」＝**族群節點旁邊直接掛「標籤式的個股名」**，
   而不是現在這種「只有小圓點、要 hover 才知道是誰」。
2. **關聯圖置中**。
3. **Default 顯示族群與相連的標籤式個股**；**點擊之後才跳出個股的下拉清單**。
4. **點個股 → 右側顯示對應資訊**（這條現在就有，不准弄丟；資訊裡的連結也要留著）。

⚠ 星際／泡泡兩種風格是他 2026-09-23 指定保留的（「保留星際及泡泡」），**不要一起砍掉**；
退的是「預設只有小圓點」這件事，不是把風格砍回去。

---

## 驗收

| 這批碰到什麼 | 要跑的段落 |
|---|---|
| C1 剖析圖工具列 | `產業`、`族群頁`、`批次29-產業分頁` |
| C2／C3／C4 剖析圖 | `產業`、`產業鏈導覽`、`新-產業與個股` |
| C5 關聯圖 | `批次25-關聯圖`、`產業關係面板`、`縮放掃描`、`手機` |

一律 `--workers 1`；`_preview.py` 照跑。純前端改動可跳過 pytest（用 `git diff --name-only` 判定）。

---

# 追加（Andy 2026-09-23 第十一批，七件）—— 幾乎全在 `site/app.js`

> 資金輪動已經有一個時間拉 Bar 幫我合併
> 圖二 格式要改成 我點選領先，底下資訊是顯示在旁邊的，另外「最近 5 個交易日換階段的族群」拿掉
> 估值篩選 拿掉
> 漲跌幅需要多一個「即時」Mode
> 上方分頁任務板移除
> 總覽 輪動階段 上方的「放大」移除，並且需要圓圈大一點，
> 下方的紅框處改成昨日的資金去向分流圖（不需要動畫，只顯示線條粗細即可，並且只顯示到族群即可。個股也不用）

## D1 — 資金輪動的兩條時間拉桿合併成一條

他紅框圈了兩條：左邊「看哪一天 － ▬▬ ＋ 最新 ▶ 即時」、右邊「最近 ▬▬ 1 天 ☑顯示軌跡」。
兩條都在講時間，合併成**一條**。
- 合併後要同時交代得出「看哪一天」與「軌跡回溯幾天」——
  做法自己決定（例如一條雙把手的區間桿：左把手＝軌跡起點、右把手＝看哪一天），
  **把你的做法與理由寫進註解**。
- 「最新」「▶ 播放」「即時」「顯示軌跡」這幾個控制項**功能不准掉**。
- ⚠ `ROT_BOARD_WIN`（象限卡的 5 天比較窗長）**與這條桿無關**，不要被合併進去。

## D2 — 象限展開面板改到圖的右邊；移除「最近 5 個交易日換階段的族群」

- 他紅框標出時鐘右側那塊空白，要求**點「領先」之後的展開面板出現在那裡**，
  不是像現在出現在時鐘下方。
- **「最近 5 個交易日換階段的族群」那一排整排拿掉**（`#rotMove`）。
  ⚠ 上一批我交代過「不准讓它消失」，**他現在親口要求移除，以他的為準**。
- 面板移到右側之後仍然不可以蓋住「資金流向排行」，也不可以把時鐘擠變形；
  窄畫面（≤1100px）放不下就退回時鐘下方，那是合理的退讓。

## D3 — 估值篩選整塊移除

`#flow`（或它所在的頁）的「估值篩選：本益比／股價淨值比／ROE 一起篩」整張卡片移除，
連同只服務它的程式碼（`gval` 那一組、它的晶片列、散點圖、右側表格、`清除條件`）。
先 grep 確認沒有別處在用再刪。

## D4 — 漲跌幅多一個「即時」模式

市場明細的「漲跌家數／漲跌分佈」目前只有盤後結算值，要多一個**即時**切換
（比照資金流向那兩張圖的「昨天／即時」）。
- 即時的分母與口徑要**誠實標示**（能抓到的只有部分成分股，不是全市場），
  比照既有 `rlvStamp()` 那種寫法把涵蓋率印在畫面上。
- 盤後仍是預設。

## D5 — 上方分頁移除「任務板」

導覽列拿掉 `任務板`。`site/tasks.js` 與路由 `#tasks` **先不要刪**（只是入口收掉），
但要確認拿掉入口之後沒有別處連過去會變成死連結。

## D6 — 總覽的「輪動階段」：移除「放大」、圓圈放大

- 總覽頁右側「輪動階段」卡片上方那顆「⤢ 放大」移除。
- 那張小時鐘的**圓圈要大一點**（現在太小看不出誰是誰）。

## D7 — 總覽下方那四格階段卡 → 改成「昨日的資金去向分流圖」★ 最大一件

他紅框圈的是總覽右下「改善／領先／轉弱／落後」那四格卡片。整塊換成一張**分流圖**：
- **資料是昨日（盤後結算）**，不是即時。
- **不要動畫**。
- **只用線條粗細**表示流量大小（不要漸層、不要流動點、不要發光）。
- **只畫到族群那一層就好，不要展開到個股。**
- 可以沿用資金去向那張桑基／河流的資料來源與畫法，但**砍成靜態、砍掉個股層**。

---

## 驗收（這一追加）

| 碰到什麼 | 段落 |
|---|---|
| D1／D2 | `資金流向`、`新-資金流向`、`新-輪動時鐘`、`批次2`、`批次3`、`批次7` |
| D3 | `資金流向`（估值篩選的斷言要一起清掉，不然必紅） |
| D4 | `市場明細` |
| D5 | 全站導覽相關：`總覽`、`任務板`（那一段要停用或改寫） |
| D6／D7 | `總覽`、`新-大盤三張圖` |

`_preview.py` 照跑。純前端可跳過 pytest。
