# 手機資訊架構：一屏可讀性稽核與改版（`docs/mobile_ia.md`）

> 起因（Andy 2026-09-24 逐字）：
> 「CEO 幫我找一名手機UI專家，我需要你幫將手機頁面整理得更直觀順暢，功能分清楚，
> 可以多點頁面沒關係，但不要太多文字，整體要看起來舒服 不要發生我看圖 結果用資訊我要一直滑下去來回看，
> 會很困擾，可以留下重要資訊就好 ，以圖為主」
>
> 這一份接在 `docs/mobile_audit.md` 後面。前一份處理的是「功能在手機上消失」（G1～G9）；
> 這一份處理的是**「功能都在，但要滑很久才找得到、看圖跟看數字不在同一屏」**。
>
> ★ 前提沒有變：**桌機不准被降級**（Andy 2026-09-23：「除了桌面不可以遷就手機」），
> 所以下面每一條都只在 `@media (max-width:820px)` ／ `innerWidth <= 820` 的分支裡，
> 而且要用像素比對證明桌機沒被動到。

## 怎麼量的

Playwright，**390×844 ＋ `is_mobile` ＋ `has_touch` ＋ DPR 2**（不是只把視窗縮窄）。
可視畫布 ＝ 844 − 頂欄 72 − 底部兩列分頁 102 ≈ **742px**，那就是「一屏」。

逐路由量三件事：
1. **整頁高度**（`document.documentElement.scrollHeight`）
2. **主圖的 `getBoundingClientRect()`** —— 進頁時它的頂在哪（＝要先捲多少才看得到圖）
3. **把主圖捲到畫面頂端之後，它的關鍵數字在哪** —— `bottom ≤ 742` 才算「同屏」

截圖：改版前 `/tmp/m-before/`、改版後 `/tmp/m-after/`（含 `probe.json` 原始量測）。

---

## 稽核表：改版前「要滑多少」

| 路由 | 整頁高 | 主圖 | 主圖位置 | 進頁到主圖要捲 | 關鍵數字在哪 | **圖↔數字距離** |
|---|---:|---|---|---:|---|---:|
| `#stock/2330` | 4245 | K 線 `#chartWrap` | 1024–1683 | **952** | 現價／漲跌／技術分 `#skPx` 127–596 | **1087** ★最嚴重 |
| `#overview` | 9233 | 大盤三張圖 `#m3c-*` | 297–557 | 0 | 卡內 `.m3-nums` 181–289 | 0（這一段本來就對） |
| `#overview` | | 資金熱力圖 `#heat` | 1292–1712 | **1220** | 點方塊才出面板 | — |
| `#overview` | | 輪動時鐘 `#rotClockMini` | 1983–2343 | **1911** | 象限卡在圖內 | — |
| `#overview` | | 昨日資金去向 `#ovFlow` | 2445–2887 | **2373** | 同一張卡的上半是時鐘 | — |
| `#flow` | 4129 | 輪動時鐘 `#rotClock` | 489–929 | 417（控制項佔 401） | 象限卡在圖內 | — |
| `#flow` | | 資金排行 `#rankFlow` | 1019–1439 | **947** | — | — |
| `#flow` | | 桑基 `#sankey` | 1733–2429 | **1661** | — | — |
| `#industry` | 1932 | 族群長條 `#gpBar` | 516–1040 | 444 | `#gpNote` 410–480 | 36（OK） |
| `#industry` | | 占比圓餅 `#gpPie` | 1097–1621 | **1025** | `#gpFocus` 1638–1679 | 17 |
| `#industry/<chain>` | 2579 | 剖析圖（預設收合） | — | — | — | 關聯圖那一段自己 1717 |
| `#season` | 6131 | 月份熱力 `#seasonHeat` | 283–923 | 211 | `#seasonNote` 在圖下 | 圖 640 高，結論被推出畫面 |
| `#season` | | 本月最強族群清單 | 1395–5981 | **1323** | — | 清單本身 4586 |
| `#delivery` | **11299** | 無圖，43 筆逐筆卡片 | — | — | — | `#dlvWrap` 自己 10754 |
| `#themes/<id>` | 1778 | 題材剖析圖 `#themeDiagram` | 900–1403 | **828** | — | — |
| `#market` | 1835 | 漲跌家數 `#chgDist` | 650–910 | 578 | — | — |
| `#themes` | 925 | `#themeMap` | 282–742 | 210 | — | OK |
| `#heatmap` | 1009 | `#indTree` | 282–842 | 210 | — | OK |

**結論**：他抱怨的形態就在第一列 ——
**個股頁看 K 線時，現價與技術分在 1087px 之外**，要看數字就得滑上去、看完再滑回來。
其次是「一張卡放兩件事」（總覽的輪動階段、產業地圖的長條＋圓餅）與
「一頁把五、六件事串成一條直線」（總覽 9233、交付清單 11299）。

---

## ★★ 主軸：一條決策動線（2026-09-24 CEO 拍板）

> 背景：Andy 指定對標 Tide 潮汐（台股板塊輪動）。
> 「大致框架要一樣……因為他的軟體有個核心主軸，所以我們必須也要有一個」。
> ⚠ **我沒有打開過它的畫面**（這個容器的出口被擋，`WebFetch`／`curl` 回 EGRESS_BLOCKED），
> 所以下面沒有任何一句是「比對過它的介面」—— 主軸是從**我們自己的 `CLAUDE.md`** 長出來的。

**我們的主軸不是一張圖，是一條動線**：

| 步 | 問題 | 對應 `CLAUDE.md` | 這一步的主圖 |
|---|---|---|---|
| ① | 錢往哪個族群跑 | M1 資金面 | **輪動時鐘（RRG 四象限）** |
| ② | 那個族群現在貴不貴 | M2 基本面 | 大盤三張圖 ／ 市場寬度 ／ 法人連續買超 |
| ③ | 什麼時候進場 | M3 技術面 | 今日候選（A 回檔承接 ／ B 突破追進） |
| ④ | 有沒有理由不進場 | M4 事件面 | 今日事件（新聞／法說／券商目標價） |

這條動線本來就寫在 `CLAUDE.md` 第一段，只是介面上從來沒講出來 ——
所有圖表平鋪在同一頁，使用者要自己猜先看哪一張。這一版把它變成**看得見的順序**：

- 首頁最上面釘一條 `.mspine`（四顆，各帶序號與一句話），**一次只顯示一步**。
- 每一步的最下面有一顆「**下一步：② 貴不貴 ›**」—— 看完一屏自然走到下一屏，
  不必回頭找分頁。走到第④步之後變成「回到 ① 錢往哪跑 ›」，動線是一個環。
- 第①步的主圖是**輪動時鐘**（原本埋在總覽中段，要捲 1911px 才看得到），
  圖的正上方補一列四個象限的族群數（改善／領先／轉弱／落後），
  數字與資金流向頁那張大圖**同一個來源**（`rotRows` ＋ `ROT_BOARD_WIN`），不另算一套。

### 每一個既有功能掛在哪一步（一個都沒有消失）

| 步 | 首頁上直接看得到 | 點進去看完整版 |
|---|---|---|
| ① 錢往哪跑 | 輪動時鐘、昨日資金去向、資金熱力圖、熱門題材 | `#flow` 資金流向（排行／桑基／族群×法人／集中度）、`#industry` 產業地圖、`#heatmap` 熱力圖、`#themes` 題材 |
| ② 貴不貴 | 大盤三張圖＋六格 KPI、市場寬度、法人連續買超 | `#market` 市場明細、`#season` 季節性、個股頁的本益比／同業分位／營收 YoY |
| ③ 何時進場 | 今日候選（預設 6 筆，可看全部） | `#stock/<code>` 的 K 線、多週期 SMC 判讀、技術面訊號 |
| ④ 別進的理由 | 今日事件（前 8 則） | 「⋯」→ 今日事件抽屜（全部 43 則、可依分類與日期篩）、個股頁的公告／新聞 |

> ⚠ 界線（CEO 硬性）：**框架可以像，識別不准抄。**
> 全站沒有出現「潮汐／漲潮／退潮／觀望」那套命名，配色沿用我們自己的 `STAGE`
> （改善 cyan／領先 up／轉弱 amber／落後 down，**紅漲綠跌沒有動**）。
> 差異點寫在動線本身：**它停在第①步，我們走完四步。**

## 這一版做了什麼

全部只在 ≤820px 生效，程式在 `site/app.js` 的 `applyMobileIA` 一帶、樣式在 `site/index.html` 末段。

### ① `.mpager` 分段導覽 —— 一屏回答一件事（對應他說的「功能分清楚」「可以多點頁面沒關係」）

每一頁最上面釘一條分段列（44px 觸控高、目前這一段會捲進視野），**一次只顯示一段**。

| 路由 | 分成幾段 |
|---|---|
| `#overview` | **兩層**：上層四步動線（見上一節），下層是那一步底下的分段<br>① 輪動時鐘／資金去向／熱力圖／熱門題材　② 大盤／市場寬度／法人買超　③ 今日候選　④ 今日事件 |
| `#flow` | 輪動 ／ 資金去向 ／ 法人 ／ 集中度 |
| `#industry/<chain>` | 剖析圖 ／ 關聯圖 |
| `#stock/<code>` | K 線 ／ 判讀 ／ 財報籌碼 ／ 產業鏈 |
| `#themes[/id]` | 題材熱力 ／ 題材細節（從熱力圖點一格進來會自動翻到細節） |
| `#season` | 月份熱力 ／ 逐年明細 ／ 最強族群 |

規則：
- 沒有被任何一段列到的元素（麵包屑、頁首說明）**永遠顯示** ——
  漏寫只會多顯示，不會讓東西消失。這是刻意選的失敗方向。
- 選到第幾段寫進 `localStorage`（`tw.mia.<key>`），回同一頁停在原地。
- 換段之後補一輪 `resize()`：ECharts／lightweight-charts 在 `display:none` 裡量到的寬度是 0，
  不補就是一張空白圖。
- 「輪動階段」那張卡在桌機是**一張卡兩件事**（上半時鐘、下半昨日資金去向，量到 1059px），
  手機把它拆成兩段；卡片外殼 `#ovRotCard` 同時列在兩段裡，所以不會留下空卡片。
- 產業地圖頁（`#industry` 不帶鏈）抓不到 `#dgPick`／`#relSec`，
  分段數 < 2 會自動不掛分段列 —— 不必為它另外寫判斷。

### ② `.mmore` 長清單限筆 —— 「留下重要資訊就好」

| 清單 | 改版前 | 預設只留 |
|---|---:|---|
| 今日候選 `#candCards` | 30 張卡 4335px | 6 張 |
| 季節性最強族群 `#seasonTop` | 4586px | 6 個 |
| 交付清單 `#dlvWrap` | 43 筆 10754px | 5 筆 |
| 產業鏈環節 `#chainList` | 1129px | 4 格 |

按「還有 N 筆 · 看全部 ›」全部回來。**收起來不是刪掉**；換頁回來會回到精簡版。

### ③ `.mfold` 長說明收合 —— 「不要太多文字，以圖為主」

| 收起來的東西 | 改版前高度 | 入口 |
|---|---:|---|
| 個股頁的產業鏈／族群／題材連結 ＋ 評級理由（`#skMeta`／`#skVerdict`） | 約 380px | 「詳細（產業鏈 / 族群 / 評級）▾」 |
| 個股頁兩段操作說明（`.skhelp`） | 169px | 「怎麼操作這張圖 / 資料到哪一天 ▾」 |
| 產業鏈頁的頁首說明（`#nbIntro`） | 146px | 「這一頁怎麼看 ▾」 |
| 產業鏈頁關聯圖的說明（`#relHint`） | 208px | 「關聯圖怎麼看 ▾」 |
| 資金流向的篩選列＋期間列（`#flowRotFilter`／`#flowRotTime`） | 233px | 「篩選與期間 ▾」 |

⚠ 收起來的那一塊如果被分段導覽藏起來，**那顆鈕也要一起藏** ——
不然會出現「一顆孤零零的鈕，按了畫面什麼都不會變」（就是 `mobile_audit.md` G7 那個毛病）。

### ④ 個股頁：現價那一列釘在 K 線上方（★ 他點名的那一件事）

三步：
1. `#skPx`（現價／漲跌／技術分／本益比／同業分位／營收 YoY／資料等級）
   **搬成 K 線卡的直接子項**，再 `position:sticky; top:58px`。
   搬節點是必要的：**`sticky` 只在它父元素的盒子裡有效**，
   留在 `#skIdent` 裡實測只釘得住 40px 就跟著捲走（量到 `top:-26`）。
   搬的是**同一個節點**不是複製 —— `#pxNow` 帶著 `data-live`，複製會讓即時層更新到藏起來的那一份。
   回到桌機會原封不動搬回原位（記了原本的 parent 與 nextSibling）。
2. `#skPx` 排成**一列可左右滑**（原本四個 pill 換行成三列、整塊 180px）。
   ⚠ 這一步差點闖禍：nowrap 讓 `#skIdent` 的 min-content 變成 782px，
   flex 子項預設 `min-width:auto` 不准縮到 min-content 以下 → 版面寬度變 803px →
   **手機瀏覽器把整頁縮到 48% 才塞得進 390px**（實測 `innerWidth` 803）。
   補 `min-width:0; max-width:100%` 才擋住。這一條寫下來是因為它不會被「整頁橫向捲軸」那條驗收抓到。
3. 工具列 `#skTools`（11 顆）由換行 404px 收成**一列可左右滑**，一顆都沒少。
4. K 線高度手機收成 `clamp(380px, 100vh - 343px, 620px)`（桌機那兩條沒動），
   讓「分段列 ＋ 釘住的價格列 ＋ 工具列 ＋ 整張圖」剛好放進一屏。

### ⑤ 產業地圖：長條圖與圓餅圖改成左右滑

`.gpgrid` 直立堆疊 1150px → 一次一張、露出下一張的邊（跟大盤三張圖同一套做法）。
兩張回答的是同一個問題的兩面，並排比對才有意義，手機用滑的達成。

### ⑥ 左右滑的位置指示（他點名的）

`.swipetip` 後面補「→ 第 N 張 / 共 M 張」，位置是量出來的（`scrollLeft ÷ 一張的寬度`）。
只給「一張一張捲」的卡片列（`.m3-grid`、`.gpgrid`）；
分頁列與表格不給 —— 「第 3 顆 / 共 8 顆」對分頁沒有意義，只會變雜訊。

---

## 改版後量到的結果

| 路由 | 整頁高（前 → 後） | 倍數 |
|---|---:|---|
| 總覽 | 9233 → **924** | **0.10** |
| 交付清單 | 11299 → **2148** | 0.19 |
| 季節性 | 6131 → **1098** | 0.18 |
| 個股 | 4245 → **1142** | 0.27 |
| 資金流向 | 4129 → **1464** | 0.35 |
| 單一產業鏈 | 2579 → **1672** | 0.65 |
| 題材細節 | 1778 → **1131** | 0.64 |
| 產業地圖 | 1932 → 1914 | 0.99（左右滑撤回，見下） |
| 市場明細 | 1835 → 1817 | 0.99（這一版沒處理） |
| 熱力圖 | 1009 → 991 | — |
| 題材 | 925 → 959 | —（多了分段列的高度） |

### 一屏可讀性（把主圖捲到畫面頂端後量，可視高 742px）

由 `scripts/_uitest.py` 的新段落 **`手機一屏`** 逐條驗，**驗的是座標不是「元素存在」**：

| 路由 | 主圖 | 圖的位置 | 關鍵數字的位置 | 判定 |
|---|---|---|---|---|
| 個股 | `#chartWrap` | 144–645 | `#skPx` **58–123（釘住，捲到頁尾還在）** | 圖整張進屏 ＋ 數字同屏 |
| 總覽（第①步） | `#rotClockMini` | 132 起 | `#ovRotKpi`（改善／領先／轉弱／落後 四個數字） | 同屏 |
| 資金流向 | `#rotClock` | 132–572 | 象限卡在圖內 | 同屏 |
| 產業地圖 | `#gpBar` | 132–656 | `#gpNote` 26–96 | 同屏 |
| 題材 | `#themeMap` | 235–695 | `#themeNote` 178–235 | 同屏 |
| 季節性 | `#seasonHeat` | 132–612 | `#seasonNote` 緊接在下 | 同屏 |

**整頁橫向捲軸：11 個路由 × 390／1440 全部是 0**（沒有變壞）。

### 做了又撤回的兩件事（被既有驗收擋下來，記在這裡不要重做）

1. **產業地圖的長條圖＋圓餅圖改成左右滑**：390px 從 1932 收到 1358，
   但撞到既有的 W3-8「[390px] 兩張卡片真的上下堆疊（不是硬擠成兩欄）」。
   那是上一批依 Andy 的要求訂的，**既有拍板優先**，整段撤回。
2. **單一產業鏈頁的分段導覽（剖析圖／關聯圖）**：390px 從 2561 收到 844，
   但它把 `#segChips`、`.seglist` 收到第二段，讓「批次29-產業分頁」「產業關係面板」
   「新-產業與個股」三段當場 `element is not visible`。撤回，改成只收兩段長說明
   （2561 → 1672）。要真的分段，得連那三段驗收一起改，屬於另一批。

### 三個踩到、寫下來免得下一個人重踩的坑

1. **`position:sticky` 的 `top` 要扣掉頂欄。** `.topbar` 是 `sticky; top:0; height:58px; z-index:40`，
   所以任何要「釘在頂欄下面」的東西都得寫 `top:58px`。第一版寫 0，結果整條釘到頂欄**底下**去，
   一捲動就看不見了（但它其實還在，量得到 `top:0`）—— 這種 bug 用「元素存在」驗不出來。
2. **釘住的元素不可以掛 `.hsc` 的淡出。** `.hsc` 是 CSS `mask`，它作用在整個盒子、
   **連背景一起淡掉**；釘住的列右緣半透明之後，底下捲過去的工具列會從那裡透出來
   （實測看到「技術分 88」後面疊著「4時」）。所以 `#skPx` 只吃 `overflow-x:auto`，不吃淡出。
3. **要滿版就不能留 `max-width:100%`。** `margin:0 -18px` 想把底色撐到卡片邊緣，
   但 `max-width:100%` 會把寬度壓回內容寬 —— 右邊留一條沒有底色的縫，一樣會透出底下的東西。
   正解是 `margin:0 -18px; width:calc(100% + 36px)`，**不要**寫 `max-width`。

### 斷點為什麼是 640px 而不是 820px

**分段／限筆／收合這一套只在 ≤640px 生效**，不是 820px。
理由是量出來的：既有驗收有一整批段落在 **800px** 跑
（「800px 時面板掉到圖的下面」「800px／產業鏈圖收合 K 線圖排在產業鏈區塊上面」
「800px 窄畫面照樣點得到個股標籤」…）—— 那是**把桌機視窗縮一半**的情境，不是手機。
在那個寬度把內容收進分段列會讓那些斷言全部變紅，而且它們是對的：800px 有寬度一次看完。
820px 那一批（左右滑提示、底部兩列分頁、字級下限）維持原樣，一個字都沒動。

---

## 順帶做完的桌機 UI 精修（`docs/ui_polish_spec.md` 第一階段第 7、8 項）

CEO 2026-09-24 指派：那兩項的檔案正好在這一批手上，一起做完。

**第 7 項 焦點樣式 —— 量下去發現已經在 main 上了，這一批沒有改動。**
1440px 量到 `.tab`／`#q`／`.evbtn`／`.howbtn`／`a.lk` 聚焦後都是
`outline: 2px solid rgb(62,224,255)`、`outline-offset: 2px`（`--focus` #3ee0ff，對 `--panel` 對比 11.30）。
規格說的「`.tab` 聚焦後 outline-width 是 0px」在這一版量不到了 ——
它在 2026-09-23 的「UI精修0923」那一批就修掉了（`:focus-visible:focus-visible` 那兩條）。
`.pill` 量到 0px 是**預期的**：它是 `<span>`，本來就不可聚焦。

**第 8 項 小字升到 12px —— 這一批做的，桌機與手機一起。**

| | 改前 | 改後 |
|---|---:|---:|
| 1440px 全站小於 12px 的文字節點 | **132**（16 種選擇器） | **0** |
| 390px 全站小於 12px 的文字節點 | 0（上一批修過） | 0 |

改前最密的幾種：`em` 11.5px×26、`.lb`／`.sg` 11.5px×20、`.cnt` 11.5px×13、
`.more` 11.5px×12、`em` 11.0px×10、`.rl` 11.0px×5、`.zbadge` 11.0px×4。
做法是把上一批那條「手機字級下限」的選擇器清單**拿掉 `@media (max-width:820px)` 的包裝**，
桌機一起吃 —— 不是另外寫一套，兩邊永遠同一份清單。
⚠ 這會讓**桌機的像素變動**（字大 4%）。那是刻意的改動，不是退化，
所以下面的桌機比對報告把它單獨列出來，不混進「零差異」。
⚠ 刻意不含 `.chainmap .co .sub` 與 `.chainmap .fct`（SVG 文字，座標由 industry.js 依字級算好，
抬字級會壓到隔壁節點），留到下一版連同排版一起改。

**第 9 項（表格分隔線／列高／表頭吸頂）留給第二波。**

---

## 刻意留到下一版（不是忘記）

1. **資金流向的「資金輪動」那張卡仍然 1170px。** 收掉控制項之後剩下的是
   時鐘（468）＋ 排行（490）兩張圖。要再短就得把它們拆成兩段，
   但 DECISIONS 有一條「這兩張合併、共用同一份篩選」是 Andy 自己拍板的
   （理由是點排行的長條時要同時看到它在時鐘上的位置）—— **不該由我推翻**。
2. **市場明細 1817px 沒動。** 它是「一張圖 ＋ 一張完整名單」，
   名單本來就是這一頁存在的理由；要短就得分頁，那是換這一頁的定位，該問過再做。
3. **交付清單雖然收到 2148，頁首那張卡仍佔 311px**（標題 ＋ 三行說明 ＋ 六顆篩選）。
   說明那三行可以收，但它寫的是「★ 有 2 件在等你」—— 那是要他看到的。留著。
4. **分段列與「看全部」都沒有做記憶以外的狀態同步。**
   例如在「候選」那一段按了看全部，切走再切回來會回到 6 張。這是刻意的（回到精簡）。
5. **`.chainmap` 的 SVG 文字仍然 11px**（`mobile_audit.md` 第三項留的），
   抬字級要連座標一起改，屬於 industry.js 的排版工作，不混在這一版。

## 桌機沒被降級的證明（以及這個證明的邊界）

做法跟 `docs/mobile_audit.md` 那一次一樣：把 `site/index.html`、`site/app.js`、`site/industry.js`
從 `git show HEAD:` 還原成一份原版網站（`/tmp/site-orig`），與改版後的網站各起一個本機伺服器，
在 **1440×950** 把 11 個路由各截一張整頁圖逐張比對。
這一次多做兩件事：① 用 `reduced_motion="reduce"` 開瀏覽器（把動畫這個最大的雜訊源關掉，
雜訊底線從一萬多個像素降到 0～22）② **把第 8 項（小字升 12px）單獨拆出來比**，
因為那一項**本來就會改到桌機**，混在一起量會看不出「手機改版有沒有外洩到桌機」。

**A. 只有手機改版、第 8 項還沒套用**（`/tmp/site-nofont`）：

| 路由 | 原版 vs 新版 | 判定 |
|---|---:|---|
| 個股 | **0** | 完全相同 |
| 題材 | **0** | 完全相同 |
| 題材細節 | **0** | 完全相同 |
| 季節性 | **0** | 完全相同 |
| 交付清單 | **0** | 完全相同 |
| 市場明細 | **0** | 完全相同 |
| 熱力圖 | **0** | 完全相同 |
| 總覽／資金流向／產業地圖／單一產業鏈 | 那一輪抓到雜訊（事件側欄被收起來、頁高差幾 px） | 見下 |

那四頁的差異**不是改動造成的**：把交付的那一版連拍三次，`#overview` 三張圖
**兩兩完全相同（0 個像素）**、頁高都是 3202、`#layout` 的 class 都是 `layout`（側欄是開的）——
也就是交付版本本身是穩定的，那一輪是抓圖時的暫態。

**B. 加上第 8 項（小字 11／11.5px → 12px，桌機也吃）**：

| 路由 | 差異像素 | 佔比 | 來源 |
|---|---:|---:|---|
| 個股 | 8584 | 0.25% | 小字變大 |
| 產業地圖 | 8521 | 0.58% | 小字變大 |
| 市場明細 | 3443 | 0.26% | 小字變大 |
| 題材／題材細節／熱力圖 | 各 735 | <0.06% | 小字變大 |
| 季節性／交付清單 | 0 | — | 那兩頁沒有 11px 的字 |
| 總覽／資金流向／單一產業鏈 | 頁高 +3～4px | — | 小字變大把卡片撐高 |

**這些差異是刻意的**（CEO 指派的第 8 項），不是退化 —— 而且方向是「字變大、更好讀」，
不是把桌機遷就手機。A 表證明「手機改版本身一個桌機像素都沒動」。

**程式化的守門**：`scripts/_uitest.py` 的 **`桌機零差異`** 段落在 1440 與 1280 兩個寬度
檢查「手機才有的東西一個都不出現、桌機原有的東西一個都不少」——
這一批跑**0 個問題**。它擋的是「以後有人把手機的規則寫到桌機去」。

### ⚠ 這個證明的邊界（2026-09-24 再確認一次）

1. **只涵蓋各頁的「初始畫面」。** 點開之後才出現的東西（下拉、面板、放大視窗、
   剖析圖零件卡）**不在像素比對裡**。那一塊靠 `_uitest.py` 的既有段落顧
   （這一批跑過 `個股`、`產業`、`產業關係面板`、`批次29-產業分頁`、`新-產業與個股`，全綠）。
2. **只量 1440px 與 1280px**。1281～1439 之間、以及 1441 以上沒有逐張比。
3. **雜訊已經壓到 0～22，但沒有壓到絕對 0**：桑基圖的小圓點與輪動時鐘仍有動畫，
   `reduced_motion` 只關掉 CSS 那一半。

## 我自己判斷的（Andy 可能不同意，列出來讓他一次否決）

1. **四步的名字與每一步放什麼**是我定的：「① 錢往哪跑／② 貴不貴／③ 何時進場／④ 別進的理由」。
   問題本身來自 `CLAUDE.md`，但**哪一張圖算哪一步**是我判斷的 ——
   例如「大盤三張圖」我放在第②步（它回答的是大盤體質，不是哪個族群在漲），
   「熱門題材」我放在第①步（題材熱度就是資金往哪跑的另一個切面）。
   這兩個都可以搬，改 `MIA_PAGER` 裡的 `s` 一個數字就好。
2. **「② 貴不貴」這一步目前回答得最弱**：族群估值那張卡是 Andy 自己要求拿掉的
   （2026-09-23「估值篩選拿掉」），所以這一步只剩大盤、市場寬度、法人買超，
   個股層級的本益比／同業分位要點進個股頁才看得到。
   要讓第②步真的回答「貴不貴」，得把 `group_valuation.json`（資料還在產，前端沒有讀者）
   接回來 —— 那是推翻他自己的決定，**我不會自己做**。
2. **限筆的數字**（候選 6、最強族群 6、交付 5、環節 4）是我抓的，沒有依據可以引。
3. **K 線在手機從 640px 收成約 500px**：換來「整張圖 ＋ 價格 ＋ 工具列一屏看完」。
   如果他覺得 K 棒變太小，把那條 `clamp` 調大即可，代價是要再滑一下。
4. **「詳細（產業鏈 / 族群 / 評級）」預設收起來**：評級（停損／目標／風報）是重要資訊，
   但它跟 K 線是兩件事，而「判讀」那一段本來就在講同一件事。
   如果他覺得評級應該一進頁就看到，就把它從 fold 裡拿出來（會多 280px）。
5. **題材頁從熱力圖點一格會自動翻到「題材細節」那一段**。
   我判斷那是他點下去想看的東西；如果他想留在熱力圖上繼續點，這條要拿掉。

---

# 2026-09-24（下午）合版回歸掃描：手機第二版 ＋ PWA 合在一起之後

Andy 回報，逐字：「**另外這個手機版我剛剛看體驗那邊有跑掉**」。
只有這一句、沒有截圖，所以第一件事是**自己把它找出來**。

背景：`077f5f0`（手機第二版）與 `9a6888b`（PWA）**各自驗過、合起來沒有**。

## 掃法（量出來，不是看起來像）

390×844 ＋ `is_mobile` ＋ `has_touch` ＋ DPR2，走過 `#overview`／`#flow`／`#industry`／
`#stock/2330`／`#themes`／`#season`／`#heatmap`／`#market`／`#delivery`，
四步動線逐步點過去，每一步都做四類量測：

1. 超出視窗右緣的元素（排除自己就是橫向捲動容器的）
2. 被 `overflow:hidden` 裁掉的文字（`scrollWidth > clientWidth` 且沒有 ellipsis）
3. 葉節點文字的兩兩邊界框重疊（重疊面積 ÷ 較小者 > 30%）
4. 字級 < 11.5px

**深色與淺色各掃一輪、390／660／720／800 四個寬度各掃一輪。**

## 找到 5 個、量到的數字

| # | 在哪裡 | 量到的壞法 | 數字 |
|---|---|---|---|
| **1** | 總覽第③④步 | 只剩一段時分段列仍然畫出**一顆孤單的晶片** | `hidden=true` 但 `display:flex`、高 58px ＋ margin 10px ＝ **68px**（844 可視區的 8%） |
| **2** | 總覽四步全部 | 換段之後留下**高度 0、卻還占著 `margin-top:16px` 的空殼容器** | 每步 2～3 個（`.grid.g21`／`.grid.g12.eqpair`／`.grid.g2`），白佔 **32～48px**；第④步分段列底 202 → 內容頂 **266** |
| **3** | 個股頁釘住的價格列 | 藏了內容卻**沒有任何「可以左右滑」的記號** | `scrollWidth 802 / clientWidth 360` ＝ 藏了 **442px**；第 4 顆「本益比 28.5」起跑點 359.4，被卡片右緣 376 **切一半** |
| **4** | 個股頁 K 線圖例 | **一個數字被折成兩半** | 「MA60 2,400.83」那個 `span` 的邊界框 **244.3 × 33.8** ＝ 橫跨兩行（「MA60」在第一行尾、「2,400.83」在第二行頭） |
| **5** | 季節性月份熱力圖 | 格內數字**整片疊成一團糊**（不是「字小」） | 繪圖區 124 × 440 塞 12 欄 × 76 列 → **每格 10.33 × 5.79px**，標籤 11px（約 30px 寬）→ 橫跨 3 欄、縱跨 2 列，912 個標籤全部相疊 |

★ 1／3／4／5 有一個共同的形態：**程式註解說會做，實際上沒生效**。
讀程式碼看起來是對的，量畫面才知道沒接上 —— 這一類只有「真的量」才抓得到。

- #1：`miaPager` 寫了 `bar.hidden = subs.length < 2`，註解還寫著「一顆孤單的晶片是雜訊」，
  但 `.mpager{display:flex}` 的權重 (0,1,0) 蓋過瀏覽器內建的 `[hidden]{display:none}` (0,0,0)。
- #3：`index.html` 的註解寫「本益比／同業分位／營收 YoY 沒有不見，往右滑就在（`.hsc` 會補淡出）」，
  但 `#skPx` **根本不在 `SWIPE_SEL` 裡**；`.mspine`（藏了 456px）也一樣。

## 修了什麼

| # | 檔案 | 做法 |
|---|---|---|
| 1 | `site/index.html` | 在 ≤820px 的區塊補 `.mpager[hidden]{display:none}` |
| 2 | `site/app.js`（`miaPager` 的 `paint()`） | 套完 `mp-off` 之後再掃一次 view 的直接子項：**本來就有元素子項、而且每一個子項都已經不顯示**的容器，一起收掉。判準刻意保守 —— 沒有元素子項的一律不碰 |
| 3 | `site/app.js` ＋ `site/index.html` | 見下面「★ 這一條我先做錯了一次」 |
| 4 | `site/index.html` | `body.miaon #lwc .legend-ov span{display:inline-block}`：每一項變成不可分割的一塊，要折就整塊折 |
| 5 | `site/app.js`（季節性熱力圖） | `label.show = !mIsM()`；並在 `#seasonNote` 補一句「手機上格子太小，數字收進點擊：點一格看它的逐年明細」。**收起來不是刪掉** —— tooltip 與「逐年明細」都是原本就有的路徑 |

修完的效果（同一套量測）：第④步 `ovEvents` 的 top 從 **266 → 154**（省 112px）；
季節性熱力圖的月份軸標籤（1 月／5 月／9 月）**從被蓋掉變成看得見**；
390px 六個路由的整頁高度全部下降（總覽 962→930、資金流向 1464→1448、
第②步 1349→1301、第③步 1603→1507、第④步 1259→1147）。

### ★ 第 3 條我先做錯了一次 —— 記下來，免得下一個人再踩

第一版的做法是「把 `#skPx` 與 `.mspine` 加進 `SWIPE_SEL`」，讓它們吃 `.hsc` 的淡出。
**那是錯的，而且上一批已經寫在 `app.js` 的註解裡了**：

> ⚠ `#skPx`（個股那條釘住的現價列）**刻意不加**：`.hsc` 的淡出是 CSS `mask`，
> 它作用在元素的整個盒子 ——**連背景一起淡掉**。釘住的列一旦右緣半透明，
> 底下捲過去的工具列就會透出來（實測看到「技術分 88」後面疊著「4時」）。

我回頭驗了一次，**那個現象真的重現**：捲到工具列剛好滑進價格列底下（量到重疊 25.6px），
右緣看得到「5 秒」那顆晶片透出來。

改成**不透明的蓋片**：`::after` 在 flex 容器裡是一個 flex 子項，
用 `position:sticky;right:-18px` 黏在捲動視窗的右緣，
底色從透明收到 `var(--bg)`（＝這一列自己的底色）。
看起來一樣是淡出，但它是**蓋在內容上**，不是讓內容透出來。
捲到最右邊時由 `paintStickyEnd()` 掛上 `.sk-end` 把蓋片收掉
（「已經到底了還在淡」跟沒有提示一樣糟）。

**客觀證據（不是「看起來還好」）**：在價格列正下方塞一條 `#ff0000` 的測試條，
捲到它剛好在價格列底下，然後數右緣 12% 寬度內的紅色像素 ——

| 做法 | 從右緣透出來的紅色像素 |
|---|---:|
| `.hsc`（CSS mask） | **480** |
| 不透明蓋片（採用） | **0** |

`.mpager` 仍然走 `.hsc`，因為它**不是** sticky —— 底下沒有東西會捲過去。

## 掃了但**沒有**問題的（列出來，免得下次重掃）

| 類別 | 結果 |
|---|---|
| **Service Worker 造成的舊資源** | **沒有問題。** 實測「先載 A1、改檔部署 B2、再開」：新 SW `sw.js?v=B2` 接手、`tw-shell-A1` 被刪、頁面與 JS 都是 B2。另外 `sw.js` 是在手機第二版**之後**才進 repo 的，所以「快取到手機第二版之前的 CSS／JS」在物理上不可能發生 —— 那時候還沒有 SW 去快取任何東西。⚠ 第一次測出「換版後還是舊的」是**我的測試寫錯**：`page.goto()` 到只差 fragment 的同一個網址是 same-document navigation，頁面根本沒有重新載入 |
| **淺色／明亮主題** | 幾何**與深色逐項相同**（六個路由的 `docW`／`docH`／溢出／裁切／重疊筆數一模一樣），新元件在淺色下沒有另外的壞法 |
| **641～820px 平板寬**（660／720／800 三個寬度） | 溢出 0、裁切 0。`.mspine`／`.mpager` 依設計**不出現**（`MIA_MAX = 640`）。掃到的「重疊」全部是底部固定分頁列壓在內容上（固定列的正常行為）；「小字」四筆是 10px 的 `▾` 箭頭圖示，不是內文 |
| `#heatmap`／`#market`／`#delivery` | 390px 溢出 0 |
| 限筆＋「看全部」 | 6/30 → 按一次 → 30/30，鈕自己收掉；再繞一圈回來不會壞 |
| 左右滑位置指示 | `.m3-grid` 的「第 1 張 / 共 3 張」正確，真的滑過去之後數字跟著變，`N > M` 不會發生（`Math.min(n-1, …)` 夾住了） |
| sticky 價格列 | 捲到哪裡都釘在 `top:58`，K 線圖（143.6～644.6）與價格列同時在畫面內；離開再回來仍在 |

## 量到、但這一版**刻意沒有動**的（下一版，或要 Andy 先拍板）

1. **K 線圖例佔掉圖的 20%**（`#legendOv` 102px / `#lwc` 501px，兩行邏輯內容折成 5 行，
   壓在 K 棒上）。這一版只解掉「折在項目中間」；要真的瘦下來得改 `industry.js` 的圖例內容
   （例如手機只留「收 ＋ 漲跌 ＋ 量」，開高低交給點擊的 `#ohlcBox`）——
   那是**刪畫面上的字**，超出「修跑版」的範圍，先問過再做。
2. **`.mnext`（下一步）在捲動位置 0 時整顆被底部分頁列蓋住**：
   量到 top 778.4 / bottom 826.4，分頁列 top 741 → 覆蓋 85.4px ≥ 元件高 48px，
   `elementFromPoint` 在它的中心回 `NAV.tabs`（點不到）。要捲 118px 才點得到。
   第①步整頁只有 962px，捲得到，所以**不是壞掉，是「初始畫面看不到方向」**。
   要改就得把它改成釘在分頁列上方的浮動鈕 —— 那會再吃掉一條可視高，**是設計決定不是修 bug**。
3. **資金流向頁大張輪動時鐘的族群標籤在 390px 擠成一直排**（13 個標籤疊在左緣，
   「電源供應器」「CCL 銅箔基板」被「落後 16」那顆徽章壓住）。那是 ECharts 畫在 canvas 上的，
   DOM 掃不到，是用截圖看出來的。**改動 labelLayout 的風險高**（會動到桌機那張同一份設定），
   留到下一版單獨處理。
4. **「看全部」展開之後沒有辦法收回去**（鈕被移除）。換段、換頁也不會歸零
   （`box.dataset.mmOpen` 留著）。這是原本就寫成這樣的行為，不是這次弄壞的。

## 這一批補了哪些斷言（`scripts/_uitest.py` → `手機一屏`）

五條，每一條驗的都是**那個具體的壞法**，不是「元素存在」：

1. 分段列只有 1 段時，**computed display 必須是 none 且高度 0**
   （刻意不驗 `hidden` 屬性 —— 驗屬性的話舊版也會綠，那就是假綠）
2. 每一步掃一次「高度 0、有元素子項、卻還帶外距」的空殼容器，必須是 0 個
3. `#skPx` 藏了內容時右緣必須有淡出（`::after` 的 `background-image` 是 gradient、寬 ≥ 20px），
   **而且那塊淡出必須是蓋片不是 mask**（元素本身不准有 `mask`、不准有 `.hsc`），
   **也不准在它後面插文字提示**；真的把它滑到底，最後一顆要整顆進得了畫面、
   蓋片要收掉（`.sk-end`）、而且滑完還要是 `sticky`
4. K 線圖例裡不准有任何 `<span>` 的邊界框高度 > 行高 × 1.6（＝被折成兩行）
5. 季節性熱力圖：`格寬 ≥ 30 且格高 ≥ 13` 與 `label.show` 必須一致；
   關掉時 `#seasonNote` 要講得出數字去哪裡

**反向驗過**：把 `site/` 還原成修之前，這幾條**紅 9 條**；換回修好的版本，**0 條**。
斷言真的抓得到那幾個壞法，不是陪跑的。
