/* 證券金控：三條腿踩在同一塊板子上 —— 大盤的成交量（族群 `securities_fhc`，financial 鏈）

   ★ 型式：純 2D（`scene: null`）。**Andy 2026-09-23 指定金融這一鏈只要 2D**
     （原話：「金融麻煩也生成 2D 圖」，格式跟軟體鏈那四張一樣、不用 3D）。
     券商沒有實體零件可以繞著看，轉一圈不會多理解任何一件事。

   ★ 風格基準 ＝ AI 伺服器鏈的族群圖（`site/dg/server_psu.js`／`liquid_cooling.js`／
     `switch_wireless.js`），做法沿用同鏈路的 `site/dg/cyber_security.js`／`bank.js`／`life_fhc.js`：
     svg 根掛 `rs` → 說明卡片由 `externalize()` 搬成 HTML（.dgc）排進畫布左右兩欄；
     材質走共用的 `D.fx`；字級吃 `--dg-fs-*`（12px 下限）、行距 16px；
     顏色一律 `--dg-*` token，JS 裡一個色碼都沒有。`native` 660。

   ★ 這張圖回答的問題（`q`）：券商的獲利為什麼跟成交量高度連動？
     骨架＝**一個共同的輸入分成三條腿**，而且三條腿的「連動方式」刻意畫成不一樣：
       最上面是一條量能帶（台股日均成交值），它是這一格所有生意的共同輸入。
       ① 經紀手續費：幾乎是成交金額的線性函數（費率上限 ×（1 − 折讓）），方向單一 ——
          量縮就掉，沒有例外。所以畫成一條直直往下的粗光束。
       ② 自營與投資部位：跟的不是「量」而是「盤勢方向」，可正可負 ——
          所以畫成一條上下擺動的波形，而不是往下的直線。
       ③ 承銷、財富管理、期貨、信用交易利息：比較穩，但也不是完全不受影響
          （IPO 與現金增資的件數看行情、融資餘額跟著量走）。畫成一條較細、有遲滯的線。
     **為什麼三條腿要用三種線型**：如果三條都畫成一樣的箭頭，就只看得到「都跟量有關」，
     看不到他真正要問的「大盤量縮時哪一塊先掉」。線型本身就是答案。
     最下面那一排是「量縮時的掉落順序」，把上面三條腿的差別收斂成一句可用的結論。

   ★ `data-seg` 的處理：**一個都不掛**。`pipeline/groups/supply_chain.yaml` 裡
     完全沒有 financial 這條鏈（機器查的：`grep -n "financial" pipeline/groups/supply_chain.yaml`
     ＝ 零筆命中）。硬掛別條鏈的環節＝在 public 網站上宣稱錯誤的公司對應，所以寧可不掛。
     跟 `cyber_security.js` 同樣的既定取捨，畫面上放一張永遠看得到的警語卡講清楚。

   ★ 事實與出處
     · 經紀手續費率上限 0.1425%（千分之 1.425）：依證券交易法第 85 條，證券經紀商的
       受託買賣手續費費率由證券交易所（集中市場）或證券商同業公會（櫃買市場）申報、
       主管機關核定。多家投資教學平台（CMoney、Mr.Market、口袋學堂等）與公會公告一致
       寫出 0.1425% 這個上限，且各券商普遍以折讓（電子下單折扣）方式競爭。
       **這是一個公告的法定上限、不是任何一家公司的數字**，依批次規格第 5 條可以印在畫面上；
       各家的實際折讓幅度則不寫（逐家不同、還會促銷變動）。
     · 買賣雙邊都收手續費、單筆未滿新台幣 20 元以 20 元計：同上多家平台一致。
       畫面上只寫「買賣各收一次」，20 元低消留在註解 —— 它跟這張圖要回答的問題無關。
     · 這一格為什麼只有 2885 元大金、2883 凱基金，以及為什麼純券商沒放進來：
       `pipeline/groups/groups.yaml` 的 `securities_fhc` 註解逐字寫著
       「兩家之間殘差相關 +0.60，對銀行六大 2885 +0.29、2883 +0.43（中位 +0.39），明顯較低」；
       以及「⚠ 純券商（2855 統一證、6005 群益證、6016 康和證、6023 元大期）沒有放進來：
       實測 6016 對元大金只有 +0.16、6023 只有 +0.05，『受成交量驅動』這個故事
       在它們的股價上量不到，而且日均成交值都在 2.5 億以下」。
       註解也明講「★ 這是本批我最沒把握的一格」以及 2883 凱基金兩邊都高的理由。
       **這種「自己講出不確定」的內容正是該印在畫面上的**，所以章節②原樣寫進去。
     不寫的：各家的經紀市占率、手續費收入占比、自營部位規模、實際折讓幅度 ——
       逐季變動又口徑不一的公司別數字，一個都不印。
     ⚠ 這張圖不寫任何「日均成交值多少億」的水位：那是每天在變的數字。*/
(function () {
  'use strict';
  const D = window.DG;
  if (!D || typeof D.register !== 'function') return;
  const { extRow, note, fold, fx } = D;

  const CW = 660;

  /* ---------------- 小工具（跟 cyber_security.js／bank.js 同一套寫法） ---------------- */
  const R = (x, y, w, h, fill, cls, rx) =>
    `<rect${cls ? ` class="${cls}"` : ''} x="${x}" y="${y}" width="${w}" height="${h}"${rx ? ` rx="${rx}"` : ''} fill="${fill}"/>`;
  const LN = (d, col, w2, extra) => `<path d="${d}" stroke="${col}" stroke-width="${w2}" fill="none"${extra || ''}/>`;
  const T = (x, y, s, cls, anchor, style) =>
    `<text class="${cls || 'sub'}" x="${x}" y="${y}"${anchor ? ` text-anchor="${anchor}"` : ''}${style ? ` style="${style}"` : ''}>${s}</text>`;
  const frame = (x, y, w, h) => `<rect class="frame" x="${x}" y="${y}" width="${w}" height="${h}" rx="9"/>`;
  const part = (id, inner) => `<g data-part="${id}">${inner}</g>`;

  /* 元件色。語意：量能帶用主強調色（它是共同輸入），經紀＝冷色（線性、可預期），
     自營＝熱色（可正可負、最刺激），穩定腿＝鋼色，警示＝警告色。全部是既有 token。*/
  const C = {
    vol: 'var(--dg-accent-2d)', bro: 'var(--dg-cold)', prop: 'var(--dg-hot)',
    stab: 'var(--dg-steel)', margin: 'var(--dg-organic)', warn: 'var(--dg-warn)',
    risk: 'var(--dg-err)', mute: 'var(--dg-mute)', au: 'var(--dg-au)',
  };
  const card = (o) => {
    const s = extRow({ part: o.part, title: o.title, sub: o.sub, no: o.no,
      side: o.side, ax: o.ax, ay: o.ay, color: o.color, order: o.order });
    return o.color ? s.replace('<g class="anc" ', `<g class="anc" style="--dg-card-c:${o.color}" `) : s;
  };

  const LX = 20, W = 620;

  /* ================================================================ ① 共同輸入：市場成交量
     畫成一條帶子 ＋ 一段量能柱：**量能柱的高低是示意的隨機波形，不對應任何真實數列**
     （檔頭最後一條：不寫任何日均成交值的水位）。
     它橫跨整張圖的寬度，因為下面三條腿全部踩在它上面。*/
  const VY = 20, VH = 58;

  function volume() {
    const bars = [26, 40, 33, 52, 44, 58, 47, 36, 50, 43, 55, 38, 30, 46, 34, 24, 18, 27, 21, 31];
    const bw = (W - 220) / bars.length;
    const g = [frame(LX, VY, W, VH),
      T(LX + 14, VY + 22, '共同輸入：台股的成交量', 'hd', null, `fill:${C.vol}`),
      T(LX + 14, VY + 40, '這一格所有的生意都踩在它上面', 'sub')];
    g.push(part('sf_volume', bars.map((v, i) =>
      R(LX + 206 + i * bw, VY + VH - 8 - v * .78, bw - 2.4, v * .78, C.vol, 'part', 1.5)).join('')
      + T(LX + 14, VY + 54, '量能示意，不對應任何真實數列', 'sub')));
    return g.join('');
  }

  /* ================================================================ ② 三條腿
     每一條腿用**不同的線型**表示不同的連動方式（見檔頭「為什麼三條腿要用三種線型」）：
       經紀＝直線光束（線性、單向）｜自營＝波形（雙向）｜穩定腿＝細線 ＋ 遲滯轉折。*/
  const LY = 142, LEGH = 196, LEGW = 196, LEGX = [LX, LX + 212, LX + 424];

  function legs() {
    const g = [];
    const head = (i, t, s, col) => T(LEGX[i] + 13, LY + 24, t, 'lbl', null, `fill:${col}`)
      + T(LEGX[i] + 13, LY + 42, s, 'sub');

    /* 腿① 經紀手續費 —— 從量能帶直直落下來的一條粗光束 */
    g.push(fx.beams([{ d: `M${LEGX[0] + LEGW / 2},${VY + VH} V${LY}`, color: C.bro, w: 3.2 }], { flow: true }));
    g.push(part('sf_brokerage', frame(LEGX[0], LY, LEGW, LEGH) + R(LEGX[0], LY, 3, LEGH, C.bro)
      + head(0, '① 經紀手續費', '幾乎是成交金額的線性函數', C.bro)
      + R(LEGX[0] + 13, LY + 54, LEGW - 26, 44, 'var(--dg-step-f)', 'part', 6)
      + T(LEGX[0] + 22, LY + 72, '手續費率上限', 'sub')
      + T(LEGX[0] + 22, LY + 90, '0.1425%（千分之 1.425）', 'lbl', null, `fill:${C.bro}`)
      + ['成交金額 × 費率 ×（1 − 折讓）', '買賣各收一次，', '上限由主管機關核定（證交法 §85）；',
        '各家以折扣競爭，折讓幅度不一。'].map((s, k) => T(LEGX[0] + 13, LY + 118 + k * 16, s, 'sub')).join('')
      + T(LEGX[0] + 13, LY + LEGH - 10, '連動：最直接、方向單一', 'sub', null, `fill:${C.bro}`)));

    /* 腿② 自營與投資 —— 一條上下擺動的波形，表示「可正可負」 */
    const wx = LEGX[1] + LEGW / 2;
    g.push(LN(`M${wx},${VY + VH} C${wx - 30},${VY + VH + 24} ${wx + 30},${LY - 24} ${wx},${LY}`, C.prop, 3, ' class="flow"'));
    const wy = LY + 72, ww = LEGW - 30;
    const wave = [0, 1, 2, 3, 4, 5, 6, 7, 8].map((k) => {
      const px = LEGX[1] + 15 + k * (ww / 8);
      const py = wy + [0, -16, 9, -20, 14, -6, 18, -12, 4][k];
      return `${k ? 'L' : 'M'}${px.toFixed(1)},${py.toFixed(1)}`;
    }).join(' ');
    g.push(part('sf_prop', frame(LEGX[1], LY, LEGW, LEGH) + R(LEGX[1], LY, 3, LEGH, C.prop)
      + head(1, '② 自營與投資部位', '跟的是盤勢方向，不是成交量', C.prop)
      + LN(`M${LEGX[1] + 15},${wy} H${LEGX[1] + 15 + ww}`, 'var(--dg-axis)', 1, ' stroke-dasharray="3 4"')
      + LN(wave, C.prop, 2.2)
      + T(LEGX[1] + 15 + ww, wy - 22, '＋', 'lbl', 'end', `fill:${C.au}`)
      + T(LEGX[1] + 15 + ww, wy + 30, '－', 'lbl', 'end', `fill:${C.risk}`)
      + ['股、債、權證與避險部位的損益。', '行情好時它是最大的驚喜，', '行情壞時它是最大的驚嚇 ——',
        '而且方向不必跟量一致。'].map((s, k) => T(LEGX[1] + 13, LY + 118 + k * 16, s, 'sub')).join('')
      + T(LEGX[1] + 13, LY + LEGH - 10, '連動：波動最大、方向不定', 'sub', null, `fill:${C.prop}`)));

    /* 腿③ 穩定腿 —— 細線 ＋ 遲滯轉折（下去得比較慢） */
    const sx = LEGX[2] + LEGW / 2;
    g.push(LN(`M${sx},${VY + VH} V${VY + VH + 16} L${sx + 24},${VY + VH + 16} L${sx + 24},${LY - 16} L${sx},${LY - 16} V${LY}`, C.stab, 1.6, ' class="flow slow"'));
    g.push(part('sf_stable', frame(LEGX[2], LY, LEGW, LEGH) + R(LEGX[2], LY, 3, LEGH, C.stab)
      + head(2, '③ 承銷、財管與利息', '比較穩，但不是完全不受影響', C.stab)
      + ['· 承銷與現金增資：按案子收，', '　 但案子的件數看行情。',
        '· 財富管理：按資產規模收，最穩。', '· 期貨與複委託：手續費，跟量走。',
        '· 信用交易利息：融資餘額的利息 ——', '　 融資餘額本身也跟著量在縮放。'].map((s, k) =>
          T(LEGX[2] + 13, LY + 64 + k * 16, s, 'sub')).join('')
      + T(LEGX[2] + 13, LY + LEGH - 10, '連動：有遲滯，掉得比較慢', 'sub', null, `fill:${C.stab}`)));
    return g.join('');
  }

  /* ================================================================ ③ 量縮時的掉落順序（永遠看得到）
     把上面三條腿的差別收斂成一句可以用的結論 —— 這是這張圖最該被記住的一行。
     ⚠ 順序是「從機制推出來的」：經紀是當下成交就結算，所以最先；融資餘額要幾天才縮完；
       承銷案是已簽的合約，遞延幾個月；自營看方向，甚至可能反向賺錢。
       **這是本圖的推論，不是任何一家公司或研究機構的說法**，畫面上照實標。*/
  const OY = 368;
  const ORDER = [
    { id: 'sf_o1', n: '①', t: '經紀手續費', s: ['當天就結算，', '立刻掉'], col: C.bro },
    { id: 'sf_o2', n: '②', t: '信用交易利息', s: ['融資餘額縮，', '利息跟著少'], col: C.margin },
    { id: 'sf_o3', n: '③', t: '承銷與財管', s: ['合約遞延，', '慢幾個月才顯現'], col: C.stab },
    { id: 'sf_o4', n: '④', t: '自營部位', s: ['看方向：', '也可能反而賺'], col: C.prop },
  ];

  function order() {
    const g = [T(LX, OY - 10, '大盤量縮的時候，哪一塊先掉 —— 這是本圖從機制推出來的順序，不是任何一家的說法', 'hd')];
    const w = 149;
    ORDER.forEach((o, i) => {
      const x = LX + i * (w + 8);
      g.push(part(o.id, frame(x, OY, w, 72) + R(x, OY, 3, 72, o.col)
        + T(x + 13, OY + 24, o.n, 'lbl', null, `fill:${o.col}`)
        + T(x + 34, OY + 24, o.t, 'lbl')
        + o.s.map((ln, k) => T(x + 13, OY + 46 + k * 16, ln, 'sub')).join('')));
      if (i < ORDER.length - 1) {
        g.push(LN(`M${x + w + 1},${OY + 36} h6`, C.vol, 1.6, ' class="flow fast"'));
      }
    });
    return g.join('');
  }

  /* ================================================================ 章節①：信用交易 —— 另一條吃量的腿
     為什麼單獨開一章：融資融券是券商「除了手續費以外」最直接吃量的收入，
     而且它同時是一條風險線（擔保維持率不足 → 追繳 → 斷頭 → 又反過來壓成交量）。
     這個回饋圈畫出來，才解釋得了為什麼券商股在急跌時會特別難看。
     ⚠ 不寫任何利率、成數、維持率的門檻數字：這些由主管機關與各家規定，而且會調整。*/
  function areaMargin() {
    const y0 = 500;
    const step = (i, t, lines, id, col) => {
      const x = LX + i * 158, w = 146;
      return part(id, frame(x, y0, w, 108) + R(x, y0, 3, 108, col)
        + T(x + 12, y0 + 22, t, 'lbl')
        + lines.map((s, k) => T(x + 12, y0 + 42 + k * 16, s, 'sub')).join(''));
    };
    return T(LX, y0 - 12, '① 信用交易：券商除了手續費以外，最直接吃量的一條腿', 'hd')
      + step(0, '融資買進', ['投資人付一部分、', '券商借一部分。', '券商收的是這筆', '借款的利息。'], 'sf_m1', C.margin)
      + step(1, '融資餘額', ['行情熱 → 餘額增 →', '利息收入增。', '行情冷 → 餘額縮 →', '利息跟著縮。'], 'sf_m2', C.margin)
      + step(2, '擔保維持率', ['股價跌，擔保品', '價值跟著跌，', '維持率掉到門檻', '以下就要追繳。'], 'sf_m3', C.warn)
      + step(3, '追繳與斷頭', ['補不上就強制賣出。', '賣壓又把股價', '往下推 ——', '同時還要扣抵餘額。'], 'sf_m4', C.risk)
      + LN(`M${LX + 600},${y0 + 54} h14 a10,10 0 0 1 10,10 v52 a10,10 0 0 1 -10,10 H${LX + 40} a10,10 0 0 1 -10,-10 v-10`, C.risk, 1.8, ' class="flow" stroke-dasharray="6 5"')
      + T(LX + 320, y0 + 136, '★ 回饋圈：斷頭賣壓 → 行情更冷 → 成交量更少 → 經紀與利息一起再掉一層', 'lbl', 'middle', `fill:${C.risk}`)
      + T(LX, y0 + 172, '這就是為什麼券商股在急跌時特別難看：三條腿同時被踩到 —— 經紀量縮、融資餘額縮、自營部位跟著跌。', 'sub')
      + T(LX, y0 + 196, '示意圖，非實物比例｜不寫任何融資利率、融資成數與擔保維持率的門檻數字：這些由主管機關與各家券商規定，而且會依標的與情況調整。', 'cap');
  }

  /* ================================================================ 章節②：誰在這一格、以及本站自己承認的不確定 */
  function areaWho() {
    const y0 = 760;
    const box = (i, t, lines, id, col) => {
      const x = LX + i * 316;
      return part(id, frame(x, y0, 300, 194) + R(x, y0, 3, 194, col)
        + T(x + 14, y0 + 24, t, 'lbl')
        + lines.map((s, k) => T(x + 14, y0 + 48 + k * 18, s, 'sub', null, /^★/.test(s) ? `fill:${C.warn}` : '')).join(''));
    };
    return T(LX, y0 - 12, '這一格收哪兩家、以及本站自己承認的不確定', 'hd')
      + box(0, '成分：2885 元大金、2883 凱基金', [
        '依 groups.yaml：獲利主體是證券、期貨',
        '與創投的金控 —— 帳面隨市場成交量與',
        '投資部位評價走，不是靠利差。',
        '兩家之間的殘差相關 +0.60；對銀行六大',
        '2885 只有 +0.29、2883 +0.43，明顯較低，',
        '所以從「金融保險」自動桶裡拆出來。',
        '★ 估值用股價淨值比＋ROE：自營與評價',
        '　 損益會讓單季 EPS 失真。',
      ], 'sf_scope', C.vol)
      + box(1, '★ 本站承認：這是最沒把握的一格', [
        'groups.yaml 的註解自己寫著這一格',
        '「是本批最沒把握的一格」：2883 凱基金',
        '旗下同時有凱基人壽與凱基證券，它對壽險',
        '二家 +0.59、對元大金 +0.60，兩邊幾乎一樣高。',
        '放這裡的依據是它四家子公司裡有三家',
        '（人壽投資收益、證券、中華開發資本）',
        '都是資本市場評價驅動，只有銀行是穩定的。',
        '★ 看這一格時要記得：2883 也會跟著壽險走。',
      ], 'sf_doubt', C.warn)
      + frame(LX, y0 + 210, W, 104)
      + T(LX + 14, y0 + 234, '★ 為什麼純券商沒有放進這一格（很反直覺，所以寫出來）', 'lbl', null, `fill:${C.warn}`)
      + ['2855 統一證、6005 群益證、6016 康和證、6023 元大期都沒有放進來。理由是本站的實測：6016 對元大金只有 +0.16、6023 只有 +0.05 ——',
        '「受成交量驅動」這個故事在它們的股價上量不到；而且它們的日均成交值都在 2.5 億以下，放進族群指數只會被雜訊主導。',
        '它們留在自動分類的「金融保險・其他」。★ 這是本站分群的實測結論，不是說它們的生意跟成交量無關 —— 是說它們的「股價」量不到這個連動。'].map((s, i) =>
          T(LX + 14, y0 + 256 + i * 18, s, 'sub', null, /^★/.test(s) ? `fill:${C.warn}` : '')).join('')
      + frame(LX, y0 + 330, W, 122)
      + T(LX + 14, y0 + 354, '這張圖沒有回答的事', 'lbl')
      + ['各家的經紀市占率、手續費收入占比、自營部位規模、實際折讓幅度 —— 逐季變動又口徑不一的公司別數字，一個都不寫。',
        '0.1425% 是依證券交易法第 85 條核定的費率上限，不是任何一家的實收費率；各券商普遍以折讓競爭，折扣幅度逐家不同還會促銷變動。',
        '「量縮時的掉落順序」是本圖從機制推出來的推論（成交當天結算 vs 融資餘額要幾天 vs 合約遞延），不是任何一家公司或研究機構的說法。',
        '點零件不會篩成分股：供應鏈資料裡沒有「金融」這條鏈的環節，硬掛一個環節等於宣稱錯誤的公司對應。'].map((s, i) =>
          T(LX + 14, y0 + 376 + i * 18, s, 'sub')).join('');
  }

  function securitiesFhc() {
    return `<svg class="dg dgm rs dgsf" viewBox="0 0 ${CW} 1260" width="100%" style="display:block">${D.STYLE}
      <style>
        /* ⚠ SVG 裡的 style：連註解都不准出現角括號（見 DECISIONS 第 231 條）。
           這張圖一個 data-seg 都沒有，所以 diagrams.js 那一整組「.dg [data-seg] …」
           的描邊與高亮規則一條都吃不到，要在這裡自己給。顏色一律走 --dg-* token。*/
        svg.dgsf [data-part] .part{stroke:var(--dg-part-mix);stroke-width:.9;transition:stroke .15s,filter .15s}
        svg.dgsf [data-part]{cursor:default}
        svg.dgsf [data-part]:hover .part{stroke:var(--dg-accent-2d);stroke-width:1.6}
        svg.dgsf [data-part].sel-part .part{stroke:var(--dg-accent-2d);stroke-width:2.4;
          filter:var(--dg-glow,drop-shadow(0 0 5px var(--dg-accent-2d)))}
        svg.dgsf .leader{fill:none;stroke:var(--dg-line-mix);stroke-width:1}
        svg.dgsf .anc .anchor{fill:var(--dg-card-c,var(--dg-accent-2d))}
      </style>
      <defs>${fx.glowDefs({ r: 4, soft: 4 })}</defs>

      <!-- 標題與說明：v2 搬到 HTML 的 .dghead（跨整個容器寬），SVG 裡不畫 -->
      <text class="ttl ext" x="0" y="0">證券金控：三條腿踩在同一塊板子上 —— 大盤的成交量</text>
      <text class="cap ext" x="0" y="0">最上面那條帶子是共同輸入：台股的成交量。下面三條腿全部踩在它上面，但連動的方式不一樣，所以線型也刻意不一樣。★ 經紀手續費是一條直直落下的粗光束：它幾乎是成交金額的線性函數（費率上限 0.1425%，各家以折讓競爭），量縮就掉，沒有例外。★ 自營與投資是一條上下擺動的波形：它跟的不是「量」而是「盤勢方向」，可正可負，是最大的驚喜也是最大的驚嚇。★ 承銷、財管與利息那一條是細線加遲滯轉折：比較穩，但承銷案的件數看行情、融資餘額也跟著量在縮放。最下面那一排是把三者的差別收斂成一句可以用的話：量縮時哪一塊先掉。</text>

      <!-- ================= §1 主畫面（永遠看得到） ================= -->
      ${fx.shadows(`<rect x="${LX + 4}" y="${VY + 4}" width="${W}" height="${VH}" rx="9"/>${LEGX.map(x => `<rect x="${x + 4}" y="${LY + 4}" width="${LEGW}" height="${LEGH}" rx="9"/>`).join('')}`)}
      ${volume()}
      ${legs()}
      ${order()}
      ${T(LX, 466, '示意圖，非實物比例｜量能柱是示意波形，不對應任何真實數列，也不表示任何水位。', 'cap')}
      ${T(LX, 484, '0.1425% 是依證券交易法第 85 條核定的費率上限，不是任何一家的實收費率；各券商普遍以折讓競爭，折扣幅度逐家不同。', 'cap')}

      <!-- ================= 說明卡片（HTML；左欄＝量能與經紀，右欄＝自營與穩定腿） ================= -->
      ${card({ part: 'sf_volume', no: 1, side: 'l', color: C.vol, ax: LX + 300, ay: VY + 30, title: '★ 共同輸入：成交量', sub: ['台股的日均成交值就是這一格的景氣指標 —— 它不是「經濟好不好」，是「大家有沒有在換手」。', '所以這一格會在資金行情裡先動，卻不一定跟著出口或製造業的循環走。'] })}
      ${card({ part: 'sf_brokerage', no: 2, side: 'l', color: C.bro, ax: LEGX[0] + LEGW - 16, ay: LY + 18, title: '① 經紀：最直接的那一條', sub: ['成交金額 × 費率 ×（1 − 折讓），買賣各收一次。費率上限 0.1425% 由主管機關依證交法第 85 條核定，各家用折扣競爭。', '★ 因為是線性、方向單一，所以看券商股先看大盤量 —— 這一條沒有例外。'] })}
      ${card({ part: 'sf_prop', no: 3, side: 'r', color: C.prop, ax: LEGX[1] + LEGW - 16, ay: LY + 18, title: '② 自營：跟的是方向，不是量', sub: ['股、債、權證與避險部位的損益，可正可負。所以同樣是量縮，有的季度自營反而賺（例如空方部位或債券部位）。', '★ 這一條是券商單季獲利落差的主要來源，也是為什麼不該用本益比看這一格。'] })}
      ${card({ part: 'sf_stable', no: 4, side: 'r', color: C.stab, ax: LEGX[2] + LEGW - 16, ay: LY + 18, title: '③ 穩定腿：慢，但不是不動', sub: ['承銷與現金增資按案子收，案子的件數看行情；財富管理按資產規模收，最穩；期貨與複委託的手續費仍跟量走。', '★ 所以它掉得比較慢，不是不會掉 —— 只是遲滯幾個月才顯現。'] })}
      ${card({ part: 'sf_o1', no: 5, side: 'l', order: 5, color: C.bro, ax: LX + 133, ay: OY + 20, title: '最先掉：經紀手續費', sub: ['成交當天就結算，量一縮當月就反映。所以券商股常常跟著月成交值的公布同步反應。'] })}
      ${card({ part: 'sf_o4', no: 6, side: 'r', order: 6, color: C.prop, ax: LX + 3 * 157 + 133, ay: OY + 20, title: '最後、而且不一定掉：自營', sub: ['看部位方向。行情急跌時，帳上的多方部位會虧，但債券部位或避險部位可能反而有貢獻 —— 所以自營是唯一可能逆著走的一條腿。'] })}
      ${note({ side: 'l', order: 96, title: '示意圖，非實物比例', lines: ['量能柱是示意波形，不對應任何真實數列，也不表示任何水位；三條腿的框寬一樣，不代表收入占比一樣。'] })}
      ${note({ side: 'l', order: 97, warn: true, title: '★ 為什麼點零件不會篩成分股', lines: ['供應鏈資料裡沒有「金融」這條鏈的環節（機器查的：一個都沒有），所以這張圖一個 data-seg 都沒掛。那不是壞掉，是誠實 —— 硬掛一個別條鏈的環節，等於在 public 網站上宣稱錯誤的公司對應。'] })}
      ${note({ side: 'r', order: 98, title: '一句話記住這一格', lines: ['券商是「大盤量能的槓桿」，不是「景氣的槓桿」。資金行情來的時候它會先動，製造業景氣好不好反而不是主變數 —— 這就是它要跟銀行、壽險分開看的原因。'] })}
      ${note({ side: 'r', order: 99, warn: true, title: '★ 畫面上刻意不出現的數字', lines: ['各家的經紀市占率、手續費收入占比、自營部位規模、實際折讓幅度一個都沒寫：逐季變動又口徑不一的公司別數字。0.1425% 是核定的費率上限，不是誰的實收費率。'] })}

      <!-- ================= 章節①：信用交易（預設收合） ================= -->
      ${fold('sf2', '① 信用交易：另一條直接吃量的腿，以及它的回饋圈',
      '融資餘額怎麼跟量走、擔保維持率與追繳、斷頭賣壓怎麼回頭壓成交量', areaMargin())}

      <!-- ================= 章節②：成分與不確定（預設收合） ================= -->
      ${fold('sf3', '② 這一格收哪兩家、為什麼純券商沒放進來，以及本站承認的不確定',
      '元大金與凱基金、2883 兩邊都高的問題、純券商量不到連動的實測、刻意不寫的數字', areaWho())}
    </svg>`;
  }

  window.DG.register('securities_fhc', {
    level: 'group', chain: 'financial',
    name: '證券金控：三條腿踩在同一塊板子上 —— 大盤的成交量',
    draw: securitiesFhc, native: CW,
    /* ★ Andy 2026-09-23 指定金融鏈「只要 2D 圖，不用 3D」—— scene 固定 null。*/
    scene: null,
    q: '券商的獲利為什麼跟成交量高度連動？大盤量縮時哪一塊先掉？',
    parts: {
      sf_volume: { name: '★ 共同輸入：台股成交量', desc: '這一格的景氣指標不是「經濟好不好」，是「大家有沒有在換手」。所以它在資金行情裡先動，不一定跟著出口或製造業循環走。量能柱為示意波形，不對應任何真實數列。' },
      sf_brokerage: { name: '① 經紀手續費', desc: '成交金額 × 費率 ×（1 − 折讓），買賣各收一次。費率上限 0.1425%（千分之 1.425）依證券交易法第 85 條由主管機關核定，各券商以折扣競爭。線性、方向單一，量縮就掉。' },
      sf_prop: { name: '② 自營與投資部位', desc: '股、債、權證與避險部位的損益，跟的是盤勢方向而不是成交量，可正可負。它是券商單季獲利落差的主要來源，也是不該用本益比看這一格的原因。' },
      sf_stable: { name: '③ 承銷、財管與利息', desc: '承銷與現金增資按案子收（件數看行情）、財富管理按資產規模收（最穩）、期貨與複委託手續費仍跟量走、信用交易利息隨融資餘額縮放。掉得比較慢，不是不會掉。' },
      sf_o1: { name: '掉落順序①：經紀手續費', desc: '成交當天就結算，量一縮當月就反映 —— 券商股常跟著月成交值的公布同步反應。' },
      sf_o2: { name: '掉落順序②：信用交易利息', desc: '融資餘額要幾天才縮完，利息收入跟著少，比經紀晚一步。' },
      sf_o3: { name: '掉落順序③：承銷與財管', desc: '已簽的承銷合約會遞延幾個月才認列完，財管按資產規模收也有慣性，所以顯現得最慢。' },
      sf_o4: { name: '掉落順序④：自營部位', desc: '看方向。急跌時多方部位會虧，但債券或避險部位可能反而有貢獻 —— 唯一可能逆著走的一條腿。' },
      sf_m1: { name: '融資買進', desc: '投資人付一部分、券商借一部分，券商收這筆借款的利息。' },
      sf_m2: { name: '融資餘額', desc: '行情熱餘額增、利息收入增；行情冷餘額縮、利息跟著縮。它是券商除手續費外最直接吃量的一塊。' },
      sf_m3: { name: '擔保維持率', desc: '股價跌、擔保品價值跟著跌，維持率掉到門檻以下就要追繳。門檻由主管機關與各家規定，會依標的與情況調整，本圖不寫數字。' },
      sf_m4: { name: '追繳與斷頭', desc: '補不上就強制賣出，賣壓又把股價往下推、同時扣抵餘額 —— 形成回饋圈：行情更冷、成交量更少、經紀與利息再掉一層。' },
      sf_scope: { name: '成分：2885 元大金、2883 凱基金', desc: '依 groups.yaml：獲利主體是證券、期貨與創投的金控，帳面隨成交量與投資部位評價走。兩家之間殘差相關 +0.60，對銀行六大明顯較低（+0.29／+0.43），所以從金融保險自動桶拆出來。' },
      sf_doubt: { name: '★ 本站承認：這是最沒把握的一格', desc: 'groups.yaml 註解自己寫著這是最沒把握的一格：2883 凱基金旗下同時有凱基人壽與凱基證券，對壽險二家 +0.59、對元大金 +0.60，兩邊幾乎一樣高。放這裡的依據是四家子公司裡有三家由資本市場評價驅動。看這一格要記得 2883 也會跟著壽險走。' },
    },
  });
})();
