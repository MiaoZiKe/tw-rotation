# 效率改善

> Andy 2026-09-20：「幫我優化效率，因為真的太久了，一個改 UI 這樣簡單的事情也花太多時間，
> 所以需要你分辨我提出的需求是否是小改動或大改動，改動的後續是否影響很大…
> 驗證可以全部做完再驗證，可以全部 Task 到一個段落你再幫我驗證，但我優先想先看結果。」

---

## 一、新的工作方式（2026-09-20 起）

### 1. 每個需求進來，先分類再開工，而且**分類要講給 Andy 聽**

| 級別 | 判準 | 做法 |
|---|---|---|
| **S 小改** | 只動版面／文字／顏色，不動資料流，不影響別的頁面 | 直接改，**只跑受影響的那一段驗收**，併進下一次推送 |
| **M 中改** | 動到某一頁的互動或版面結構，可能影響同頁其他元件 | 改完跑該頁 ＋ 相鄰段落 |
| **L 大改** | 動到資料流（`pipeline/`）、共用元件、或會讓別的頁面跟著變 | 一定跑完整一輪；**改之前先講清楚會影響什麼** |

**判斷「後續影響大不大」的機器判準**（不准靠記憶）：
```bash
git diff --name-only <上次 push>..HEAD
```
- 出現 `pipeline/`、`tests/`、`scripts/`（`_uitest.py` 除外）、`requirements.txt`、`.github/`
  → **L 級，一定跑 pytest**
- 只有 `site/**` → 至少 S 級起跳，看改的是單頁還是共用元件
- 改到 `site/index.html` 的 CSS **且選擇器不只一頁在用** → 自動升級成 L

### 2. **批次驗證**（Andy 明確要求）

以前：每一件事都「改完 → 跑完整一輪 → 推」。
現在：**一批 Task 做完到一個段落，才做一次完整驗證，然後一次推。**

- 做的過程中只跑 `--only <受影響段落>`（單段 11～90 秒）
- **agent 不准再跑完整輪** —— 那是重複勞動，完整輪只由 CEO 在推之前跑一次
- Andy 要「優先看結果」，所以**中途可以先推前端改動讓他看**，
  但推之前仍然要跑受影響的段落 ＋ `_preview`（不能推一個壞掉的版面上去）

### 3. 一律記進 [[100-任務板]]

新需求進來**先寫進任務板再開工**。做完一段回來更新：做到哪、花了多久、卡在什麼。
Andy 直接看任務板就知道有沒有漏，不用回頭翻對話。

---

## 二、要做的三個技術改動（照 CP 值排）

| # | 改什麼 | 現在 | 改完 | 狀態 |
|---|---|---|---|---|
| **E1** | **`_uitest` 平行化** —— 31 個段落現在是序列跑，改成多個瀏覽器 context 平行 | 14.6 分 | 估 4~5 分 | ⬜ |
| **E2** | **agent 只跑受影響段落**，完整輪只由 CEO 跑一次 | 每件事驗兩遍 | 驗一遍 | ✅ 已改成交辦時的規則 |
| **E3** | **`build_payload` 加「只重算前端要用的」旗標** —— 純前端改動不需要重跑「個股頁（全市場）」那 235 秒 | 292 秒 | 估 60 秒 | ⬜ |

E1＋E2 做完，一件事的驗證時間大約從 **35 分鐘壓到 10 分鐘以內**。

---

## 三、2026-09-20 實測數字（不是估計）

| 步驟 | 秒 |
|---|---|
| `_uitest` 完整一輪 | 814 / 820 / 827 / 830 / 864 / 920 / 921 / 925 / 939 |
| `pytest` | 195 / 198 / 200 / 202 / 204 / 205 |
| `build_payload`（SKIP_INTRADAY=1） | 292（個股頁 235＝80%） |
| `_preview` | ~120 |
| GitHub 部署 | ~270 |
| `--only` 單段 | 批次7 11 秒／一般電子鏈 19 秒／資金流向 57 秒／產業 83 秒／題材 91 秒 |

`--only` 已經支援**逗號分隔多段**（例：`--only 新-資金流向,批次7`），
而且埠可以用 `TW_UITEST_PORT` / `TW_PREVIEW_PORT` 指定，所以多個 agent 可以同時跑。

---

## 四、一個我要認的

這個慢**有一部分是我造成的** —— 我一直讓 agent 也跑完整輪，那本來就是重複的。
E2 不需要寫任何程式，只是我交辦時改一句話。
