__pycache__/
*.py[cod]
.venv/
venv/
.pytest_cache/
.DS_Store
.idea/
.vscode/

# 執行狀態：額度計數是本機的，不要進版控
data/_state/finmind_quota.json

# 前端 JSON 由工作流從資料湖產出（每天重寫幾十 MB，進版控會把 repo 撐爆）
site/data/

# 預覽截圖
docs/preview.png
docs/*.png

# 金鑰與推送暫存：永遠不進版控（repo 是 public）
.env
.env.*
*.token
.ghtoken
secrets/
.gh_push/

# 本機推送腳本的產物：commit 訊息檔（UTF-8，讓 .bat 保持純 ASCII 也能寫繁體中文訊息）
# 與各種 log。這些只存在於某一台電腦上，不該進 repo。
COMMITMSG.txt
commit-msg.txt
push-log.txt
push2-log.txt
*-log.txt
# 一次性推送腳本的 log（push-log-0916.txt 這種名字不符合上面的 *-log.txt，
# 2026-09-16 就這樣被 commit 進 public repo 一次，裡面有本機使用者路徑）
push-log*.txt
push*-log*.txt

# Claude 沒辦法透過遠端檔案橋直接寫 .github/workflows（被平台保護），
# 所以改把更新後的工作流放在這裡，由 push.bat 複製過去。
_pending/
.uitest_times.json
