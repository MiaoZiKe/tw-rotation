# DECISIONS.md — 已拍板的決策（不要重新討論）

每條格式：**決策** — 理由 — 影響的檔案。要推翻任何一條，先由 CEO 問 Andy，得到答覆後改這裡。

---

## A. 架構

1. **資料只用免費且合規的來源**：證交所 OpenAPI（`openapi.twse.com.tw`）、FinMind、集保 OpenAPI、櫃買（可失敗）、
   鉅亨／TechNews RSS、FRED。**絕不碰 `www.twse.com.tw/rwd/...`**（使用條款禁爬）、不爬券商分點、不繞反爬。
   — 這是 Andy 對「合規換來 T-1 延遲」的明確取捨。— `pipeline/sources/*`
2. **Parquet append-only 資料湖放在 repo 的 `data/`，由 Actions 每天 commit；雲端是權威來源。**
   本機永遠 `git pull` 後只讀；不 force push、不覆寫。— `util/store.py`、`daily.yml`、`backfill.yml`
3. **每日增量走 OpenAPI，歷史走 FinMind**（免費層 600 req/hr，402 即停，每小時排程接續）。— `run_backfill.py`、`util/http.py`
4. **不引入 TA-Lib**，指標自己算；Python 端只用於判定與評分，前端 K 線指標在瀏覽器算（參數要即時可調）。
   兩邊口徑一致：KD 9,3,3 初始 50、RSI Wilder（SMA 種子）、MACD 12/26/9。— `indicators.py`、`site/chart.js`
5. **前端是純靜態網站**，圖表庫全部 vendored 進 `site/vendor/`（Andy 公司網路擋 CDN），零外部請求。
6. **`site/data/` 不進版控（v3）**：每天重寫幾十 MB 會把 repo 撐爆；`pages.yml` 與 `daily.yml` 在工作流內從資料湖產出 JSON 再部署。
7. **部署兩條線**：`daily.yml` 跑完管線後自己部署；`pages.yml` 在 `site/**`／`pipeline/**` 有 push 時部署。
   共用 `pages` concurrency group 且 **`cancel-in-progress: false`**（改 true 會出現「repo 新、網站舊」）。
   資料寫入類工作流共用 `tw-rotation-data-write` group 排隊，避免互相 push 衝突。
8. **工作流一律 `checkout ref: ${{ github.ref_name }}`**（分支最新），不用觸發當下的 SHA — 排隊中別人可能已推新資料。
9. **程式碼由 Claude 透過 GitHub Git Data API 推上 main**（blob → tree → commit → ref，`force:false`），
   Andy 不在本機執行任何 git；token 只存 Claude 暫存區與 Andy 的 GitHub 設定，永不進 repo。
10. **這是決策輔助，不是交易系統**：不接下單、不自動執行；repo 是 public，不放持股、成本、損益。
11. 註解、commit 訊息、文件用繁體中文。

## B. 金融口徑（金融專家定，改動要同步 docstring）

12. PE 一律自算＝收盤／近四季 EPS 合計；四季不連續不給 TTM，**不用單季 ×4**；虧損股 NaN 不排序；PE 在 [3,200] 外視為離群；族群用中位數。
13. 估值分位只在同族群內算；樣本 < 5 退回法定產業別、< 3 留白、永遠顯示 n。`groups.yaml` 的 `valuation_metric`：金融 pb_roe、生技 ps、其餘 pe，**絕不跨族群比 PE**。
14. 財報「可用日」用法定期限（Q1 5/15、Q2 8/14、Q3 11/14、Q4 隔年 3/31、月營收次月 10 日），寧遲勿早，否則 walk-forward 偷看未來。
15. 獲利資料**依法只有季報**（毛利／營益／淨利／EPS／累計 EPS 皆按季），頁面要註明「每季」而非每月。
16. 券商目標價是**新聞引述**，不是我們的預估；頁面標券商名、日期、來源。（v2 拍板）
17. 「預估漲幅」改為**營運動能分數**。（v2 拍板）
18. 季節性：**2000 年起**，觀察期 all／10y／5y／3y 可切，統計**相對大盤（TAIEX）的超額報酬**與勝率；樣本 < 3 留白；權重壓到 0.05。
19. 一檔股票可屬多個族群，**族群量能按 1/n 拆分**（避免市場總量灌水）；**題材熱度不拆分**（同一筆成交額可同時貢獻多個題材）。
20. 集保層級：15＝千張以上（大戶）、12–14 中實戶、1–3 散戶、17 總計；大戶散戶用「持股比例變化」呈現。
21. **主力（券商分點家數差）用「集保千張大戶增減＋法人動向」替代**並在頁面明講；不去爬分點。（v3 拍板）
22. 除權息：事件（現金／股票、除息日、發放日）與結果（填息天數）分開存；殖利率用現金股利 TTM。

## C. 技術分析（SMC）

23. 台股**紅漲綠跌**；前端配色已照做，不要「修正」。
24. 支撐壓力：多源交集區間（score ≥ 3、≥ 2 來源、寬 ≤ 2×ATR、依距離排序）；A／B／觀望／不要碰四級；
    突破停損放在被突破前高之下；有測量目標；週線衝突降級；三段式文字。
25. FVG 門檻 max(0.6, 0.3×ATR%)；週線／月線結構回看 3 根、日線 2 根。
26. 多週期同看：有分 K 用 15 分／1 時／4 時／日；沒分 K 明講改「週線 vs 日線」統整。（v3）
27. 4 小時線用 240 分鐘、以 09:00 為錨重採樣；週／月線在瀏覽器端由日線重採樣。

## D. 前端／UI（v2、v3 拍板）

28. **K 線引擎換成 TradingView Lightweight Charts**（不再用 ECharts candlestick）；其餘圖表仍用 ECharts。（v3）
29. 時間週期 15m／1H／4H／1D／1W／1M；指標 MA／KD／MACD／RSI／BOLL 可勾選、參數可調（存 localStorage）；價格軸滾輪縮放。（v3）
30. **分 K 先用 Yahoo Finance（60m/730 天、15m/60 天，不進資料湖）；之後接 Fugle 做即時**（前端 WebSocket 直連，Cloudflare Workers 藏 key）。（v3 拍板，Fugle 等 Andy 給 key）
31. **個股頁與產業鏈頁合併**：產業地圖 → 單一產業鏈 → 個股；點股票時上方產業鏈同步高亮。（v3）
32. **資金流向不用直條圖**：RRG、桑基、河流／市占面積。（v3）
33. 題材資金熱力圖獨立分頁。（v3）
34. 整體風格**深色科技／AI／專業**，字體加大；每處都要有股票簡稱（v2）；零文字重疊、零水平捲軸、手機 390px 可用。
35. 市占率產品要有剖析圖：網路找不到可用的就**自己畫原創動畫 SVG**，零件對應供應鏈環節。（v3）
36. **「不用稿圖，直接做完給我看」**：不做 mockup 往返，直接實作 → 本機 `_preview.py` 驗證 → push → Andy 重新整理網頁看。（v3）
37. Andy **只重新整理網頁**，不點任何按鈕做更新（v3）；因此所有更新都要自動化在 Actions 內。

## E. 開發約定

38. 每個抓取步驟都必須可以失敗（`run_daily.step()` 吃例外寫 `last_run.json`）；一個來源掛掉不能拖垮整天。
39. 新增資料表先在 `config.TABLES` 註冊 key 欄位，否則 `store.append()` 拒收。
40. 改指標必跑 `pytest`：`test_no_lookahead_in_moving_averages`、`test_append_is_idempotent` 是底線。
41. push 前跑 `scripts/_preview.py`（真圖表庫、全頁面、重疊偵測、手機寬）。
42. 非交易日 API 會靜默回上一交易日，**一律用回應裡的日期**；`MI_MARGN` 沒日期欄位由同批行情補。
43. `groups.yaml` / `themes.yaml` / `supply_chain.yaml` 是唯一人工維護檔；程式只產「建議報告」不自動改。
44. 回補計畫 `PLAN_DEFAULT`：財報＋資產負債（2016 起）→ 股利＋除權息結果（2016）→ 資券＋集保（2021）→
    族群成分股價量（2000 起）→ 每月股利更新（tag 帶年月，跨月自動再跑）。done key 格式 `key@start:code`／`key@tag:code`。
45. 排程時段避開 UTC 09:20／10:20（每日管線 10:30 也要用 FinMind 額度）。GitHub 免費層的 cron 會延遲，
    當天實測每日管線延到 UTC 14:33 才跑，屬正常，不要為此改成外部排程。
46. **「最新交易日」＝上市資料到齊的最後一天**（`build_payload.last_complete_date`），不是 `max(date)`。
    證交所 OpenAPI 與櫃買更新時間不同；只有櫃買到了新的一天時，整個上市股會被算漏（2026-09-11 實際發生：
    個股頁 404、族群統計只剩上櫃）。各日表一律裁到同一天再算。櫃買抓失敗不得拖住更新（只看上市檔數）。

## F. 前端互通與視覺（2026-09-12 拍板）

47. **全站互通**：任何地方出現的股票、族群、產業鏈、題材名稱一律是連結（`App.L.stock/group/chain/theme`），
    圖表下方一定有同內容的可點連結列（`linkRow`，手機沒有 hover）。表格列可點進個股，列內的族群連結點了是去族群頁（連結攔截在捕獲階段）。
48. **顏色全站一致**：供應鏈環節色（`App.L.scolor`，依 `supply_chain.yaml` 環節順序取色盤）是唯一來源；族群色＝它第一個環節的顏色，
    剖析圖零件、環節色標、關聯圖欄位、族群卡片、族群連結圓點、RRG／河流／桑基都用同一色。點族群卡片，剖析圖上同色零件一起亮。
49. **剖析圖版面規則**：零件畫中間、說明文字排右欄用引線接過去、文字不疊在零件上；每個零件都有 `data-seg`；
    沒有台股直接對應的環節（HBM、雲端業者）用 `FALLBACK` 接到最相近族群並列出外商，不能點了沒反應。動畫可關（存 localStorage）。
50. 個股頁上方的產業鏈區塊預設**收合**（環節色標＋同族群公司列永遠可見），展開後剖析圖與關聯圖全寬；狀態存 localStorage。
51. K 線：成交量／KD／MACD／RSI 各面板左上角有標題與即時數值，KD 80/20、RSI 70/30、MACD 0 參考線，SMC 區間標籤靠右不疊 K 棒，
    BOS／CHoCH 每種只留最近 5 個，圖中央有淡浮水印（代號＋週期）。

52. **每一檔上市櫃證券都有個股頁，差別只在資料多寡**（Andy 2026-09-12：「不在範圍內就不顯示 → 需要全部顯示」）。
    分三層寫在 `meta.tier`：`full`（族群成分股＋成交值前段，共 150 檔，有 Yahoo 分 K、6 年日線）／
    `daily`（其餘有 ≥60 根日線的，4 年日線、指標與多週期判讀都算）／`thin`（歷史還在回補，只有最近幾天日線，
    頁面直接寫「資料回補中」並說明還差什麼）。名單取自**整個資料湖**而不是只有今天，今天停牌或沒成交也要有頁。
    另出一份 `stocks.json` 全市場索引（代號／簡稱／市場／族群／收盤／漲跌／成交值／PE／tier），搜尋與所有連結都以它為準。
    個股頁不再重複寫 `ohlcv`／`series`（指標與週月線由前端 `chart.js` 的 `KInd` 現算），單檔從 155KB 降到 116KB。
53. **法定產業別名稱要正規化**（`util/roc.norm_industry`）：上市寫「其他電子業」、上櫃寫「其他電子類」，不統一會產出兩個
    同義族群頁與兩塊重複的熱力圖板塊。`compute/flow.py` 與 `build_payload.py` 兩邊都要呼叫同一支；
    「大盤」「所有證券」「Index」與純數字代號不是產業別，一律歸「其他」。
54. **題材產品圖＝上游 → 中游 → 下游的等角 3D 供應鏈剖析圖**（`site/themes3d.js`，參考 Andy 2026-09-12 給的三張圖）。
    版面固定：三段橫幅、五個站點由左到右、站點之間有流動彩帶、每個物件正下方寫標題＋兩行說明＋該環節台股代號標籤（直接可點）、
    最下面一條編號製程流程。3D 物件庫（晶格／晶圓／晶片／堆疊／模組／主機板／機櫃／機台／馬達／面板…）與剖析圖共用
    `window.DG` 工具箱，所有圖同一套等角角度與光影。站點顏色優先用供應鏈環節色，同一張圖裡顏色不重複
    （不然五個站都掛「電源」環節會整張同色）。
55. **長表格一律加高度上限**（`.tw.cap-sm/md/lg`）讓表身自己捲、表頭固定，每個分頁都要能在一個畫面內看完（Andy 2026-09-12）。
    `scripts/_preview.py` 的重疊偵測要略過「已被可捲動容器裁掉」的元素，否則會誤報。
56. **候選名單分四個面向評分，而且每一檔都要說得出被挑中的理由**（Andy 2026-09-12：「候選名單需要有可以篩選的方式，
    分別是綜合，籌碼 技術 基本面 類似方式去區分 你為何選擇他」）。做法在 `pipeline/compute/scoring.py`：
    - **技術 `score_tech`**：以 `indicators.technical_score` 為底，再依 `technical.evaluate` 的 A/B 判定、均線位置、
      風報比、量比、乖離、60 日新高加減分；被硬性條件排除的直接扣分並把排除原因寫進 cons。
    - **籌碼 `score_chip`**：投信／外資連續買超天數、三大法人 5 日淨買**佔同期成交值的比例**（不是絕對張數 ——
      買一萬張對台積電和對小型股的意義完全不同）、投信外資 20 日是否同向、集保大戶（400 張以上）四週持股增減、
      券商調升目標價。沒有法人資料就回 `None`，前端顯示「—」。
    - **基本面 `score_fund`**：加權平均而不是「50 分逐項加」（加法會讓好股票全部擠在 95 分以上，排不出順序）。
      四個營收欄位講的是同一件事，整塊只給 45%，估值分位 35%、ROE 20%。營收 YoY 超過 100% 只當警示不當成長。
      可用成分少於兩個就回 `None`。
    - **綜合 `score_all`**：技術 40%／籌碼 30%／基本面 30%，**某一面向缺資料時權重按比例讓給其他面向**（不是補 50 分）。
    每個面向同時回 `pros`/`cons` 人話理由，而且**一定要帶實際數字**（「投信連買 5 天，5 日合計 24,763 張」，
    不是「籌碼不錯」）。前端候選卡片上方四顆面向鈕：切換會同時換欄位組、換預設排序、換說明文字；點任一列展開理由。
    理由列在橫向捲動的表格裡要 `position:sticky; left:0` 並把寬度綁住容器，否則會被推到畫面外。
57. **`candidates.json` 不放全市場**：回補跑完後有評分的會超過兩千檔，整包丟給瀏覽器是好幾 MB。
    但也不能只照綜合分砍 —— 籌碼特別強、綜合普通的那種正是要能篩出來的。所以**四個面向各取前 300 名再取聯集**，
    A/B 級一律保留（`build_payload.shortlist()`）。全市場的查找一律走 `stocks.json`。
58. **K 線／SMC（Andy #43）**：
    - **供需區寬度給上下限**：以前只用 `2×ATR`，低價股會畫出價格 11% 寬的帶子，看起來像整片塗色。
      改成 `max(價格×1.2%, min(2×ATR, 價格×4%))`，再用 `_dedup()` 丟掉互相重疊的區，多空各只留 2 個。
      理由：支撐壓力要能當成進出場依據，寬到 10% 的「區間」沒有任何操作意義。
    - **每個區帶 `since`（最早那個來源出現的日期）**，前端從那天開始畫，不再從圖表最左邊拉到最右邊。
      一條 2023 年才形成的頸線壓在 2021 年的 K 棒上會誤導人。
    - **`apply()` 要能自己重建圖表**：切到沒有資料的週期（例如冷門股沒有 60 分 K）時圖會被拆掉，
      以前切回日線只會 `if (!kchart) return;`，整張空白到重新整理為止。這是「當使用者用一遍」才抓得到的錯。
    - **手繪線每檔每週期各存一組**（`localStorage['tw.draw.<代號>.<週期>']`）。
      同一檔的日線與 60 分線畫的線不該互相汙染；換股或換週期時 `enableDraw()` 會換成新的 key。
    - **繪圖事件綁在 capture 階段並 `stopPropagation`**，同時把 `handleScroll.pressedMouseMove` 關掉，
      否則按住拖曳會被 Lightweight Charts 當成平移圖表。放開游標工具時再開回來。
    - **`_preview.py` 要真的操作**：拉線寬滑桿、按新增均線、加自訂週期、用滑鼠拖曳畫線、清空、按重設縮放，
      並檢查 `localStorage` 真的寫進去。拖曳前一定要 `scrollIntoView({block:'center'})` ——
      圖高 640px、視窗 1000px，不捲到中間的話拖曳終點會落在視窗外，`pointerup` 收不到，
      測試會誤報「繪圖沒存下來」（這個假警報花掉一整輪除錯）。
59. **真人操作驗收（`scripts/_uitest.py`）**：Andy 的原話是「全部做完後一定要當自己為使用者實際操作每個功能」。
    所以驗收拆成兩支腳本，問的問題不一樣：
    - `_preview.py`：**有沒有畫出來、有沒有壓到字**（render 與版面）。
    - `_uitest.py`：**我按下去之後，畫面真的因此改變了嗎**（互動）。
      每一項都寫成「操作前記一次狀態 → 真的用滑鼠操作 → 操作後再記一次 → 兩次必須不一樣」，
      只驗「元素存在」「有 render」一律不算數。
    這樣寫第一次跑就抓到 6 個只看 render 絕對看不出來的錯（見下），所以**新功能一定要同時在 `_uitest.py` 裡
    加一段真的操作它的驗收**，沒有就當作沒驗收。
    第一次跑抓到的：
    - 切到沒資料的週期再切回日線，K 線圖空白到重新整理為止。
    - RRG 的「20 日」按鈕沒反應 —— 後端只存 10 天軌跡（`rrg(trail=10)`），按 20 日跟 10 日畫出來一模一樣。
      改成存 20 天（前端最長的選項），並在 e2e 測試加上 `trail_days >= 20` 的斷言。
    - 設定面板刪到剩 5 條均線後，「＋ 新增均線」還是 disabled（只在開面板時算一次）。
    - 手機上股票代號只有 10.25px（`a.lk .code` 是 `.82em`，父層 12.5px 乘下來）。
    - 手機上剖析圖被 `width:100%` 壓成 360px 寬，viewBox 1200 的標籤實際只剩 3px。
      改成可以左右滑（`overflow-x:auto` + `min-width:940px`），寧可多滑一下也要看得清楚。
    - 量「字有沒有太小」時，SVG 文字要乘上 `getScreenCTM()` 的縮放才是螢幕上真正的大小，
      直接讀 `font-size` 會漏掉被 viewBox 壓縮的情況。
    另外兩個踩過的坑：拖曳前一定要把圖 `scrollIntoView({block:'center'})`（圖高 640、視窗 1000，
    不捲的話終點落在視窗外，`pointerup` 收不到）；判斷「圖有沒有重畫」要取 canvas **正中間**那塊取樣，
    邊角常常是背景，會誤判成沒變。
60. **資金流向頁改版（Andy #41）**：
    - **整頁由一個期間切換列控制「什麼時候」**（本週／上週／上上週／本月／上月／近三月）。
      Andy 的原話是「好比說上週 上上週 上個月等等 可以查到不同時期 資金走向為何」，
      所以期間用**人講得出口的日曆單位**（週一為一週之始、自然月），不是「近 5 日／近 20 日」。
      `flow.period_flows()` 每個期間都回答同一組問題：這段時間錢在誰身上（佔比）、
      比上一段多還少（佔比變化 pp）、名次跑到哪（rank / rank_chg）、族群漲了多少（期間報酬）、法人買賣多少。
      「上一段」一律是**同樣長度的前一段**（本週比上週、本月比上月、近三月比再前三月），不然比較沒有意義。
    - **期間報酬用複利不是相加**：每日族群漲跌幅累乘，跟看 K 線同一個口徑（測試會擋）。
    - **法人圖跟著期間走**，不再只有「今日」。今日的法人數字一天到晚在變臉，看一週合計才看得出誰在收貨。
    - **名次變化（bump 圖）**：近 8 週、最新一週前 10 大族群的佔比排名。
      這是「趨勢」的載體 —— 單看某一週的排行分不出「連續三週往上」和「這週剛好爆量」，那是完全不同的兩件事。
    - **RRG 重做**（Andy：「完全看不懂這張圖」）：四象限塗色並寫白話（領先＝比大盤強而且還在變強…）、
      **軌跡末端往前延伸一支箭頭**標出行進方向、象限可篩選、下方一行圖例寫出順時針循環。
      箭頭要用 `markLine` 的 `symbol:['none','arrow']`（它會自己轉向）；
      line series 的 `symbol:['none','arrow']` **不會轉向，會變成一個小方塊**。
      座標範圍只照「現在的位置」算、不含尾巴（某條尾巴甩很遠會把整張圖壓扁），尾巴超出去就裁掉。
      `markArea` 的邊界要給**數字**，用 `'max'`/`'min'` 字串在 scale 軸上會被畫成三角形。
      畫軌跡的族群上限 8 條，再多就糊成一團。
    - **估值篩選（「本益比需要多可篩選功能 全部在一起」）**：族群／市場／本益比區間／股價淨值比／ROE／市值／
      只看低於族群中位／排除虧損，全部排在同一條，改任何一個右上角檔數就跟著變。
      左邊散布圖橫軸本益比、縱軸 ROE、大小是市值（右下＝貴又賺得少）。
      本益比只有**同族群**比才有意義，所以「低於族群中位」是獨立的一個條件而不是排序。
    - **每張圖一顆「怎麼看」**：展開是白話說明，而且一定要寫「所以我該怎麼用」，不是只解釋軸是什麼。
      說明文字只在第一次展開時才塞進 DOM，八段長文一開始就寫進去會拖慢首次進頁。
    - **資金集中度加前 5／前 10 切換**與一句現在是縮圈還是擴散的判讀。
      前 5 看「主流有多獨」，前 10 看「主流圈子有多大」，兩條走勢分岔時是在換主流。
61. **treemap 不要開 roam（Andy 遇到的空白畫面就是它）**：ECharts treemap 的 `roam:true` 會把整張圖平移縮放，
    而且**縮放狀態存在 instance 裡，`setOption` 不會重設**。使用者拉一拉把圖拖出可視範圍，
    換分頁再回來還是空白，只能重新整理 —— 線上的「題材資金熱力」整片黑就是這個。
    而且它也解決不了原本的問題（「小方塊看不到」），因為放大的是整張圖不是那一塊。
    改成**產業鏈下鑽**：上面一排晶片，點半導體就只畫半導體那幾個族群（方塊自然變大），
    點「全部」回到分組圖；點方塊還是進族群頁。題材熱力圖同樣關掉 roam。
62. **圖表容器要掛 ResizeObserver**：容器在 `display:none` 或還沒排版時 `echarts.init` 出來是 0×0，
    畫完就是一片空白而且不會自己好。切分頁、展開「怎麼看」、視窗縮放都會踩到。
    `chart()` 現在會盯著容器尺寸，一變就 `resize()`。空狀態也加 `.isempty` 把 min-height 收掉，
    不要留一個 420px 的黑方塊讓人以為壞了。
63. **相對輪動不用散點圖（Andy：「不要用這種方式 看不懂」）**：RRG 散點要同時讀兩個軸、還要自己判斷象限，
    本來就不是拿來快速決策的。改成**四張階段卡**（改善 → 領先 → 轉弱 → 落後），
    每張直接寫「這個階段是什麼意思、該怎麼辦」，底下列出族群與相對強弱、動能方向。
    最上面另外拉一條「最近 N 天換階段的族群」—— **已經在領先的使用者早就知道了，
    剛從落後轉進改善的才是新訊號**，那條是整張表最有價值的部分。比較天數可切 5／10／20 天。
64. **總覽上方的數字要點得開**：「567 / 1545」看完沒辦法做任何事。
    `build_payload.movers()` 產出今天漲停／跌停／漲幅前段／跌幅前段／成交值前段各 60 檔（含族群），
    `breadth.by_group` 產出每個族群有多少成分股站上 MA20／MA60。
    前端四顆 KPI 點開就是對應的股票明細，每一列都進得了個股頁。
    漲停門檻用 **9.5%** 而不是硬比 10%：成交價要照檔位跳，實際常落在 9.7~10.0，硬比會漏掉一堆真的漲停。
    今天沒有 A/B 級時，候選明細改列綜合分前 20 並講清楚「這是觀察名單不是進場訊號」，
    比列一句「沒有符合的股票」有用。
65. **SMC 供需區右邊界要停在最後一根 K 棒**（Andy：「移動 K 線圖，他卻沒有跟著移動縮放」）：
    以前填色一路填到**畫面右緣**，平移時左邊界有跟著跑、右邊界永遠貼著邊，
    看起來就像一整片色塊黏在畫面上不會動 —— 實測左邊界確實有動（x0 366→75），
    但使用者只會看到「右邊沒動」。現在右邊界跟著最後一根 K 棒（`setLastTime()`），
    平移時兩邊一起跑（實測 x1 915→621），一眼看得出它是跟著 K 線的。
    標籤也從「壓在區間中央的 K 棒上」改成放到區間上緣外面、優先塞在最後一根 K 棒右邊的空白處。
66. **每個指標的顏色／線寬／透明度都可以個別設定**（Andy 2026-09-12）：
    `cfg.st[指標] = { c, c2, w, o }`，BOLL／成交量／KD／MACD／RSI 各一列（顏色、線寬、透明度），
    SMC 供需區另外一列（需求色、供給色、填色濃度 0–45%、框線濃度、框線粗細、標籤開關）。
    透明度用 `hexa(hex, pct)` 把使用者選的色碼轉成 rgba，不要在程式裡寫死半透明色 ——
    寫死的話使用者改了顏色，透明度就跟著跑掉。全部存在 `localStorage['tw.kcfg']`。
67. **熱力圖：位置永遠固定，只給「放大」和「點擊」兩個動作**（Andy：「需要固定原始大小不動，
    他只有放大功能 或是點擊後 出現對應個股，我移動的時候不會影響他熱力圖整張變化」）：
    - **不做拖曳平移**。treemap 一旦可以拖，整張圖就會被拖走而且沒有回復的入口
      （這就是線上題材熱力圖整片空白的成因，見 #61）。方塊位置固定＝每天打開看到的版面是一致的，
      「左上角那塊最大」這種空間記憶才有用。
    - **放大**：右上角一顆鈕開全螢幕罩，同一張圖用整個視窗畫（小方塊的 `visibleMin` 一併下修，
      原本被藏起來的小族群在放大時才顯示得出來），罩裡一樣可以切產業鏈，Esc 或點背景關閉。
    - **點擊**：點方塊**在原地列出成分股**（每檔可點進個股頁），不要直接把人帶離當前頁面 ——
      使用者通常只是想知道「這塊裡面有誰」，跳頁之後還要按返回。想看全部再點面板裡的「進族群頁 →」。
    - 放大罩與原圖共用同一份 option 產生器，兩邊才不會畫出不一樣的東西。
68. **長期工作 Agent 落地成 `.claude/agents/`**（Andy：「我需要建立長期工作 Agent，
    當提出需求時會對應不同 Agent 工作」）：AGENTS.md 原本只是「角色說明文件」，
    要靠人記得把段落貼進 subagent。現在六個角色各有一個定義檔，Claude Code 會自動載入，
    CEO 用 Task 直接派工。每個定義檔開頭都硬性要求：**先讀 CLAUDE.md → AGENTS.md 自己那節
    → HANDOFF.md → DECISIONS.md 才動手**，而且 DECISIONS.md 拍板過的事不重開。
    AGENTS.md 加了一張「需求長這樣 → 派給誰」的對照表。
    跨領域的需求（例如「換一種圖，而且報酬要用複利」）由 CEO 先派口徑那一邊定案、再派 UI 實作，
    不讓兩個 Agent 同時改同一個檔。
69. **瀏覽器縮放會讓 ECharts 圖縮成中間一小塊**（Andy：「你覺得這樣縮小好看嗎」）：
    ECharts 在 `init` 當下記住 `devicePixelRatio`，使用者按 Ctrl +/− 之後 DPR 變了，
    只呼叫 `resize()` 的話畫布尺寸對了、內部座標還是舊 DPR，結果就是圖畫在畫布一角、周圍一片黑。
    **沒有別的解法，DPR 一變就得整個 dispose 重建**。現在用 `matchMedia('(resolution: Ndppx)')`
    監看，一變就丟掉所有圖表實例並重畫當前分頁。
70. **總覽下方三張圖不用長條圖**（Andy：「不要用長條圖表現，用其他更生動方式」）：
    - **市場寬度** → 半圓儀表（站上 20 日均線比例，含分區文字判讀）＋ 漲／平／跌環圈。
      四根長條裡有兩根常常是 0%，那不是資訊是版面浪費。
    - **投信連續買超** → 氣泡圖：橫軸連續天數、縱軸期間累計張數、泡泡大小＝累計張數。
      長條圖只講得出「買幾天」，講不出「買多少」；右上角那幾顆才是真的在收貨。
    - **族群估值** → 「貴不貴 × 錢有沒有進來」散布圖（橫軸本益比中位、縱軸資金流向 pp、
      泡泡大小＝成交值佔比，四象限標白話）。單看本益比高低沒有用（IC 設計本來就比航運貴），
      **左上「便宜且資金流入」才是這張儀表板真正要找的地方**。
71. **總覽上方的數字是摘要，明細給它一個獨立分頁**（Andy：「上面是簡說，點擊後會出現所有資訊」）：
    新增「市場明細」分頁（`#market/<kind>`），四個子頁：漲跌家數（漲停／跌停／漲跌幅前段／成交值前段）、
    站上均線（拆到每個族群）、資金集中（前十大族群與成分股）、今日候選。
    總覽的 KPI 點下去直接跳這個分頁，不再在總覽塞一個小面板 —— 塞在總覽裡鋪不開，
    而且會把首頁越拉越長。
72. **滾輪放大，倍率下限鎖在 1（原始畫面）**（Andy：「希望可以用滾輪放大，但縮小最多就是原始畫面」）：
    - **不用 CSS transform 縮放**。那是把已經畫好的點陣圖拉大，字會糊。
      改成把裡面那個元素的寬高乘上倍率、外框 `overflow:auto`，
      ECharts `resize()` 之後是用新尺寸重畫，放多大都清楚；SVG 本來就是向量。
    - **倍率下限 1**，所以永遠不會縮成一小塊（那正是 #69 那個 bug 的觀感）。
    - **已經 1 倍還往下滾就不攔截 `wheel`**，讓頁面正常往下捲 ——
      不然滑鼠滑過圖表時頁面會卡住，比看不清楚更煩。
    - 右上角一個徽章顯示目前倍率，雙擊還原。
    套用在：總覽資金熱力圖、題材資金熱力圖、產業地圖 treemap、產業鏈與題材的產品剖析圖。
73. **點剖析圖的零件「只亮不切」**（Andy：「當我點擊圖片時，不用馬上切換到下方股票」）：
    以前點零件會 `scrollChainTo()` 把畫面捲到關聯圖、同時把下面的成分股表整個篩掉，
    使用者只是想知道「這個零件是誰做的」，結果畫面跳走、表也變了。
    現在拆成兩個狀態：`segHi`（點圖只亮起來＋在原地展開環節說明）與 `segFilter`（真的篩成分股）。
    要篩就按說明框裡的「只看這個環節的成分股 →」，或用下面的環節晶片／族群卡片 —— **明確的動作才有明確的後果**。
74. **放大只發生在框內，外框高度鎖死**（Andy：「放大部份會影響到整頁面 需要調整」）：
    `wheelZoom` 在 `.zwrap` 裡再包一層 `.zpane` 捲動層，放大時把 `pane.style.height` 鎖成原始高度，
    只讓裡面的內容變大並用捲動去看。不鎖的話卡片會被撐成好幾倍高，整頁版面都被推開，
    而且會出現整頁的水平捲軸。`linkRow()` 也改掛在 `.zwrap` 外面，
    不然放大時底下那排「看成分股」的連結會被裁掉。
    驗收條件寫死在 `_uitest.check_zoom()`：**放大前後卡片高度必須完全一樣、整頁不能出現水平捲軸**。
75. **每一張擠得下太多資訊的圖都要能滾輪放大**（Andy：「這邊資訊太小看不到 需要新增縮放」）：
    除了原本的兩張熱力圖，再加上總覽的市場寬度／投信連續買超／族群估值／輪動時鐘，
    以及資金流向頁的排行、名次變化、輪動時鐘、資金去向、河流、族群×法人。
    一律走同一個 `zoomChart(wrapId, chartId)`，行為完全一致（下限 1 倍、雙擊還原、右上角徽章）。
76. **輪動階段配一張「資金輪動時鐘」**（Andy：「輪動族群 需要搭配圖表，可視化點 需要直觀看圖明白」）：
    RRG 的 XY 散布圖 Andy 看不懂（#63），純文字看板又少了「現在往哪走」的感覺。
    改用圓盤：四段照循環順序排成四個象限（落後左下 → 改善左上 → 領先右上 → 轉弱右下，順時針），
    一個族群一顆點，點大小＝成交值佔比，另外拉一條直線到「N 天前的位置」（小圈圈那端）。
    **兩個軸要各自正規化再算角度**：相對強弱常常差好幾點、動能只差零點幾，
    直接算角度的話所有點會擠在水平線上（正好是兩段的交界），等於沒有資訊。
    除以各自的最大偏離量之後正負號沒變，四段歸屬完全一樣，但分佈攤得開。
    **尾巴只畫「N 天前 → 現在」一條直線**，畫完整軌跡會變成一團毛線，反而看不出方向。
77. **名次變化要跟著期間換刻度**（Andy：「資金流向排名不會變 需要修正」）：
    以前不管切到哪一段期間，名次變化都固定是「近 8 週」，所以看起來就是不會變。
    `period_flows()` 現在同時產出 `bumps.week` 與 `bumps.month`（各 8 段），
    前端看期間 key 選一份：`w*` 用週、`m*`／`q` 用月，卡片副標也跟著改。
    切換時一定要 `notMerge`，因為兩份的族群數會不一樣，不清乾淨會留下上一次的殘線。
78. **週期按鈕上就要標出哪些這檔沒有**（Andy：「K 線圖 時間週期 為何 1 日以下都不見」）：
    分 K 逐檔跟 Yahoo 要，成本高，本來只給前 150 名，所以大多數股票點 15 分／1 時／4 時
    會看到一行「這檔沒有分 K」——但要點下去才知道。現在兩件事一起做：
    - `INTRADAY_LIMIT` 150 → **400**（族群成分股仍然全給，其餘照成交值補）。
    - 沒有資料的週期在按鈕上**直接劃掉**並在 `title` 寫清楚原因，點下去還是會展開說明。
    驗收會檢查「劃掉的一定畫不出圖、沒劃掉的一定畫得出來」，標示不能和實際資料不一致。
79. **個股頁沒產生時要給得出替代路徑**（Andy：「為何會出現沒顯示的資訊，需要每檔股票都可以觀看」）：
    索引（`stocks.json`）與個股頁檔案現在是 1:1（2334 : 2334，零缺口），
    所以「還沒產生」實務上不會出現；真的出現時（新上市、當天才補到歷史）
    要寫清楚是「至少要 60 根日線」以及下一次盤後就會有，並附上它所屬族群的連結，
    不能只丟一句「還沒產生」讓人卡在那裡。個股頁也加一行「資料更新到 YYYY-MM-DD」。
80. **缺完整個股頁時給「簡版個股頁」，不給死路**（Andy：「不可以出現沒有資訊狀況」）：
    `stock/<代號>.json` 偶爾會缺 —— 新上市、日線不到 60 根、或預覽版只帶了一部分。
    以前是一句「還沒產生」＋一個返回鍵，那就是死路。現在改成用**已經載入的**資料重組一頁：
    `stocks.json` 給今日價量、`groups_detail` 給三大法人與技術分與判定、`fundamental` 給
    本益比／PB／ROE／市值／營收 YoY／營運動能、`news` 給相關新聞、族群給同業名單，
    上方產業鏈照樣同步高亮。**沒有值的格子直接不放**（不留「—」佔位），寧可少一格也不要看起來像壞掉。
    只有「這個代號根本不存在」才會是單純的說明頁。
81. **放大後要能用游標抓著移動**（Andy：「需要新增游標抓取可以移動功能」）：
    `.zpane` 上掛 pointerdown/move/up，只在 k > 1 時生效；位移超過 4px 才算拖曳，
    拖過就把接下來那一次 click 吃掉（不然放開手會順便點到熱力圖的方塊）。
    1 倍時完全不攔，點方塊看成分股照常。
82. **還原成 1 倍的瞬間不能把整頁帶著往下衝**（Andy：「還原成正常大小 他會導致整體頁面往下」）：
    「已經 1 倍就把 wheel 交還給頁面」這條規則在剛還原的那一刻是錯的 —— 使用者手還在滾，
    整頁會立刻被帶著往下。改成還原後留 **450ms 緩衝**，這段時間內的 wheel 一律吃掉並延長緩衝，
    停手後才恢復正常頁面捲動。驗收會量 `scrollY`：還原前後不能差超過 2px。
83. **卡片被同一列的鄰居撐高時，圖要自己長高把空間吃掉**（Andy：「不滿當前版面」）：
    總覽的資金熱力圖卡片和右邊的輪動階段卡在同一個 grid 列，鄰居 1533px、圖固定 420px，
    結果卡片裡有 985px 是空的。加 `.card.fillchart` 讓卡片變 flex column、
    `.zwrap`／`.zpane`／`.chart` 一路 flex 撐滿，ECharts 的 ResizeObserver 會自己重畫。
84. **產業與個股的剖析圖不加縮放**（Andy 明講）：剖析圖本來就可以左右滑，包成縮放框只是多一條捲軸。
    `wheelZoom` 只留給熱力圖類（總覽資金熱力、題材資金熱力、產業地圖 treemap）與資訊密度高的統計圖。
85. **資料不夠就不要畫圖**（Andy：「籌碼下方無法抓取到數據，就替換其他方式，或是直接刪除」）：
    籌碼頁的線圖未滿 3 個點一律不畫 —— 一兩個點連起來是一條假的斜線，比沒有還糟。
    改成把「現在的數字」用 kvs 直接列出來，並寫清楚累積到幾筆就會變成走勢圖；
    一筆都沒有的那張卡片整張不出現。這條規則套用到所有「歷史序列」型的圖。
86. **K 棒寬度可調，預設比函式庫寬**（Andy：「K棒長度需要可以調整，default先長一點」）：
    Lightweight Charts 預設 barSpacing 是 7px，K 棒細得像頭髮。預設改 11px，
    設定面板加 3–28px 滑桿，存 `localStorage['tw.kcfg'].bar`，每次 `apply()` 都套回去。
87. **中長期計畫獨立成 `ROADMAP.md`**：`HANDOFF.md` 是「現在做到哪」，`DECISIONS.md` 是「拍板過什麼」，
    `ROADMAP.md` 是「要做什麼、誰做、誰驗、什麼時候」。人力擴編到九個 Agent
    （新增 strategy-backtest、visual-explainer、data-steward），reviewer 每批要簽名。
88. **證交所季報是「年度累計數」，用之前一定要還原成單季**（專家審查抓到的 P0）：
    `t187ap14_L` 的 Q2 ＝上半年合計、Q3 ＝前三季合計；FinMind 給的是單季。兩者混在同一張
    `financial_q`，而 `ttm()` 直接把最近四列相加 → 走證交所那條路的股票 **TTM EPS 被灌水、
    自算本益比系統性偏低約 20%**（1,035 檔受影響）。
    `fundamental._decumulate()` 在算 TTM 之前先還原：同一 code 同一年度從 Q1 往後走，
    邊走邊記「到上一季為止的單季合計」，累計列就減掉它。**兩種來源會混在同一年**
    （Q1 來自 FinMind 單季、Q2 來自證交所累計），所以不能只在「前後都是累計」時才減。
    季別不連續（缺 Q2 直接跳 Q3）就停止還原 —— 寧可不減也不要減出一個兩季合計。
    **驗收標準：自算 PE ÷ 證交所官方 PE 的中位數必須落在 [0.97, 1.03]**（修正前 0.8366、修正後 0.9998）。
89. **週 K 與月 K 的日期一律標「該期間最後一個交易日」**（前後端必須同一套）：
    以前月線後端用 `MS` 停在月初、前端標月底，結果 `chart.js` 的 `setMarkers` 把
    **2,022 個月線 BOS/CHoCH 整批過濾掉**、供需區的 `since` 也對不上而從畫面最左緣開始畫。
    而且是靜默失敗 —— 使用者只覺得「月線好像沒結構」。任何新增的重採樣週期都要遵守這條。
90. **同一張圖只能有一種紅綠語意**：熱力圖的顏色是資金流向、文字卻只寫漲跌，於是出現
    「紅底寫 −0.7%」。現在方塊上兩個數字都寫並標明哪個是哪個。往後任何圖若要表達第二個維度，
    用邊框／發光／大小，不要再借用紅綠。
91. **`visualMap` 的中點色不可以等於面板底色**：季節性熱力圖中點用 `#0f172b`（就是 `--panel`），
    ±5% 以內的格子全部隱形，324 格看起來像一張數字表。中點改 `#16203a`。
92. **3D 剖析圖走 Three.js**（Andy 拍板「先試試看 three.js」，POC 四條硬性驗收全過，2026-09-13）：
    three.js 0.160.1 內建在 `site/vendor/`（公司網路擋 CDN，不可用 CDN），**動態載入**——
    沒按下「3D 立體」就不會付那 670KB。場景資料寫在 `site/three3d.js` 的 `SCENES`，
    每個零件 `{seg, name, note, box, at, n, gap, axis}`，`seg` 對得上 `supply_chain.yaml` 的環節 id，
    所以點 3D 零件和點 SVG 零件走的是同一條路（`highlightSegments` 同時餵 SVG 與 3D）。
93. **從傳統 `<script>` 動態 import 一定要寫 `./`**：`import('vendor/three.module.min.js')` 會被當成
    bare specifier（像 `import 'react'`）而解析失敗，必須 `import('./vendor/three.module.min.js')`。
    這個錯誤只在瀏覽器出現、`node` 端看不到，所以 POC 才會卡在「載入 3D 中…」不動。
    同時 `wire3D()` 一定要 try/catch 並把錯誤寫進畫面 —— 不可以讓使用者停在「載入中」以為當掉了。
94. **模組層級的函式不可以用 render 函式的區域變數**：`wire3D()` 寫在模組層級卻直接用了
    `segHi`／`segFilter`／`syncHighlight`（都是 `renderChain()` 的區域變數），
    畫面看起來正常、但每次點零件都丟 `ReferenceError`，而且只在 console 看得到。
    改成把要用到的動作當參數傳進去（`wire3D(el, chainId, {onSeg, sync})`）。
    **驗收腳本一定要收 `pageerror`** —— 這個 bug 就是靠 `_uitest` 收到的 PAGEERROR 才抓到的。
95. **CSS2D 標籤要能點，也不能擋住轉視角**：標籤 `pointer-events:auto` 之後，
    按在標籤上時畫布收不到 `pointerdown`，OrbitControls 沒啟動 → 整台機櫃轉不動（驗收 1 就是這樣掛的）。
    解法是標籤的 `pointerdown` **原樣轉發給畫布**（OrbitControls 收到後會 `setPointerCapture`，
    之後的移動與放開自然走畫布那條路），再用「位移 < 5px 才算點選」區分「點名字」與「拖著轉」。
96. **WebGL 的驗收不可以比畫布像素**：canvas 沒開 `preserveDrawingBuffer`，
    `readPixels` 在繪製後一律回 0，兩次雜湊永遠相同 → 「轉不動」和「轉得動」看起來一樣。
    所以 `Rack3D.mount()` 回傳 `cam()`（相機座標）與 `screen(seg)`（零件現在投影在螢幕哪裡），
    驗收改成比相機座標、點擊改成問場景要座標，不再猜像素。
97. **`OrbitControls.reset()` 之後要先關阻尼再 update**：不然上一次拖曳殘留的慣性會讓相機
    停在預設視角旁邊一點點（`[85.36, 62.13, 96.53]`），「重設視角」看起來像沒完全重設。
98. **換頁一定要 `dispose3D()`**：瀏覽器的 WebGL context 有數量上限（一般十幾個），
    離開產業鏈頁不收掉，逛幾頁之後整個網站的圖都會掛。`route()` 進來第一件事就是收。
99. **沒有真的 fixture 就不准寫 parser**（重申並制度化）：Claude 的容器連不到官方端點，
    所以新增 `.github/workflows/probe.yml` ＋ `scripts/probe_sources.py`：手動觸發 → 在 Actions 上
    真的打一輪候選端點 → 把狀態碼、欄位名、前三筆樣本 commit 成 `docs/fixtures/<probe>_probe.json`。
    連不上也是結果（記成 `error`），探測器本身不可以丟例外。重大訊息這條走這個流程。
100. **滾輪縮放只留熱力圖類**（Andy 09-13：「將這邊的縮放功能取消」，並指定「只保留資金熱力圖、板塊熱力圖、題材熱力圖」）：
     保留 → 總覽「資金熱力圖」（`#heatWrap`）、產業地圖板塊圖（`#indTreeWrap`）、題材「資金熱力」（`#themeMapWrap`）。
     **拿掉** → 總覽下方三張小圖與輪動時鐘、資金流向頁六張（資金流向排行／名次變化／輪動時鐘／
     資金去向桑基／族群佔比河流／族群 × 法人）、題材頁的產品剖析圖。
     理由：這些圖本來就用滿卡片寬度，縮放帶來的「滾輪放大 · 放大後可拖曳」徽章反而壓在圖上（有幾張直接蓋住圖例），
     滑鼠經過時還會搶走頁面捲動。熱力圖不一樣 —— 方塊多、字被壓到很小，放大是真的有用。
     `.zwrap` 的 class 也一併從那些外框拿掉（留著只是讓 CSS 規則空轉）。
     驗收：`_uitest.py` 的 `check_nozoom()` 會對每一張確認「沒有縮放框與徽章、滾輪不放大、滑鼠停在圖上往下滾頁面照常捲」。
     **這條推翻了 09-12 的「每張小圖都加縮放」**；要看大圖的路徑改成右上角的「放大」按鈕（明確、不搶捲動）。
101. **K 線圖高度跟著視窗走，並加「⤢ 寬版」**（Andy 09-13：「K 線圖太小，版面需要擴大」）：
     以前 `#lwc` 寫死 640px，螢幕再大也只有那麼高。改成
     `clamp(560px, calc(100vh - 300px), 1180px)`（扣掉頂部列＋標題＋工具列＋說明約 300px），
     手機 `clamp(420px, calc(100vh - 260px), 760px)`，四週期同看的每一格也跟著視窗算。
     **寬版**：工具列的「⤢ 寬版」把右側事件欄收起來，整個視窗寬度都給圖
     （1680×1000 實測：1190×700 → 1550×790），狀態存 `localStorage['tw.kwide']`。
     **只在個股頁生效** —— 離開個股頁一定要把事件欄還回來，不然使用者會覺得它莫名其妙消失；
     回個股頁再自動復原。Lightweight Charts 是 `autoSize`，容器變寬會自己重畫。
102. **驗收「SMC 區間跟著平移」要比左右邊界，不能只比左緣**：圖變高之後畫面裡的價格範圍變大，
     常常有一個區間的左緣本來就在畫面外（`x0` 永遠是 0），只看左緣會假性失敗（這次就是）。
103. **K 線圖預設就是寬版**（Andy 09-13：「K 線圖 default 就大一點」）：`tw.kwide` 沒設定過時視為開啟，
     只有自己按過「⤢ 寬版」關掉才會是窄的。預設高度再放大一級：
     `clamp(600px, calc(100vh - 250px), 1280px)`；寬版 `clamp(640px, calc(100vh - 185px), 1500px)`。
     1680×1000 實測：預設 **1550×815**，關掉寬版才是 1190×750。
104. **「縮放」的最終狀態，全站只有三個地方有**（Andy 反覆要求，這條是最終版）：
     總覽「資金熱力圖」`#heatWrap`＋`#heatZoom`、產業地圖板塊 `#indTreeWrap`、題材「資金熱力」`#themeMapWrap`＋`#themeZoom`。
     **其他一律沒有**：沒有 `.zwrap` 框、沒有 `.zbadge` 徽章、沒有任何寫著「放大 / 縮放」的按鈕。
     這次還抓到題材剖析圖右上角的「放大」**是一顆沒有接任何功能的死按鈕**（只有 markup，沒有 handler），一併移除。
     驗收：`_uitest.py` 新增 `t_zoom_sweep()`，走遍總覽／資金流向／產業地圖／產業鏈／題材／市場明細／季節性／個股
     ＋ 6 個題材細節頁，任何一頁出現不在白名單裡的縮放入口就算失敗。
105. **開工第一件事是確認推得上去，不是開始改**（2026-09-13 的教訓）：
     這個 session 沒有 `MiaoZiKe/tw-rotation` 的寫入授權（git proxy 擋、GitHub API 也 403），
     但這件事是在做完 8 個 commit 之後才被發現的。期間 Andy 連續四次回報「縮放還在」，
     每一次其實都早就改好了 —— 他看到的永遠是正式站的舊版。
     **規則**：每個 session 一開始先 `git push --dry-run origin deploy:main`；被擋就立刻回報，
     不要埋頭做。授權是跟著 session 走的，新 session 一定要重驗。
     **並且**：push 之後要真的打開線上網址確認版本換掉，才算交付完成。
106. **資料時效：排程改多時點、盤後第一輪只抓價量**（2026-09-14，Andy：「資訊更新太慢了，
     我今天盤後才看到等到隔天才買，不管是回測、報酬等等都會影響到數據真實性」）：
     根因有兩層。第一層是排程：`daily.yml` 只設台北 18:30 一個時點，而 GitHub 免費層的
     排程會延遲 —— 09-11 那次實際延到 22:35 才跑，抓到的是前一天，網站就永遠停在 T-1。
     第二層是「抓到 0 筆」被當成成功：09-11 資料湖裡上櫃 982 檔、上市 **0 檔**，
     `last_run.json` 的 `errors` 卻是空陣列，Actions 摘要是綠的，網站上完全看不出來。
     **決定**：
     - 排程改成台北 15:30 / 18:30 / 21:30（`30 7,10,13 * * 1-5`）。重跑是安全的，
       `store.append()` 以 key 去重。
     - 加 `--phase`：`price` 只抓價量（證交所＋上櫃＋估值＋指數＋大盤），`full` 是完整管線。
       15:30 那輪跑 `price` —— 三大法人、融資券、財報、新聞那時根本還沒出，
       硬抓只會把「還沒出」記成「沒回資料」，讓網站每天下午都掛黃燈。
       延遲到 UTC 09:00 之後才跑的話自動改跑 `full`。
     - `RESULT["empty"]` 記下所有「抓得到端點但回 0 筆」的步驟，不再只寫 log。
     - `last_run.json` 改寫兩次：build_payload **之前**先寫一份這一輪的快照。
       以前只在最後寫，`build_payload` 讀到的永遠是上一輪的狀態，整整慢一輪。
107. **資料狀態攤在網站頂端**（2026-09-14，同一件事的另一半）：
     `meta.json` 多帶 `price_latest`、`price_ahead_of_payload`、`last_run_at`、
     `last_run_phase`、`last_run_empty`；前端 `renderFreshness()` 把它畫成一條三色橫幅：
     綠＝到齊、黃＝落後或有來源沒回資料、紅＝排程掛了或來源出錯。
     `price_ahead_of_payload` 就是 09-11 那個情形：資料湖已經有更新的一天，
     但那天上市 0 檔，所以畫面只好停在前一天 —— 現在這句話會直接寫在頂端。
     以前的行為是「3 天沒更新才跳一行字」，來源失敗則完全沒有出口。
     驗收：`_uitest.py` 新增 `t_freshness()`，真的把 `meta.json` 換成四種狀態、
     真的讀橫幅的文字與顏色，四種狀態的文字必須彼此不同。
108. **`mis.twse.com.tw` 進白名單，作為盤中即時與「當天資料」的來源**（2026-09-14 Andy 拍板）：
     #1 原本寫「只用 `openapi.twse.com.tw` 與 FinMind，絕不碰 `www.twse.com.tw/rwd/...`」。
     現在多一個：`https://mis.twse.com.tw/stock/api/getStockInfo.jsp`（證交所「基本市況報導」
     官網前台自己在打的那支）。**它跟被禁的 `www.twse.com.tw/rwd/...` 是不同主機、不同服務。**
     2026-09-14 15:09 實測到的事實：
     - 一個請求 120 檔、120ms 回全；上市 `tse_` 與上櫃 `otc_` 可以混在同一個請求；
       `tse_t00.tw` 是加權指數。回應帶開高低收、累計成交量與報價時間。
     - **當時它已經有當天（20260914）的資料**，而 `openapi.twse` 那支到 14:32 都還停在 09-11。
       所以這條不只解決盤中即時，也是「網站慢一天」真正的解法。
     使用上的自我約束（寫在 `workers/quote-proxy/worker.js` 裡）：只轉這一個端點、
     `ex_ch` 嚴格檢查格式、只給白名單網域 CORS、邊緣快取 10 秒。
     **只抓畫面上看得到的代號**，不做全市場輪詢 —— 資金輪動是族群層級的事，
     全市場每分鐘沒有意義，對官方端點也不禮貌。
109. **盤中即時只能在瀏覽器做，而且中間一定要有一層代理**：
     GitHub Actions 的 cron 最短 5 分鐘，而且免費層會延遲（本專案實測延遲過 4 小時），
     所以「每分鐘」不可能走 Actions。但瀏覽器也不能直接抓 —— 2026-09-14 15:08
     從 `https://miaozike.github.io` 實測：正常 `fetch` 回 `TypeError: Failed to fetch`，
     `mode:'no-cors'` 卻通。**連得到但讀不到 ＝ CORS 擋的，不是網路不通。**
     因此新增 `workers/quote-proxy/`（Cloudflare Worker，免費方案每天 10 萬次請求，
     盤中每分鐘一次一天約 330 次）。這一層之後富果即時也共用（#30 本來就規劃了）。
     Worker 網址存在瀏覽器的 `localStorage['tw.live.proxy']`，不是密鑰，可以放。
     Andy 部署好把網址給 Claude 之後，會填進 `live.js` 的 `DEFAULT_PROXY` 當預設值。
110. **更新頻率：盤中每分鐘、盤後每 30 分鐘，另外一顆手動「更新」鈕**（Andy 2026-09-14 指定）：
     盤中判定為台北時間週一～五 09:00–13:35（收盤 13:30，多留 5 分鐘讓尾盤撮合落地）。
     **不在前端維護國定假日行事曆** —— 假日照跑只會拿到上一個交易日的數字，頁面有標報價時間，
     為此維護一份行事曆不划算。分頁切到背景就暫停輪詢（`document.hidden`），切回來補跑一次。
     手動「更新」除了立刻抓即時報價，還會比對 `meta.json` 的 `generated_at`，
     **只有真的重新部署過才整頁重載** —— 每次都 reload 會把展開的列、勾選的族群、
     K 線縮放全部弄不見。
111. **這條推翻 #37**（「Andy 只重新整理網頁、不點任何按鈕做更新」）：
     Andy 2026-09-14 明確要求「需要新增更新按鍵」。#37 的精神（不要逼他去點才有資料）
     仍然成立 —— 自動更新預設開著，按鈕只是「我現在就要看最新的」的捷徑。
112. **即時價只更新標記過的元素**（`[data-live][data-lc]`）：`live.js` 不去猜畫面上哪一格是價格，
     只動明確標記過的：候選表與候選卡片的收盤／漲跌、總覽的加權指數、個股頁頂端報價、
     產業鏈成分股表。要讓新的地方跟著跳，就在那裡加 `data-live="close|chg|idx|vol"` 與 `data-lc="<代號>"`。
     數字真的變了才閃一下（`.liveflash`）—— 沒有這個提示，盤中盯著看根本分不出有沒有在動。
     順帶把候選表的收盤欄從 1 位小數改成 2 位，跟個股頁與產業鏈表一致（本來就它一個不一樣）。
113. **補齊第 7〜9 個 Agent**（Andy 2026-09-14：「提示詞裡的第 7–9 個幫我建好」）：
     `strategy-backtest`（策略回測）、`visual-explainer`（圖解教學）、`data-steward`（資料治理）。
     名稱沿用 ROADMAP 缺口表裡已經規劃好的代號，不另取新名。`AGENTS.md` 從六個改成九個，
     派工對照表與 `.claude/agents/_README.md` 的名單一起更新。
     三個定義檔各自寫死了該領域最容易犯的錯，這些不是泛泛的原則，是這個專案真的踩過的：
     - `strategy-backtest`：訊號一律「收盤後產生 → 隔日開盤成交」；財報用法定可用日；
       要有紙上帳本與交易成本；**結果不好照實寫，不准調參數湊**；樣本數一定要寫在旁邊。
     - `visual-explainer`：每則解釋固定四段，第四段「什麼時候會騙人」最重要；
       **不確定的不要講成確定的**；不要用術語解釋術語；站上同一個概念只能有一個名字
       （現在「掃蕩」與「假跌破」是同一件事，要統一）。
     - `data-steward`：「沒設定 / 還沒到 / 真的壞了」三件事永遠分開記；
       綠燈必須代表真的有資料；**「上次成功是什麼時候」比「這一輪成不成功」重要**；
       每個來源容忍天數不同；排程時間用量的不要用猜的；**不准為了讓燈變綠而放寬判定**。
     同時把 `data-scraper` 的紅線更新成白名單寫法（#108 加入 `mis.twse` 之後，
     原本那句「只用 openapi 與 FinMind」已經過時，而且要講清楚 `mis.twse` 跟被禁的
     `www.twse.com.tw/rwd/...` 是不同主機，不要搞混）。
114. **Worker 網址寫進 `live.js` 的 `DEFAULT_PROXY`**（2026-09-14 15:48 部署並端到端驗過）：
     `https://tw-quote.kcq01010909.workers.dev`。這不是密鑰 —— Worker 本身只轉一個端點、
     `ex_ch` 嚴格檢查、只給白名單網域 CORS，寫進 public repo 沒有風險，
     換一台電腦／換一個瀏覽器就不用再設定一次（⚙ 面板填的值仍然會蓋過它）。
     **從正式站 `https://miaozike.github.io` 實際跨源打過的結果**：
     `/health` 200；`/quote?ex_ch=tse_2330.tw|otc_6488.tw|tse_t00.tw` → 200、420ms、
     3 檔一次回全（上市＋上櫃＋大盤混在同一請求），資料日期 20260914（當天）；
     `ex_ch=evil` → 400 `bad ex_ch token`，護欄有效。
     這一段補上了 #109 當時「只用攔截的假回應驗過」的缺口。
115. **管線也從 `mis` 取當天價量，但只把它當「暫定值」**（2026-09-14，Andy：「讓管線也抓 mis.twse，
     這樣資料才會是當天的」）。背景：`openapi.twse` 的日收落後一個交易日 —— 09-14 收盤後一小時
     （14:32）它回的還是 09-11，到 15:09 也一樣；而 `mis` 當時已經有 09-14。
     **做法**（`pipeline/sources/mis.py` ＋ `run_daily.fill_today_from_mis`）：
     先只問一檔拿 mis 手上的交易日，比資料湖已知的新才去抓全市場（約 2,959 檔、30 個請求）。
     **★ 口徑差異必須知道**（同一天用 FinMind 官方值逐檔對過）：
     | 代號 | mis（張） | 官方（張） | 差 |
     |---|---:|---:|---:|
     | 1101 台泥 | 35,582 | 35,779 | −0.55% |
     | 6488 環球晶 | 3,954 | 4,180 | −5.4% |
     | 2330 台積電 | 18,172 | 21,449 | **−15.3%** |
     `v` 是**盤中累計、不含盤後定價交易**，而且**差幅逐檔不同**，所以不能用固定比例校正。
     開高低收則三個來源一字不差（2330 開 2385／高 2395／低 2380／收 2380）。
     因此：**開高低收直接採用；量與值標成 `px_source="mis"` 的暫定值**，
     `store.append()` 後到覆蓋，隔天 openapi 給官方值時自動修正。
     成交金額 mis 沒有欄位，用「股數 × 典型價 (高+低+收)/3」估（1101 實測誤差 −0.3%，
     估價法本身很準，誤差幾乎都來自上面那個量的口徑）。
116. **暫定值要在網站上講出來，不能默默用**：`meta.provisional` 帶
     `{is_provisional, rows, share}`（由 `build_payload._provisional_share()` 直接數資料湖裡
     那一天有多少列是 `px_source="mis"`，不依賴 last_run.json）。
     超過一半是暫定值就在頂端橫幅寫明：「開高低收準確，但成交量與成交值偏低（不含盤後定價交易，
     逐檔少 0.5%～15%），資金流向與成交值排名今天請打折看」。
     理由：M1 資金面是這個儀表板最核心的模組，用偏低 5～15% 而且**逐檔不一致**的成交值去算
     族群佔比一定會失真。與其假裝沒事，不如講清楚哪一天的哪個數字該打折看。
117. **部署前給自家 JS/CSS 加版本戳**（`scripts/stamp_assets.py`，`pages.yml` 與 `daily.yml` 都跑）：
     `index.html` 寫的是 `<script src="app.js">`，沒有版本號，GitHub Pages 又給快取標頭，
     所以**部署成功之後重新整理看到的還是舊的 JS**。2026-09-14 實測：
     瀏覽器載到的 `app.js` 是 124,428 bytes（舊版），伺服器上是 125,114。
     Andy 因此回報「這連結我沒看到最新資訊」—— 東西其實早就部署好了。
     戳記用 commit SHA 前 8 碼（本機退回時間戳）。**`vendor/` 不戳** ——
     ECharts、three.js 一個好幾百 KB 又幾乎不變，每次部署讓人重抓是白白浪費頻寬。
     重複執行安全（舊戳會被換掉，不會疊成 `?v=a?v=b`）。
118. **即時報價連續失敗三次就停掉自動輪詢**：Andy 公司網路會擋外部網域，
     打不通的端點每分鐘敲一次既沒有意義又會洗版 console。停掉之後狀態列寫
     「連續 N 次抓不到，已暫停自動更新（按「更新」重試）」，按「更新」會歸零並把輪詢接回去。
     對應地，`_preview.py` 與 `_uitest.py` 的 console 檢查要排除
     `ERR_TUNNEL_CONNECTION_FAILED`／`workers.dev` 這類連線錯誤 ——
     本機與 CI 本來就連不到那個 Worker，那是預期行為不是 bug。
119. **週末也要更新：新增 `--phase news`**（Andy 2026-09-14：「週六日有新增也必須更新上去」）：
     `daily.yml` 的 cron 原本只有 `1-5`，週末完全不跑。但週末會變的東西是**新聞、國際盤
     （美股週五夜盤、歐股）與總經**，價量不會變。
     新增 cron `0 2,12 * * 6,0`（台北 10:00／20:00），phase 判斷加一條：UTC `date +%u` ≥ 6 就跑 `news`。
     `phase=news` **整段跳過價量**（打了只會拿到週五的，而且會被 `empty` 記成「沒回資料」，
     每個週末都掛黃燈），也**不動 FinMind 額度**（法人週末不會變，把額度留給週一）。
120. **盤後的自動更新要真的把新資料load進來**（Andy：「盤後 30min 更新」）：
     `live.js` 以前只有**手動**按「更新」才會比對 `meta.generated_at`，
     所以開著的頁面永遠不會自己拿到 18:30 那輪跑完的新資料 —— 自動輪詢只是讓報價數字在跳。
     改成**每一輪都比對**：盤後（或手動）直接重載；**盤中不自動重載**（reload 會把展開的列、
     勾選的族群、K 線縮放全部弄掉，而盤中價格本來就靠即時報價在更新），
     改在狀態列掛一句「有新資料，按『更新』載入」。
121. **總覽最上面放三張大盤圖（加權／櫃買／台指期），資料走 Worker 的 `/chart`**
     （Andy 2026-09-14：「總攬最上方需新增 加權 櫃買 期貨 指數 K線圖以及走勢圖 上下對應」
     「需出現 加權與櫃買 台指期 即時 走勢圖並且可以切換K線型態，且指標 格式 可以參考原本個股做好的執行」）。
     資料源就是證交所「基本市況報導」那三張圖自己在用的檔，跟 `getStockInfo.jsp` 同一台主機
     （`mis.twse.com.tw`，已在白名單，DECISIONS #108）：
       `/stock/data/mis_ohlc_TSE.txt`（t00.tw）、`mis_ohlc_OTC.txt`（o00.tw）、`futures_chart.txt`（taifex）。
     2026-09-14 17:30 實測：TSE/OTC 270 筆（09:01–13:33），FUT 300 筆（08:46–13:45），
     每筆 `{t: epoch毫秒, ts:"090100", c: 指數, s: 該分鐘張數}`；
     `infoArray[0]` 直接附當天 `o/h/l/z` 與 `y`（昨收），所以卡片上那排數字不用再打一次報價端點。
     Worker 用 `CHART_FILES` 白名單鎖死這三個檔名（**不接受任意路徑**，理由同 #108：不要變成開放式代理）。
     ★ 這代表 Worker 要重新部署一次，舊版只有 `/quote`；沒重貼的話三張圖會直接在畫面上寫
     「到 Cloudflare 重貼 workers/quote-proxy/worker.js」，不是無聲失敗。
122. **走勢圖用 ECharts、K 線用 KChart，兩套不是重複**：走勢圖要的是「固定 09:00–13:30 的時間軸、
     未到的時間留白、分鐘量柱、昨收虛線」，ECharts 的類目軸最自然；K 線要的是縮放、指標面板、
     跟個股同一組設定，那是 Lightweight Charts 的強項。兩邊共用 `localStorage['tw.kcfg']` ——
     在個股頁調好的 MA/KD/MACD 這裡直接吃到。
     **分 K 是合成的，而且要講出來**：來源只給每分鐘的指數收盤價，所以
     開＝前一分鐘的收盤、高低是分鐘收盤的極值，**不是真正的盤中極值**；
     卡片上的「高／低」那兩個數字才是當天真正的極值（來自 `infoArray`）。畫面上的說明文字就是寫這件事。
     三張並排時每張只有 ~250px 高，**只放均線＋成交量**；按「展開」變成整列大圖才把使用者勾的指標全放出來。
     `KChart` 因此多一個 `compact` 選項（面板高度 52/58/60/110），舊的寫死「主圖至少 280px」
     放進小卡會整個爆出去。按鈕刻意叫**「展開／收合」不叫「放大」**——
     Andy 的規矩是「只有三張熱力圖可以縮放」，這顆是版面切換不是縮放（`_uitest.py` 的縮放掃描會擋）。
123. **今日事件的日期要吃事件自己的日期，不是 `meta.data_date`**
     （Andy 2026-09-14 截圖：標題寫 2026-09-11，清單裡卻列著 2026-09-14 的券商目標價）。
     側欄標題旁邊改成顯示「清單裡真正最新的那一天」，而且跟著分類切換重算
     （切「券商」跟切「總經」最新日期本來就不同）；不是今天就標成琥珀色並寫「最新 YYYY-MM-DD」。
     連帶修掉一個更早的 bug：新聞的 `published_at` 是 RFC 2822
     （`Fri, 11 Sep 2026 22:00:36 +0800`），以前直接 `slice(0,10)` 會切出 `Fri, 11 Se`，
     排序也變成照星期幾的英文字母排。現在統一過 `Date.parse` 再轉成台北時區的 `YYYY-MM-DD`。
124. **明亮主題只換 CSS 變數，圖表靠重畫**（Andy 2026-09-14：「版面內容需要新增切換明亮色調」）。
     `:root[data-theme="light"]` 覆蓋整組變數就搞定版面；以前寫死的
     `#061018`／`#0a1020`／`rgba(7,11,22,.78)` 那些也全部收成變數（`--ontop`／`--chartbg`／`--topbar`／`--grid`／`--ovl`…）。
     麻煩的是圖表：ECharts 與 Lightweight Charts 的顏色是**畫的當下**寫死進去的，不會跟著 CSS 變。
     所以切換時做三件事：把 CSS 變數讀回 `CH`/`axisStyle`/`tip`（app.js `refreshPalette`）與
     `KUtil.colors`（chart.js `refreshTheme`）→ 把現有 ECharts 實例全部 `dispose()` →
     清掉 `rendered` 旗標重跑 `route()`。**不用 `location.reload()`**：那會把展開的列、勾選、K 線縮放全弄掉。
     `<head>` 裡有一段小 script 先把 `data-theme` 設好，不然明亮主題的人每次開頁會先閃一下深色。
     **產業鏈示意圖（`.chainmap`／`.dgwrap`／`.dg3d`）兩種主題都維持深色底**（`--illus`）——
     那是一張手繪 SVG 插畫，裡面幾十個色塊寫死在圖裡，當成照片看待比較實在。
125. **mis 補了個股就要一起補大盤那一格**（2026-09-14 線上實測抓到的矛盾）：
     `price_daily` 用 mis 補上今天之後，橫幅寫「資料更新到 2026-09-14」，
     但 hero 的加權指數還是 **46,184.85 —— 那是 09-11 的收盤**。
     原因是那格來自 `market_daily`（證交所 FMTQIK），而 FMTQIK 跟日收檔一樣落後一個交易日。
     兩個數字擺在同一個畫面上就是在騙人。
     解法：`mis.market_snapshot()` 抓 `mis_ohlc_TSE.txt`，
     它的 `infoArray[0]` 直接給 `z`（成交指數）`y`（昨收）`v`（成交金額，百萬元）`m`（成交量，張）；
     換算回 FMTQIK 的口徑（turnover 用元、volume 用股）後寫進 `market_daily`，
     key 是 `date`、`keep="last"`，所以隔天官方值進來自動蓋掉。
     **只在 mis 真的補了個股的那一輪才抓**（`filled` 非空才呼叫），
     openapi 已經給今天的時候不會多打這一支。
     成交金額與成交量一樣是盤中口徑（不含盤後定價交易）偏低 —— 橫幅那句警語已經涵蓋。
126. **個股的即時分 K 要兩個來源接起來（證交所沒有個股的分時檔）**
     （Andy 2026-09-15：「當我點擊一般股票時也能做到這樣的效果」）。
     2026-09-15 把 mis 網站的 53 個 JS chunk 全部掃過，確認官方只提供
     **大盤 / 櫃買 / 期貨 / 少數指數**的當日分時檔（`mis_ohlc_TSE|OTC|FRMSA|TW50|CG100|IX0126.txt`、
     `futures_chart.txt`），**個股沒有**，個股只有「當下這一筆報價」。所以 `site/livek.js` 是：
       早盤～20 分鐘前 ← Yahoo 的 1 分 K（有量，但**延遲 20 分鐘**；10:22 實測只給到 10:02）
       最近 20 分鐘～現在 ← mis 每 5 秒一筆，自己疊成 K 棒（即時）
     兩邊接在一起才會「從 09:00 到現在都完整，而且右緣是即時的」。接縫處用
     Yahoo 最後一根的收盤當自己第一根的開盤，線才連得起來。
     收集到的 5 秒序列存在 localStorage（當天當檔），中途重新整理不會把累積的細節丟掉。
127. **最細是 5 秒，不是 1 秒**（Andy 原話要「1 秒 K」）：mis 的報價本身就是每 5 秒一張快照
     （回應裡自己寫 `userDelay: 5000`，證交所網站也是每 5 秒問一次）。
     2026-09-15 10:25 實測：每 1.2 秒問一次連問 14 次，撮合時間從 10:24:55 直接跳到 10:26:00 ——
     中間那一分鐘它沒有新資料。問更密只是白打人家的端點。
128. **成交價要從 `trade.z` 拿，`z` 在兩次撮合之間是 `-`**（2026-09-15 實測修正）。
     抓到的 2330：`{ z:"-", tv:"-", v:"5280", trade:{ ft:20, t:"10:25:35", v:1, z:"2395.0000" } }`。
     舊版的 `normalise()` 遇到 `z='-'` 會退去用**最佳買價**，買價永遠比成交價低一檔 ——
     等於盤中有一大段時間畫面上顯示的根本不是成交價，看起來還會像一直在跌。
     正確順序：`z` → `trade.z` → 最佳買價 → 開盤 → 昨收。`live.js` 與 `livek.js` 用同一套。
129. **三張大盤圖的週期改成下拉選單，並加上歷史週期**
     （Andy 2026-09-15：「時間週期需要新增1H 4H 日 周 月 季K 太多的話可以改清單式選項」）。
     分「當天即時」（1/5/15/30 分，來自 mis 分時檔）與「歷史」（1 時/4 時/日/週/月/季，來自 Yahoo `^TWII`）。
     4 小時與季 K 是拿 1 小時 / 月線再合成的（Yahoo 沒有這兩個原生週期）。
     **櫃買與台指期沒有歷史**：Yahoo 的 `^TWOII` 已經壞掉（2026-09-15 實測最後一筆停在 2026-07-17、
     現價給 269.45 而實際是 395），台指期則沒有免費代號。Worker 的白名單刻意不放行 `^TWOII` ——
     寧可在畫面上說「沒有」，也不要畫一條錯的線。
130. **四週期同看那四格要能自己選**（Andy 2026-09-15：「不然我看不到我要的」）。
     以前是程式挑的（有 15 分就 15m/60m/240m/1d），使用者換不掉。改成每格一個下拉，
     選擇存在 `tw.kcfg` 的 `mtfTfs`，換股票也還在。
131. **MACD 背離要自動標出來**（Andy 2026-09-15：「這是很好的訊號」，附了頂背離的截圖）。
     `KInd.divergence()`：兩側各 5 根都比它高（低）才算一個轉折點，兩個轉折點相距 8～90 根才比較；
     頂背離＝價創更高的高點但 DIF 更低，底背離＝價創更低的低點但 DIF 更高。只留最近 4 組。
     主圖與 MACD 面板**各畫一條虛線**把那兩點連起來 —— 只畫一邊看不出「背」在哪。
     指標列有開關（`macdDiv`），預設開，關掉就不算。
132. **K 線的操作細節三則**（Andy 2026-09-15）：
     (a) 游標移過去要在旁邊顯示開高低收／日期／時間／漲跌／量額 ——
         做成跟著十字線跑的資訊框，靠近右邊界會自動翻到游標左側。
         左上角那條 legend 保留（它是「這根 K 的摘要 ＋ 均線值」，兩者用途不同）。
     (b) 拉線時按住 **Shift** 鎖成水平（往上下拉得比較多就鎖垂直）。
     (c) 畫線 **5 段粗細**（1/1.5/2.5/4/6）、方框可選**填滿或透明**；
         選擇存在 `tw.draw.style`，換股票換週期都還在。
133. **三張大盤圖要自己有計時器，不能寄生在報價那一輪**
     （Andy 2026-09-15：「當我只要開啟走勢圖跟K線圖 他會自動更新 而非我要按下更新才更新」）。
     原本 `Market3.refresh()` 掛在 `live.js` 的 tick 裡、而且排在 `fetchQuotes` **後面**：
     (a) 最快也只有每分鐘一次；(b) 報價連續失敗三次時 `live.js` 會把計時器關掉（DECISIONS #118），
     連帶三張圖也不動了 —— 使用者只能一直按「更新」。
     改成 Market3 自己一組計時器：**盤中 10 秒、盤後 5 分鐘**，與報價完全脫鉤。
     10 秒的理由：mis 的 `infoArray`（卡片那排數字）本來就每 5 秒更新，分時檔每分鐘多一筆，
     10 秒足以讓數字一直跳、新的一分鐘一出現就補上。背景分頁不打端點，切回來 `visibilitychange` 補一次。
     「更新」按鈕仍然會連三張圖一起刷（`Live.tick(manual)` 時才呼叫）。
     **K 線模式改成就地換資料**（`setBars(..., keepView)`）而不是每輪 destroy 再 new ——
     不然每 10 秒縮放與位置就被彈回最右邊，等於不能往左看早盤。
134. **個股日線的最後一根要是「今天」，不是「上一個交易日」**
     （Andy 2026-09-15：「為何個股會是 9/14，而非 9/15呢?…我的目的就是要即時訊息」）。
     資料湖今天那一筆要等 15:30 那輪管線才寫進去，所以盤中打開個股頁，日線最後一根永遠是昨天。
     但報價本身就帶著今天的開高低收與累計量（`o/h/l/z`、`v`），拿來當「今天這根還沒收的日 K」剛好。
     `LiveK.todayBar()` 提供，`industry.js` 的 `withToday()` 接上去：日期一樣就覆蓋、比較新就 append。
     週線／月線是從日線合成的，所以也會跟著長出今天。晚上正式資料進來後日期相同、直接蓋掉，不會變兩根。
135. **指標面板可以上下拖，而且要記住**
     （Andy 2026-09-15：「下方MACD KD 成交量等範圍上下可以拉大」）。
     Lightweight Charts 的 `layout.panes.enableResize` 本來就開著，問題是**分隔線用 grid 的顏色、幾乎看不見**，
     使用者根本不知道那裡可以拉。改成 `--line` 的顏色、hover 時亮起來，說明文字也寫出來。
     拖完（`pointerup`）把 `paneHeights()` 存進 `tw.kcfg.paneH`，下次打開／換股票／換週期都沿用；
     按右上角「重設縮放」會連同它一起還原。
136. **調好的面板高度不可以被自動更新推回去**
     （Andy 2026-09-15：「底下每次更新都會動到我調整好的上下範圍會一直出現跳動，很麻煩」）。
     #135 把高度存進 `tw.kcfg.paneH` 之後，`applyIndicators()` 每次被呼叫都會把存檔值套回去 ——
     而盤中它每 5 秒就被呼叫一次（即時分 K 那一輪）。結果是：手動拖完、幾秒後彈回存檔值，看起來就是「一直跳」。
     改成**只在圖表第一次建立時還原**（`this._paneInit` 旗標），之後的重畫一律不動高度。
     另外 `industry.js` 的 `pointerup` 存檔會**拒絕任何副圖高度為 0 的讀值** ——
     分頁在背景時 `getHeight()` 會回 0，存下去就等於把面板存成消失。
137. **指標面板預設要更寬**（Andy 2026-09-15：「我需要下面的成交量 MACD 這些指標上下間隔寬點」）。
     非 compact 的預設高度：成交量 120px、單一指標 150px、指標之間的間隔 145px、主圖下限 260px。
     compact（總覽那三張小卡）不動 —— 那是 230px 高的卡片，套這組會擠爆。
138. **櫃買與台指期的日線走 FinMind，不是 Yahoo**
     （Andy 2026-09-15：「櫃買 台指期怎麼可能沒有日線數據，CEO幫我處理，這應該是可以解決」——他是對的）。
     實測：Yahoo 的 `^TWOII` 已經壞掉（最後一筆停在 2026-07-17、現價給 269.45 而實際 395），
     台指期 Yahoo 根本沒有代號；TPEX 的 `tpex_index` 只給當月、TAIFEX 的 `DailyMarketReportFut` 只給一天。
     FinMind 三個都有完整歷史：`TaiwanStockPrice` 的 `TAIEX`／`TPEx` 與 `TaiwanFuturesDaily` 的 `TX`
     （近月＝同一天裡成交量最大、`contract_date` 不含 `/` 的那個月份，`trading_session=='position'`）。
     資料表 `index_ohlc`（key＝`date`＋`symbol`，symbol 是 `TSE`／`OTC`／`FUT`），
     payload 每個 symbol 給最近 1300 根日 K；前端 `market3.js` 的日／週／月／季都從這裡合成，
     只有 1 小時／4 小時還走 Yahoo（那是日線合成不出來的週期，而且只有加權有）。
139. **法人連續買超那張圖：三種法人 × 可調天數 × 可縮放**
     （Andy 2026-09-15：「圖表可以縮放，並且可以游標抓取移動，還能切換買超週期 不限只有3天，
     還要加上外資買超，以及綜合」）。
     `flow.trust_streak()` 改成吃 `who` 參數（`trust`／`foreign`／`total`），門檻放寬到 2 天，
     payload 出 `inst_streak`＝三份各 60 檔（舊的 `trust_streak` 留著當換版退路）。
     前端加「投信／外資／合計」三顆鈕與 `≥2/3/5/8/12 天` 下拉，篩選在前端做（不必重抓）。
     縮放**不用 ECharts 的 dataZoom** —— `inside` 型會把 wheel 吃掉，滑鼠停在圖上就捲不動頁面，
     那正是 #100 要拿掉其他圖縮放的原因。改用全站那一套 `App.wheelZoom()`：
     1 倍時把 wheel 交還頁面，放大後才攔，可拖曳、雙擊還原，跟資金熱力圖完全一致。
140. **事件側欄：當天的要當天到，而且日期改成下拉**
     （Andy 2026-09-15：「事件需要同步更新今天 9/15 發生的 但保留前 1 個禮拜資訊，
     日期那邊可以變成清單選項選擇日期」）。三件事：
     (a) **新聞的「今天」以台北時間為準**。`cnyes` 以前直接拿 UTC 日期當 `date`，
         台北 00:00–08:00 發的新聞會被記成前一天；RSS 的 `published_parsed` 也是 UTC。兩邊都改成 `astimezone(+08)`。
     (b) **`--phase price` 也抓新聞**，而且加開盤中兩輪（UTC 01:00／04:00＝台北 09:00／12:00，跑 `--phase news`）。
         新聞是免費 RSS／JSON，不吃 FinMind 額度，沒有理由等到 18:30。
         自動判斷 phase 的條件改寫成**區間**（UTC < 07 → news、07–08 → price、其餘 full），
         因為 GitHub 免費層的排程常延遲數十分鐘，列舉小時一延遲就掉進錯的 phase。
     (c) payload 從「每類各 40 則」改成**保留最近 7 天**（以最新一則往回推，抓不到新聞的那輪才不會清空），
         每類仍保底 40 則；`published_at` 兩種格式（ISO 與 RFC 2822）先 parse 成時間再排序。
         一週的量是原本的四倍多，所以把前端用不到的 `summary`／`keywords` 從 payload 拿掉，體積反而更小。
     前端 `#evDate` 從純文字改成 `<select>`：「全部（N）」＋最近七天各一個選項（帶當天筆數），
     選單跟著分類重算（切到「券商」時沒有券商目標價的日子就不出現）。
141. **族群成分股預設照漲幅排**（Andy 2026-09-15：「族群 Default 排序適用漲幅」）。
     以前預設是成交值，打開族群頁永遠先看到那幾檔權值股，今天真的在動的中小型股要自己按一次表頭。
     預設改 `chg_pct` 由高到低；按過表頭就存進 `tw.memberSort`，下次沿用他自己選的那一欄。
     題材頁的「成員」表同理：組內排序從成交值改成漲幅（族群之間仍照成交值排，那是「哪一塊比較大」）。
142. **K 棒被壓扁的真因是價格軸，不是棒子寬度**（Andy 2026-09-15：「切換到不同時間週期，
     K棒會很窄需要解決這問題，自動調整好的寬度」）：
     量過才知道方向反了。橫向其實一直是對的 —— 15 分／1 時／4 時／日線量到的
     `barSpacing` 都是 11px、畫面上都是 134 根。壞的是**縱向**：
     `chart.js` 的價格軸滾輪縮放（滑鼠移到右邊價格軸上滾）會呼叫 `setAutoScale(false)`，
     而那是**永久**關掉 —— 之後換股票、換週期都不會再自動貼合。
     於是日線那個 240–480 的範圍被原樣套到只走 330–345 的分 K 上，K 棒就剩一條線。
     Andy 四張截圖的價格軸都是 240–480，一模一樣，就是這個。
     **修法**：`setBars()` 在 `!keepView`（換股票／換週期）時重新 `setAutoScale(true)`。
     即時更新那條路（`keepView`）不動，不然使用者自己拉的範圍每幾秒被彈回去。
     順手把「畫面上放幾根」從寫死 160 根改成用容器寬度 ÷ 每根寬度算，視窗一窄才不會被擠扁。
     驗收：`_uitest.py` 真的去滾價格軸把它拉開，再切週期，驗軸有沒有重新貼合
     （修之前量到 6.45 倍，修之後 0.33 倍）。
143. **本益比河流圖：兩種模式，倍數不寫死**（Andy 2026-09-15：「需要新增像是財報狗那樣的
     河流圖…上方也多一個選項新增河流圖，且兩種模式可切換」）：
     做法與 Goodinfo／財報狗一致 —— 近四季 EPS 是一條**階梯函數**（每次財報公布才跳一次），
     乘上倍數就得到「那個倍數對應的股價」，白色收盤線穿梭其間。
     **倍數刻意不寫死 13/15/17/19/21/23**（那是 Goodinfo 對台積電的預設）：
     金融股合理倍數十倍出頭、AI 股三十倍，寫死對大多數股票沒有意義。
     改用該檔自己的歷史本益比分位數 10/30/50/70/90%，五條線切出六個區間
     ——低估／價值／合理／觀望／高估／警示，等於「跟自己比貴不貴」。
     兩種模式：**色帶分區**（fugle 那種，堆疊面積＋區間名標在右側）與
     **倍數線**（Goodinfo 那種，五條虛線＋線尾標倍數）；選擇存 `tw.periver`。
     資料全部在前端算（`pe_history` 的 `ttm_eps` ＋ `daily` 收盤），不動管線。
     K 線圖那條：`cfg.peRiver` 打開就把同一套倍數線疊在主圖上，並且
     **`autoscaleInfoProvider: () => null`** —— 25 倍 ≈ 497、9.5 倍 ≈ 189，
     讓它們參與取景的話價格軸會被拉成 160–520，K 棒又被壓扁（就是 #142 那件事）。
     所以線畫得出來但不影響取景，落在畫面外就切掉，圖例仍然把五個價位都寫出來。
     原本那張「每季本益比高低」保留，改成 `#peQ` 放在 EPS 與三率旁邊。
     **平滑**（Andy 同日追加：「本益比河流圖需要平滑點 更貼近走勢」）：
     財報一季才公布一次，照階梯畫就是一格一格的台階，跟他給的參考圖差很多。
     把 EPS 那條階梯用**只看過去**的指數平滑抹平（半衰期約 30 個交易日）。
     **刻意不往前內插** —— 那等於在財報還沒公布前就先用它的數字，是這個專案一直禁止的前視偏誤；
     往回平滑只用已經公布的資訊，新財報會在一個多月內被吃進帶子裡。
     顯示的「目前本益比」仍然用真實的近四季 EPS，不用平滑值。
     兩種模式的線與面積另外加 `smooth: 0.3`，讀起來才像河流。
144. **設定面板改成貼著按鈕開**（Andy 2026-09-15：「設定出現的位置應該要在 設定按鈕旁邊」）：
     `.cfgpop` 以前是 `position:absolute; right:18px`，錨點是最近的 positioned 祖先、跟按鈕無關，
     所以常常飄到整張 K 線圖下面或畫面另一頭（他的截圖就是浮在左邊）。
     改成 `position:fixed` ＋ `placePop()` 直接用按鈕的 `getBoundingClientRect()` 算位置：
     右緣對齊按鈕右緣、預設開在按鈕正下方；**上下空間比較大的那一邊贏**，
     並把 `max-height` 夾到那一邊的可用高度 —— 面板比視窗高是常態
     （均線＋五個指標＋SMC＋本益比），要的是「貼著按鈕、裡面自己捲」，
     而不是為了塞下整個面板跑到畫面另一頭。自訂週期那個彈窗走同一條路。
     **坑（2026-09-15 線上實測）**：`position:fixed` 只要祖先有 transform / filter /
     backdrop-filter 就會改以那個祖先為基準，不是視窗。本站 `.view.on` 的進場動畫
     正是 `transform: translateY(4px) → none`，在那 0.25 秒內開面板就整個偏掉（實測偏 195px）。
     所以 `placePop()` **不直接把視窗座標寫進 left/top**：先把面板擺到 (0,0)、
     量出「(0,0) 實際落在視窗的哪裡」，再用差值校正 —— 這樣不管祖先是什麼都準。
     `_uitest.py` 會故意先給外層加上 transform 再開面板，把這條守住。
145. **本益比河流的樣式可調**（Andy 2026-09-15：「需要新增本益比河流圖的顏色 線條粗細 透明度 等設定」）：
     存在 `cfg.st.pe = { z: [六個區間顏色], w: 線寬, o: 色帶透明度 }`，沒設定過就回預設，
     舊的 localStorage 不用搬。下方河流圖與 K 線上那五條倍數線吃的是**同一組**設定，
     所以兩邊永遠長得一樣；K 線上的線再額外加 35% 不透明度，不然疊在 K 棒上會看不見。
146. **排序沒壞，是即時層把排好的數字換掉了**（Andy 2026-09-15：「為何我點選排序後 漲幅對不上」）：
     先做了一次全面稽核 —— 今日候選與產業鏈成分股，每一欄、升冪降冪各點一次，
     讀**畫面上渲染出來的文字**回頭驗單調性，**48 組排序全部正確**。
     所以問題不在排序邏輯，在**即時層**：`live.js` 的 `paint()` 每分鐘把畫面上
     `[data-live]` 的收盤／漲跌**就地**改掉，但列的順序不會跟著換 ——
     表頭還標著 ▲，那一欄卻已經不是排好的了。他的兩張截圖就是同一張表的前後兩刻。
     **修法兩件一起做**：
     - `App.liveMerge(rows)`：排序**之前**先把即時值疊回列資料，排的就是使用者看到的數字
     - `App.onLive(el, fn)`：即時層更新完（`paint` 發 `tw:quotes` 事件）就重排一次；
       只有正照著收盤／漲跌排序時才重排，而且元素離開畫面會自己解除監聽
     兩張表（今日候選、產業鏈成分股）都接上。
     **代號那一欄是字串排序**，不是數字 —— ETF 是 0050 / 00631L，當數字排會變成 50 / 631；
     驗收另外用字串單調性驗它，不要被「數字化」的稽核誤判。
     驗收：`_uitest.py` 新增 `t_sort()`，走完每一欄每個方向，再灌一組假即時報價、
     呼叫 `Live.paint()`，驗表格真的照新的漲跌重排。
147. **「今天的上市補過了沒」要看檔數，不能看資料湖最新日期**
     （Andy 2026-09-15：「為何我沒看到最新的」—— 程式是最新版，資料停在 09-14）：
     `fill_today_from_mis()` 舊的守門寫成
     `if mis_date <= max(openapi_date, store.latest_date("price_daily")): return 空`。
     這個前提是「資料湖最新日期＝上市已經到了那一天」，但**上櫃有自己的來源、當天就進得來**，
     它一個人就能把最新日期推到今天 —— 管線於是判定「今天有了、不用補」，
     上市那一半永遠補不進來。09-15 線上實測：09-15 上櫃 1,006 檔、**上市 0 檔**，
     `mis.date_check` 是 `lake_latest: 2026-09-15, will_fill: false`；
     `build_payload` 的 `last_complete_date` 看到上市沒到齊不讓日期前進，網站整天停在 09-14。
     **改成按市場數檔數**：`twse_rows_on(mis_date) < MIS_FILL_MIN_TWSE(=800)` 才補。
     800 是對著 `last_complete_date` 的門檻挑的（0.6 × 約 1,324 檔 ≈ 794），
     兩邊的「到齊」是同一個標準，才不會出現「補了還是不算到齊」或「算到齊卻沒補」的夾縫。
     另外保留一個 `stale` 條件：mis 的交易日比 openapi 還舊（假日、或 mis 那邊還沒換日）就不補，
     否則只是把昨天的重寫一次、白打人家的端點。
     `twse_rows_on()` 只讀那一年的分割檔（`store.read(..., years=[YYYY])`）——
     `price_daily` 是 2000 年到現在的歷史，為了數一天的檔數整張讀進來太浪費。
     `mis.date_check` 加記 `twse_rows_on_mis_date` 與 `min_needed`，下次再卡住不用猜。
     **順手補上的第二道**：`session_closed()` —— 那一場還沒收盤就不寫當天的日 K。
     因為這個判斷改成「數檔數」之後，盤中手動觸發 `--phase price` 會把中午的即時價
     寫成當天的日 K，而後面幾輪看到「上市已有 1,300 檔」就不會再補 ——
     整天的收盤價會被中午的價格佔住。這道只擋盤中，正常排程（台北 15:30）本來就在收盤後。
     用的是執行當下的**時鐘**而不是拿它當交易日（交易日一律取自 API 回應），
     而且只有「mis 說的日期就是台北的今天」才需要問，比今天早的日期那一場當然收了。
     **不需要另外補 09-15 的洞**：openapi 是 T-1，09-16 那輪它自己會給 09-15 的上市。
     驗收：`tests/test_mis_fill.py` 14 項，照 09-15 當天的資料湖擺出情境；
     **已實測舊版程式跑這組會紅 6 項**，確定是真的回歸測試。
148. **網頁自己要講自己是哪一版**（Andy 2026-09-16：「我現在對網頁有沒有更新這事情不清楚，
     每次說有更新，但打開來網頁卻跟原本的一樣，所以網頁需要新增版號，確認是否更新」）：
     以前只有 `?v=<戳記>` 那一層（給瀏覽器看的、不讓它吃舊快取），**人看不到**，
     所以「到底換版了沒」永遠只能靠我說。現在 `index.html` 有 `<meta name="tw:build">`，
     部署前由 `stamp_assets.py` 填成「commit 前 7 碼｜建置時間（台北）」，
     前端畫在右上角徽章（點得開，直接跳到 GitHub 上那個 commit）與頂端橫幅那一行。
     手機頂部塞不下，徽章藏起來，但橫幅照樣寫，所以手機也看得到。
     沒經過部署流程的版本寫 `local`，一眼就知道那不是線上版。
     **時間一定會往前走**，所以同一個 commit 重新部署一次也看得出來換過版。
     驗收：`tests/test_stamp_assets.py` 6 項；`_uitest.py` 的 `t_buildver()` 用兩組不同版號
     各開一次頁面，驗**畫面上的字真的跟著換**（不是只驗那顆徽章存在）。
149. **本益比河流圖第三種模式：填滿**（Andy 2026-09-16 給的圖四，財報狗 PE 區間評價法）：
     `band`（半透明色帶）／`fill`（整片實色）／`mult`（倍數線）三選一，選過記在 localStorage。
     填滿模式帶與帶之間補一條細分隔線，不然六塊顏色黏成一片；收盤線再描一圈相反色的外框，
     不然壓在實色帶上會被吃掉。
     **踩過的坑**：填滿的透明度原本寫成 `Math.max(0.8, S.o/100)`，
     結果滑桿從 5% 拉到 80% 畫面完全沒反應 —— 等於 #150 的透明度在這個模式下是壞的。
     改成填滿有**自己的**透明度（`cfg.st.pe.of`，預設 90），色帶模式仍用 `o`（跟 K 線上的帶共用，#145）。
150. **下方河流圖可調透明度、可縮放、可拖曳**（Andy 2026-09-16）：
     圖旁邊一根滑桿，寫回 `cfg.st.pe`（依模式寫 `o` 或 `of`），所以設定面板與 K 線上的帶同步。
     縮放用全站那套 `App.wheelZoom()`，**不用 ECharts 的 dataZoom** —— dataZoom 會把 wheel 吃掉，
     頁面就捲不動了（#139 踩過一次）。1 倍時 wheel 交還給頁面，放大後才攔，並提供拖曳與雙擊還原。
151. **設定面板「叫不回來」**（Andy 2026-09-16：「當我按下其他地方時，沒有點到設定內的範圍，
     設定面板會消失，我需要會再呼叫」）：⚙ 的開關原本只看 `pop.hidden`，
     但面板可以在 `hidden === false` 的情況下從畫面上消失（換分頁再回來、圖表重畫、被定位到畫面外）。
     這時按 ⚙ 走的是「關閉」那一條 —— 從使用者角度就是「按了沒反應，要按第二次」。
     改成用 `popVisible()`（真的量 `getBoundingClientRect()` 有沒有在視窗內）決定開或關，
     並補上正規的「點面板外面就收起來」（用 `mousedown` 而不是 `click`，
     否則在面板裡拖滑桿、放開時滑鼠已經在面板外，會把面板關掉），收的時候把 `dataset.kind` 一起清掉。
     `hashchange` 一律收掉，狀態不會殘留到下一頁。
152. **淺色主題：整組色盤跟著主題走，而且用機器驗**（Andy 2026-09-16：「由於一開始製作是黑色底，
     很多數據都是白色線條及文字，檢查所有切換回白色 UI 後需要更改的顏色」）：
     **這件事不能用眼睛掃，一定會漏。** 所以先做兩道機械化驗收（`_uitest.py` 的 `t_lightink()`）：
     - **圖表**：同一張圖在深色／淺色各量一次「跟背景不一樣的像素占比」。
       淺色掉到深色的一半以下 → 有東西是白的、消失了。
     - **文字**：掃整頁每一段文字，算它跟自己背景的對比度，低於 1.9 就是幾乎看不見。
       （背景是漸層就跳過 —— 不然 logo 的白字印在青紫漸層上會被誤報。）
     它抓到的真問題與修法：
     - `STAGE` 四個階段色寫死深色主題螢光色，淺色下對比度只有 **1.41～1.57**。
       改成 `color` 是 **getter**，讀 `CH`（切主題時 `refreshPalette()` 會就地改寫），既有讀法都不用改。
     - `chart.js` 的指標色（MA／KD／MACD／RSI／布林／量）整組是螢光色，同時用在線與圖例文字上。
       淺色主題換成同色相壓深的一組；第六條均線在深色是純白，淺色一定要換（畫在白底＝沒畫）。
     - `PALETTE`（分類色盤）同理，新增 `PALETTE_LIGHT`。
       **切換時就地改寫陣列**（`length=0` 再 push），不是換一個新陣列 —— 外面拿走參照的地方才跟得上。
     - 族群色 `L.gcolor` 被十幾個地方直接讀，改成函式風險太大 → 改成換主題時 `L.recolor()` 整包重算。
       環節色 `L.scolor` 改成記索引、讀的當下才取色（`L.segColor()`）。
     - 輪動時鐘格線、名次圖格線、儀表板底圈、散佈圖中線、MACD 零軸、本益比河流的收盤線：
       寫死的 `rgba(255,255,255,…)` 與 `#ffffff` 一律改成吃 `CH.grid`／`C.line`／依主題選色。
153. **驗收不准依賴本機快照有多新**（2026-09-18 踩到）：
     `t_events` 有一條「日期下拉列得出選項」。日期下拉只列最近七天，而本機 `site/data/news.json`
     是某一天跑管線留下的快照 —— 放個幾天它就整份掉出七天窗口，驗收於是報「列不出日期」。
     那是快照過期，不是程式壞了，但每次都要重新查一遍才知道。
     改成把那份真實新聞的日期**整批平移到今天附近**再測（只動日期，其他欄位原樣），
     這條驗收才是在驗程式。**注意前端的 `dt()` 是先看 `published_at` 才看 `date`，
     只改 `date` 等於沒改** —— 第一次就是這樣以為修好了其實沒有。
154. **只差 hash 的 `page.goto` 不會重新載入頁面**（2026-09-18 花了半小時才抓到）：
     `_uitest.py` 的 `t_events` 單獨跑會過、接在 `t_market3` 後面就掛。
     原因不是程式，是驗收自己：`pg.goto(base + "#overview")` 在頁面已經停在
     `base + "#market"` 的時候，屬於 same-document navigation —— **整頁不會重載**，
     前一個測試留下的 App 狀態與已經抓好的 JSON 都還在，
     那個「餵假新聞」的 `pg.route` 於是一次都不會被呼叫（實測：完全沒有 news.json 的請求）。
     **規矩：任何要靠 `pg.route` 餵假資料的測試，一律先 `pg.goto("about:blank")` 再進站。**
     順手也把驗收用的靜態伺服器改成一律回 `Cache-Control: no-store` ——
     瀏覽器快取裡的回應同樣攔不到，那是同一類的假故障。
155. **★ 抓下來會重複用到的東西，一律存進資料湖 —— 不准每次重抓**
     （Andy 2026-09-18：「K 線圖資訊沒有儲存這也是大問題，幫我紀錄，
     若是像這類有延續性的項目，需要記錄下來，避免每次都浪費」）：

     **這是一條通則，不是只講分 K。** 判準只有一句：
     **這份資料明天還會用到嗎？會 → 進資料湖；不會 → 才准當場抓當場丟。**

     **這次踩到的實例**：`pipeline/sources/yahoo.py` 的 docstring 當初白紙黑字寫著
     「這層資料**不進資料湖**，build_payload 每天直接抓、直接用，所以不用在 config.TABLES 註冊」。
     結果是每一次部署都從零重抓 400 檔 ×（60 分 730 天 ＋ 15 分 60 天）——
     昨天抓過的今天再抓一次，改一行 CSS 也照抓。實測：
     **部署 14 分鐘裡有 13 分 43 秒在這一步**（Actions run #16／#17 的 jobs API 逐步秒數）。
     不是「第一次比較久」，是**每一次都跟第一次一樣久**。

     **通則的三個推論**：
     1. 新增任何一個「會抓外部資料」的步驟，先問「這份資料明天還會用到嗎」。
        會 → 先在 `config.TABLES` 註冊、走 `store.append()`，**再**寫抓取邏輯。
     2. 已經存在資料湖裡的東西，`build_payload` 只准**讀**，不准**再抓一次**。
     3. 增量優先：只抓「上次之後新增的那一段」，不要每次重抓全部歷史。

     **例外（唯一）**：盤中即時報價。它每分鐘就過期，存下來沒有意義。

     這條規矩同時寫進 `CLAUDE.md` 的工作規則，並列為 `efficiency-planner` 與
     `resource-auditor` 兩個 Agent 的第一檢查項。
156. **分 K 的儲存策略：60 分進湖、15 分即時、1 分只能短期**
     （Andy 2026-09-18：「剛剛的 1 分 K 還是需要存在，並用 60 分 K 推後續的週 月 K，
     如果這樣比較快 就抓取 1K 推 5 分 K 15 分 K 60 分 K 再由 60 分 K 推後續」）：

     方向是對的（用細的推粗的，只抓一次），**但 Yahoo 的保留期限不允許全部這樣做**：

     | 週期 | Yahoo 最多給多久 | 結論 |
     |---|---|---|
     | 1 分 | 約 7 天（`livek.js` 實際只要 1 天）| **推不出歷史**，只能存最近幾天 |
     | 5 分 / 15 分 | 約 60 天 | 需要時即時抓 |
     | 60 分 | 約 730 天 | **這一層要自己抓、自己存** |

     所以「1 分推 60 分」只在**當天**成立，歷史推不出來 —— 想要兩年的 60 分 K，
     就得有兩年的 1 分 K，而 Yahoo 只給 7 天。**推導方向要反過來**：
     - **60 分 K 自己抓、進資料湖**（730 天），240 分／週／月 K 全部由它推
     - **15 分 K 即時抓**（開哪一檔抓哪一檔），不存 —— 它是儲存量最大的一塊
     - **當天**的 1／5／15 分改由一次 1 分 K 請求推出來：一次請求換三個週期，比打三次便宜

     **★ Andy 2026-09-18 拍板的保留期限（以後照這個做，不要再討論）**：

     | 週期 | 要保留多久 | 存哪裡 | 怎麼來 |
     |---|---|---|---|
     | 1 分 / 5 分 / 15 分 | **當天就好** | 不存 | 開個股時瀏覽器即時抓 1 分，5／15 分由它推 |
     | 60 分 | **730 天** | **資料湖**（增量） | 盤後管線抓當天新增的幾根 |
     | 日 K | **至少 5 年** | 資料湖（本來就有，2000 年起） | 既有管線 |

     照這個算，個股頁的 JSON 反而**變小**：
     現在是 日線 1500 ＋ 60 分 1800 ＋ 240 分 800 ＋ 15 分 1100 ＝ 5,200 根；
     改完是 日線 1250～1500 ＋ 60 分約 2,500（730 天 × 每天 5 根）＝ 約 3,750 根 ——
     **而且 60 分從約一年變成兩年、15 分變成即時**。240 分不再預先產出，由前端從 60 分合成。

     另外 `SLIM_PAGE_BARS` 目前是 1000 根（約 4 年），**不符合「日 K 至少 5 年」**，
     要提到 1250 以上。有分 K 的那 400 檔是 `FULL_PAGE_BARS = 1500`（約 6 年），已達標。
157. **拉 Bar 做成全站共用一支**（Andy 2026-09-18 在四個地方都要求「改成拉 Bar、用拖曳的方式」）：
     `App.rangeBar(box, {min,max,value,step,label,fmt,key,onChange})`。
     RRG 天數、族群佔比河流天數、之後的族群×法人天數共用它，所以三張圖的互動語彙一致 ——
     而不是每張圖各長一個樣子、各有各的脾氣。行為刻意定成這樣：
     - **拖的當下就重畫**（`oninput`），手感才連續；離手才寫 localStorage，免得拖一次寫幾十筆
     - **選過就記住**（`key`），換頁回來還是同一個天數
     驗收用的 `set_range()` 一定要發 `input` 事件 —— 用 `fill()` 不會觸發重畫，
     那就變成只驗「值寫進去了」，不是驗「畫面真的因此改變」，違反 Andy 的驗收規矩。
158. **RRG 天數改 5–20 拉 Bar，不用改後端**（Andy 2026-09-18：「右上角的 5 10 20 天改成拉 Bar」）：
     payload 的 `rrg.points[].trail` 本來就有 **20 個交易日**的逐日座標，
     所以 5～20 之間任何一個值都畫得出來，前端切片就好。
159. **資金去向：垂直、電流流動感、占比 %**（Andy 2026-09-18 三件一起）：
     - **垂直**：ECharts sankey 的 `orient: 'vertical'`，標籤轉 90 度貼在節點右側。
     - **占比 %**：分母用「最上層流出去的總量」＝今天的總成交值；
       節點標籤直接寫 %，不用滑過去才看得到；連線的 tooltip 也寫。
     - **電流流動感**：sankey 沒有內建流動效果，硬改成 graph + lines effect 會**失去 sankey
       自己算好的版面**，不划算。改用「相位脈動」：每條連線的透明度依它的**深度**錯開相位、
       逐格推進（90ms 一格），看起來就是一波亮度由上往下掃過去。
       只改 `lineStyle.opacity`、不重算版面，所以很便宜。
       另外三件事：`prefers-reduced-motion` 就不動；`document.hidden` 就跳過；
       **不在資金流向這一頁就跳過**。
       ★ 最後那條是驗收抓到的：原本寫成「離開就 clearInterval」，
       但回來的時候圖表不會重畫（頁面是 `.view.on` 切換、圖還在），**離開再回來電流就死了**。
       改成計時器一直在、不在這一頁就 return，回來自己會動。
160. **族群佔比河流：占比 % ＋ 天數拉 Bar（Max 60）**（Andy 2026-09-18）：
     payload 的 `share` 本來就給滿 60 天而且**值就是百分比**，所以天數只是「只畫最後 N 天」，
     不用重抓也不用重算（`sliceShare()`）。標題的「近 60 日」改成「成交值佔比 %」——
     天數已經寫在拉 Bar 上，標題再寫死一個 60 就會跟畫面打架。
161. **版號改成「西元日期＋今天第幾版」**（Andy 2026-09-18：「版號用西元＋日期，
     以及第幾次改動命名」）：從 `1b28dfc|09-18 11:16` 改成 `2026-09-18 第 3 版|11:16`。
     為什麼要換：commit 前 7 碼**對得上 GitHub，但人看不出「這是今天第幾版」**，
     也看不出兩個版本誰新誰舊 —— sha 是亂碼、沒有順序。
     「第幾版」怎麼來：`git rev-list --count --since=<今天 00:00 台北> HEAD`。
     用 git 而不是自己存計數器，是因為部署跑在乾淨的 checkout 上、沒地方存狀態，
     而 commit 紀錄本來就在。數不出來（淺 clone、沒有 git）就只寫日期，不讓部署掛掉。
     **commit 短碼沒有丟掉**，移到 `<meta name="tw:commit">`，徽章的 tooltip 與連結還在用，
     要跟 GitHub 對照時照樣點得開。
162. **族群 × 月份加曲線圖**（Andy 2026-09-18：「還需要新增圖表方式表示 包含曲線圖，
     這樣看圖更直觀」）：熱力圖看得出「哪一格特別紅」，但看不出「一整年怎麼走」，
     曲線圖補的就是後者。x 軸 1–12 月、一條線一個族群。
     **預設只打開波動最大的 8 個族群** —— 26 條線疊在一起是一團毛線，什麼都看不出來；
     其餘收進可捲動的圖例，想看自己點開。0（勝率則是 50）畫一條虛線基準。
     兩張圖吃同一份資料、同一個期間與指標，切過去不用等；選過的呈現方式記在 localStorage。
     **坑**：藏起來的容器量不到寬高，切過來要 `resize()` 一次，否則圖是壓扁的。
163. **族群 × 法人：0–30 天拉 Bar ＋ 占比 %**（Andy 2026-09-18）：
     原本只有 `period_flows()` 的**期間**彙總（本週／上週／本月／近三月），拉不出「最近 N 天」。
     後端新增 `flow.inst_daily_series(group_hist, 30)` —— 逐日、最近 30 天、26 個族群，
     約 2,340 個數字，很小。前端要幾天就自己加幾天，拖的當下重算，不用回頭問後端。
     **缺值一定要是 `None` 不是 0**：0 代表「那天法人剛好買賣相抵」，
     None 代表「那天根本沒資料」，混在一起前端加總就是錯的（測試釘住這條）。
     **拉 Bar 的 0 保留成「跟著上方期間走」** —— 0 天本身沒有意義。
     **占比的分母用「畫面上這些族群淨買超**絕對值**的總和」**：
     法人有買有賣，直接加總會正負相抵、分母趨近 0，百分比會爆掉。
164. **輪動階段改成「四段軌道」**（Andy 2026-09-18：「我覺得很醜，幫我想其他方式呈現，
     不夠直觀，不要那麼方方角角」）：原本是四個直角方塊並排 ——
     看起來像四張沒有關係的表格，**看不出這是一個循環**。
     改成：底下一條圓角漸層軌，四段依序坐在上面（每段頭上一顆發光圓點），
     段與段之間用箭頭接起來、最後一段是 `↩`；每段是圓角（20px）玻璃面板、
     顏色取自該階段；成員從方角清單改成圓角膠囊（999px），hover 往右推一點。
     **形狀本身就在說「錢是照這個順序繞圈」**，不用讀文字才懂。
     手機與 compact 版把軌道與箭頭收起來（那時是兩欄，箭頭會指到錯的地方）。
165. **產業鏈頁最上方加類別切換列**（Andy 2026-09-18 E5：「產業鏈頁上方要有類別切換列，
     不用退回去」）：以前要換一條鏈，只能捲到卡片最底下那排「其他產業鏈」連結，
     或退回產業地圖再點一次 —— 剖析圖很長，等於每次都要捲一大段。
     現在標題上方固定一列晶片（五條鏈 ＋ 法定產業別，各帶檔數），現在這條標起來，
     有剖析圖的那兩條前面多一個 `◧`。按自己＝捲回頁首，不重畫。
     **順手修掉一個真的漏洞**：`renderChain()` 開頭補 `dispose3D()`。
     `innerHTML` 一換，舊的 3D canvas 就離開 DOM，但它的 `requestAnimationFrame` 還活著 ——
     用切換列連按幾條鏈就會累積好幾個在背景空轉的 WebGL context（瀏覽器上限約 16 個）。
     驗收 `t_chainnav()` 有一條就在釘這件事（換鏈後 `#prod3d canvas` 必須是 0）。
166. **跨產業鏈的環節同時給兩張架構圖與兩邊內容**（Andy 2026-09-18 E6：
     「ABF 這種跨類別環節要同時出現兩張架構圖與兩邊內容」）：
     ABF 載板／封測／晶圓代工／先進封裝／HBM 這五個環節同時掛在半導體與 AI 伺服器兩條鏈上
     （`chainSegments()` 的兩條 include 規則），但以前不論從哪條鏈點進去，
     看到的都只有當下這條鏈 —— 另一半的上下游關係整個看不到。
     現在環節說明框多一塊 `#xChains`：**兩條鏈各一張剖析圖縮圖**（這個環節在各自圖上亮起、其餘壓暗）、
     各自的上游／下游環節、各自的相關族群，以及 `#industry/<另一條>/<環節>` 的直接入口。
     **坑**：同一批剖析圖畫兩次，SVG 裡的漸層 `id` 會撞在一起（後畫的把先畫的蓋掉，顏色整個跑掉）。
     縮圖一律經過 `uniqIds()` 加後綴，連 `url(#…)` 與 `href="#…"` 一起改。
167. **3D 標籤改成引線 ＋ 外部文字框**（Andy 2026-09-18 E1：「3D 圖文字太小 → 引線＋外部文字框」）：
     原本用 CSS2DRenderer 把標籤釘在零件的投影點上 —— 字 11.5px、只有名字、
     零件一擠在一起就疊成一團（避讓演算法只好把後面的整個藏掉）。
     現在自己做標籤層：字放大到 12.5px、名稱底下多一行說明、左邊一條環節色的粗邊，
     排在畫布左右兩欄，用折線引線接回零件（零件端一顆同色小圓點）。
     **兩件事情不做就會壞**：
     ① 分欄不能只看 `sx < w/2` —— 機櫃是直立的，大部分零件都投影在正中央，
       會整排擠到左欄去（第一版就是 13：3）。改成「明顯偏一邊的放那邊，卡在中間的放比較空的那欄」。
     ② 排版要「上而下擺完再由下往上收」，收完最上面還是負的才叫塞不下，
       這時從**最不重要的**（離相機最遠、且沒被選起來的）開始讓位。
       只做上而下、超出就整欄平移，標籤會直接互相壓住（第一版量到 10 組重疊）。
     模型也要跟著讓位：`fitCamera()` 照「扣掉兩欄之後還剩多寬」再退一點，不然模型會被文字框壓到。
     手機（< 560px）只留右邊一欄。
     CSS2DRenderer 從此不再載入。
168. **3D 去螢光**（Andy 2026-09-18 E2：「3D 圖螢光感太重」）：兩個來源加起來造成的 ——
     ① 每顆材質都帶 `emissive` ＋ `emissiveIntensity: 0.08`（選起來時 0.65），
     ② 場景有一盞青色 rim light（`0x3ee0ff`, 0.5）。所以不管什麼零件都像在發光。
     改法：底層 emissive 一律 **0**、金屬度 0.55 → 0.22、粗糙度 0.42 → 0.55；
     rim light 換成中性冷白（`0xc7d2e6`, 0.34）並補一盞很弱的底部回彈。
     高亮不再是「整顆發光」：**選起來的維持原色、其餘降到 0.12 透明度**，
     選起來的只給 0.22 的 emissive 當提示。套族群色時只把原色往那個方向拉 55%，
     保留零件本身的明暗結構（整顆刷成同一個平色會把 E3 做的細節全洗掉）。
     **唯一准發光的是指示燈**（PSU 電源燈、BBU 電量條、交換器埠燈、光模組鏈路燈）。
     **怎麼驗**：截畫布的圖，數「很亮的青／綠而紅色偏低」那種自體發光像素 ——
     舊版 **3.27%**、新版 **0.14%**（24 倍）；同時平均亮度 35.9 → 39.2，
     所以不是靠把畫面調黑做到的（驗收同時釘住這兩個數字，只調黑一樣會紅）。
169. **3D 零件不再是一顆方塊**（Andy 2026-09-18 E3：「零件要更細膩，風扇有扇片、電池有電壓感」）：
     場景資料多一個 `kind`，對應一支建造函式（`mkBuilders()`），每支回一個 Group。
     目前 21 種：機櫃（框＋立柱＋U 位孔）、背板（板＋連接器列）、托盤（板＋晶片＋散熱鰭片）、
     GPU 模組（載板＋中介層＋晶粒＋周邊 HBM）、晶片（載板＋金屬上蓋＋被動元件）、
     HBM（六層 DRAM 疊起來＋四根 TSV 貫穿柱＋base die）、主機板（板＋IC＋插槽＋電容）、
     CCL（銅箔／玻纖／銅箔三層）、CDU（機箱＋幫浦＋進出水管）、UQD（本體＋鎖環＋軟管）、
     **風扇（外框＋輪轂＋七片有攻角的扇片，會轉）**、PSU（機殼＋進氣孔陣列＋把手＋電源燈）、
     **BBU 電池（托盤＋十二顆圓柱電芯＋銅色正極／深色負極端子＋四格電量燈）**、
     光模組（本體＋LC 雙埠＋拉環＋鏈路燈）、交換器（機殼＋十五個埠＋埠燈）、
     ABF 載板（core＋上下增層＋表面細線路）、BGA（**球**，不是方塊）、RDL、矽橋、晶粒、探針卡。
     零件數 16／11 不變，mesh 從約 30 變成 **483／188**。
     **兩個坑**：
     ① 電池外殼原本是密閉方塊 —— 電芯做得再細也看不到。改成**托盤**（底盤＋矮側板）。
     ② 零件變成 Group 之後 raycast 要遞迴，而且打到的是某顆小零件，
       要往上走 `parent` 找到帶 `seg` 的那一層才知道它屬於誰（不然點下去什麼都不會亮）。
     重複件（n > 1）用 `clone(true)`：幾何與材質共用，highlight 一次吃到整排。
     **順手補一個場景本來就缺的東西**：AI 伺服器機櫃加了「後門風扇模組」（`thermal`）——
     真的機櫃後門本來就有風扇牆，原本的場景等於把散熱只畫了液冷那一半。
170. **3D 動態／靜止切換**（Andy 2026-09-18 E4）：`#dgAnim` 以前只給 SVG 加 `.noanim`，
     切到 3D 之後那顆鈕等於是壞的。現在它同時管兩邊：
     **動態** ＝ 場景緩慢自轉（`autoRotateSpeed` 0.55，慢到可以邊看邊讀）＋ 扇葉轉 ＋ 指示燈呼吸；
     **靜止** ＝ 完全不自己動。偏好存 `tw.dganim`，3D 一掛上去就照它走（不要先動起來再被關掉）。
     **兩個細節**：
     ① 使用者一按下滑鼠就先停自轉，放開兩秒半後才接回去 —— 不然會跟他搶方向。
     ② 按「關」要**立刻**停住：OrbitControls 的阻尼會把剛才的自轉殘量再吐好幾秒
       （swiftshader 上量到 0.81 的漂移）。用 `dampingFactor = 1` 跑一次 `update()`
       把殘量一次吃光並歸零，量到的差變成 **0.000**。
     **怎麼驗**：`stats().spinAt`（所有會轉的東西的角度和）＋ `cam()`，
     兩個都要「開的時候會變、關的時候不變」。只看 `anim` 旗標不算數 —— 旗標是自己寫的。
171. **E6 縮圖：量完再用 transform 縮，不要相信 CSS 算得出 SVG 的寬度**
     （2026-09-18 推上線之後，我自己開線上抓到的）：
     視窗縮到半邊（約 1100px 以下，兩欄各只剩 320px）時，縮圖的 SVG **量到 940px**，
     完全沒有縮小，被 `overflow:hidden` 切掉一半 —— 而且剛好切在亮起來的那個環節上，
     那張縮圖等於白畫。**只在寬螢幕驗就會整個放過**（我第一次驗是 1440px，看起來完全正常）。
     查到的事實（同一個父層、同一時刻）：
     - 放一個 `width:100%` 的空 div 進去 → 量到 **320px**（正確）
     - 那張 SVG → 量到 **940px**，而且 `width:200px !important`、把 `width="100%"` 屬性拿掉、
       改成絕對定位 `inset:0;width:100%`、整段從 DOM 拔掉再插回去 —— **四種都改不動它**
     所以不是「百分比寬」本身的問題，是這張 SVG 的寬度根本不吃 CSS。原因沒有查到底，
     但**不需要查到底也能修對**：改成「先量它實際多寬，再用 `transform: scale()` 等比縮到框裡」。
     transform 是畫的時候套的，不吃排版那一套規則，量到多少就一定縮得對。
     兩個實作細節：
     ① 框寬要用 `getBoundingClientRect().width`，**不能用 `clientWidth`** ——
       這一塊的 `clientWidth` 會回 grid 的最小值（300），不是它實際佔的寬度（334），
       照它算會永遠少縮一截、右邊空一塊。
     ② 縮完會改框的高度，所以要記住「上次用哪個寬度算的」，寬度沒變就不重算，
       否則 ResizeObserver 會自己觸發自己。
     **驗收**：`t_chainnav()` 加了 800px 與 1040px 兩個寬度，驗兩張縮圖都整張縮進框裡。
     實測舊版程式跑這組會紅（`over: 619`、`fill: 2.92` ——圖比框寬將近三倍），是真的回歸測試。
     順帶一提，這條測試自己也踩到 DECISIONS #154：`goto` 到一模一樣的 hash 不會重新載入，
     上一輪選好的環節會留著，再點一次反而是**取消**選取。要先繞去 `#industry` 再回來。
172. **部署 14 分鐘的真正原因，以及「不准再用推估代替實測」**
     （Andy 2026-09-18：「為何跑這麼久」「其他能減少時間及優化部分幫我做下去」）：
     **先講三次錯，因為方法錯得比結果重要**：
     ① 第一次我**猜**是 `upload-pages-artifact` 慢 —— 錯，它只花 5 秒。
        我是在它剛好處於「進行中」時去查 API 的，就當成它是瓶頸。
     ② 第二次我拿**有完整歷史的大型股**取樣去乘以 2,334 檔 —— 錯，
        全市場七成是小型股（個股 JSON 中位數只有 3KB），推估值高估了 35%。
     ③ 第三次我（和兩個 agent）**優化了只佔 26% 的那一塊** ——
        個股那一圈確實慢，但它不是最慢的。
     三次都是同一個病：**用推估代替實測**。所以 `build()` 現在有 `_Lap` 分段計時，
     每次部署的 Actions log 都會直接印出每個階段幾秒，以後不用再猜。

     **實測（GitHub Actions run #20 的逐步秒數）**：整個部署 15 分 23 秒，
     其中「從資料湖產出前端 JSON」**866 秒＝94%**；checkout 5s、pip 15s、
     版本戳 0s、**上傳 5s**、部署 Pages 22s。
     **再往下拆（本機分段計時，總計 559.7 秒）**：

     | 階段 | 秒 | 佔比 |
     |---|---|---|
     | **M1 資金面** | **393.1** | **70.2%** |
     | 個股頁（全市場 2,334 檔）| 146.5 | 26.2% |
     | M2 基本面 | 13.2 | 2.4% |
     | 其餘全部 | < 7 | 1.2% |

     而 M1 那 393 秒**全部在一個函式**：`flow.group_daily`
     （concentration 2.3s、trust_streak×3 0.9s、rotation_radar 0.1s、relative_strength 0.1s）。
173. **`flow.group_daily`：groupby.apply 的 236,641 次 Python 迴圈**（393.3s → 2.2s，**179 倍**）：
     原本是 `groupby(["date","group_id",...]).apply(_agg)`，`_agg` 是一個 Python 函式。
     groupby 的組數 ＝ 交易日數 × 族群數 ＝ **236,641 組**，等於在 Python 裡跑二十三萬次迴圈，
     每一次還建一個九個元素的 `pd.Series`。改成「先把要加總的東西算成欄位，再讓 groupby 一次加完」。
     **兩個不顧到就會改到數字的地方**：
     ① `min_count=1`：外資／投信／自營／融資四項，一整組全是 NaN 要維持 NaN
       （＝那天法人資料還沒到），不可以變成 0（＝法人剛好買賣相抵）。
       `groupby.sum()` 預設會把全 NaN 變 0，所以這四項要分開用 `.sum(min_count=1)`。
     ② **欄位型別**：舊版把 int 與 float 混在同一個 `pd.Series({...})` 裡，
       pandas 會把整串轉成 float64 —— 所以 advancers／decliners／constituents
       在舊版**是浮點數**，JSON 寫出來是 `5.0` 不是 `5`。新版刻意 `astype(float)` 對齊。
174. **個股那一圈的四刀**（146.5 秒是優化後的數字）：
     ① **K 棒轉 JSON 不准用 `iterrows()`**：1,500 根日 K 用 iterrows 要 56.8ms，
       改成 `to_numpy(dtype=object).tolist()` 只要 1.6ms —— **35 倍**。
       iterrows 每一列都建一個 Series 物件，1,500 根就是 1,500 個。
     ② **迴圈裡不准對整張資料湖做 `df[df["code"] == code]`**：
       九個 stockpage 函式每檔各掃一次全表（`monthly_seasonality` 一檔 20.8ms、
       全市場 48.6 秒，因為 price 有 127 萬列）。先 `groupby` 建一次索引（約 0.5 秒），
       迴圈裡就是 dict 查表。**安全性**：那幾支內部都是 `df[df["code"] == code]`，
       餵已篩過的切片進去等於那一行變 no-op。
     ③ **`mtf.build` 多收 `daily_ind`**：日線指標主迴圈剛算完，mtf 內部每個週期各呼叫一次
       `compute_all`，日線那次完全重複。有分 K 的 2.0 倍、沒分 K 的 **4.8 倍**
       （線上 2,334 檔裡只有約 400 檔有分 K，其餘走的都是後者）。
     ④ **`indicators` / `technical` / `scoring` 合計 3.8 倍**（55.8ms → 14.8ms）：
       `market_structure` 本來被算兩遍（`order_blocks` 內部又算一次）、
       `swing_points` 逐根切窗改 `sliding_window_view`、`fair_value_gaps` 的三根關係改整批比較、
       `rsi`／`atr` 整段走 numpy、`technical._candidates` 的 O(n×候選數) 改前綴表。
       **刻意不用 `ewm` 取代 Wilder／KD 遞迴** —— 數學等價但浮點差 1e-15，
       `k > d`、`rr >= 1.8` 這種臨界比較在 2,334 檔裡總會有人踩到，一翻就是判定變了。
     **總成果：本機 719.4 秒 → 171.0 秒（4.2 倍），而且 2,357 個 JSON 逐檔逐欄位完全一致。**
175. **前端 JSON 快取：改前端的推送不該重算任何東西**（`pages.yml`）：
     即使上面全部改完，改一行 CSS 還是要等 171 秒重算 2,334 檔個股頁 —— 而算出來一模一樣。
     快取的 key ＝ `hashFiles('data/**', 'pipeline/**', 'requirements.txt')` 的內容雜湊，
     **刻意不含 `site/**`**（前端改動不影響 JSON，那正是要吃快取的情況）。
     命中就整個跳過產出那一步，部署只剩 checkout ＋ pip ＋ 版本戳 ＋ 上傳 ＋ 部署，約 50 秒。
     `workflow_dispatch` 多一個 `force_rebuild` 開關可以強制重算。
     key 的前綴 `payload-v1-`：以後 JSON 格式改版，加一號就能讓舊快取全部失效。
176. **重構效能的護欄：`tests/test_perf_golden.py` ＋ 逐檔前後對照**：
     改快很容易順手改到答案，而且改到了不會有人發現 —— 網站照樣有圖有數字，只是數字是錯的。
     所以這次的每一刀都要過兩關：
     ① `tests/test_perf_golden.py`：固定 5 檔 × 700 根日 K（`tests/fixtures/perf_bars.json`），
       比對指標欄位、`technical.evaluate`、`scoring.evaluate`、`mtf.build` 的輸出，
       浮點容許相對誤差 1e-9。跑一次 0.8 秒，隨時可驗。
       **純粹改快的重構不准重跑 `scripts/_make_perf_fixture.py`**，重跑等於把護欄拆了。
     ② **整包前後對照**：同一份資料湖，舊版跑一次、新版跑一次，
       把產出的 **2,358 個 JSON 逐檔逐欄位比對**。這是唯一能證明「只有變快」的方法。
     第一版的比對腳本有個要命的 bug：`/usr/bin/time` 不存在導致兩邊都沒產出，
     它卻印「✅ 逐檔完全一致」—— **空集合比對永遠會通過**。
     現在檔案數少於 2,000 就直接判定「這次比對不算數」。
177. **版號的「第幾版」在 Actions 上永遠是 1**（Andy 2026-09-18 在線上發現）：
     `builds_today()` 用 `git rev-list --count --since=<台北今天> HEAD` 去數，
     但 `actions/checkout` 預設是**淺 clone（fetch-depth: 1）** —— 整個 repo 只有一個 commit，
     所以那個指令**永遠回 1，而不是 0**，連「數不出來就只寫日期」那條保險都失效了。
     結果是 Andy 一天推兩次，兩次版號都寫「第 1 版」，這個數字等於是壞的。
     **不改成 `fetch-depth: 0`** 的理由：這個 repo 的 `data/` 每天 commit parquet，
     把歷史整個抓下來要好幾百 MB，為了一個數字不值得。
     改法：工作流先用一個 `gh api` 呼叫問「今天跑了幾次 pages.yml」，
     用 `TW_BUILD_SEQ` 環境變數傳給 `stamp_assets.py`；
     同時 `builds_today()` 先問 `git rev-parse --is-shallow-repository`，是淺 clone 就回 0。
     **寧可不寫數字，也不要寫一個錯的數字** —— 這個徽章存在的唯一理由就是讓 Andy
     判斷「線上是不是新版」，它寫錯比不寫還糟。
     驗收：`tests/test_stamp_assets.py` 加了三條（淺 clone 回 0、環境變數優先、環境變數是垃圾就當沒有）。
178. **.bat 的 `echo` 不准出現沒跳脫的 `>` `<` `|` `&`**：
     2026-09-18 我在 `push-20260918-09.bat` 的提示訊息寫了 `deploy 14m26s -> ~3min`，
     cmd 把 `->` 裡的 `>` 當成**輸出轉向**，產生一個叫 `~3min` 的檔案，
     接著 `git add -A` 把它一起 commit 進 **public repo**。
     這跟 2026-09-18 早上「push-log 檔名沒被 gitignore 蓋到」是同一類錯：
     **.bat 產生的任何東西都會被 `git add -A` 掃進去**。
     規矩：要寫就跳脫成 `^>`，或改用文字；打包完自檢
     `grep -nE 'echo[^|]*[<>|&]' push-*.bat`。
     清除方式一律是「再一次正常的 commit 刪掉它」—— **絕不 force push**。
179. **ECharts 的 treemap 是用 `itemStyle.borderColor` 當整片底色的**（Andy 2026-09-18 圖13
     「產業板塊圖切到明亮主題，圖還是深底」）：方塊之間的那條縫就是 borderColor，
     方塊本身只蓋住自己的面積，所以 borderColor 寫死 `#0b1224` 等於整張圖鎖死深色底。
     全站三張 treemap（產業板塊 `#indTree`、總覽熱力圖、題材熱力圖）都犯同一個錯。
     改法：抽一支 `App.treeSkin()` 回傳 `{border, label, upper}`，三處共用，
     `border` 讀 `CH.panel`（切主題時 `refreshPalette()` 會就地換掉）。
     **順帶改掉 `chgColor()`**：它把紅綠透明度一路拉到 0.95，白底上是「很濃的紅塊配白字」，
     字幾乎讀不到；淺色主題的透明度上限壓到 0.62，標籤改深色字＋白色描邊。
     規矩：**新的圖表元件一律不准寫死色碼**，一定要經 `CH` / `hexA` / `treeSkin` / `heatColor`，
     否則 `t_lightink` 又會漏掉（它只掃它知道的那幾頁）。
180. **`visualMap` 沒寫 `dimension` 就會取最後一維**（Andy 2026-09-18 圖19
     「熱力圖根本沒有依據數字變換顏色」）：季節性熱力圖的資料列是
     `[月, 族群, 數值, 原始格子物件]` 四維 —— 第四維是為了 tooltip 塞進去的物件。
     ECharts 的 visualMap 預設取最後一維，於是拿那個物件去轉數字＝`NaN`，
     每一格都落在範圍外＝全部同色。加 `dimension: 2` 就好。
     順帶把色階上下界從 `max|v|` 改成 **`|v|` 的 90 分位**：只要有一格離群（某族群某月 +48%），
     其餘三百格就全部擠在色階中央，看起來還是同色；超界的走 `outOfRange` 用最濃的顏色。
     淺色主題另外換一組色階（中點 `#f3f5fa`，深藍中點在白底上反而是最顯眼的東西）。
181. **不要用 `position:fixed` 掛在 `<body>` 上的浮動卡**（Andy 2026-09-18 圖12
     「#stock/6116 右下角會浮一張台積電 2330 的卡」）：產業鏈圖點公司時
     `showCompany()` 建一張 fixed 卡掛在 body，**同時**又呼叫 `handlers.onSelect` 跳去個股頁。
     那張卡不屬於任何 view，換頁不會被清掉，於是一路跟著使用者跑。
     改法：① 有台股代號的**直接進個股頁**（卡片本來就是為了進去看的，不需要中間那張卡）；
     ② 外商／無代號的才在產業鏈圖**正下方原地**展開小面板（`#chainMap` 的兄弟節點）；
     ③ `route()` 開頭一律 `document.getElementById('coBox')?.remove()` 當保險。
     規矩：**任何不屬於某個 view 的浮動元素，都要在 `route()` 裡有人負責清掉。**
     順手修掉同一張卡上的「成長：[object Object]」—— `supply_chain.yaml` 的 `growth`
     是一個 dict（`capacity_plan` / `capacity_source` / `drivers`），以前整個丟給 `fmt.esc`。
182. **四格輪動板改成「固定高度＋自己的捲軸」，不要截斷清單**（Andy 2026-09-18 圖五
     「當點擊族群會拉長清單，看到對應股票，若清單太長記得不要延伸原本格式，以拉Bar 方式呈現」）：
     以前每格只列前 12 個（總覽小圖 4 個）再寫一句「還有 N 個」—— 看不到的就是看不到。
     現在全部列出來，`.stage ul` 給 `max-height:380px; overflow-y:auto`，
     所以四格永遠等高、版面也不會被撐長。
     **點族群不再跳頁**，改成在那一列底下原地插一列成分股膠囊（依成交值前 12 檔），
     再點一次收合；真的要進族群頁的話展開列右邊有「進族群頁 →」。
     資料走 `groups_detail`，所以 `renderFlow()` 也要一起 `load('groups_detail')`。
183. **播放拉Bar 只寫一支共用元件 `App.playBar()`**（Andy 2026-09-18 圖二/四/六/七/八都要
     「拉Bar 再多新增 + & - 符號」「具備播放功能」）：五張圖各寫一套的話，
     五個計時器會有五種停不下來的方式。做法是**包住既有的 `rangeBar()`**（不動它），
     加 ＋ − ▶ 三顆鈕，改值一律走 `input.value = x` → `dispatchEvent(input)` + `dispatchEvent(change)`，
     這樣原本掛在 `rangeBar` 上的 `onChange` 與 `localStorage` 寫入完全照舊。
     **★ 一定要會自己停**，否則計時器會對已經 `dispose()` 的 ECharts 實例 `setOption` 而拋錯：
     `route()` 換頁、`applyTheme()` 換主題（它會 dispose 全部圖表）、
     分頁切到背景（`visibilitychange`）、使用者自己動手拉 Bar，四個時機一律先停。
     驗收也只寫一支 `check_play()`，驗的是「值真的自己在動、按暫停真的停住」，不是「按鈕存在」。
184. **Worker 改用 `wrangler` 自動部署**（Andy 2026-09-18 同意）：
     `workers/**` 有 push 到 main 就跑 `.github/workflows/deploy-worker.yml`
     （`cloudflare/wrangler-action@v3`，`workingDirectory: workers/quote-proxy`）。
     在此之前都是手動 —— Cloudflare → Workers & Pages → `tw-quote` → Edit code →
     全部刪掉 → 貼上 `workers/quote-proxy/worker.js` → Deploy。
     2026-09-14 與 09-15 兩批都栽在這一步：程式碼推上去了、Worker 還是舊版，
     網站上三張大盤圖直接不出現，而且要 Andy 自己記得去貼。
     **★ 沒設金鑰時是「綠燈跳過並在摘要印待辦」，不是紅燈**（Andy 指定，
     同 data-steward 的三態原則：「沒設定」「還沒到」「真的壞了」要分得開 ——
     天天紅燈就再也沒人看紅燈）。
     驗證那步刻意打 `/quote?ex_ch=tse_2330.tw` 而不是 `/health`：要 HTTP 200 **且內容含 `"c"`**
     （成交價欄位），這才等於「網站真的拿得到報價」，而不只是「Worker 有回應」。
     金鑰只能 Andy 自己在 repo 設定頁放 —— 實測這個雲端 session 打
     `GET /repos/.../actions/secrets` 與 `.../secrets/public-key` 都是 **403**，
     也沒有 `gh`，所以「幫他設」在這裡物理上做不到。
185. **縮放白名單加上輪動時鐘的兩顆放大鈕**（Andy 2026-09-18 圖二：
     「右上角 可以放大這圖 包含資金流向那頁一併修改」）：
     `_uitest.py` 的 `t_zoom_sweep()` 是「全站只有指定那幾張可以縮放」的最後一道防線
     （DECISIONS #104），任何一頁冒出多餘的縮放框或「放大」鈕都算失敗。
     這次新增兩個入口：總覽的 `rotMiniZoomBtn` 與資金流向頁的 `rotZoomBtn`，
     兩個都進白名單。**白名單只能因為 Andy 開口而變長 —— 不准為了讓測試變綠而加。**
     已拍板的分工是：卡片上只留一顆「放大」，天數拉 Bar、族群篩選、回放通通在放大視窗裡；
     卡片本來就只有半個版面，再塞三排控制項就沒有圖了。
186. **ECharts 沒有 `click` 這個 action**：`dispatchAction({ type: 'click' })` **不會**觸發
     `chart.on('click')`。2026-09-18 寫圖四的驗收時踩到 —— 用 dispatchAction 去「點」長條，
     測試回報「點了但沒有展開成分股」，看起來像功能壞掉，其實是**點根本沒發生**。
     要驗「使用者真的點得到」就得用真的滑鼠：`convertToPixel` 把資料座標換成畫面座標，
     加上容器的 `getBoundingClientRect()` 偏移，再 `pg.mouse.click(x, y)`。
     這條寫進來是因為它會**製造假的紅燈**，下次再看到「點了沒反應」要先確認點是不是真的發生了。
187. **標籤排到畫布外**：輪動時鐘的左右兩欄標籤，左欄錨點在左緣就必須 `align: 'left'`
     （文字往右長），右欄錨點在右緣才是 `align: 'right'`。
     2026-09-18 第一版寫反了 —— 左欄從 x=6 往左長成負座標、右欄超出右緣，
     被 `t_batch2` 的「每個族群名稱都在畫布內」抓到（1440px 是「晶圓代工」、800px 是「電子零組件業」）。
     **這就是為什麼驗收要量矩形而不是看截圖**：截圖上只會看到名字「怪怪的」，量了才知道是跑出去了。
188. **用真滑鼠點圖表時，座標要「每次點之前重算」**：`getBoundingClientRect()` 是相對**視窗**的，
     而 `heatPanel()` 打開成分股面板時會 `scrollIntoView()` 把頁面捲動一段 ——
     捲完之後先前算好的座標就指不到那根長條了。
     2026-09-18 踩到：圖四的「再點一次會收起來」判定失敗，看起來像切換邏輯壞掉，
     **其實是第二次點根本沒點在圖上**。
     連同 #186（ECharts 沒有 `click` action）與這一條，今天同一類錯踩了三次：
     **驗收方法本身有 bug，製造出看起來像功能壞掉的假紅燈。**
     規矩：驗收裡任何一次「真的點下去」，座標一律當場重算，不要沿用上一次的。
     另外選擇器要對得上**真實 DOM** —— `heatPanel` 產生的是 `.ms > a[href^="#stock/"]`，
     不是 `a.lk-stock`（那是 `L.stock()` 的樣式），找錯就會數到 0 個成分股。
189. **面板就在圖正下方時，不要再 `scrollIntoView`**（2026-09-18 圖四驗收抓到的真 bug）：
     排行的成分股面板 `#rankPanel` 貼在圖表正下方、同一張卡片裡，
     但 `heatPanel()` 內建會 `scrollIntoView({behavior:'smooth', block:'nearest'})` ——
     面板一展開就把頁面往下推 **895px**，**使用者剛點的那張圖直接被捲出畫面外**。
     量到的證據：點第一下之後 `scrollY` 0 → 895，長條的視窗座標 y 從 403 變成 **-491**。
     驗收顯示的是「第二次點沒收起來」，追下去才知道**圖已經不在畫面上，點當然點不到**。
     改法：`heatPanel(..., { scroll: false })`，面板本來就在旁邊的呼叫端一律關掉自動捲動。
     **這條是「當自己是使用者實際操作」才抓得到的**：只驗 render 的話面板確實展開了，
     一切看起來都正常；是「連點兩次」這個真人動作把它暴露出來的。
190. **`_uitest` 那 39 條「總覽」紅字，全部來自同一個點錯位置的動作**
     （Andy 2026-09-18 拍板「先清掉」，查出來的結果）：
     測試想驗「點候選名單一列 → 展開為何選它」，但它點的是**列的正中央**，
     而那裡是股票名稱、是一個 `<a class="lk">`。`app.js` 的處理是
     `if (e.target.closest('a')) return;` —— **刻意**讓點在連結上不要展開，
     因為連結本來就該連到個股頁。
     所以那一點不是「展開」，而是**跳去了個股頁**；接下來整個 `t_overview`
     都在錯的頁面上執行，38 條全部找不到元素、各卡 30 秒。
     **一個錯誤動作 → 39 條紅字 → 每輪驗收多花 20 分鐘 → 整個關卡失去意義。**
     它藏得住的原因：紅字長得像「元素找不到／逾時」，看起來像環境問題。
     改法三件：
     ① 改點「沒有連結的那一格」`td:not(:has(a))`；
     ② **加一條「點完之後還留在總覽頁」** —— 以後再誤點連結會直接說是誤點，
        而不是丟一堆看不懂的逾時；
     ③ `click()` 改用 `locator`（每次重試都重新解析選擇器，表格整段 innerHTML
        重寫也抓得到）並把逾時從預設 30 秒降到 6 秒 ——
        **點不到就是點不到，等 30 秒不會變成點得到**。
     教訓：**驗收失敗時，先確認「這個動作本身有沒有做對」，再去懷疑功能。**
     今天同一類（驗收方法本身有 bug）踩了四次：#186、#188、#189 與這一條。
191. **「點兩次順序要反過來」對低變異欄位不成立**（2026-09-18 清紅字時查出來）：
     候選名單的 `grade` 欄，380 檔裡有 **377 檔是空值**、只有 3 檔是 A。
     `renderCandidates()` 的排序把空值一律排到最後（`if (x == null) return 1`，
     不分升冪降冪 —— 這是對的，空值不該跑到第一名），而那 3 個 A 彼此相等，
     `Array.sort` 又是穩定排序，所以**正排反排的第一名本來就是同一檔**。
     這不是 bug，是驗收的假設太強。
     改法不是把這條刪掉（那會放過真的排序 bug），而是**把條件改對**：
     先數「這一欄在畫面上有幾種不同的值」，≥2 種才要求順序反轉；
     幾乎同分的欄位只驗箭頭有跟著跑。
     規矩：**驗收條件要跟資料的實際分布相稱** —— 對一個 99% 是空值的欄位
     要求「排序後第一名會變」，等於每天報一次假紅燈。
192. **本益比河流不准用 ECharts 的 `dataZoom`**（2026-09-19 踩到，而且是**重蹈覆轍**）：
     `dataZoom: {type:'inside'}` 會註冊 wheel 事件並吃掉它，`#peWrap` 的**滾輪放大**就失效了 ——
     而滾輪放大是 Andy 2026-09-16 親口要的功能，`peWrap` 本來就在 `t_zoom_sweep` 的白名單裡（#104）。
     **這件事 `site/industry.js` 1631 行早就寫過一次**：「用全站那一套 wheelZoom，
     不用 ECharts 的 dataZoom —— dataZoom 會把 wheel 吃掉」。
     我 2026-09-19 做圖七 B 的區間拉Bar 時沒看到那行註解，又加了一次 dataZoom，
     驗收立刻抓到「本益比河流圖往上滾沒有放大」。
     正確做法：**切資料**（`sliceRiver()`，跟 `sliceShare()` 同一套），完全不碰 wheel。
     教訓：**改一張既有的圖之前，先把那張圖附近的註解讀完** ——
     這個 repo 的註解裡有很多「為什麼不那樣做」的結論，重蹈覆轍的成本是一整輪驗收。
193. **季節性的超額報酬全是空的，而且前端「無聲退回」**（Andy 2026-09-19 圖三
     「超額 & 絕對報酬沒變化，幫我確認是否出問題」—— 他是對的，是真的出問題）：
     查出來兩件事疊在一起。
     ① **資料**：`benchmark_monthly()` 只找 `TAIEX` / `^TWII`，但資料湖裡根本沒有那麼長的
        大盤歷史 —— `index_ohlc` 只有 2026-08 起的 **32 天**，而且代號是 TSE/OTC/FUT 不是 TAIEX；
        `^TWII` 也只有 12 個月。結果超額報酬 **312 格裡 0 格算得出來**。
     ② **前端**：`renderSeason()` 發現超額全空就**靜靜**把指標換成絕對報酬 ——
        使用者按了「超額報酬」看到一模一樣的圖，只會以為按鈕壞了。
     改法：
     - 指數來源覆蓋不到八成月份時，改用**全市場等權月報酬**當基準。
       這比硬湊加權指數**更對** —— 族群月報酬本來就是等權算的，
       用等權市場當基準才是同口徑；拿等權族群去減市值加權指數，
       差出來的有一大塊是「大型股 vs 中小型股」而不是族群本身的強弱。
     - `benchmark_source` 寫進 payload，畫面上明講基準是什麼、幾個月。
     - 真的退回絕對報酬時用橘字講出來，**不准無聲退回**。
     成果：超額報酬 0/312 → **312/312**（四個期間都是）。
     規矩：**任何「算不出來就自動換一個給你看」的行為，都必須在畫面上講出來。**
194. **季節性的期間改成「最近 12×N 個完整月」滾動視窗**（Andy「近三年就會只有到當前月份」）：
     以前用**日曆年**切（`year > last_year - N`），近三年＝2024/2025/2026，
     而 2026 的 9–12 月還沒發生 → 那四個月只有 2 個樣本，被 `n>=3` 門檻擋掉 → 整排留白。
     改成 `grp["ym"] > last_ym - 12*N`：每一格剛好 N 個樣本，12 個月全滿。
     近三年因此變成 2023-09～2026-08（36 個完整月），而不是 2024-01～2026-08。
195. **供應鏈關聯圖（圖十）的連線走法：端點依相對位置決定，一律走欄與欄之間的空白通道**
     （Andy 2026-09-19：「供應鏈關聯圖 連線對不起來」）：
     三個根因疊在一起，缺一個都修不好。
     ① **端點寫死**：每條邊都是「來源右緣 → 目標左緣」，所以目標在左邊的邊
        整條倒著走、從卡片底下穿過去 —— 畫面上看起來就是連錯人。
     ② **邊排在卡片之前**：SVG 是 `${edges}${nodes}`，卡片是後畫的，把線蓋掉一半。
     ③ **粗細被 CSS 蓋掉**：JS 算了 `stroke-width="${0.8 + strength*0.5}"`，
        但 `index.html` 的 `.chainmap .edge{stroke-width:1.2}` 是選擇器、贏過 presentation attribute，
        所以 `strength` 從頭到尾沒有作用，每條線都一樣粗。
     改法：
     - **四種走法**：目標在右且只隔一欄 → 貝茲走通道；隔好幾欄 → 右緣出、下到匯流道、橫過去、
       上到目標左緣；目標在左 → 左緣出、「先下再橫」正交折線、回到目標右緣；
       同一欄 → 右緣出、繞 24px 通道再回來。**通道裡沒有任何卡片**，所以線不會穿過不相干的公司。
     - **匯流道要壓多低，只由「這條橫線真的會經過誰」決定**，不是整張圖的最底下。
       半導體鏈最高那一欄有 900px，一條其實整段都走在 30px 通道裡的回頭線，
       以前要繞到最底再繞回來，多走 1200px，中間一大片空白，看起來像線斷了。
     - 邊改畫在卡片之上、`pointer-events:none`（不然卡片點不到）。
     - 粗細改用 CSS 變數 `--w` 傳（`stroke-width:var(--w,1.2)`），
       `.hi` 用 `calc(var(--w) + 1.2)` —— 改 inline style 會反過來蓋掉 hover 的加粗。
     - 線型由 `rel` 決定：`supplies`／`produced_by` 實線＋箭頭、`outsources_to` 虛線、
       **`competes` 不畫**（那不是上下游，畫了會被誤讀成供貨）；
       segment 的 `role: equipment|material` 走灰色細線。
     - 箭頭 `markerUnits="userSpaceOnUse"`（不然細線的箭頭會跟著縮到看不見）、
       `fill="context-stroke"`（線變色箭頭才跟著變）。
     驗收改成量幾何，不是看有沒有 render：**每條邊兩端落在卡片邊緣 ±2px、
     沒有邊穿過非端點的卡片**（沿路徑取 60 點），1440px 與 800px 各驗一次。
196. **孤立節點標「?」，而且 `supply_chain.yaml` 的內容不自動改**（2026-09-19）：
     圖十修好之後，AI 鏈 33 家有 10 家、半導體鏈 25 家有 13 家**完全沒有任何上下游關聯**。
     那不是畫圖的問題，是 `edges` 沒寫。畫面上一律在卡片右上角標一個橘色「?」，
     **不要假裝它有線**。要補哪些邊寫成建議稿 `docs/supply_chain_suggest.md` 給 Andy 校訂 ——
     CLAUDE.md 寫明 `groups.yaml / themes.yaml / supply_chain.yaml` 是他自己校訂的，
     補一條「聯電 → 瑞昱」是**事實主張**，不是版面調整，不准自己填。
     `_uitest` 的 `SC_ISO_MAX` 是**棘輪**：孤立節點只准變少，不准變多。
197. **繪圖拆成三個人，而且開畫之前要先有規格書**（2026-09-19）：
     繪圖一直是同一個人畫、同一個人驗，而「結構對不對」（鰭片方向、增層順序、線連對人沒有）
     跟「好不好看」（配色、字級、淺色主題）是兩種完全不同的判斷，同一個人扛就兩邊都放水。
     圖十的線連錯人、淺色主題一堆白字白線，都是這樣過關的。
     現在：`tech-illustrator` 畫、`mechanical-engineer` 驗結構、`art-director` 驗視覺，
     **衝突時結構優先**。另一半原因是成本：批次6 前面幾張圖「先畫再想」，
     一張來回三輪，每輪三道關卡 18 分鐘 —— 成本全在重畫不在畫。
     所以硬性規定：**沒有 `docs/diagram_specs/<id>.md`、而且沒有 `mechanical-engineer` 簽掉，不准開畫**。
     規格書六段，格式在 `docs/diagram_specs/_TEMPLATE.md`，缺一段退回。
198. **3D 剖析圖的顏色一律走 `--dg-*`，JS 不准寫死 `#xxxxxx`**（圖九 2-2，2026-09-19）：
     三種色票 `tech`／`soft`／`calm` 全部定義成 CSS 變數
     （`--dg-mix`／`--dg-mix-k`／`--dg-sat`／`--dg-led`／`--dg-hemi`／`--dg-key`／`--dg-fill`），
     `three3d.js` 的 `applyPal()` 是**讀**這些變數去算零件顏色與打光的。
     這條的理由很實際：淺色主題一堆白字白線，根因就是顏色寫死在 JS 裡，`refreshPalette()` 換不掉。
     `soft` 的 `--dg-led:0`＝零件完全不發光，Andy 要拿去給客戶看的時候印得出來。
     這組變數的最終決定權在 `art-director`。
     **踩到兩次 TDZ**：`applyPal()` 會回頭呼叫 `highlight(lastHi…)`，而 `setAnim()` 在色票區塊
     「之前」就被呼叫一次 —— `const` 箭頭函式與 `let lastHi` 都會炸。
     所以 `palNum`／`palCol` 寫成 function 宣告（會提升）、`lastHi` 宣告在色票區塊之前。
     症狀很不直覺：畫面只會顯示「3D 起不來（Cannot access 'lastHi' before initialization），已退回平面剖析圖」。
199. **WebGL 的畫面「有沒有變」不能用 `canvas_hash`**（2026-09-19 圖九驗收）：
     `canvas_hash()` 走的是 `getContext('2d')`，在 WebGL canvas 上一律回 `null`，
     所以換色票前後拿到同一個值 —— 那是一條**永遠不會紅、也永遠證明不了任何事**的驗收。
     改成量真正被畫出去的東西：`stats().colorSig`＝所有材質顏色的指紋。
     同一個道理，電流有沒有在跑量 `flowAt`（所有粒子座標和），不是看有沒有粒子物件。
200. **3D 剖析圖的「不准掉幀」是一個要量的數字，不是一句話**（2026-09-19 圖九 2-1 踩到）：
     走線第一版是「一段畫一個細長方塊」，看起來沒問題 —— 直到量下去：
     整台機櫃的 mesh 從 **483 → 1846**，frame rate **29.8 → 14.1 fps**（容器裡的軟體渲染）。
     一塊板 160 段 × 六塊板＝960 個 draw call，全部只為了畫銅線。
     改法兩件事：
     - **一層走線併成一個 mesh**：每段兩個三角形、貼在板面上，沿線方向各延長半個線寬
       （不然轉角會有缺口），整層一個 `BufferGeometry`。
     - **BGA 用 `InstancedMesh`**：25 顆球一個 draw call（以前一顆一個 Mesh，六塊板就是 150 個）。
     結果 **671 個 mesh、28.6 fps**，跟改之前幾乎一樣。
     `_uitest` 的 `t_batch6_n9` 加了一條棘輪：`stats().meshes <= 900`。
     **量產圖11 時這個數字只准往下。** 教訓：繪圖的效能要在**做完當下就量**，
     不要等 Andy 說「怎麼卡卡的」——他用的是真 GPU，問題會晚很多才浮出來，那時候已經五張圖了。
201. **供應鏈的關係一律要有出處，查不到就讓它掛「?」**（Andy 2026-09-19：「請科技業專家協助
     回答…請查明確實填寫」）：
     我先憑印象寫了 14 條建議，再派三位 `industry-analyst` 分頭查證 —— **錯了 5 條、半對 2 條**。
     錯得最兇的幾條，如果照著填就會在公開網站上出現「看起來很專業但是錯的」資訊：
     - **方向寫反**：設計服務（創意／世芯／智原）是 turnkey，**向**晶圓廠買晶圓與封裝產能，
       所以晶圓廠才是供應方。YAML 的慣例是 `from = 供應方`，要寫 `tsmc → gud` 而不是反過來。
       照原本的字面寫會一次畫反三條。
     - **陣營認錯**：智原 3035 是**聯電**陣營（聯電持股 13.77%，智原原為聯電 IP／NRE 部門切出），
       先進製程走三星／Intel／GlobalFoundries，不是台積電。
     - **層級認錯**：智邦的資料中心交換器用 **Broadcom Tomahawk**，瑞昱的 switch IC 是消費／SMB 等級。
       但瑞昱 2026-01 確實進了 NVIDIA 鏈 —— 入口是 **SSD 控制 IC**，不是交換器晶片。
     - **十年前的舊印象**：南電 8046 **2014 年就退出 Intel 供應鏈**，現在網通載板占 65～70%、對應 Broadcom。
     - **被數字直接反證**：穎崴 6515 營收地區別是**北美 81%／台灣 12%**，
       所以它不可能以日月光／京元電為主要出貨對象；兩大客戶是 NVIDIA 與 AMD。
     - **方向相反**：聯亞 3081 → 華星光 4979 是 **`outsources_to`（委外代工）**，錢是聯亞付給華星光。
     規矩：**公開來源查不到具名客戶就不畫線，讓節點掛「?」。** 台燿 6274 就是這樣 ——
     「台燿供 UBB 用 CCL」＋「UBB 主要 PCB 廠是金像電」是兩段推論接起來的，不該進公開網站。
     `_uitest` 的 `SC_ISO_MAX` 因此從 ai_server 10／semiconductor 13 降到 **1／0**，而那個 1 是刻意留的。
202. **新增 `rel: designated_by`（終端指定料號 AVL）**（2026-09-19，兩位分析師獨立指出）：
     AI 伺服器鏈跟一般製造業鏈的關鍵差別是「**CSP／NVIDIA 直接指定 CCL 與 PCB 的料號，
     但實體出貨走 ODM**」。只有 `supplies` 一種語意的話，不是畫錯（CCL 廠→GPU 廠）
     就是漏掉最重要的商業關係。畫成點虛線，跟實體出貨的實線分開。
     同一批還把 `competes` 整個搬出 `edges` → `competitors:` 區塊
     （圖十不畫競爭關係，留在 edges 只會讓之後讀 YAML 的人以為圖上有）。
203. **`role: material` 不等於視覺上要弱化**（2026-09-19）：
     `equipment`（設備）畫灰色**細**線、粗細鎖 1；`material`（材料）畫灰色但**粗細仍依 strength**。
     理由：CCL 占高階 AI 伺服器 PCB 材料成本 **50% 以上**，是這輪行情的「因」不是「果」，
     畫成跟設備商同級的細線會誤導。真正該弱化的是再上一層（銅箔、玻纖布、樹脂、ABF 膜）。
204. **沒有台股的環節不要一律寫「台股無直接對應」**（2026-09-19 踩到）：
     三家設備商搬去 `pkg_equipment` 之後，「先進封裝 CoWoS/SoIC」變成空的，
     畫面上就寫「（台股無直接對應）」—— 但 CoWoS 明明是**台積電自己做的**，那是錯的。
     改成「環節有 `note` 就顯示 note」。而且那幾行字**要先算進版面高度**，
     不然會整段壓到下一個環節的標題列（實測到「先進封裝」的說明蓋在「封測 / 測試」上面）。
205. **點關聯圖要講得出「是什麼關係」，不是只有一條線**（Andy 2026-09-19：
     「點擊關聯圖時，在旁邊新增這類說明，更加明白產業關係」）：
     圖上一條線只能表達「有關係」。公司面板新增「產業關係」區塊，把那條線攤開講：
     **上游誰供什麼給它、它又把什麼賣給誰**，每一條帶品項、依存度（strength 畫成長條）、
     以及 **confidence（官方揭露／媒體報導／產業推論）**。
     最後一項是這一塊最重要的設計 —— 這份資料有一半是產業邏輯推論而不是公司揭露，
     不標出來的話使用者會把推論當成事實，那正是這張圖最容易造成的傷害。
     面板排在圖的**旁邊**（flex，340px），窄畫面（<1100px）自動 wrap 掉到下面 ——
     800px 硬並排會把關聯圖擠到看不清。
206. **驗收要量的那個數字，本身一定要真的會動**（2026-09-19 第二次踩到同一類坑）：
     圖九的「電流有沒有在跑」第一版是量 `flowAt ＝ 所有粒子座標的總和`。
     那是一個**幾乎不會變的數字** —— 粒子沿著同一條路徑等距排 6 顆，
     整排往前推的時候，離開前面的量剛好被後面補回來，**總和近乎守恆**，
     取到小數第二位常常一模一樣（實測 `131.68 → 131.68`），驗收就誤判成「電流沒在跑」。
     而且它會**兩邊都錯**：既可能誤判成壞掉（這次），也可能在真的壞掉時仍然通過。
     改成兩個一起量：`flowT`（相位，只要在跑就單調前進）＋ **第一顆**粒子的座標
     （不是全部加起來）。
     這跟 #199（`canvas_hash` 對 WebGL 一律回同一個值）是同一個教訓的第二次：
     **選驗收指標的時候，先問「這個數字在正常情況下會不會變？」——
     問不出來就換一個**。一條永遠不會變的指標，不管紅綠都沒有資訊。
207. **驗收不要綁顯示名稱，要綁 id**（2026-09-19）：
     E6 的「切過去之後同一個環節已經選好了」寫成 `seg.startswith("ABF")`。
     這次把環節從「ABF 載板 / 高階 PCB」改名成「IC 載板（ABF / BT）」
     （金像電不做 ABF 載板，拆去 `hdi_pcb`）之後，那條驗收就紅了 —— **但行為完全正確**。
     顯示名稱是會被產品決策改的東西，`data-seg` 這種 id 才是契約。

208. **「這家公司做什麼」跟「這個代號還在不在」是兩件事，後者只有機器查得到**（2026-09-19）：
     Andy 問「先前交代的 task 是否有完成，我發現還是有很多沒加入」，
     我派了五位分析師逐檔查族群成分。他們抓出一票放錯格的（直播平台 6165 浪凡被放進
     「半導體材料」、醋酸纖維絲束 4763 材料*-KY 被放進「半導體設備」、
     雷射全像**包裝**材料 2461 光群雷被放進「先進封**裝**」），
     但**沒有任何一位回報 2311 日月光與 6806 森崴能源已經下市**。
     那兩檔是我拿 `data/price_daily` 對出來的：
     2311 在 2018-04 與矽品合併為日月光投控後下市，2019 年起連續八年 0 筆；
     6806 因子公司富崴承攬台電離岸風電二期虧損、淨值轉負，2026-06-23 終止上市。
     這種錯**不會報錯**：`data/company_info` 對下市公司留了一筆欄位幾乎全空的殘影，
     網站照樣印得出名字，只是那一格的量能永遠是 0，M1 的族群資金流被它稀釋掉。
     規矩：**族群成分的正確性要兩道關 —— 人查「是什麼產業」，機器查「還在不在」。**
     機器那道是 `tests/test_group_codes_alive.py`，每次 pytest 都掃。
209. **回補的節流閥只能有一個，而且必須是額度，不能是清單長度**（2026-09-19，
     Andy 說「我發現還是有很多沒加入」的真正根因）：
     `backfill.yml` 的排程把 `LIMIT` 寫死 500，而 `run_backfill.target_codes(500)`
     是「族群成分股 ＋ 成交值排名」取前 500 —— 於是**第 501 名之後的股票在設計上永遠補不到**。
     實測：2,336 檔個股裡，價量有 250 個交易日以上的只有 **392 檔**，
     月營收算得出 YoY 的只有 **488 檔**，1,933 個個股頁上那句
     「歷史價量還在回補，補齊後才會出現」是一句**永遠不會兌現的承諾**。
     真正該當節流閥的是 FinMind 額度（402 就停、寫進度檔、下一輪接續），那個機制本來就在；
     `LIMIT` 只是多押了一道把清單砍掉的上限，而且它砍掉的是**最需要被補的那一段**。
     排程的 LIMIT 改成留空（＝整個市場）。
210. **「整組資料集每一檔都回空」不等於「這些股票沒資料」**（2026-09-19）：
     `run_backfill` 原本的規則是「額度還在卻拿不到東西 → 這檔真的沒有這種資料，記成做過」。
     這條在單檔層級是對的（新掛牌、KY 股缺財報），但 2026-09-12 那輪
     **holding（集保股權分散）500 檔全回空**，因為 FinMind 免費層根本不開
     `TaiwanStockHoldingSharesPer` —— 500 個 done 鍵全被寫死，再也沒有任何一輪會重問。
     後果在畫面上是：「大戶 4 週變化」需要 5 筆週資料，集保只有 2 週，**全市場每一檔都顯示「—」**。
     改成：某個 key 這一輪只要成功拿到過一次資料，其餘回空的照舊記 done；
     一次都沒拿到（而且問了 3 檔以上）的，判定是資料集不開放，**不寫 done、不標 complete**。
211. **hash 裡有中文就一定要 decodeURIComponent**（2026-09-19，34/35 個族群頁全空）：
     `site/app.js` 的 `route()` 拿 `location.hash` 直接 `split('/')`。
     法定產業別的族群 id 是中文（`ind_半導體業`），瀏覽器把 hash 存成百分比編碼，
     不解碼的話 `industry.js` 的 `g.id === state.group` 永遠比不中 ——
     **35 個法定產業別裡有 34 個的族群頁是「0 檔 · 沒有符合的股票」**，
     只有純 ASCII 的 `ind_ETF` 躲過。候選名單、市場明細、個股頁的族群連結全部通到這裡。
     這個洞活了很久，因為 `_uitest` 從來沒有打開過任何一個 `#industry/group/<中文 id>`。
     新增 `t_group_pages` 補上。
212. **前後端共用的欄位，口徑要有一邊是唯一真相，而且要有測試釘住**（2026-09-19）：
     `fundamental.py` 的 `vs_median` 回的是**比值**（`v / median`），
     但前端**兩處**都當成「相對中位的百分比差」在用 ——
     `app.js` 的「只看低於族群中位」判 `< 0`，`industry.js` 個股頁印「高於／低於中位 N%」。
     本益比恆正，所以比值永遠 > 0：那個勾選框**從掛上去的第一天就永遠篩出 0 筆**
     （畫面寫「沒有符合條件的股票」，看起來像今天剛好沒便宜股），
     而個股頁對每一檔都印「高於中位」、族群中位那一欄整欄永遠是綠色（違反紅漲綠跌）。
     兩個前端都指向同一種語意、只有後端是例外 → 改後端成 `(v/median - 1) * 100`。
213. **`all([])` 是 True —— 空集合一定要先當成紅的**（2026-09-19，
     這是 #212 那個 bug 能活這麼久的直接原因）：
     `_uitest` 驗「只看低於族群中位」寫成
     `ok(..., all(a[0] < a[1] for a in below))`，而 `below` 恆為空 list，所以一路綠燈。
     同一輪抓到另外兩條同型的假通過：
     「題材成員有依族群分組」的選擇器含 `#themeParts`（那是剖析圖零件框，**不管有沒有分組都存在**）；
     `t_themes` 只跑 `tids[:3]`，18 個題材裡有 15 個從來沒被打開過。
     規矩：**任何「篩出來的每一筆都要符合條件」的驗收，前面一定要先加一條
     「有篩出東西（空集合不算通過）」。**
214. **註解說「不要用 CDN」，就不要在上面三行掛一個 CDN**（2026-09-19）：
     `site/index.html` 的 `<!-- 圖表函式庫直接放在 repo 裡：公司網路常擋 CDN -->`
     上面一行是 `<link rel="stylesheet" href="https://fonts.googleapis.com/...">`。
     AGENTS 檢查清單第 7 條也明訂 `site/` 內不得有 CDN 連結。
     在 Andy 公司的網路會等到連線逾時才 fallback。實測擋掉之後畫面照樣正常
     （字退到 PingFang TC／微軟正黑），所以直接拿掉這一行。

---

## #216　4xx 一律把回應前 200 字記進 log（2026-09-20）

`pipeline/util/http.py` 原本對不重試的 4xx 只寫「回 400，不重試」。
2026-09-19 傍晚起 FinMind 全面回 400，歷史回補**連續三輪 success 但寫入 0 列**，
三輪都查不出根因 —— 因為日誌裡只有狀態碼。

把回應 body 前 200 字印出來之後，**一次就看到 `Token is illegal.`**。

規則：所有不重試的 4xx（含 401/403）一律記 body 前 200 字。
不是只給 FinMind —— FRED 那六個 400（#204 記過、一直沒解）同樣受惠。

★ 但**只有 `finmind_get` 會把錯誤 body 當回傳值**（`error_body=True`）。
其他來源仍然拿到 `None`，否則「拿到東西就當資料解析」的呼叫端會把錯誤訊息當成資料。

## #217　確定不開放的資料集要封印，不可以讓它把整個回補計畫鎖死（2026-09-20）

2026-09-19 加的
`finished_all = (not exhausted and failed == 0 and not summary.get("unavailable"))`
本意是「整組回空 ≠ 沒資料，不要誤標完成」，但沒想到「永遠不開放」這個狀態
會讓計畫**永遠停在第 1 步**，後面幾千檔個股的價量永遠輪不到。

而且 `unavailable` 只活在那一輪的記憶體裡、沒有寫進進度檔 ——
所謂「下一輪會重問」實際上是「每一輪都重問全部 506 檔」，
必定失敗的 506 次請求把 510 的額度吃光。**每小時燒一次，寫入 0 列。**

決議：
1. 封印寫進 `prog["unavailable"]`，帶 `last_reason`（上游回應原文）。
   封印期 24 小時內一次請求都不發；超過就只拿 5 檔去探；探到立刻解封並恢復全量。
   **不做「永遠不再問」** —— 資料集有可能之後開放。
2. 連續 20 檔回空且本輪一次都沒成功 → 本輪停問它。第一次整組掛掉也不准燒光額度。
3. **已經封印過的資料集不算未完成。** 它不會因為我們一直等就開放，
   拿整個計畫陪葬才是真的損失；新判定的只擋一輪，給它一次重問機會。
4. 每輪第一件事跑 FinMind 健檢（問「台積電最近 10 天日線」，1 次額度）。
   拿不到就整輪立刻停、把原文寫進進度檔、日誌明寫要換 token。
   **這是「機器自己區分 token 失效 vs 個別資料集不開放」的裝置。**

## #218　回報進度有固定流程（2026-09-20）

Andy：「幫我將目前交代的任務一一列出，並且有完成的有標示出來，進行中的做到哪裡，
未完成的（在等）什麼，後續可以執行的都執行完畢，有重大問題再提問，
並且幫我寫個 SKILL，當我說『回報進度』就是 Follow 上述流程回報」。

寫成 `.claude/skills/report-progress/SKILL.md`。三條不可妥協的：
- **先把能做的做完，再報告** —— 順序不能反過來
- **沒有 push 的不准標成完成**
- **打不開線上頁面就說打不開**，不可以假裝驗過

## #219　`_uitest.py` 預設只驗「這批需求對應到的功能」，不跑整輪（2026-09-21）

Andy：「我覺得每次要這樣全部驗證太麻煩，以後改成我提出的內容對應功能做驗證」。

數字：整輪 38 個段落 395 秒；一批需求通常只碰 3～6 段、60～120 秒。**一批省約 5 分鐘。**
再加上部署 8～15 分鐘，這 5 分鐘是他等在螢幕前的 5 分鐘。

做法與對照表寫在 `CLAUDE.md` 的「push 前跑三件事」底下（段落名 → 功能的查表，
不准靠記憶挑）。**回報裡一定要寫「這批我驗了哪幾段」** ——
他要看得出我驗了什麼、更要看得出我**沒**驗什麼。

### 例外只有一個（2026-09-21 當天修訂）
**Andy 說「幫我全部驗一次」的時候才跑整輪。**

原本這裡寫了三個例外（CSS、`app.js` 的共用工具、全站共用資料換掉）。
同一天他看到我為了「族群從 28 換成 110」要跑整輪，第二次講：
> 「驗證不要跑全部，針對需要的部分跑，太花時間了」

所以例外從三條縮成一條。改到 CSS 或共用工具時**自己多挑幾段會用到它的加進去**，
但不必跑滿 38 段 —— 判斷權在我身上，不要再用「規則說要」把 6 分半塞回他的等待時間。

### 代價（講在前面，不是事後解釋）
**這是拿「改 A 弄壞 B」的風險換速度。** 2026-09-20 的 `_uitest` 平行化實測就抓到
三個「只在別的段落才會紅」的缺陷（族群頁沒導航就抓相對網址、KPI 捲動假設 scrollY 0、
以及一個真的產品 bug：KPI 在圖還在長高時就捲了）。
那三個如果當時只驗「這批的功能」，會全部漏掉。

所以配套是：**每批 push 完在 HANDOFF 記一句「這批只驗了哪幾段」** ——
下次有人回報怪象時，才找得到「那批沒驗到哪裡」。

### 不在精簡範圍內的
`_preview.py` 照跑（只要 2 分鐘）。它抓的是文字重疊與多寬度溢出，
那正是「改 A 弄壞 B」最常見的形態 —— 砍掉它省不到時間，卻放掉最大的那張網。
pytest 的機器判準不變（#見 CLAUDE.md）。


## #220　改完先展示、不馬上推；未驗證的累積起來，等他說「驗證」再一次跑（2026-09-21）

Andy：「我想要把效率的問題處理，我覺得跑東西太久，每次修改都要全部重新測試太花時間…
你改完之後可以先不用推上去，先在 Claude 這邊展示給我，並且還沒驗證的部分先記錄下來，
並且接下來都會持續紀錄哪些沒有驗證，之後我說驗證，再全部一起執行。
初步功能我先人為測試，我確認後你再看細項，關於功能數據等方面」。

### 他在解決的是哪個浪費
以前每一次小改都是「跑關卡（3~7 分）→ 推 → 等部署（4~15 分）→ 他打開網頁 →
不對 → 再來一輪」。而**大多數來回只是在確認「長得對不對」**——
那既不需要關卡也不需要部署，卻要付全額。

### 新流程
1. 改完 → `scripts/_show.py` 本機截圖 → 用 SendUserFile 傳給他看（秒級）
2. 他說「這樣對」→ 登記進 `obsidian/unverified.yaml`
3. **推到 `claude/wip` 分支**（不是留在工作區）
4. 他說「驗證」→ 跑清單裡 gates 的聯集 → 合併 main → 推 → 清空清單
5. 順序：**他先人工測基本功能，確認後我才查細項**

### 為什麼「先不推」要寫成「先推 wip」
這個容器是暫時的，**沒推的東西在容器被回收之後就消失**。
而 `pages.yml` 的觸發條件是 `branches: [main]`，
所以推 wip 分支不會部署、不會動到他看的網站 —— 兩個目的同時達成。
把改動留在工作區等他回話是拿「他的時間」換「弄丟工作的風險」，不划算。

### 代價
**累積越多，一次驗證失敗時要拆的東西越多。**
所以 `unverified.yaml` 每一筆都要記清楚「動到哪些檔、要跑哪幾段」，
驗證紅了才知道是哪一筆造成的。清單超過 6 筆時主動提醒他驗一次。

### `scripts/_show.py` 不是關卡
它只證明「長這樣」，不證明「對」。
任何「已經展示過」的東西在跑完驗收之前，一律不准標成完成。

## #221　「科技」新聞用來源自己的標籤重新分類，而且在讀取端做（2026-09-21）

Andy：「今日事件這邊的 科技新聞請確實篩選跟科技有關的，我發現很多無關的，例如醫療科技等等」。

### 根因
`news.py` 的 `technews()` 把 TechNews RSS 的**每一則都無條件標成「科技」**，
而科技新報本身涵蓋天文、醫療、健康、生活科學。
★ 但 `_rss()` **早就把 RSS 自己的分類標籤存進 `keywords` 欄了**，只是沒拿來用。
所以不靠關鍵字猜標題 —— 用來源自己的分類，那比猜可靠得多。

### 判準
這是台股資金輪動儀表板，「科技」這一格回答的是
**「跟台股科技族群的資金流有關的事」**，不是泛科技新聞。

### ★ 分類在「讀取端」重跑，不寫回資料湖
`build_payload` 讀完 news 之後重跑一次 `classify_technews()`。
一開始的做法是寫一支腳本把湖裡的舊記錄改寫，改掉了。兩個理由：
1. 資料湖只增不改是紅線，原始分類要留著；
2. TechNews 的 RSS 一次只給最新 20 則，舊記錄永遠不會被重抓 ——
   不在讀取端重跑，Andy 重新整理還是看到 CAR-T 掛在科技格，看起來像「修了沒用」。
附帶好處：判準之後再調，下一次部署就自動套用到全部歷史，不用再補資料。

同時濾掉 `category == "其他"`：側欄的篩選晶片只有台股／科技／總經／券商四顆，
「其他」沒有自己的晶片**卻還是會出現在「全部」清單裡**，等於篩了跟沒篩一樣。

### 數字（121 則人工標註）
precision 0.981 ／ recall 0.946 ／ 三類一致率 0.967。
363 則重分類：科技 173、總經 52、台股 5、不收 133。
清單放 `config.py`（要給人調的），並有一條 precision/recall 護欄擋在 pytest 裡（門檻 0.85）。

### 已知的鬆處
「AI 人工智慧」這個標籤佔了三分之一的新聞，放在「弱允許」，
所以純 AI 政策與安全論述會留在科技格。**之後如果還是覺得太鬆，第一個該動的就是它。**


## #222　金十數據不接（2026-09-21）

Andy：「並且需要多一項 金十數據」。查證之後**建議不接**，理由是合規不是技術。

### 探測結果（在 Actions 上真的打過，fixture 在 `docs/fixtures/jin10_probe.json`）
| 端點 | 結果 |
|---|---|
| `robots.txt` | 200，但整份是 Cloudflare 的 content-signals 樣板，**沒有任何 Disallow、也沒設任何訊號值** → 依它自己的規則等於「既不授權也不限制」 |
| 官方 RSS | 502，沒有 |
| `flash-api`（不帶偽造標頭） | 502 → 確認這條路**必須繞反爬** |
| `flash_newest.js` | 200、32KB，公開靜態檔，唯一可行的免費路 |
| `open.jin10.com` | 200，**官方 API 是付費的** |
| 版權／條款頁 | 200，但內文是**前端渲染**的，簡單 GET 讀不到原文 |

### 為什麼不接
1. **它有官方付費 API** —— 那強烈暗示免費路徑不是拿來給人轉載的。
2. 條款讀不到、robots 沒有明確授權。**「無法確認合規」不等於「合規」**，
   而 CLAUDE.md 第一條就是「資料只用免費且合規的來源」。
3. 性質不同：抓證交所公開資料是「使用資料源」，
   把別人的快訊搬到我們的**公開網站**上是**轉載**，風險層級不一樣。
4. `flash-api` 那條要偽造 `x-app-id`＝冒用它的用戶端身分，
   那正是 `www.twse.com.tw/rwd` 被永久封殺的同一類問題。

### 解除條件
Andy 開官方付費帳號（走授權的路），或他自己確認條款沒問題並明確指示要做。
**不要在沒有這兩者之一的情況下重開這個題目。**


## #223　驗收再收斂一次：沒動到的檔案不跑，動到的只跑改動處（2026-09-21，同一天第三次）

Andy：「驗收集測試部分 若是沒動到的檔案就不需要跑驗證，
即使動到的檔案，也先跑原本改動的地方即可」。

同一天他講了三次同一件事，一次比一次緊：
1. #219「只驗這批需求對應到的功能」（整輪 → 對應段落）
2. #219 修訂「例外只剩『他說全部驗一次』」（拿掉我自己加的三個例外）
3. **#223「沒動到的檔案不跑；動到的只跑改動處」（拿掉『整批取聯集重跑』）**

### 做法
- `git diff --name-only` 決定範圍，不准靠印象。
- 每一件各自登記 gates；他說「驗證」時**不取聯集重跑**，
  只跑「上次跑綠之後檔案又被動過」的那些。
- 同一個檔案改了 A 功能就跑 A 的段落，不要因為檔案大就全跑。

### 代價（講在前面）
**合在一起才會發作的互動就驗不到。** 這一輪（V1~V6）就有現成的例子：
V2 改了 localStorage 的格式，把 V5 之前寫的 25 條夜盤斷言連鎖打掛 ——
那是兩批之間的互動，只跑各自的段落**也**會抓到（因為夜盤段落屬於 V2 自己的 gates），
但如果哪天的互動剛好落在兩批都沒登記的段落上，就會漏掉。
配套只有一個：**每批在 HANDOFF 記「驗了哪幾段、沒驗哪幾段」**。不准省。


## #224　功能做完就先部署，不要累積（2026-09-21，取代 #220 的「先不推」）

Andy：「以後優先部署做好功能，我才能快速確認」。

### 為什麼 #220 的做法被推翻
#220 當時的設計是「改完先在 Claude 展示 → 累積 → 他說『驗證』再一次跑一次推」。
出發點是省關卡時間，但實際跑了半天之後暴露一個更大的成本：
**他只看網頁。東西沒上線，他就沒辦法確認**，所以那半天裡他看到的都是我的描述與截圖，
而不是真的能點的東西 —— 來回變成盲的，而且累積越多，出問題時越難拆。

### 新的預設
一件功能做完 → 跑它自己那幾段驗收（#223）→ **直接推 main → 部署 → 告訴他**。
**一件一件推，不要綁在一起** —— 綁成一包，他說「有問題」我還要先拆是哪一件。

### 留下來的東西各自降級為
- `scripts/_show.py`：部署前我自己先看一眼、以及解釋我改了什麼。**不再是他的驗收管道。**
- `obsidian/unverified.yaml`：只登記「還沒部署」的（卡在別人手上、或高風險不敢直接上）。
  **清單應該常常是空的。**
- `claude/wip`：只給「還沒做完」的在途工作。做完就合併，不要停在 wip。

### 唯一的例外
會弄壞既有功能的高風險改動（改資料口徑、動到全站共用的東西）——
先問他一句「這個要直接上還是先看截圖」，問完照他說的做。

## #225　資金去向：「位置固定」改成分模式；點族群展開全部成分股（2026-09-21）

Andy（原話）：
> 「我這邊提到的即時包含 他突然某個族群、個我變大，會自動交換位置，這點功能需要達成
>   另外需新增當我點擊族群 會延伸顯示其他股票，意思是水流原本出現前三名，
>   但因為我點擊了那個族群，他就會延伸前三名之後的全部顯示，
>   並且這功能都在這圖表內建立 不是即時的也要，一樣顯示名稱百分比」

這條**推翻了 2026-09-20 拍板的「所有族群固定在原位」的一半**，是他本人推翻的，照做。
但只推翻一半 —— 剩下那一半的理由到今天仍然成立，所以寫清楚界線在哪，
不要讓下一個人以為整條作廢、順手把回放也改成會洗牌。

### ① 「位置固定」現在分模式

| 模式 | 順序 | 為什麼 |
|---|---|---|
| 歷史回放（拖時間軸／▶ 播放） | **固定**：產業鏈用最後一天排一次、族群沿用 `sd.groups` 原始順序 | 回放要看的是「同一個族群這 60 天變粗還是變細」。一洗牌，那個比較就消失了 —— 這正是 2026-09-20 改成固定的原因，一個字都沒過期 |
| 即時（`SKL.on`） | **依當下成交值重排**，產業鏈層與族群層都排；抓不到即時的自動桶（`ind_*`）排到最後 | 盤中要看的是「現在誰突然變大」，而「名次換了」本身就是那個訊號。位置鎖死的話，第 8 名衝到第 2 名在畫面上只會變成「那顆點大了一點」 |

副標（`#sankeySub`）**一定要跟著模式換字**：即時那一版寫「依當下成交值即時排名，
名次變了就會換位」，回放那一版才寫「位置固定」。
**不准出現「圖在動、字說不會動」** —— 那比不寫還糟，因為人會照字去讀圖。

### ② 換位一定要補間動畫，所以 `notMerge` 不再是常數

ECharts 的 `animationDurationUpdate` 只有在 series 身分被保留（`setOption` 合併）時才有用。
原本這張圖固定 `notMerge: true`，每次都是整張重建 —— 重建不會補間，名次一換就是瞬間跳，
人眼根本追不到誰跟誰換了。現在：

- **只有版面型態變了**（窄畫面翻面，四層 ↔ 三層）才 `notMerge: true`（`el._skShape`）
- 其餘（換日期、即時重排、展開／收回、聚焦）一律合併，讓 ECharts 自己補間
- ECharts 對 tree 是用**節點名稱**對位的，而這張圖的名稱唯一（`uniqName`），所以對得上
- `series[].data` 在合併時是整個換掉（不會深層合併），所以「收回」時多出來的葉子會真的消失

**兩個一定會踩到的坑（都實測過）：**

1. **高度一變，`resize()` 會把動畫吃掉。** 展開時容器要長高，而 `resize()` 是立刻重排、
   不補間。原本的順序是「改 height → setOption（用舊高度算版面、開始補間）→
   ResizeObserver 下一幀補一次 resize」，動畫在第一幀就被切斷（實測 y 從 885 直接跳到 1007）。
   改成**先自己 `resize()` 到新高度，再 `setOption`**，補間就出來了
   （實測 885 → 1087 → 1078 → 1040 → 1010 → 1007）。
2. 配套要在 `chart()` 的 ResizeObserver 加一道閘門：**尺寸沒有真的變就不要再 resize**
   （`el._roSize`），否則上一步自己做的 resize 之後它還會再補一次，一樣切斷動畫。
   ResizeObserver 本來就只在尺寸變動時才發，所以這道閘門平常是 no-op。

### ③ 點族群 → 水流延伸出全部成分股

掛在**既有的 `DRILL.gid`** 上（點節點、點晶片、點輪動時鐘都是同一份狀態），
不另開一個「展開中」的旗標 —— 再點一次／點背景／ESC 都已經會 `drillClose()`，
收回自然就跟著發生，不用寫第二套復原。

- **上限 `SANKEY_EXPAND_MAX = 20`**，超過收成一顆灰色虛線的「其餘 N 檔 X.X%」（量是加總，沒有丟掉）。
  20 是量出來的：這張圖上 18 個族群，除了 `ind_ETF`（356 檔有量）之外最大的是
  `bank` 16 檔、其餘 ≤ 11 檔 —— 這條線只夾得到自動桶，真正的題材族群全部畫得完整。
  畫滿 356 檔要 5,700px 高、每片葉子分不到 16px，那不是「完整」是「看不懂」。
- **當天成交值為 0 的不列。**
- **容器高度改成數真正的葉子數**（`leafRows`），不再用「族群數 × 3」估；
  一片葉子 16px（現況量到的：54 片 → 928px 無重疊），展開時上限從 1040 放到 1400。
  順手修掉一個舊問題：使用者從右欄點開的個股本來就會多長葉子，高度卻沒跟著加。
- **位置會動，這是 ECharts tree 的物理限制**：它按葉子數分配縱向空間，
  某個族群從 3 片變 21 片，兄弟一定被推開。繞不過去，**接受，用同一套動畫緩解**。
- 窄畫面（`#sankey` < `SANKEY_NARROW`＝700）本來就沒有代表股那一層，所以不展開；
  完整名單在圖下方的面板裡，字還讀得到。
- 展開之後全部成分股本來就都在圖上了，所以「從右欄點個股」不再是「多長一顆」，
  而是**把已經在的那一顆標起來**（亮邊框＋虛線）。不標的話點了清單圖上毫無回應。

### ④ 百分比的分母：跟右邊面板同一個，不是族群節點的值

展開的葉子用「**該族群成分股成交值的加總**」當分母，不是族群節點自己的值。
兩者在多數族群相等，但**有股票同時掛兩個板塊時不相等** —— 後端的族群成交值是 1/n 拆分過的
（HBM 少 12%）。只畫前 3 檔時看不出來，一展開全部成分股，用族群值當分母就會加到 111%，
一眼就是錯的。改用成分股加總之後剛好 100%，而且和 `renderDrillPanel` 的 `sum` 是同一個數 ——
實測 HBM 南亞科兩邊都是 78.5%（改之前圖上寫 88.0%、面板寫 78.5%，那是舊的不一致）。
差額寫在 tooltip 裡（「成分股加總比這一格大，因為有股票同時掛在別的板塊」），不藏起來。

即時模式下葉子換成 `SKL.stv` 的估算、分母同步換成有報價的成分股加總；
右邊面板仍然是盤後那一份，**差異寫在面板的說明行裡**，不假裝一樣。

### ⑤ 順帶修掉：面板一開就把圖壓成三層（`.stack`）

右側面板固定吃掉 314px。**1280～1440 的筆電一點族群，圖就掉到 700px 以下、
被判成窄畫面、代表股那一整層被收掉** —— 結果是「點族群想看它的成分股，反而看不到成分股」。
（量到的：1440 面板關 998px / 面板開 684px。）

改法：`renderSankey` 先量 `#sankeyRow` 的寬度，`rw - 314 < SANKEY_NARROW` 就掛上 `.stack`，
面板改放到圖下面、圖拿回整列寬度。**判準是「圖剩幾 px」不是「螢幕幾 px」**
（中間隔著側欄與卡片內距），所以只能用 JS 量，媒體查詢算不出來。
1500px 以上維持並排（原本的版面沒有改變）。

面板變成整列之後格子會被排成一堆 168px 的窄欄，「元大台灣50正2」會被折成一個字一行，
所以 `.stack` 時格線改成 `minmax(272px,1fr)`（名稱 80 ＋ 代號 40 ＋ 數字 150），名稱過長就截字。

### ⑥ 驗收

新增 `_uitest.py` 的「桑基展開與即時」段：1500px 與 800px 各跑一輪
**真的用滑鼠點圖上的節點**（不是呼叫 onclick），驗葉子數真的從 3 變成該族群的檔數、
葉子文字是「名稱 + 百分比」、百分比和右邊面板逐字相同、容器真的長高、
再點一次與點背景真的收回；ETF 那一格驗 20 檔上限與「其餘 336 檔」。
即時換位用 **stub 假報價**（容器打不到證交所）餵兩輪不同名次，
驗**節點的 y 座標真的交換**（不是只驗排序陣列 —— 陣列變了但畫面沒動也會過），
外加一輪「名次沒變就完全不動」。**那一段在備註裡明講是假數字，不能拿來推論任何真實值。**

兩個寫驗收時踩到的坑，記下來免得下次再花時間：
- `html{scroll-behavior:smooth}` 會讓 `window.scrollTo(0,y)` 變成動畫捲動，
  算好的座標在滑鼠點下去時已經過期 —— 看起來像「功能壞了」，其實是**根本沒點到**。
  一律寫 `behavior:'instant'`。
  同一個坑在 `t_batch3` 的「點集中度圖的某一天」也踩著（它寫 `scrollIntoView({block:'center'})`）：
  單獨跑永遠綠，預設 4 個 worker 平行跑時 CPU 被搶、600ms 捲不完，就報一次假紅
  （我加了「桑基展開與即時」這段之後負載變重，它就固定紅了）。一併改成 `instant`，
  連跑兩輪 4-worker 都綠。**「只有平行跑才紅」幾乎都是這一類，不是功能壞掉。**
- 展開後圖高 1216px 而視窗只有 1000px，節點座標會落在視窗外：點之前一定要先把它捲進可視範圍。

> ⚠ 2026-09-21 合併時撞號：資金去向那批與剖析圖那批各自寫了 #225。
> 依 commit 時間先後，資金去向保留 #225，剖析圖這兩條往後挪成 #226／#227。
> 內文裡若還有互相引用的舊編號，以標題為準。

---

## #226　剖析圖從「一鏈一圖」改成「族群優先、鏈為預設」（2026-09-21）

Andy：「之前請你幫忙協助繪製產業與個股的 2D 3D 圖 記得要補充，這個一直都沒進展」。

### 為什麼一直沒進展 —— 根因不是沒人畫，是**沒地方掛**

改之前：

```js
// site/diagrams.js 結尾
window.Diagrams = { ai_server: aiServer, semiconductor };
// site/industry.js:99
const HAS_DIAGRAM = (cid) => ['semiconductor', 'ai_server'].includes(cid);
```

**一條產業鏈只有一個 slot**，key 只能是鏈 id。

而 `docs/diagram_specs/` 那五張已經寫好、`mechanical-engineer` 也簽過的規格書，
畫的全部是**族群層級**的東西：

| 規格書 | 族群 | 鏈 |
|---|---|---|
| `mlcc_stack.md` | 被動元件 MLCC | electronics |
| `panel_stack.md` | 面板 | electronics |
| `switch_board.md` | 網通設備 | electronics |
| `naphtha_cracker.md` | 塑化 | traditional |
| `transformer_gis.md` | 重電設備 | infrastructure |

electronics 一條鏈就要掛三張，而 traditional 那條更糟 ——
塑化、生技、觀光、食品共用一個位子，所以「輕油裂解圖會被掛到金融／生技／觀光六個族群上」。
**寫完五張規格書、一張都畫不出來，原因就是這個。** 不先改掛點，畫幾張都一樣。

### 新的規則

**key 可以是族群 id，也可以是產業鏈 id。查找順序：**

1. 目前選到的**族群**有沒有專屬圖（而且那個族群要屬於這條鏈）
2. 沒有 → 這條**鏈**的預設圖（半導體、AI 伺服器有；一般電子沒有）
3. 還是沒有 → **產業鏈頁**退回「這條鏈裡成交值最大、而且有專屬圖的那個族群」，
   **個股頁不准退回**（`strict`）—— 一檔面板股掉到 MLCC 那張圖上，
   等於在網站上宣稱它做 MLCC，那是錯的資訊，比沒有圖傷害更大
4. 都沒有 → 不畫（既有的 `hasMap` 邏輯不動：關聯圖跟剖析圖本來就是分開判定的）

實作在 `site/diagrams.js` 的 `SLOTS`（一張圖一列）＋ `window.DiagramSlots`：

```js
const SLOTS = {
  semiconductor: { level: 'chain', chain: 'semiconductor', name: '…', draw, scene: 'semiconductor' },
  ai_server:     { level: 'chain', chain: 'ai_server',     name: '…', draw, scene: 'ai_server' },
  mlcc:          { level: 'group', chain: 'electronics',   name: '…', draw, scene: 'mlcc', native: 980 },
};
window.DiagramSlots = { pick, groupsOf, anyIn, chainDefault, chains, draw, name, scene, native, has };
```

**名單只有這一份。** `HAS_DIAGRAM` 與 `DG_CHAINS` 這兩個寫死的陣列都改成查表，
跨鏈面板的縮圖（`industry.js` 的 `crossHtml`）也走同一個 API，不留任何舊路徑。

### 三件配套（都是為了「不要騙人」）

1. **標題一定寫出現在顯示的是哪一張**，而且選到的族群沒有自己的圖時
   會多一句「你選的族群還沒有專屬剖析圖，這張是這條鏈目前有的那一張」。
2. **換圖有淡出淡入（180ms）**，不會閃一下白。換圖前一定先 `dispose3D()` ——
   不收的話畫面會停在上一張的 WebGL 場景上，看起來像「換圖沒生效」。
3. 一條鏈有兩張以上時會出現一排「圖別」晶片；**點它＝點那個族群**
   （族群卡片、環節色標、成分股全部跟著走），不另開一套選取狀態。

### 代價

- 一般電子鏈本來沒有剖析圖，現在有了，**390px 的整頁高度從 3944 變成 6082**
  （`_uitest` 的「390px electronics 整頁高度」當場紅）。
  處理方式是「**<640px 預設收合、一顆鈕展開、選擇會記住**」，不是把圖拿掉。
  640 這條線刻意比手機斷點 820 低 —— 800px 的筆電半視窗還是直接看得到圖。
  收合狀態下**不掛 3D**：背景多一個 WebGL context 空轉，手機最吃不消。
- 族群層級的圖**不進**跨鏈面板的縮圖：那個面板回答的是「這個環節在哪幾條鏈上」，
  不是「這個族群長什麼樣」。

---

## #227　量產圖的字級守不住，根因是**欄寬**不是字級（2026-09-21）

Andy 從 2026-09-15 講到現在三次「文字太小」，每次都被當成字級去調。這次先量再改：

| 螢幕寬 | `main` 寬 | `#prodDiagram` 實際寬 | viewBox 1220 的縮放 | 12px 的字實際多大 |
|---|---|---|---|---|
| 1440 | 1080 | 984 | ×0.81 | 9.7px |
| 1100 | 740 | 644 | ×0.53 | 6.3px |
| 900 | 540 | 444 | ×0.36 | **4.4px** |

產業鏈頁的 `main` 右邊有側欄，所以欄寬永遠遠小於螢幕寬。
**1220 寬的圖被壓到 0.36 倍，字級怎麼調都沒用** —— 這就是三次抱怨的根因。

### 兩個一起做的修法

1. **圖畫成 980 寬**（不是 1220）。1440px 螢幕的欄寬量到 984 —— 剛好不用捲。
2. **slot 宣告 `native` 寬度**：圖維持原尺寸、字維持 12px，欄位不夠寬就**左右滑**
   （`#prodDiagram` 改成 `overflow-x:auto`，樣式用 inline 寫，
   因為 `site/index.html` 當時有別的 agent 在改，不碰它）。

實測結果（`.sub` 在畫面上的真實 px）：1440→12.05、1100→12.0、900→12.0、800→12.0、390→12.0。
**這才是「縮放時文字不跟著縮」**（AGENTS §11）。

新圖一律掛 `.dgm` 這個 class，它把 `.sub` / `.cap` / `.tag` 的下限拉到 12px
（舊圖的 `.sub` 是 10.5px、`.cap` 11.5px、`.tag` 10px —— 那是 12px 規則之前畫的）。

### 還沒做、要記著的

既有的兩張圖（半導體、AI 伺服器）**還是舊的**：`.sub` 是 10.5px，而且照樣被欄寬壓縮，
所以在 900px 的螢幕上它們的說明字只有 3.8px。它們沒有一起改，因為改字級會讓版面整個位移、
要重排一次。**下一批量產圖時一起換成 `.dgm` ＋ native。**

## #228　三題答覆與它們推翻的事（2026-09-21）

Andy 對三個待決事項的答覆是「1.依據查到的資訊進行 2.OK 3.不用」。
三條各自的結論與**它們推翻了什麼**：

### ① 凱美（2375）：查完之後，原本的判斷是錯的

原本的說法（我講給 Andy 聽的）是「凱美主體是鋁質電解電容、不該出現在 MLCC 剖析圖上」。
那是照 `supply_chain.yaml` 自己的 note 寫的，**沒有自己查**。

查到的（WebSearch 摘要，信心中）：凱美 2025H1 營收 27.7 億創高，
**晶片電阻佔比約 49%**、是最大且成長最快的一塊（旗下 RALEC 旺詮晶片電阻、ASJ 電阻）。
而 `passive_comp` 這一格的名字就是「被動元件 **MLCC / 電阻**」——
**凱美用電阻進到這一格是合理的，沒掛錯**。舊 note 才是過期的。

真正的錯是**少了兩家**：3026 禾伸堂（自主粉末配方、1kV 以上高壓 MLCC、NP0）
與 6173 信昌電（華新科集團，從粉末到元件自製，車規 MLCC）。
兩家在 `groups.yaml` 的 `mlcc` 族群裡早就有，供應鏈這一格卻漏了 ——
於是一張在講 MLCC 的圖，點下去列不出兩家真的做 MLCC 的公司。

**這條留下來的教訓**：`supply_chain.yaml` 的 note 是**當時查的結果**，不是永久事實。
拿 note 當證據等於拿二手資料當一手。要下判斷就自己再查一次。

### ② 時間軸下限 1 → 0

兩張卡合併之後排行也跟著「看哪一天」走，下限 1 等於「最新」只到前一天，
而頁首寫著資料更新到最新那一天 —— **圖跟字對不起來**。
0 ＝ 資料裡的最後一個交易日。標籤在 0 的時候寫「最新」不是「0 天前」。
（取代 A4 第 3 條 2026-09-20 的「前一天～前三十天」。）
localStorage key 換成 `tw.rot.back3` —— **改預設值一定要一起換 key**，
不然舊使用者拿到的還是舊值，等於沒改。

### ③ 桑基展開：自動桶（`ind_*`）一律不展開

原本的設計是「上限 20 檔 ＋ 其餘 N 檔」，為的是夾住 `ind_ETF` 的 356 檔。
Andy 答「不用」。改成自動桶根本不展開 —— 它們是「沒有被歸進任何族群的股票」的收容桶，
攤開幾百檔讀不出東西。點它一樣會開右邊的清單。

自動桶排除之後真正的族群最大 16 檔，所以 20 檔上限與「其餘 N 檔」都不會再被觸發。
**程式留著當防線**（YAML 是人工維護的，哪天有人加到 21 檔也不該爆版面），
但正常情況下畫的一律是完整名單。

## #229　MLCC 雙審：五個「會被乘以 13」的缺陷（2026-09-21）

`mechanical-engineer` 與 `art-director` 獨立複驗第一張量產圖，共同結論是
**「骨架可以當標準，配色與互動那一層不可以」**。

這條決策要留下的不是那 12 條修正本身，而是**為什麼要在畫第 2 張之前修**：

> MLCC 是圖11 量產的第一張，後面還有 13 張。
> 其中四條缺陷在**共用函式**裡（`processBar` 行高 1.0、SMIL 動畫關不掉、
> 發光選取、46 個寫死色值），不修就是把缺陷寫進標準再乘以 13。
> 而且共用函式那四條**當下就已經在既有兩張圖上發作**，不是未來的風險。

一併記下兩件方法論：

1. **雙審要真的派兩個獨立的人。** 畫圖的那一層沒有派生 agent 的能力，
   它做的是「照清單自審並標明這不等於雙審」—— 那是對的做法，但**不可以當成通過**。
   實際派出去之後，兩位各自抓到自審沒發現的東西
   （結構：受力箭頭與變形方向矛盾、裂紋走向與文獻相反、Ni 厚度不一致、圖例順序自相矛盾；
   視覺：全場發光、電荷疊色把陶瓷色相從 42° 推到 100°、行高 1.0、SMIL 關不掉）。
2. **截圖要確認抽屜是關的。** 兩位都獨立發現 800／390 的截圖裡「今日事件」抽屜
   蓋住右半邊 —— **等於窄畫面沒驗過**，而 CLAUDE.md 明文要求 800px 要驗。
   `scripts/_show.py` 已補 `--ls`（進頁面前寫 localStorage）解決這件事。

### ⚠ 一條反向的教訓：審查意見也會過期

修正那一批開工時，main 已經多了「供應鏈補兩家 MLCC 廠」（#228①），
**直接推翻了視覺審查 A5 立論的一半**（「真正做 MLCC 的兩家在圖上完全不出現」）。
執行的那一層沒有照單全收，重新判斷後留下真正還成立的部分
（環節五家但 3D 只排得下四個晶片，照 YAML 順序切會留下凱美、擠掉信昌電）。
**審查意見和 YAML 的 note 一樣，是當時的判斷，不是永久事實。**

## #230　剖析圖的配色掛在 `<html data-dgpal>`，2D 與 3D 讀同一份（2026-09-21 深夜）

Andy：「幫我圖片色系色調多個**休閒風格，更平易近人**」＋「**2D3D 都需要新增那樣的風格**」。

### 改之前的兩個事實

1. `#dgPal` 這顆鈕**只在 3D 模式才顯示**（`industry.js` 的 `setMode()` 會連同
   「重設視角」「拖曳」一起 `hidden`）。也就是 **11 張 2D 剖析圖從頭到尾只有一種配色**。
2. 九個圖檔各自把自己的 `--dg-*` 定義在**圖自己的 scoped `<style>`**（`.dgabf` / `.dgcool` /
   `.dgair` / `.dg.psu` / `.dgpcb` / `.dgpn` / `.dgsw`）—— 當時不准動 `index.html`。

### 改之後

- 配色掛在 `<html data-dgpal="tech|soft|calm|casual">`，**2D 的 SVG 與 3D 的 `three3d.js`
  讀的是同一組 `--dg-*`** —— `K.css('--dg-cer')` / `palNum('--dg-sat')` 本來就是讀 CSS 變數的。
  這是「不准兩份硬編碼」的實作：休閒在 2D 是暖的，3D 一定也是暖的。
- 配色鈕從 `wire3D()` 搬到獨立的 `wirePal()`，跟有沒有 3D 完全解耦。
- **79 個 scoped token 收進 `:root`**（同值搬家）。不收的話 `:root[data-dgpal]` 那一層
  蓋不掉圖自己 scope 裡的變數 —— 等於「休閒配色只有一張圖有效」。
  撞名兩組改名：`--dg-core`（硬板）→ `--dg-pcb-core`、`--dg-glass`（玻纖紗束）→ `--dg-yarn`。
- 外觀零變化的證明方式：把 `main` 的 `site/` 取出來另起一個伺服器，兩邊各截 11 張圖
  （1440、深色、科技、動畫關掉）**逐張比像素，11 張全部 0 個不同像素**。

### 三條不准違反的

1. **`tech` 一個 token 都不覆寫。** 預設配色必須跟改動之前一模一樣，
   不然「新增一個配色」會變成「順手把大家看習慣的那張圖改掉」。
2. **語意色在任何配色下都不覆寫**：`--dg-err`／`--dg-warn`／`--dg-cold`／`--dg-hot`／
   `--dg-sw-sig|fiber|pwr`／`--dg-pn-r|g|b`。驗收**量色差**，不是看有沒有定義。
   ⚠ 這條真的抓到缺陷：休閒的 `--dg-ink-3` 本來 `#bfae99`，跟 `--dg-warn` 色差只有 **73**，
   警語跟一般說明在暖底上分不出來 → 改 `#a99f92`（色差 95，對背景 5.7:1）。
3. **休閒不是白底。** 11 張圖的材質色全是**為深底調的亮色**（陶瓷 #d3cbb7、錫 #e2e7ec、
   反射片 #eef1f7…），換白底會整片消失 —— 就是 #198 記的「白字白線」同一個坑。
   休閒走的是**色相轉暖（225° → 30°）＋ 關掉發光 ＋ 降低螢光感**，不是換底色明暗。

配色記在 `localStorage['tw.dg3d.pal']`（沿用舊 key，換 key 等於把已經選過的人重設回預設），
**深淺主題共用一份** —— 剖析圖的畫布底在兩個主題下都是深底，配色效果跟主題無關。

## #231　SVG 裡的 `<style>` 不是純文字：註解裡不准出現角括號（2026-09-21 深夜）

實際踩到：在 `site/diagrams.js` 的 `STYLE`（那是一段畫進 **SVG** 的 `<style>`）註解裡
寫了一組角括號包起來的 `html data-dgpal`，結果**整張樣式表的 `cssRules` 變成 0 條** ——
畫面上所有說明框變成實心黑、標籤文字整批消失。

原因：HTML 的 `<style>` 是 raw text 元素，但**在 SVG 裡它是被當標記解析的**。
寫一個長得像標籤的東西進去，解析器就會把樣式表吃掉。

症狀很不直覺：圖還是畫得出來（形狀用的是 inline 的 `fill="var(--dg-*)"` 屬性，不吃 CSS），
但所有靠 class 上色的東西（`.frame`、`.lbl`、`.sub`…）一起變成預設的黑色。
所以**看起來像「顏色改壞了」，實際上是「整張樣式表沒生效」**。

規矩：**SVG 的 `<style>` 裡（含註解）一律不准出現角括號。**
要提到標籤或屬性就寫「html 的 data-dgpal」這種形式。
查法：`getComputedStyle(node).fill` 回 `rgb(0, 0, 0)` 而 `:root` 的變數讀得到值時，
先去數 `document.querySelector('#prodDiagram style').sheet.cssRules.length` ——
是 0 就是這個病，不要再往顏色那邊找。

## #232　量產圖的漸進揭露：收納住在 `diagrams.js`，不動 `industry.js`（2026-09-21 深夜）

Andy：「MLCC 圖片排版我覺得有點奇怪…需要更直觀且看起來更舒服，**可以收納就收納**」。

MLCC 改之前量到：畫布切成 8 塊、73 個文字節點、主角只佔 12.88%。
上一輪只能「降權」（#229 的 P2），因為當時**沒有收的授權**。這一次有了，改成漸進揭露。

### 機制（後面 13 張量產圖共用）

- 每一段在原始碼裡畫在**自己的自然位置**（＝全部展開時的版面），並宣告 `data-y0` / `data-y1`。
- `wireFolds()` 只做一件事：由上往下重新堆一次，收合的段落跳過，最後改 `viewBox` 的高度。
- 掛點是 `stampParts()` —— 它本來就是「圖插進 DOM 之後會被呼叫一次」的那支，
  所以**收合機制完全住在 `diagrams.js` 裡，`industry.js` 的版面一行都不用動**。
- ★ 好處是「JS 沒跑到也不會壞」：靜態 SVG 本身就是一份全部展開、座標正確的版面
  （不呼叫 `stampParts` 的縮圖路徑吃的就是這一份）。

### 三條紅線

1. **收納 ≠ 刪除。** 一個字都不准少。MLCC 這次只有兩處「不再中途斷行」
   （欄位加寬之後原本被硬斷的句子各自一行），沒有任何一句被拿掉。
2. **收合狀態下要看得出「還有什麼可以展開」。** 章節列上寫的是**裡面有什麼**
   （「消費級／車規兩張放大剖面、四層各自在幹嘛、板彎裂示意」），不是「更多」——
   寫「更多」等於叫人先點開再猜，那是把東西藏起來，不是收納。
3. **章節列不准掛 `data-seg`。** 掛了的話 `wireDiagram()` 會把它接成「點零件」，
   按一下展開就順便把下方成分股篩掉了。驗收有專門的一條在量這件事。

⚠ 收納會讓既有驗收失效：`批次11-MLCC` 的 B4（流程列那顆 SMIL 白點停不停得住）
原本假設流程列永遠在畫面上，改完之後它收在第 ③ 段裡 → 那一條直接紅在「前提不成立」。
**這正是把「前提」獨立成一條驗收的價值**：它指出的是「東西不在畫面上」，
不是「動畫沒停」—— 兩件事的修法完全不同。

## #233

**他已經下過的指令，不准再問一次 —— 有指令就執行。**（Andy 2026-09-22）

Andy：「**為何沒有執行，我明明說要執行了**」「**這樣就浪費了時間跟 Token**」。

### 實際發生的事

| 時間順序 | 他說的 | 我做的 |
|---|---|---|
| 先 | 「執行下去，請一口氣把所有圖完成，若是有卡住的就先把其他張優先完成」 | 照做 |
| 後 | 「這部分（半導體分族群）**就先評估**需要繪製幾張花多久時間」 | 交出評估 |
| 然後 | —— | **問他「要畫 1 張、6 張還是 15 張」** ← 錯在這裡 |

**他早就答過了：全部。** 那一問白白燒掉一輪來回，而他只能再講一次同樣的話。

### 根因

不是「我不知道他講過什麼」—— 我甚至在同一段回覆裡引用了他那句話。
根因是**我把「他要一份評估」誤讀成「他要在評估之後做一個決定」**。
他要評估是想知道成本（會花多久、要幾張），不是想開一個新的決策點。

**「評估」是資訊需求，不是決策點。** 這兩件事在他的用語裡從來沒有綁在一起過。

### 規矩

1. **開口問他之前，先回頭查他自己講過的話。** 既有指令涵蓋得到 → 直接做。
2. **「給我評估／估一下時間」≠「停下來等我選」。** 交完評估就照既有指令繼續，
   順手講一句「我照你之前說的全部做，先從 X 開始」就夠了。
3. **唯一可以問的情況**：兩種做法會導出**實質不同的工作**，**而且**既有指令沒涵蓋。
   兩個條件要同時成立；只滿足一個就自己挑一個做下去，把假設講明白。
4. 問之前自問一句：**「這題他是不是已經答過了？」** 答得出「答過了」就不要問。

### 跟既有規則的關係

這條是 `CLAUDE.md`「有疑問先上網查，不要把問題推回給 Andy」的延伸：

- 那條說：**不准把查得到的事推給他。**
- 這條說：**不准把他已經答過的事再推給他一次。**

兩條的共同根源是開頭那條「判斷依據是證據，不是誰在問」—— 把問題丟回去不是謹慎，是迴避。

### 配套（不是只寫一條規則就算了）

Andy 同一句話裡直接指定了做法：「**CEO 請規劃排程的 Agent 出來協助**」。
所以剩下的圖（5 張已有規格的 ＋ 半導體 15 張）由 `delivery-planner` 排成批次表，
連同**「已經拍板、不准再問一次」的清單**一起寫進 `HANDOFF.md`。
那張清單就是這條規則的執行面 —— 下一個 session 讀 HANDOFF 就知道哪些事不用再問。

## #234

**半導體鏈那兩張 CoWoS 剖面是重複的，留族群層級那張、退掉鏈層級那張。**（Andy 2026-09-22）

Andy：「圖二 圖三 是否重疊題材，是的話幫我刪除一個，並且拿幾個半導體內的元件出來說明，可以參考 AI Server」。

### 是不是真的重複：是。證據是它們自己宣告的問題

| 檔位 | 層級 | 名稱 | 它自己寫的 `q` |
|---|---|---|---|
| `SLOTS.semiconductor`（`site/diagrams.js`） | `chain` | 半導體：CoWoS 2.5D 封裝剖面 | 一顆 AI 晶片**是怎麼被封在一起的**？從晶圓、中介層到載板，每一層是誰在做、台廠吃到哪幾層？ |
| `site/dg/ai_adv_packaging.js` | `group` | 先進封裝：晶粒 → 凸塊 → 中介層 → 封裝體 | 一顆 AI 晶片被「包」起來的時候，**裡面到底多了哪幾層**？ |

兩張畫的都是同一顆 CoWoS 封裝的剖面。原本的分工是
「鏈層級講誰做、族群層級講內部細節」，但**畫面上看起來就是同一張圖畫了兩次**。
使用者的感受就是事實 —— 分工只存在於我們的設計文件裡，不存在於他的螢幕上。

### 決定：留 `ai_adv_packaging`，退掉 `SLOTS.semiconductor` 的 2D 剖面

理由三條：

1. **鏈層級那張的獨門價值（「每一層是誰在做」）已經被別的東西取代了。**
   零件小卡（`docs/diagram_purpose.md` §4）現在在**任何一張圖**上都回答「這個零件是誰做的」，
   下面的環節清單與關聯圖也在講同一件事。
2. **Andy 自己指了 AI 伺服器鏈當範本**：那條鏈的做法是
   「鏈的入口顯示圖別選單 → 底下掛很多張族群層級的細節圖」（液冷／氣冷／PSU／交換器／PCB／ABF…）。
   半導體鏈接下來會補上 14 張族群圖，正好走同一個模式。
3. 鏈層級那張是 `native: 1220`（唯一還沒收到 980 的）、而且有 95 個寫死的色值 ——
   本來就是已知的技術債。

### 不准弄丟的東西

退掉不等於刪內容。鏈層級那張有兩塊獨門資訊要**併進 `ai_adv_packaging`**：

- 右側「每一層對應到哪個供應鏈環節」
- 底下「設計 → 晶圓製造 → CoWoS 堆疊 → 上蓋測試 → 上板」的製造流程列

另外 3D 場景（`scene: 'semiconductor'`）要**跟著搬到 `ai_adv_packaging`**，不是刪掉 ——
Andy 這一輪要求的是把 3D 畫得**更細緻**，不是拿掉。

### 同一輪的另一條：圖要畫得像那個東西

Andy：「**不能看起來只有像是一般的方塊，他是電路圖就是要有電路圖的樣貌。有 IC 就有 IC 在上面**」。

這條是 `docs/diagram_purpose.md` R6（「模擬設備的樣貌是手段，不是裝飾」）的加強版：
**方塊只在「那個東西本來就是方塊」時才准用**。板子上該有走線、焊墊、被動元件、
該有的 IC 就要畫在該在的位置。R6 原本的但書「不需要照片級寫實」仍然成立 ——
底線是**認得出來是什麼**，不是像照片。

## #235　產業鏈頁重排：控制台上移、五個區塊加天花板、亮色系配色重算（2026-09-22）

Andy 2026-09-22 的原話：

> 「除了剖析圖全面優化之外，**當前產業鏈這分頁 全面需要排版優化**，
> **需要於使用者立場思考，並重新規劃 哪個圖表放哪裡、顏色怎麼配置、切換到亮色系版面怎麼配色**，
> 目的是**方便且直觀閱讀**，但**版面優化不會動到功能，要小心**，
> **能固定大小就固定**，避免不同題材數量不一致導致版面不同，
> 若過多資訊採用**清單或是拉 Bar** 呈現。」

### 一、先回答「使用者打開一條產業鏈，他依序想知道什麼」

| 順序 | 他想知道 | 回答它的區塊 |
|---|---|---|
| ① | 這條鏈今天整體怎麼樣（幾檔、漲跌、貴不貴） | 標題列的三顆摘要膠囊 |
| ② | **這條鏈有哪些族群、今天誰在漲** | 族群清單 `#groupCards` |
| ③ | 這條鏈分成哪幾段 | 環節色標 `#segChips` |
| ④ | 這東西長什麼樣、每一段誰在做 | 剖析圖 `#dgSec` ＋ 零件小卡 |
| ⑤ | 誰供給誰、我手上那檔在哪一段 | 環節卡／關聯圖 `#chainList` / `#chainMap` |
| ⑥ | 給我清單，我要挑股 | 成分股表 `#memberTable` |

**改之前的由上到下是 ④ → ③ → ⑤ → ② → ⑥**，兩個毛病：

1. **族群清單是剖析圖的選擇器，卻排在剖析圖下面 1300px。**
   `resolveDg()` 的第一條就是「使用者選到的族群有自己的圖 → 就是那一張」，
   所以點一列族群，上面那張圖**真的會換** —— 但在 1440×900 的視窗裡圖早就捲出畫面了，
   使用者看到的是「我點了一下，好像沒事發生」。**控制項必須在它控制的東西旁邊。**
2. 網站自己的第一個問題是「錢往哪個族群跑」，而那個答案要先捲過一張 560px 的剖析圖才看得到。

**改之後：① → ②∥③（並排成一條「控制台」）→ ④ → ⑤ → ⑥。**

### 二、代價講在前面（不准假裝沒有）

1440×900、捲到頂時，剖析圖**畫布**露出來的高度從 **342px 掉到 70px**。
這是這個排序真實的代價，換到的是「控制項就在圖上面」。
之所以判斷划算，是因為**回饋還是看得到**：剖析圖那一區的標題、圖別晶片、
「這張圖回答什麼」那一句**三樣都在視窗裡**，而且都會因為換圖而改字
（`批次20-產業鏈版面` 的 D 段逐條量過）。加上整頁矮了 28%，
往下捲到整張圖比改之前更快。

順手把控制台上面的贅高砍掉 25px：`.chainhd` 在 ≥821px 不准換行 ——
`.row.spread` 是 flex-wrap，左邊那一塊（標題＋說明）在 1080px 的 main 裡吃滿整列，
四顆摘要膠囊整排掉到第二行，白白多 63px。

### 三、五個新的天花板（Andy：「能固定大小就固定」）

判準只有一條：**這一塊的高度是不是「資料有幾筆」的函數？** 是就給上限＋框內捲。

| 區塊 | 改之前（半導體鏈、深色） | 改之後 | 怎麼做到的 |
|---|---|---|---|
| 成分股 `.tw.memcap` | **1315px**（而且只是預設 30 筆；全部 149 筆會超過 6000px） | 504px | `clamp(320px,56vh,620px)`＋框內捲。`th` 本來就是 `position:sticky`，所以**表頭自動黏在框頂** |
| 環節卡 `#chainScroll` | 1920:339／1440:518／1366:645／**390:1076** | 321–420px | `clamp(240px,44vh,460px)` |
| 關聯圖 `#chainMap` | 只有 `overflow:auto`，**沒有 max-height** | ≤486px | `clamp(300px,54vh,560px)` |
| 零件小卡 `#partCard` | 沒有上限 | ≤270px | `clamp(150px,30vh,300px)` |
| 環節詳情 `#segBox` | 沒有上限 | ≤260px | `clamp(140px,26vh,260px)` |
| 族群清單 `#groupCards` | 已有 27vh | **往下收到 24vh**（243→216） | 控制台不准比它控制的東西還胖 |

★ **環節卡為什麼是外面包一層 `.chainscroll`，不是直接給 `.seglist` 一個 max-height**：
`.seglist` 是 CSS multi-column（`columns:236px`）。多欄容器一旦被指定高度，
放不下的內容**不會往下溢出，而是再開一欄往右長** —— 會變成橫向捲軸，
正好是最不該出現的東西。高度限制放在外層的一般區塊上，讓多欄自己算自然高度。
⚠ `#segTools` 也要一起放進那一層：`drawSegList()` 是用
`host.previousElementSibling` 找那顆手機版總開關的，兩者不能被拆開。

整頁高度（半導體鏈）：

| 寬度 | 1920 | 1500 | 1440 | 1366 | 800 | 390 |
|---|---|---|---|---|---|---|
| 改之前 | 3492 | 3727 | 3727 | 3892 | 4092 | 4339 |
| 改之後 | 2639 | 2674 | 2674 | 2674 | 2817 | 2868 |

### 四、並排：欄數要看「框有多寬」，不是「視窗有多寬」

這一頁的 `main` 不等於視窗 —— 右邊還有 360px 的事件側欄，所以「視窗 1366px」
在這裡只有 1006px。用視窗的 media query 會在 1366／1440 這兩個 Andy 真的在用的寬度上判錯邊。
控制台用 `repeat(auto-fit,minmax(340px,1fr))`，量的是**容器**：
1920/1500/1440/1366/**800** 都排兩欄，390 自動疊成一欄（內寬只有約 328px）。
340 這個數字是量出來的：380 會讓 800px 疊成上下兩欄，控制台從 283px 變成 519px，
把剖析圖整個推出第一畫面。

### 五、配色：先列「哪個顏色在講哪件事」，再決定誰讓路

| 這個顏色在講 | 用什麼 | 深色 | 亮色 | 對比度（對 `--panel-3`） |
|---|---|---|---|---|
| 漲 | `--rise` | `#ff4d6d`（不動） | `#dc2440` → **`#cf1f3a`** | 4.71 ／ 4.28 → **4.79** |
| 跌 | `--fall` | `#2ee59d`（不動） | `#0a9b6a` → **`#077a51`** | 9.23 ／ 3.17 → **4.78** |
| 平 | `--flat` | `#8ea0c4`（不動） | `#7a879f` → **`#606d84`** | 5.75 ／ 3.23 → **4.66** |
| 這是供應鏈的哪一段 | `PALETTE[color_idx]` 34 色（不動） | 螢光組 | 壓深組 | 最差 4.60 ／ 3.79 |
| 這個可以點／現在選中 | `--cyan` | `#3ee0ff` | `#0b7fa6` | 9.60 ／ 4.07 |
| 還沒查到具名上下游（畫在深色畫布） | **新** `--dg-iso` | `#d9a441` | 同左 | 6.73 |
| 還沒查到具名上下游（畫在頁面面板） | **新** `--dg-iso-ink` | `#d9a441` | **`#9a6100`** | 6.73 ／ **2.25 → 4.58** |
| 關聯圖的材料邊／設備邊 | **新** `--dg-edge-mat`／`--dg-edge-eq` | `#9aa6bd`／`#8792ab` | 同左（畫布恆深） | 6.17 ／ 4.85 |
| 「壞掉了」的警示膠囊 | **新** `--err-*` | `#ff8fae` | **`#c2185b`** | 8.31 ／ **2.14 → 5.87** |
| 剖析圖材質 | `--dg-*` 79 個（不動） | 深底專用 | 同左（畫布恆深） | — |

**★ 紅漲綠跌沒有動** —— 紅還是紅、綠還是綠，動的只有**明度**，
因為量出來舊值在亮底上讀不到（族群清單的漲跌是 15px 正文，WCAG AA 要 4.5:1，舊值只有 3.17）。

**拿掉的顏色（「顏色太多＝沒有重點」）**：

- `.relbox .cf-verified` 原本是 `#6ee7a8`（亮綠）。兩個問題：亮色主題下對白卡只有 **1.53:1**，
  而且跟「綠＝跌」撞在同一頁。改成**灰階＋實線框** ——「已查證」是常態，不需要用顏色喊。
  整頁的可信度標記只留一個強調色：**琥珀＝這是推估，要小心**。
- 標題列的「本益比中位」膠囊從 `pill violet` 改成中性 `pill`。紫色在那裡沒有語意，
  拿掉之後那一列只剩「今日 ±x%」一個有顏色的東西 —— 一眼看得出重點。

**發光克制**（Andy 2026-09-15／16 講過兩次「螢光感太重」）：
`.segchip i` 與 `.segcard .sh .dot` 的 `box-shadow:0 0 8px var(--c)` 改成**只有選起來的那一顆才發光**。
半導體鏈一次擺 14 顆色點，每顆都發光＝14 顆一起亮＝沒有重點。
色相一個都沒有動，動的只有光暈。驗收直接量 `getComputedStyle(i).boxShadow`：沒選時 0 顆、選了 1 顆。

### 六、字級：產業鏈頁一律拉到 12px 下限

環節卡、關聯圖、鏈切換列、產業關係面板裡還留著 10.5／11／11.5px。
環節卡清單這一輪有了天花板，卡片變高不會再把頁面撐長，所以沒有理由再讓任何字低於 12px。
⚠ 唯一留在 12px 以下的是 `.chainmap .iso text`（9.5px）：那是畫在半徑 7px 的小圓圈裡的
一個「?」符號，不是可讀文字；放大它會戳出圓圈，屬於幾何問題（`tech-illustrator` 的範圍）。

### 七、順手抓到一個**潛伏的驗收缺陷**（比這一批的改動更重要）

`_DGL_BG`（在剖析圖上找一塊真的空白來點）裡的
`h.scrollIntoView({block:'center'})` **從來沒有真的捲動過**：
`site/index.html` 有 `html{scroll-behavior:smooth}`，所以那是**非同步的平滑捲動**，
下一行 `getBoundingClientRect()` 量到的還是捲動前的位置。
以前沒發作，只是因為剖析圖本來就在畫面上（y≈558），根本不需要捲。
這一輪圖掉到 y≈893、整張落在視窗外，於是每個掃描點都被「超出視窗」濾掉，回 null ——
症狀長得像「圖上找不到空白」，其實是「根本還沒捲過去」。
修法：`behavior:'instant'`。

同一輪還修掉驗收自己的兩個假結果：

1. **`pg.goto(同一頁的另一個 hash)` 不會重新載入 JS**（same-document navigation），
   所以「切主題」不生效 —— 第一次跑時亮色那四條其實全部在量深色
   （失敗清單跟深色一模一樣，連數字都一樣）。新段落的 `land()` 一律 `reload()`。
2. **`color-mix()` 在 Chromium 算出來是 `color(srgb 0.77 0.86 0.92)`**，那三個數是 0~1 不是 0~255。
   不正規化就會把淺底當成近黑，量出 1.19 這種**假的**低對比。

### 八、改掉的既有斷言（逐條）

| 斷言 | 改之前 | 改之後 | 為什麼 |
|---|---|---|---|
| `批次19` ④「圖頂到色標底 ≤ 視窗高」 | `segChips.bottom - prodDiagram.top` | `prodDiagram.bottom - chainband.top ≤ 1.35 個視窗高` | 色標搬到圖**上面**之後，原本的減法會算出**負數**，斷言變成永遠綠 —— 假綠比紅燈危險。改量同一件事的新形狀，並補兩條更嚴的：剖析圖區自己 ≤ 一個視窗高、控制台 ≤ 視窗的 65% |
| `批次19` ④「桌機一定 ≥2 欄」 | 用**視窗寬**判（`w > 480`） | 用**色標那個框自己的寬度**判（≥420px 才要求多欄） | 色標現在只佔控制台的半欄。800px 的視窗裡它自己只有約 362px 寬，排兩欄會把「IP / EDA / 設計服務」擠到剩三個字。框寬才是欄數真正的自變數 |
| `批次19` ④ 量測寬度 | 1440／800／390 | **1920／1500／1440／1366／800／390** | 補齊 Andy 實際在用的桌機寬度（既有棘輪只守 1440／1500） |
| `批次19` ⑥「全開之後 total ≤ 視窗高」 | 比固定門檻 | 比**全開前後同一個量**（必須完全相等） | 這一條真正要守的是「章節全開之後版面沒有被撐開」，比門檻嚴 —— 多 1px 都抓得到 |

**沒有任何一條是因為「功能變了」而改的。** 新增的 `批次20-產業鏈版面` D 段
把每一條既有互動重新量了一次：點族群（網址變／圖換／筆數變／再點取消）、
點色標（筆數變／亮起來／再點取消）、點零件（只亮不篩，DECISIONS #73）、
點背景（全部恢復全亮、小卡收掉）、看關聯圖 ↔ 看清單、成分股排序、點一列進個股頁。

## #236

**輪動時鐘的即時位移一律拆成兩段：慣性（灰虛線）＋今天真正推出來的（亮色箭頭）。**（2026-09-22）

Andy：「輪動時鐘理論上也有辦法與資金去向做到即時對吧？有成交價以及數量，
就可以透過這方式計算每個族群的方向」。做得到，而且接一天的公式是**精確**的
（`rs_new = rs_last × (1+族群報酬) ÷ (1+大盤報酬)`，因為 rs 的定義就是族群指數除以大盤指數）。

### 但直接畫一整條「昨收 → 現在」是一張會騙人的圖

實測（拿線上這份 `live_state` 餵不同假報價量的）：

| 情境 | Δ強弱 | 畫面像素 |
|---|---|---|
| **每一檔都餵 0%**（族群報酬 ＝ 大盤報酬，什麼都還沒發生） | 中位 **0.59**、最大 **1.42** | 最長 **18px** |
| 某族群贏大盤 2%，**今天真正貢獻的那一段** | **0.342** | **4.3px** |
| 盤面 x 的分布 | 90.05 ～ 106.29（16.2 點） | |

**開盤什麼都還沒發生，「昨收→現在」的箭頭就已經比「贏大盤 2%」還長。**

根因在指標本身，不是資料錯：EMA 只讓當天的相對報酬進來 α＝2/11≈18%，
而 40 日基準線的窗口**每天會往前滾一格、掉掉最舊那一筆**。
所以一整條箭頭裡有一大半是**指標的慣性**，跟錢往哪裡跑無關。

實際撞到的例子（模擬盤）：電源供應器族群報酬 −2.18%、**輸大盤 2.07%**，
但總位移是 **+0.294（往上）** —— 只畫一整條會**指錯方向**。

### 所以

- **灰虛線** ＝ 收盤 → 持平基準（族群報酬剛好等於大盤時會走到的地方）＝ 慣性
- **亮色箭頭** ＝ 持平基準 → 現在 ＝ **今天的成交價與成交量真正推出來的那一段**
- 圖下方那排數字與排序**一律用亮色那一段**；tooltip 三個數字都給

### 不准做的事

**不准為了讓它看起來有動而放大位移。** 箭頭大小跟著真實長度縮放（`L × 0.45`，上限 9px）——
固定大小的箭頭會讓 4px 的位移看起來像 12px。
要做的是把真實的小位移**畫清楚**，不是誇大它。這一條有驗收在量像素比對得上資料比。

### 兩條誠實界線（常駐在狀態列上，不准收進 tooltip）

1. **權重是估的、報酬是真的**：成交值用「價 × 量」估算（即時端點沒有每檔的累積成交金額），
   漲跌幅是真的（現價 vs 昨收）。所以族群方向可信，只有「誰權重大一點」是估的。
   ⚠ 這個估法跟資金去向那張圖**共用同一個口徑**，不准兩張圖各算各的。
2. **即時的大盤是代理值**：原本是全市場成交值加權，盤中只抓得到族群成分股那一批（291 檔／3 個請求）。
   畫面上要印出**涵蓋率**（這批的估算成交值 ÷ `Market3.marketAmt`）。
   ⚠ 涵蓋率**可能超過 100%**（分子「價×量」會高估、這批含上櫃，而分母只有上市）——
   **超過就照實印，不准夾在 100%**，後面講明為什麼。

### 已知限制（寫在畫面上，不是藏起來）

- 自動桶（`ind_*`，9 個含 ETF）沒有即時：成分股 1367 檔＝14 個請求／分鐘，太貴。
  它們留在盤後位置、不畫箭頭。個股點同理（後端沒給個股 `live_state`）。
- 總覽頁那張小時鐘**刻意不套用即時** —— 那裡沒有地方放誠實標示。
- 兩個即時（資金去向、輪動時鐘）同時開著每分鐘共 5 個請求。沒有合併共用報價，
  因為兩邊抓的股票集合不同（111 vs 291）。

## #237

**`native` 只設 min-width；要讓畫布是固定尺寸，一定要同時給 `max-width`。**（2026-09-22）

Andy：「幫我將所有 2D 3D 圖的圖片及文字縮小一半大小，**我發現是大小問題導致整理版面塞太滿**，
或是整理統一大小…希望能一次看到完整資訊。」

### 他說的「大小問題」，根因之一在這裡

`DECISIONS #227` 訂的 `native` 是**最小寬度**：它擋的是「欄位太窄把圖壓小、字跟著縮」。
**另一邊完全沒擋** —— svg 是 `width:100%`，欄位一變寬整張圖就跟著放大。

在 main 上實測（1440 螢幕、MLCC）：

| 狀態 | 欄寬 | 畫布 |
|---|---|---|
| 「今日事件」抽屜**開**著 | 998 | 984 × 749 |
| 抽屜**關**掉 | 1358 | **1344 × 1023**（放大 **1.366 倍**） |

1920 只會更誇張。所以**同一張圖會有好幾種尺寸，而且沒有上限** ——
「一次看完」在抽屜關著的狀態下一張都不成立。

### 修法

```css
.dgwrap svg.dgm{max-width:984px;margin-inline:auto}
```

加上之後不管欄位多寬都是固定 984 —— 那才是 Andy 說的「統一大小」。

⚠ **只給 `.dgm`**（產業鏈的量產剖析圖）。題材頁那幾張是 `.dg3`、畫布 1180 寬，
套上去會被縮成 0.834 倍、12px 的字變成 10px。

### 這件事是怎麼被抓到的：同一個畫面有兩種寬度，就要兩種都量

做的人自己量是抽屜開著、`_uitest` 跑起來是關著的 —— **兩邊差 1.366 倍**。
判準：**任何跟「寬度」有關的量測，抽屜開與關都要量一次。**

### 順帶記兩個同一類的假綠（都是驗收自己抓到的）

1. **「≤ 上限」的斷言，量不到的時候會過。**
   `tw.dg3d` 是全站偏好、會跨段汙染；3D 開著時 `#prodDiagram` 被藏起來，量到高度 **0**，
   而「0 ≤ 上限」**是會過的**。
   規矩：**任何「≤ 上限」的斷言，都要先問「量不到的時候它會不會過」**，
   並補一條「這個東西真的畫出來了（> 0）」把洞塞住。
2. **展開工具與測試互相抵銷。** 共用的 `force_open` 會開章節列，而某一段自己又**無條件** toggle 一次
   —— 剛打開的被關回去，症狀跟「沒展開」一樣、原因剛好相反。
   規矩：toggle 一律寫成 `if (!已經開著)`，不要無條件切換。

## #238

**剖析圖的最終風格：兩種模式（亮色「閱讀」／暗色「科技」），卡片式標註、細引線、垂直爆炸拆解。所有 2D／3D 圖都改。**（Andy 2026-09-22）

Andy 給了兩張參考圖（同一張圖的暗色科技版與亮色閱讀版並排）與四段提示詞，並說：
「請安排人力執行這項工作…這邊是針對**所有圖片進行大改風格**，麻煩**執行到底**。」

### 視覺語言（兩種模式共用）
- **垂直爆炸拆解**：層與層之間留呼吸空間，看得出組裝關係
- **編號圓點 ＋ 圓角卡片標註 ＋ 細引線**指到精確位置；卡片在畫布兩側欄，不壓圖
- 流動（光／液／訊號）用箭頭與虛線；暗色可發光**但只給流動線與被選零件**
- 大量留白、乾淨排版、「一張圖一個主色，其餘灰階」

### 兩種模式
| | 亮色「閱讀」 | 暗色「科技」 |
|---|---|---|
| 底 | 暖白／淺灰 `#F4F3EF`／`#F8F9FA` | 深藍炭 `#0B132B`／`#0D1117` |
| 色相 | 低飽和粉彩（鼠尾草綠、霧藍、赤陶、暖赭） | 霓虹點綴（青、綠、橘、白），只給流動 |
| 卡片 | 白色圓角卡、深灰字、編號圓點 | 半透明深色 HUD 卡、發光小節點 |
| 引線 | 深灰 0.85～1px | 1px ＋ 發光節點 |
| 字級 | 正文 13px 起 | 12px 下限 |

**預設跟著全站主題**：深色主題 → 科技、淺色主題 → 閱讀；`#dgPal` 仍可手動切。
原本的「柔和／沉穩／休閒」三個配色**移除**（Andy 改口只要這兩套）；舊 `localStorage` 值 fallback 到科技。
⚠ 這推翻了「剖析圖畫布恆為深底」（`--illus`）—— 淺色主題下畫布變暖白。

### 一個必須守住的物理前提
參考圖是 AI 生成的**點陣圖**。我們的圖是 **SVG／WebGL**，而且靠互動吃飯
（點零件只亮不篩、零件小卡「誰做的」、環節色標篩選、四模式切換）。
**所以不是去生圖，是把那套視覺語言搬進 SVG／WebGL** —— 互動一個都不准少。

### 怎麼做（順序有理由）
1. **地基先行**：共用元件（`labelRow`／`processBar`／引線／陰影）與兩套色票改一次，**所有圖的標註與配色立刻跟著變**。
   3D 同時根治「零件底色來自環節色」（一個場景幾個環節就幾個色相，違反一張圖一個主色）。
2. **逐張改造**：每張圖的**繪圖本體**改成爆炸拆解，**同一次一起**收進 ≤ 700px（不然同一個檔要動兩次）。
   每波 3 支 agent、每支 2～3 張；17 張 2D ＋ 3 個 3D 場景 ＋ 18 張題材 2.5D。
3. 並行上限維持 **3 支** —— Andy 說「多請點人」，但 2026-09-21 同時派 9 支全部被速率上限打掉，
   那一輪等於整個歸零；多派不會更快，只會全賠。

### 承接自 #237 的收納工作
尺寸標準化那一批（13 張舊圖收進章節列）**併進**每張的風格改造，不另外合併 —— 同一個檔動一次。

### 3D 補充（Andy 2026-09-22 稍後，三個推薦 ＋ 一張融合範例圖）
Andy 針對 3D 圖再給三個方向，並附一張把（一）＋（二）融合的範例（半透明機櫃、發光液冷流線、風扇氣流線、磨砂玻璃卡片、柔和淺底）：

| | 名稱 | 要做的事 |
|---|---|---|
| （一） | 現代透明玻璃與發光流線（Glassmorphism & Glowing Flow） | 機櫃外框與不重要的金屬板改**半透明／霧面玻璃**；托盤用溫和但有區隔的半透明彩色分區（**訊號藍、電力橘、液冷青綠**）；液冷／CDU 與光模組／CPO 加**微發光流線或箭頭**；引線改**帶發光端點的細線**；卡片加磨砂玻璃 |
| （二） | 立體爆炸拆解與元件抽離（Detailed Isometric Exploded View） | 托盤像抽屜一樣**半拉出**（不是全散開）；拉出來的 PCB 上晶片看得見；**留白** |
| （三） | 2.5D 平面微透視資訊圖表 | 去陰影漸層、色彩分區、卡片與元件**同色系串聯**（主要給題材頁 18 張 2.5D） |

落地方式（併進地基那一波，不另開波次）：
- 材質新增三種：`glass`（半透明外殼）、`flow`（發光流線，可動可靜止；靜止時要停，走既有 E4 驗收）、`airflow`（風扇氣流細線）。
- 三組語意色進 token：`--dg-sig`（訊號藍）、`--dg-pwr`（電力橘）、`--dg-cool`（液冷青綠），兩模式各一套；卡片文字對比 ≥ 4.5、圖形 ≥ 3。
- 卡片與元件同色系串聯是 **2D／3D 共用**：卡片吃 `--dg-card-c`（由 `data-dgcolor` 餵），預設落回 `--dg-ink`；引線端點一個元件色小圓，科技模式帶 glow、閱讀模式只留實心點。
- 淺底（閱讀模式）上的發光要收斂成細線＋端點，不准刺眼 —— 這是我判斷的，範例圖本身是淺底但發光很輕。
- 先拿 AI 伺服器機櫃當範本（外框 glass、托盤三色、CDU 水路 flow、CPO 光路 flow、風扇 airflow、前排托盤半拉出），截 1440 深／淺各一張確認後再套另外兩個 3D 場景。

### 版面補充（Andy 2026-09-22 稍後，看了線上矽晶圓那張之後）
Andy：「這邊的版面需要**左右對齊**，可以適當分配左右間隔，讓版面**更滿**，看起來舒適，並且會**依據螢幕大小變化**。」
他看到的是 #237 之後的樣子：畫布固定 984、置中、1440 寬時左右各空 250px，而標題列從最左邊起。

**怎麼填**：不是把 SVG 放大（放大會讓高度破 700，違反「一頁看完」），而是**把說明卡片放到畫布左右兩欄**，
整個 `.dgwrap` 用 grid 填滿容器 —— 跟 3D v2 的機櫃截圖一樣（左欄卡片／畫布／右欄卡片，1440 一格空白都沒有）。
- ≥1280：`minmax(220px,1fr) minmax(0,984px) minmax(220px,1fr)`，左右各一欄卡片
- 960–1279：畫布 ＋ 右欄（卡片全靠右）
- <960：單欄，卡片在畫布下面；390 一欄、字 12px 下限
- 標題列、章節列、卡片欄與畫布**左緣對齊**；引線在 resize 時重算，不寫死座標
- 2D 與 3D 共用同一組 grid class，不各寫一套
- **欄數用容器寬（container query）判，不用視窗寬。** 理由：產業鏈頁的 main 旁邊有 360px 的事件抽屜，
  視窗 1440 時 main 只有 998（抽屜開）或 1358（抽屜關）；用視窗量會在 Andy 真的在用的寬度上判錯邊
  （`.dgwrap` 是容器、`.dggrid` 是格線 —— 元素不能查自己的寬，所以要分兩層）。落地見 #239。

## #239　風格系統地基：兩種模式跟主題走、共用元件卡片化、v2 版面卡片外掛（art-director 2026-09-22，分支 `claude/style-system`）

#238 拍板的方向，這一條是「改一次全部圖跟著變」的地基。規格書 `docs/diagram_specs/_STYLE.md`、介面 `docs/diagram_restyle_plan.md`「卡片介面」。

### 一、兩種模式，預設跟主題走（推翻 #124／#230 的「畫布恆為深底」）
- `<html data-dgpal="tech|read">`。柔和／沉穩／休閒**移除**。`palPref()`：存過 `tech`／`read` 就用它；
  舊值（soft／calm／casual）**當成沒設定並清掉**，退回「跟主題走」—— 深色主題下就是科技（＝那些人以前的預設）。
- `tw:theme` 事件 → 沒手動偏好的人跟著換模式，3D 開著也一起換（`view3d.setPal`）。
- **科技模式一個 token 都不覆寫**（跟改動之前同一張）；閱讀在 `:root[data-dgpal="read"]` 整組覆寫，含 79 個材質 token 全部為亮底重調。
- `--illus` 沒動：產業關聯圖 `.chainmap` 仍是深底（它有自己的一組對比校準，#235）。

### 二、語意色的規矩改寫（蓋掉 #230 的第 2 條）
「任何配色下都不覆寫」是四個配色全部深底時寫的。閱讀模式亮底上 `#ff4d6d` 只有 3.0:1、`#ff8fab` 2.0:1，警語讀不出來。
新規矩：**色相不准動（跨模式 < 15°），明度可以依底色重算**；驗收量對比（對底 ≥ 3、當文字 ≥ 4.5）與色相差，不再比字面值。
新增三組元件語意色 `--dg-sig`／`--dg-pwr`／`--dg-cool`（3D 托盤分區也用）；`--dg-sw-sig`／`--dg-sw-pwr` 變成別名。

### 三、環節色不直接用，用 `--cc`
`--c` 是 inline（industry.js 寫的），樣式表蓋不掉，所以 `STYLE` 全部改讀 `--cc` ＝ 消費端換算：
深色＋科技原色；深色＋閱讀 `color-mix(--c 58%, #4a463f)`；淺色＋閱讀 86%；淺色＋科技往白 74%。
科技＋深色的 computed 值跟改動之前逐位元相同（不是 color-mix，是直接 `var(--c)`）。

### 四、共用元件（API 不變、只多可選參數）
- `labelRow(seg,x,y,title,sub,tx,ty,w,dropY, no, side)`／`lrow3(o,x,y,w,i, no, side)`：卡片化（圓角、細邊、左側色條、編號圓點、引線到卡片邊緣）。
  `no`＝編號；`side`＝'l'／'r' → **卡片離開 SVG 變成 HTML**（v2 版面，見五）。
- `note({side,title,lines,warn,order})`：沒有錨點的說明卡（公式、結論、警語）。
- `processBar(x,y,steps,w,{cols})`：卡片化 ＋ 可分列（窄畫布用 3＋2）。
- `pointer(ax,ay,bx,by,{elbow,drop,node})`：細線 ＋ 端點小圓；`explode`／`explodeZ`／`shadow`／`fitTexts`。
- `stampParts()` 多做三件事：`data-dgcolor` → `--dg-card-c`；`.rs` 的圖跑 `externalize()`；HTML 卡片補 `dgkey`。

### 五、v2 版面：卡片是 HTML、畫布維持原尺寸、欄數看容器寬
- `.dgwrap`（容器）→ `.dggrid`（格線：head／l／c／r）→ `.dgcards`（所有卡片）→ `.dgcol.l`／`.r`（分組）。
  ≥1280 三欄；960～1279 畫布＋右欄（左欄的卡片排進右欄）；<960 卡片在畫布下面（800 兩張一列、390 一張）。
- **併成一欄時卡片照 `order`（＝編號；公式 0、警語 99）排**，`grid-auto-flow:dense` 不留落單格。
- 引線畫在 `.dglead` 疊層：卡片邊緣 → 畫布欄外側 8px → 錨點；`ResizeObserver`／章節開合／切模式都重算。單欄不畫引線，靠編號。
- HTML 卡片本身就是 `[data-seg]`／`[data-part]` 節點：`wireDiagram`／`highlightSegments` 把它當零件，
  **主角永遠剛好一個**（錨點群組沒有身分，只鏡射卡片的狀態；點錨點＝點卡片）。SVG 裡的 `.lrow.ext` 只是資料來源，永遠不畫。
- MLCC 是樣板：畫布 980 → 660、標題進 `.dghead`、六條說明＋公式＋警語進卡片，三段章節重排到 660 寬（一個字沒刪，只改斷行與位置），
  收合 574（≤ 700）。`native: 660`。

### 六、閱讀模式的字級升階只給 `.rs` 的圖
把 13px 套到全部 18 張實測有 8 張同一行的字撞上（伺服器電源、ABF、AI 伺服器、矽晶圓、HBM、氣冷、液冷、變壓器 GIS）。
版面是為 12px 排的，字級要升就得連版面一起改 —— 那是逐張改造那一波的事。所以 `:root[data-dgpal="read"] .dg.rs{--dg-fs-min:13px…}`，
沒掛 `.rs` 的圖在閱讀模式維持 12px。配套 `fitTexts`：右緣超出畫布或卡片的字壓 ≤ 12%。

### 七、發光只給兩個地方
`--dg-glow` 統一管：零件的選取光暈、主角卡片的光暈、引線端點的光暈。單一環節的圖（`.dg1`）與閱讀模式 `none`。
流動虛線本身不加濾鏡（幾十條動態虛線每幀重算濾鏡會拖慢 `#dgAnim`）。

### 八、改掉的既有斷言（逐條，都不是放寬）
| 斷言 | 改前 | 改後 | 為什麼 |
|---|---|---|---|
| 批次13「四個配色都走得到」 | tech→soft→calm→casual | tech→read→tech | 選項真的只剩兩個 |
| 批次13「語意色的值完全一樣」 | 字面比 `#ff4d6d` | 色相差 < 15° ＋ 對底 ≥ 4.5 | 改成量對比，比字面更嚴 |
| 批次13「重整記得住」 | casual | read | 同上 |
| 批次13「提示 > 14 字」 | 14 | > 8 且不含「更多」 | v2 畫布 660，wireFolds 砍短提示（全文掛 title） |
| 批次11「800px 原尺寸 ≥ 960」 | 960 | ≥ 圖自己宣告的 native | MLCC native 980 → 660 |
| 批次6-圖九／3D零件字彙／批次21「四個配色」 | 4 | 2（read／tech） | 同第一列 |
新增 `批次22-風格系統`（40 條）：模式跟主題走、記住、舊值 fallback、逐元素對比、語意色、12px／13px、v2 三個寬度、引線重算、互動全過。

### 參考圖第二批（Andy 2026-09-22 晚，三張，存 `docs/diagram_refs/`）
Andy：「3D 圖希望可以做到配色風格跟如圖片所示那樣，2D 圖也是如圖三。」
- **蓋掉上表「閱讀＝低飽和粉彩」**：參考圖的亮色版本托盤是藍／綠／橙、水路青綠帶柔光 —— 閱讀模式是「亮底、顏色清楚、柔光」，不是灰掉。
- **2D 走 2.5D 玻璃層疊**：帶厚度、圓角、半透明漸層的玻璃板；光束穿層；小元件立體化；層疊類主題用等角爆炸，迴路／流程類維持示意構圖但同材質。
- 抽出來的規格在 `docs/diagram_refs/README.md`。

## #240　波 1b：伺服器電源 PSU＋BBU 與 ABF 載板改成最終風格（tech-illustrator 2026-09-22，分支 `claude/restyle-w1b`）

#238／#239 的版面 ＋ Andy 晚間參考圖（`docs/diagram_refs/2d_panel_dark_light.webp`）的材質語言，落在 `site/dg/server_psu.js`、`site/dg/ic_substrate.js`。

### 一、兩張圖的數字（1440、抽屜關）
| 圖 | 畫布寬（native） | §1 | 章節 | 收合 | 全開 | 卡片 | `parts` |
|---|---|---|---|---|---|---|---|
| 伺服器電源 | 980 → **640** | 643 → **432** | 1 → **3** | 1806 → **568** | 2461 | 10 ＋ 1 警語 | 0 → 18 |
| ABF 載板 | 980 → **520** | 606 → **504** | 2（草稿）→ **2** | 1300 → **598** | 1933 | 10 ＋ 1 警語 | 13 → 14 |

### 二、材質語言：`D.fx`（`site/diagrams.js` 檔尾 `// ===== 2.5D 材質（玻璃／發光／光束）=====`）
- `fx.glass(x,y,w,h,{fill,t,rx,cls,iso:{dx,dy},top})`：帶厚度、圓角、半透明漸層的玻璃方塊；給 `iso` 就是等角玻璃板（頂面＋前面＋右側面）。
- `fx.beam(d,{color,w,flow,glow,dots})`：光束＝blur 光暈 ＋ 實線 ＋ 流動虛線 ＋ 端點光點。
- `fx.shadows(shapes)`：一整組柔陰影套一次濾鏡。`fx.glowDefs()` 放進 defs。
- 兩種模式共用同一支函式，差別全在 STYLE 的 `--fx-*` 旋鈕（科技：玻璃較透、光束發光；閱讀：sheen 拉高、光暈約四成）。
  sheen 用 `--dg-sn`、暗面 `--dg-sh0`、陰影 `--dg-drop`，一個色值都沒寫死。
- **發光預算**：一張圖 ≤ 3 個帶 `feGaussianBlur` 的元素（PSU：直流光束、晶片光暈、一組陰影＝3；ABF：訊號光束、一組陰影＝2）。驗收數這個數字。

### 三、構圖
- ABF＝層疊類 → **等角爆炸層疊**：九片玻璃板浮著（防焊×2、增層×6、core），剖面內容畫在每一片的前緣（規格書 S／V 組一條沒動），
  微孔另外畫成穿過層間空隙的小銅柱，疊孔靠虛線軸心。
- PSU＝迴路類 → 維持單線圖構圖：電源架上第 4 顆拉出來、往下拆成上蓋／主板（PFC → LLC → 同步整流 → 輸出，四級由上往下）／底殼，
  方塊全是玻璃，直流路徑一條 `--dg-pwr` 光束、交流一條 `--dg-sig`。拓樸是常見組合（TI nest036 ＋ NXP AN12617／USPTO 11616446 摘要一致），畫面標「示意」。

### 四、卡片與零件是同一個身分（改寫 #239「主角永遠剛好一個」的量法）
卡片（HTML）與畫布上的區塊掛**同一個 `data-part`**：點卡片亮區塊、點區塊亮卡片。所以主角是**一個 data-part**，畫面上是兩個節點同時 `.sel-part`；
驗收比 data-part 的集合大小＝1，並且**同時要求卡片與區塊都亮**。卡片只放一句話（欄寬 220 時卡片欄不准比畫布高），長說明在零件小卡與章節裡。

### 五、順手修掉的三個共用缺陷（`site/diagrams.js`）
1. **同一個 `#prodDiagram` 連續裝兩張 v2 圖，第二張的卡片不外掛**：`externalize` 用 `host.dataset.dgv2` 當旗標，`innerHTML` 換圖時旗標還在。
   改成看 svg 自己有沒有被包進 `.dgcanvas`，並用 `teardownV2()` 收掉上一張的觀察器與 class。
2. **章節一打開，內容被推到下面一千多 px、中間一片空白**：`processBar` 的光點是 `circle r=3` 沒有 cx/cy、位置全靠 `animateMotion`，
   `getBBox()` 不含動畫位移，永遠回 (-3,-3) → 有流程列的章節 bbox.y 變成 -3。`bodyBBox()` 量的時候把帶 animateMotion 的元素暫時 display:none。
   **MLCC 的 ③ 在 main 上就中**（全開 3542 → 1814）。
3. **章節列的起點**：候選 ①「第一段內容的上緣往上退一列」只對手寫 y0/y1 的段落有意義；自動量測的段落改成只看 §1 底部＋8。
   MLCC 收合 574 沒變（它以前是靠缺陷 2 的假 bbox 才成立的）。

### 六、被改的斷言（逐條，都不是放寬）
| 斷言 | 改前 | 改後 | 為什麼 |
|---|---|---|---|
| 原尺寸 | svgW ≥ 960／970 | ＝ 圖宣告的 native（640／520） | v2 畫布收到主角寬（#239 五） |
| 主角剛好 1 個 | `.sel-part` 節點數 ＝ 1 | data-part 集合 ＝ 1，且卡片與區塊同時亮 | 卡片與區塊同身分；條件變多不是變少 |
| 只有主角發光 | computed filter ≠ none ≤ 2 | 帶 feGaussianBlur 的元素 ≤ 3 ＋ 主角以外的 `.part` 不發光 | #238 允許端點與光束發光；舊數法會把設計要的東西當 bug |
| 非講不可的話 | 在 svg text | 在整張圖的文字（svg ＋ HTML 卡片 ＋ 標題列） | v2 把它們搬進警語卡 |
| 百分比只在效率表 | 只掃 svg | svg 效率表以外 0 個 ＋ HTML 卡片 0 張 | 範圍變大 |
| ABF core 座標 | 寫死 246～324 | 從 `[data-part=abf_core]` 量 | 爆炸拆解之後座標全變 |
新增：收合 ≤ 700、§1 ≤ 560、章節每段開合、兩模式三樣真的變、對比逐元素、13px 升階、四個寬度、卡片欄不比畫布高、
E4 兩種動畫都停、層間小銅柱 > 0、發光預算 ≤ 3。舊的 `t_psu`／`t_abf` 留在檔裡當對照，`SECTIONS` 指到 `t_psu_v2`／`t_abf_v2`。

### 七、查不到、自行拍板的
- 各家 PSU 用哪一種拓樸：查不到 → 只畫常見組合、標示意，小卡 note 明講。
- 超級電容／LIC 的台股供應商：查不到 → 小卡寫查不到，不編。
- 卡片欄在 3 欄模式只有 220px（`.dggrid` 的中欄吸掉全部剩餘寬度）—— 這是地基的 grid 行為，這一批沒動 index.html，先用短卡片（標題 ＋ 一行）配合。
- 預設進來（族群選起來）時另一個環節的卡片 `.dgc.dim` 是 .3，字讀不到：兩張圖各自用 `.dgwrap:has(svg.xxx) .dgc.dim{opacity:.55}` 放寬到跟 SVG 同一階。
## #241　波 1a：液冷／氣冷兩張改成最終風格 v2 ＋ 2.5D 玻璃材質 `D.fx`（tech-illustrator 2026-09-22，分支 `claude/restyle-w1a`）

`site/dg/liquid_cooling.js`、`site/dg/air_cooling.js` 照 #238／#239 重畫：主角改**垂直爆炸拆解**、說明卡片外掛 HTML（左右欄）、
畫布收到 680、三段章節（`D.fold()`）。收合高度液冷 565／氣冷 583（ai_server 額度 656）。材質照 Andy 稍後給的 2D 參考圖
（`docs/diagram_refs/2d_panel_dark_light.webp`）改成玻璃板。

### 一、材質走波 1b 的 `D.fx`（`site/diagrams.js` 檔尾 `// ===== 2.5D 材質（玻璃／發光／光束）=====`，以 main 為準）
- 我原本自己寫了一套（`fx.glass／tube／disc／beam／dot／pad／defs` ＋ `--dg-fx-*` 三個 token）；波 1b 先合併，
  收尾時整個改成呼叫它的介面：`fx.glass(x,y,w,h,{fill,cls,rx,iso:{dx,dy}})`、`fx.beam(d,{color,w,glow,flow})`、
  `fx.shadows()`、`fx.glowDefs()`；模式差異全在 STYLE 的 `--fx-*` 旋鈕，**不留兩套**，我的 token 也拿掉了。
- 圓盤（風扇沿轉軸拆開的那五層）`D.fx` 沒有現成的，`air_cooling.js` 用它的 class（`fxg／fxb／fxs／fxe／fxr`）在檔內拼，沒有另開共用區塊。
- **濾鏡元素一張圖 ≤ 3**（#239）：液冷＝兩條主幹的光暈 ＋ 拆解那一疊的一組柔陰影；氣冷＝穿過導風罩的三條氣流光暈；其餘 beam 一律 `glow:false`。

### 二、順手抓到的兩個地基缺陷（最後以 main 的修法為準）
1. `processBar` 的 SMIL 光點待在原點 (0,0)：章節 body 的 `getBBox` 從 y＝−3 起算，**MLCC 的 ③ 一展開多出一千多 px 空白**（全開 2917）。
   我先改成 translate＋相對路徑；波 1b 改成 `bodyBBox()` 量的時候暫時藏掉帶 animateMotion 的元素 —— 合併後取 main 的。
2. `wireFolds` 的候選 ①（第一段內容上緣退一列）只看**手寫 y0/y1** 的段落：`D.fold()` 的自動段落本來就會整段 translate。
   光點修好之後這條才發作（MLCC 收合 574 → 653），兩邊改法相同，取 main 的。

### 三、卡片與零件共用 `data-part`
液冷／氣冷的卡片直接帶零件的 `data-part`（`extRow({part})`）：點卡片 → SVG 零件亮、點零件 → 卡片亮，主角是「同一個身分的兩個節點」。
`data-dgcolor` 同時寫到 `.anc`，圓點／色條／引線端點才是同一個元件色（地基只搬到卡片上）。

### 四、改掉／新增的驗收（`批次12-散熱` → `t_cooling_v2`，舊函式留在原位不接）
| 斷言 | 改前 | 改後 | 為什麼 |
|---|---|---|---|
| 圖以原尺寸顯示 | svgW ≥ 960 | ＝ 圖宣告的 `native`（680）且 > 0 | 畫布收到主角寬是 #238 的要求；讀宣告值比寫死更嚴 |
| 氣冷點兩格軸承 | 直接對 `[data-part]` 派事件 | **先點開章節 ②** 再點 | 舊寫法對 display:none 的元素派事件，不是真人操作 |
| 12px 下限 | 兩種模式都 12 | 閱讀 13（`.rs` 升一階） | #239 六 |
| 新增 | — | 收合 ≤ 656、章節逐條開合（高度／字數真的變）、兩模式底色卡片引線真的變、逐元素對比、1100 兩欄照編號、800 單欄、卡片 data-part 對得到零件、點卡片亮零件、小卡寫對零件（含「不在環節裡」那一種）、柔光 ≤ 3 且不掛動態 | #238／#239 的驗收清單 |

### 五、密度預算的一個偏離（寫在這裡，不藏）
`_TEMPLATE.md` §0-C 說 §1 標註列 ≤ 10；液冷 §1 有 11 張編號卡（第 11 張是「這條線右邊是機房基礎設施」的邊界說明）、氣冷 12 張（風扇拆五層 ＋ 四線 ＋ 氣流路徑五個 ＋ P-Q）。
那條上限的理由是「一條 42～54px、十條就吃掉 §1」—— 卡片外掛成 HTML 之後不再佔畫布高度，所以沒有砍內容；
代價是 960～1279 的兩欄模式右欄比畫布高（約 1000 vs 583）。要收就是把四線／邊界說明併進別張卡。

## #242　波 2b：面板疊層與高速互連改成最終風格（tech-illustrator 2026-09-22，分支 `claude/restyle-w2b`）

#238／#239／#240 那一套（v2 卡片欄版面、玻璃／光束材質、章節收納 ≤ 700、兩種模式）落在 `site/dg/panel.js`、`site/dg/ai_interconnect.js`。
**面板是 Andy 親自給參考圖（`docs/diagram_refs/2d_panel_dark_light.webp`）的對象**，當全站 2D 的示範品。

### 一、兩張圖的數字（1440、抽屜關）
| 圖 | 畫布寬（native） | §1 | 章節 | 收合 | 全開 | 卡片 | `parts` |
|---|---|---|---|---|---|---|---|
| 面板疊層 | 980 → **660** | 471 → **533** | 0 → **3** | 1164 → **669** | 1824 | 9 ＋ 1 警語 | 0 → 13 |
| 高速互連 | 980 → **560** | 282（路徑帶）→ **529**（半層疊場景 ＋ 光束尺） | 0 → **2** | 1432 → **623** | 2604 | 9 ＋ 結論卡 ＋ 警語 | 20 → 33 |

發光預算兩張都剛好 3 個 feGaussianBlur 元素（面板：白光光束 ＋ R／G／B 三道共用一個光暈群組 ＋ 一組陰影；高速互連：線纜光束 ＋ 強的那道對照光束 ＋ 一組陰影）。

### 二、面板：照參考圖逐項對
- **13 層垂直爆炸玻璃層疊**（`D.fx.glass` 等角板，由下往上：背板 → 反射片 → 導光板 → 下擴散片 → 稜鏡片 ①② → 下偏光板 → 下玻璃 → TFT → 液晶 → 彩色濾光片 → 上玻璃 → 上偏光板），層序照規格書 §3-A，一片都沒少；背光那六片是同一個零件身分 `pn_backlight`（一個模組），LED 燈條與驅動 IC 各自獨立。
- **光束**：背光上來的是一道**白光**（顏色不是它給的）射到彩色濾光片為止，穿過色阻之後才是 R／G／B 三道往上穿出頂面。三道共用一個光暈濾鏡 —— `D.fx.beams(list, o)` 是這一批加進共用材質區塊的新能力（向下相容，單條仍走 `beam()`）；液晶分子的小橢球是 `D.fx.molecule()`。
- **小元件立體化**：液晶分子＝小橢球（左半躺平、右半立起來）、TFT＝一格格小磚 ＋ 兩組正交的線、色阻＝黑色矩陣上 R│G│B 並排的小色塊、導光板前緣有網點、側邊 LED 燈條、端子區的驅動 IC 是小晶片 ＋ 往下折的 COF。
- **左側兩個說明框含小示意**留在 SVG（① 兩片偏光板＝一道光閥、② 顏色是濾出來的）；右側 7 張卡片外掛，背光的兩張走左欄（錨點放在說明框 ② 的下緣以下，引線不壓框）。
- 畫布 660 而不是 680：1100 視窗兩欄時畫布欄只有 660，680 會被切掉 20px（實測）。
- 章節：② 放大看（像素 ＋ 液晶兩種排列）、③ OLED 對照 ＋ 製程五格（3＋2 兩列）、④ 族群九檔 ＋ 2026 結論 ＋ 示意標示。舊圖一個字沒刪，只改斷行與位置。

### 三、高速互連：半層疊場景 ＋ 光束尺
- §1 從「一條路徑帶」改成**半層疊場景**：綠色玻璃板 ＋ ASIC 方塊、① 沿板面的弱光束到板邊金手指（一排金色接點）→ 插槽（板邊外的深色殼）②、③ 雙軸線纜從 ASIC 拱起來接到 ④ 直立的前面板（四個籠開口，也是玻璃）、⑤ 更右後方直立的線纜匣（成束銅纜）。**線材剖面畫成玻璃圓柱剖開**（圓柱身 ＋ 端面：外被 → 遮蔽 → 兩個介電 → 兩根等徑導體）。站點編號 ②③④⑤ 印在場景上，跟四格標題一致（X2）。
- **損耗對照條改成光束強弱**：走線纜那道 `--dg-sig` 光束又亮又長（約 22 吋 ＝ 400px、發光），走板子那道又弱又短（約 4.5 吋 ＝ 82px、不發光、半透明）；長度比例仍是 22 : 4.5，數字旁邊一樣帶來源與前提（N2）。
- 2×2 四格收進兩個章節（② 金手指與插槽 ＋ 內部線纜、③ 光模組籠 ＋ 背板盲插），每格 528×440 等高等寬（X1）；格內零件與標註列一個字沒動，整組平移進新的格子（`SH2`／`SH3`／`SH4`／`SH5`）。格內標註列仍是 SVG 卡片：章節收合時錨點看不到，外掛卡片沒有意義。
- 規格書 §5 那一塊「連接器賣的是四件事」固定說明框，舊圖只有一句話帶過，這次做成左欄的結論卡（電／機／熱／裝四行）。
- 場景零件（`st_*`）與四格零件是不同的身分：點場景亮場景、點格子亮格子。

### 四、被改的斷言（逐條，都不是放寬）
| 斷言 | 改前 | 改後 | 為什麼 |
|---|---|---|---|
| 批次22「面板（卡片還在 SVG 裡的舊版面）… anchors ＝ 8」 | 8 個錨點 | 9 個錨點 ＋ 三欄時 9 條引線 | 面板改成 v2，多一張 LED 燈條卡 |
| 批次22「面板還沒掛 .rs，維持 12px」 | 面板下限 11.9 | 兩張都吃閱讀 13px 的下限 | 面板掛了 `.rs` |
| 批次14b-高速互連 原尺寸 | svgW ≥ 970 | ＝ 宣告的 560 | v2 畫布收到主角寬 |
| 批次14b-高速互連 X3「路徑帶 ≤ 全圖 1/3」 | 量 `path_band` 高度 | 拿掉（改驗 ASIC 沒有內部細節） | v2 的 §1 就是主角本體，四格是章節，「路徑帶」這個元件不存在了 |
| 批次14b-高速互連 2×2 的溝（`_b14b_gutter`） | 兩段高度的溝 | 拿掉 | 四格改一欄四列，沒有溝 |
| 批次14b-高速互連 四個零件小卡 | `heroKey` | data-part 集合 ＝ 1 ＋ 卡片與區塊同時亮（`_v2_common`） | #240 的量法 |
新增 `批次23-面板`（共用 `_v2_common` ＋ 規格書硬規則：層序 y 嚴格遞增、下玻璃比上玻璃長且驅動 IC 在多出來的那一段、色阻每一列三色都在、液晶橢球兩種角度、TFT 小磚 ＋ 正交線、兩片偏光板紋理方向不同、導光板網點 ＋ LED 在側邊、白光止於彩色濾光片而 R／G／B 從那裡往上、§5-D／§5-E／§6-C 的字都在、無百分比、四張零件小卡）。
`批次14b-高速互連` 指到 `t_hsio_v2`（E1／E2／E6／B1／B2／K1／G1／G3／G4／X1／X2／X4／X5／N1／N2／N3 逐條照舊 ＋ 光束尺比例與強弱 ＋ 場景端面兩根導體同徑並排）；舊的 `t_b14b_hsio` 留在檔裡當對照。

### 五、順手修掉的共用小缺陷
- 無。`site/diagrams.js` 只在材質區塊加了 `fx.beams`／`fx.molecule` 兩個向下相容的能力。

### 六、查不到、自行拍板的
- 面板：共通電極／配向層／平坦化層在哪一片依顯示模式而異，本輪仍查不到可引用的定論 → 不畫，畫面上明講（照舊）。
- 面板：背光模組（6176 瑞儀）、偏光板（8215 明基材／4960 誠美材）在族群裡有、但 `supply_chain.yaml` 沒建成環節 → 零件小卡的 `none` 明說「有人做、只是還沒建檔」，不編對應、也不動 YAML。
- 面板卡片元件色：`--dg-pn-pol` 在深底上對編號數字不到 4.5，偏光板卡片改用稜鏡片那組亮藍灰（`--dg-pn-bef`）。
- 高速互連：卡片欄 8 張比畫布高（#240 也撞到的地基 grid 行為）→ ASIC 卡片搬到左欄、兩個標題縮短成兩行。
- 高速互連：twinax 的線規／阻抗、籠架尺寸、線纜匣的纜線數量與頻寬照舊不寫（查不到可引用的通用值）。

## #243　波 2a：硬板 PCB 與交換器板卡改成最終風格（tech-illustrator 2026-09-22，分支 `claude/restyle-w2a`）

#238／#239 的版面 ＋ #240 的材質語言（`D.fx`），落在 `site/dg/pcb_rigid.js`、`site/dg/switch_wireless.js`。共用介面一行沒改，只用。

### 一、兩張圖的數字（1440、抽屜關）
| 圖 | 畫布寬（native） | §1 | 章節 | 收合 | 全開 | 卡片 | `parts` |
|---|---|---|---|---|---|---|---|
| 硬板 PCB | 980 → **560** | 502 → **451** | 0 → **3** | 1446 → **587** | 2451 | 12 ＋ 1 警語 | 14 → 24 |
| 交換器板卡 | 980 → **600** | 502 → **300** | 0 → **3** | 1482 → **436** | 1886 | 10 ＋ 1 警語 | 0 → 31 |

### 二、構圖
- **硬板 PCB＝層疊類 → 等角爆炸玻璃層疊**：十三片玻璃板浮著（上防焊、L1 銅箔、pp、core×4 與 pp×5 交替、L10 銅箔、下防焊），
  剖面內容畫在每一片前緣（core 的兩條銅、訊號層一段一段、參考面整片、prepreg 紗束）。四種孔畫成穿過層間空隙的小銅柱，
  背鑽掉的那一段空隙裡沒有銅柱、殘餘用 `--dg-warn`、PTH 殘端用 `--dg-err`。頂視走線（差動對成雙、蛇行兩條一起繞、45°）畫在 L1 的頂面。
  訊號光束 1 ＋ 陰影 1 ＝ 2 個 blur。
- **交換器板卡＝板卡＋光模組 → 半層疊**：板子是一片帶厚度的綠玻璃板（前緣看得到一疊層，掛 `ccl`），只拆主角 ——
  ASIC 的載板／晶片／散熱片往上浮；前面板立起來、16 個籠架；風扇與 PSU 是後方玻璃方塊。斜投影（`fx.glass` 的 iso）所以前面板在畫面下、後方在右上。
  **光＝`--dg-cool`、電＝`--dg-sig`、供電＝`--dg-pwr`**：光纖進 → 籠架 3 → 差動對到 ASIC → 另一條到籠架 6 → 光纖出（雙向、不同的埠）。
  等長蛇行改成**量畫面上的長度**逐折補到跟最遠的埠一樣長（最長／最短 1.08），不再靠固定折數。電光束 1 ＋ 光纖光束 1 ＋ 陰影 1 ＝ 3。

### 三、章節切法（dg-scale 草稿 `847838d` 的切法撿來用，只併掉一段）
- PCB：②「四種孔 ＋ 走線」（草稿的 ②③ 併成一段，不然 4 段 ＋ §1 451 會破 ai_server 鏈的 656）｜③ 材料（稜面三格、織紋、載板對照）｜④ 製程五格 ＋ 兩塊結論 ＋ 誰做哪一塊。
- 交換器：② 疊構節律 ＋ 背鑽 ＋ 為什麼 40 層 ＋ **速率換算**（原本 §1 左欄的公式框搬進來；它是檢查用的算式，不是結構）｜③ 光模組 ＋ 價值鏈五格｜④ 可插拔 vs CPO。

### 四、自行拍板的（規格書沒寫死或跟 v2 衝突的地方）
- **交換器 ASIC 不再帶 `data-chain="semiconductor"`**（規格書 §6-D 寫「點了跳半導體鏈」）：v2 的畫布區塊點下去要亮它的卡片，直接跳走會跟卡片的行為打架。
  改成跟 ABF 的灰色剪影同一種做法 —— 小卡 `none` 明說「外商供應、台股沒有直接對應、看半導體鏈」；仍然**不掛任何 data-seg**（驗收數這一條）。
- 光纖顏色從 `--dg-sw-fiber`（暖黃）改成 `--dg-cool`（任務單指定光路用 `--dg-cool`／`--dg-sig`）。`--dg-sw-fiber` 這張圖不再用。
- PCB 的頂視走線群組改掛 `hdi_pcb`（規格書 §2 第 13 項），舊的 `pcb_foil_top`（掛 `ccl_material`、列銅箔廠）拿掉 —— 線是 PCB 廠蝕的，不是銅箔廠的。
- 警語卡寫「高階 PCB 這一格收錄六家」（YAML 現在是 2368／4958／3044／2313／6191／5469；規格書寫三家是 2026-09-21 補人之前的數字）。
- CPO 對照放在章節 ④ 而不是 §1：§1 要 ≤ 560，兩格對照各 164 高放不進主角旁邊；§1 的卡片與說明都指到 ④。
- 卡片欄 220px 的限制（#240 七）沒動 index.html，兩張的卡片一律「標題 ＋ 一行」；長說明在零件小卡與章節。

### 五、驗收：新增 `批次23-硬板PCB`、`批次23-交換器板卡`（`_v2_common` ＋ 各自規格書的硬規則，全部從 DOM 量、不寫死座標）
- PCB 25 條結構：十三片都在、S1～S8（core 上下緣各一條銅、prepreg 沒銅、10 層、厚度序列＝倒序、銅 4 ＜ 介電 11／12、兩處開窗、鎳內金外）、
  V1～V6（PTH 貫穿、殘端紅色、背鑽大孔徑貼底＋殘餘、盲孔只在 L1 與 core1 之間且上寬下窄、埋孔不碰外層、四個跨距互不相同）、
  T1／T2／T4（三組各兩條、蛇行 ≥ 8 段）、T3（每層訊號上或下是整片銅）、M1～M5、P1～P3、N1／N2／N4；加 5 句非講不可的話、三個 seg 筆數互異、
  「銅箔稜面」只列銅箔廠、「玻纖織紋」換玻纖廠、prepreg 列 CCL 廠。
- 交換器 19 條結構：§3-A（銅層序列裡 sig 上下都是 gnd、鏡像對稱、外層是線路層）、§3-B（6 條差動對都從 ASIC 到籠架、最長／最短 ≤ 1.35 且折數不同、
  飛越纜線兩條且那兩埠沒有重複走線、光束一進一出）、§3-C（VRM 間距 ≤ 24、供電線比差動對粗）、§3-D（風扇在前面板上方、鰭片 ≥ 8 且深度 ≥ 10 厚度 ≤ 4）、
  §4（16 個同規格籠架、背鑽特寫下半段孔徑大）、§3-E 五格順序、§6-D ASIC 沒 seg、§6-C 沒有 %／W／mm；加 6 句非講不可的話、三個 seg 筆數互異、ASIC 小卡不列公司。
- **既有斷言一條都沒改**；這批只驗了：`批次23-硬板PCB`、`批次23-交換器板卡`、`批次22-風格系統`、`零件誰做的`、`點背景恢復`、`淺色主題`、`產業鏈導覽`、`批次13-配色與收納`（後者跑遍 14 條剖析圖路由 × 三個寬度的字級與重疊）＋ `_preview.py`。pytest 依規則跳過（只動 `site/dg/*.js` 與 `scripts/_uitest.py`）。

### 六、查不到、沒做的
- 交換器板厚 mm、ASIC 封裝尺寸、各家用的光模組瓦數：照規格書一個都不寫（驗收有 regex 擋 `%`、`W`、`mm`）。
- 3 欄模式畫布置中在 984 的中欄裡、兩側留白 —— 是 `.dggrid` 的地基行為（#240 七），這批仍然沒動 index.html。

## #244　3D 精緻化：環境貼圖、真陰影、收透明、依模組配色（tech-illustrator 2026-09-22，分支 `claude/3d-pbr`）

> Andy：「我希望達到**精緻的科技產品 3D 展示／半寫實資訊圖表**風格。
> 請從**配色、材質、燈光、陰影與後製**一起改善，**不要只提高飽和度或更換背景**。」
> 規格與診斷在 `docs/diagram_3d_pbr_spec.md`（七個根因是機器查出來的，不是感覺）。

### 一、這一批真正修掉的是什麼

| 症狀 | 根因（規格書〇） | 這一批做了什麼 |
|---|---|---|
| 灰、金屬像塑膠 | **完全沒有環境貼圖**。註解自己寫「只有方向光、沒有環境貼圖，所以 metalness 壓在 0.45 以下」 | 用 `PMREMGenerator.fromScene()` 自己搭一個**程序式棚**（上方大柔光板＋左冷右暖兩片補光＋後上方一片輪廓光＋下方暗板）→ `scene.environment`。兩種模式各一份。**metalness 上限跟著解除**，閱讀模式的 `--dg-metal-k` 從 .6 拉回 1.0 |
| 扁、看不出上下層 | 沒有真陰影，只有一片貼在底下的 radial sprite | `shadowMap.enabled` ＋ PCFSoft；**只有 key 投影**、shadow camera 貼著外接盒收緊；castShadow **只給每個零件體積最大的前三顆、且 ≤600 三角形、且不透明**的 mesh（機櫃 44 顆、封裝 28 顆、MLCC 9 顆）。原本那片 sprite 留著當軟接觸陰影，兩者疊加 |
| 偏淡、前後分不清 | 透明疊五六層（玻璃機櫃**連立柱帶橫樑**、角色外殼 .55、ghost .14、引線、光點、AO 墊） | `shell:true` **不再等於半透明的角色色**。主要零件一律不透明；只剩機櫃外殼板（`--dg-glass-a` .28）、液冷（`cool:true`，.62、有厚度的內外兩層）、示意層可以透明。實體透明件補 `depthWrite:true` |
| 整台被青藍洗掉、關掉標籤認不出模組 | 顏色依**角色**（訊號／電力／液冷）整片上色 | 顏色改依**模組功能＋材質**：新增一組 `--dg-m-*`（機架銀灰／PCB 墨綠／走線金／晶片深藍／石墨灰／散熱蓋銀／銅件香檳銅／液冷青綠／風扇框藍灰／扇葉深灰藍／電源暖橘）。`role` 留著，只餵卡片與引線的顏色 |
| 像色塊、沒有面積感 | 只有四盞平行光 ＋ 半球光 .8 | 暖白主光 1.25（投影）／冷白補光 .4／兩模式都開的 rim（科技 .8、閱讀 .35）／半球光降到 .35；曝光科技 1.0、閱讀 1.12 |
| 閱讀模式霧化、對比掉下去 | `--dg-sat` 1.2、`--dg-mix-k` .08 往奶油白收 | `--dg-sat` 1.05、`--dg-mix-k` .02（差異交給 `--dg-m-*`，不要再用全域色溫洗掉）；底色換成規格書七的淡藍灰 `#E8EDF2 → #D6DEE6` |

### 二、刻意沒照規格書做的三件事（連理由）

1. **背景不生 canvas texture、不設 `scene.background`。**
   `.dg3d` 的 CSS 本來就是 `radial-gradient(...var(--dg-1), var(--dg-2))`，而畫布是 `alpha:true` ——
   換掉那兩個 token 就得到規格書七要的那個底，**零 draw call、零貼圖記憶體**。
   另外，既有驗收量的是 `getComputedStyle('#prod3d').backgroundImage`（「切模式畫布底真的變了」），
   改走 `scene.background` 會讓那一條變成量不到。
2. **陰影的開關 token 叫 `--dg-shadow-on`，不是 `--dg-shadow`。**
   `--dg-shadow` 已經被「模型底下那片軟接觸陰影的**顏色**」佔用了，再拿它當 0/1 會直接把顏色讀成 NaN。
3. **不引入 EffectComposer／bloom。** 依規格書七用「envMap 的上下色差 ＋ rim 光」取代；
   多一支 vendor 檔、多一次 render pass 的成本，換到的東西 Andy 已經說過不要（「不是電競 RGB」）。

### 三、規格書沒寫、實測之後自己拍板的兩件事

1. **銅件的顏色從 `#C98A5E` 改成 `#D6A886`（閱讀 `#D9AE7E`）。**
   規格書給的暖銅跟電源的暖橘 `#E08A3C` 在 Lab 上只差 **ΔE76 22** ——
   驗收第 1 條（關掉標籤要認得出七類）**直接不過**。往香檳金那一端拉到 ΔE 34。
   這是「驗收條件反過來修正規格」的例子，不是放寬標準。
2. **`mats()` 的「這個零件現在是什麼顏色」不再算掛在它身上的流線。**
   以前 `ag_cdu` 量到的是那條紅色熱水管（TubeGeometry 橫跨半個機櫃、外接盒體積最大），
   所以「液冷模組是什麼顏色」永遠量到熱水紅 —— **量錯了東西，不是顏色錯了**。

### 四、加陰影之前先減面（規格書一-2 的前提）

機櫃場景原本 **39,822 / 上限 40,000**，只剩 178 的餘裕。減的都是「看不到的面」：
鍍通孔與沖孔改 `openEnded`（孔是管，兩端的圓盤不是被板子夾住就是被對方擋住）、
細分 8/6 降到 6/5、板上 IC 與插槽與電容從 12 個獨立 Mesh 併成 3 個、焊墊陣列 6×5 → 5×4、
錫球細分 [6,4] → [6,3]、風扇氣流環 5×20 → 4×12。
結果 **39,822 → 33,108（−16.9%）、draw call 447 → 393**，畫出來的**形狀一個都沒少**。

### 五、順手修掉的版面缺陷（Andy 的「圖二」：MLCC 的卡片被切掉、要左右滑）

- `applyDgNative()` 的「圖以原尺寸顯示、放不下時左右滑」是**給 2D 的**（2D 的字不能縮）。
  3D 沒有「原尺寸」這回事 —— 它是 WebGL，相機可以退、畫布該吃剩餘欄寬。
  **3D 開著時一律不給橫向捲動**，`#prod3d` 釘死 `max-width:100%`。
- 卡片欄的斷點以前**只看視窗寬度**，欄寬卻是從**容器寬度**算的：
  視窗 1280 而側欄開著時容器只有 836，兩欄各 220 吃掉 440，畫布只剩 396。
  改成兩個條件都要成立（視窗夠寬 **而且** 兩欄之後還留得下 54% 給模型），塞不下就退一階。

### 六、驗收（`_uitest.py` 的 `3D風格兩模式`，新增 `t_dg3d_pbr`）

用 **CIE76 ΔE**（Lab 的歐氏距離）而不是 RGB 距離量色差 —— RGB 對「深藍 vs 墨綠」這種都很暗的配對
會嚴重高估。門檻 25（ΔE 2.3 是「看得出不一樣」的下限，25 大約是「一眼就是兩種顏色」）。
新增的斷言：關掉 `.lbl3d` 之後七類模組兩兩 ΔE76 ≥ 25、`scene.environment` 不是 null、
最高 metalness ≥ .8、`shadowMap.enabled` 且 castShadow 落在 [5,60]、
**在「點過背景、沒有選取」的靜止狀態下**半透明 mesh ≤ 25%、三角形與 draw call 上限、
四個寬度下 `#prodDiagram` 不橫向捲動且卡片完整在容器內。
⚠ 透明比例一定要先點背景再量：多環節場景一進來就有選取，其餘零件被高亮淡到 0.12 ——
那是**高亮狀態**不是設計，直接量會得到 68.5% 的假數字（第一次量就踩到）。

### 七、幀率：envMap 是這一批最貴的一件事（量出來的）

AGENTS.md 對繪圖寫的是「不准掉幀」，所以這一批**量了改前改後**（同一個容器、同一支腳本、
`requestAnimationFrame` 數 4 秒）。容器裡是 swiftshader 軟體渲染，絕對值沒有意義，**比例才有**：

| 狀態 | fps（機櫃場景、1440、動態） |
|---|---|
| 改前 | 8.1 |
| 改後（envMap ＋ 陰影） | 2.8 |
| 改後但關掉陰影 | 3.1 |
| 改後但關掉 envMap | 8.8 |

結論很清楚：**陰影只吃掉 0.2～0.3 fps，3 倍的代價全部是 envMap**。
成本在 fragment shader 的 `textureCubeUV`（依粗糙度在多階模糊之間混合），
所以**把貼圖改小沒有用** —— 實測把 PMREM 的來源從 `fromScene()` 的 256 換成
先拍一張 64 的 cubemap 再 `fromCubemap()`，fps 一樣是 2.9（那個改動留著，
因為記憶體與建立時間還是省的）。軟體渲染沒有材質取樣單元，這一段特別慘；
**真的 GPU 上這是 three.js 每個 PBR 場景都在走的標準路徑**。

做了這幾件事把代價收住：
1. **陰影貼圖不再每幀重畫**（`shadowMap.autoUpdate = false`）。這個場景會動的只有相機與
   爆炸拆解那 1.6 秒，而陰影只跟光源與物體的位置有關 —— 相機怎麼轉它都不會變。
   只在 `applyExplode`／換模式／改寬度那幾幀補一次。
2. **`--dg-env` 設 0 ＝ 完全不掛 envMap**（不是只把強度歸零 —— 強度歸零一樣要付取樣的錢）。
   這是給「慢的裝置」的逃生門。
3. **< 700px 直接不掛 envMap**（陰影本來就 < 960 關掉）：小螢幕多半也是弱裝置，
   而且那個尺寸下反射根本看不到。
4. ⚠ `--dg-shadow-on` 第一版寫成 `parseFloat(x) || 1`，token 是 `"0"` 時 `0 || 1` 變成 1 ——
   **開關寫了等於沒寫**，而且是量 A/B 的時候才發現的。已改成 `Number.isFinite` 判斷。

**還沒驗到的一條**：任務單要求「1440 下 FPS ≥ 30」。這個容器**改之前就只有 8.1 fps**，
所以它從來就不是那條線的量尺 —— 我能證明的是「陰影幾乎免費、envMap 貴 3 倍（軟體渲染）」，
**不能證明 Andy 的機器上 ≥ 30**。要確認只能在他的機器上開一次。
真的卡的話第一刀就是把 `.dg3d` 的 `--dg-env` 設成 0（金屬會退回比較灰，但立刻回到改前的幀率）。

## #246　3D 剖析圖：預設收攏、游標移過去才爆開（tech-illustrator 2026-09-23，分支 `claude/3d-hover`）

> Andy 2026-09-23：「**所有 3D 圖都需要預設是收攏的，游標移動過去才會自動分開，變成爆炸圖**」。

### 一、行為表（三個場景 `ai_server`／`semiconductor`／`mlcc` 完全一致）

| 情境 | 行為 |
|---|---|
| 進場 | `expT = 0`，**完全收攏**（看起來是組裝好的成品，不是散開的零件） |
| 游標移進 `#prod3d` | `0 → 1`，820ms ease-out cubic |
| 游標移開 `#prod3d` | `1 → 0`，500ms（比進去快，像彈回去） |
| 進出快速切換 | **從目前的 `t` 接著補間**（`expAnim` 記 `from`／`to`），補間長度照剩下的距離等比縮短；不從 0 或 1 重來 |
| 沒有 hover 的裝置（`(hover: none)`：手機／平板） | 不掛 hover，改成**點背景切換**展開／收攏；點零件仍然只是選零件，兩件事不打架 |
| 沒有 hover 的裝置的記憶 | 寫 `localStorage['tw.dg3d.exp']`（`'1'` ＝展開），下次進來照上一次。**hover 裝置不記**（hover 本來就是暫時的，記住只會讓下次進來看到一張已經散掉的圖） |
| 動畫：關 | 兩個狀態**都切得動**，但**不補間**，一幀到位 |
| `prefers-reduced-motion: reduce` | 同上，一幀到位 |

### 二、實作上刻意這樣做的五件事

1. **監聽掛在容器 `#prod3d` 上，不是畫布。** 標籤卡片疊在畫布上方，掛畫布的話游標一滑過卡片就收回去。
   用 `pointerenter`／`pointerleave`（子元素之間移動不會重複觸發），不用 `mouseover`。
   而且**具名**（`onEnter`／`onLeave`）並在 `dispose()` 拆掉 —— `#prod3d` 是跨 mount 重用的同一個 div，
   匿名函式掛上去就拿不下來，開關幾次 3D 之後一次 hover 會觸發好幾組。
2. **`dirty`／`lastDraw`／`markDirty` 的宣告提前到 `applyExplode` 那一段。**
   #246 的補間在「建完場景、還沒進 `tick()`」就會呼叫 `markDirty()`，留在原本 `tick()` 上面那個
   `const` 宣告會踩到 TDZ，3D 直接退回平面圖。`applyExplode()` 自己會 `markDirty()`，
   所以補間期間每一幀都真的畫得出來（不會被 #245 的「沒變就不畫」擋住）。
3. **補間的那一段搬到 `tick()` 的 `if (anim)` 外面。** 展開／收攏是使用者用游標控制的**狀態**，
   不是「動態效果」的一部分。動畫關著時 `setExplode` 根本不會建 `expAnim`，
   放外面是為了「補間跑到一半才按關」那一瞬間也收得乾淨。
4. **`setAnim(false)` 不再強制攤開。** 以前（#238）是「動畫關＝一律停在拆開的狀態」——
   那是因為當時展開是**進場動畫**、沒有「目標」可言。現在展開與否是使用者決定的，
   關動畫只該關掉**過場**，不該替他決定要不要展開。改成「跳到目前的 `expTarget`」。
5. **相機仍然取景在「拆開之後」的外接盒**（`applyExplode(1)` → `fitCamera()` 的既有順序沒動）。
   所以收攏態的模型在畫面中間、四周留白，爆開時剛好長到滿版，
   **hover 進出不會重新取景、不會跳鏡頭**。這是刻意的取捨：收攏態小一點，換到零鏡頭位移。

### 三、`replay()` 的語意改了（這是這一批唯一動到的既有斷言）

以前 `replay()` ＝「重播進場那段自動爆開的動畫」（進場本來就會自己散開）。
進場改成收攏之後，那個語意不存在了，所以 `replay()` 變成「**先收攏、再跑一次展開補間**」——
「我想再看一次它怎麼拆開」的程式入口。回傳值的意思不變：`true` ＝真的有過場、
`false` ＝不補間直接到位（動畫關掉或系統要求減少動態效果）。

`_uitest.py` 的兩條斷言只改**敘述**與「誰觸發它」，**嚴格度一條都沒放寬**：
一樣要 `0 < 中途 < 1`、一樣要 2 秒後 `== 1`、一樣要在動畫關掉時一幀到位。
「進場是不是收攏、hover 進出會不會動」不靠 `replay()` 驗 —— 由新的 `t_dg3d_hover()` 直接用
**真的滑鼠移動**去量。

### 四、驗收怎麼量（`_uitest.py` 的 `3D風格兩模式` 擴充，新增 `t_dg3d_hover`）

- **量的是 `exMove`（零件離原位最遠跑了多少，世界座標、跟相機無關），不是只看 `expT` 這個變數。**
  變數改了不等於零件動了 —— 收攏時 `exMove` 必須**正好是 0**，展開時機櫃是 11、封裝 20、MLCC 9。
- 用**真的 `pg.mouse.move`** 進／出容器，不是 `dispatchEvent` 造一個假事件。
  移出去走**垂直**方向（同一個 x、y 拉到畫面最上緣）：往左移在窄畫面會還踩在容器裡，
  `pointerleave` 根本不會來，測出來是假紅。
- ⚠ **「快速進出不跳」不可以用 rAF 取樣的 Δt 當判準。** 這個容器是 swiftshader 軟體渲染、
  只有 3～8 fps，一個**正常**的補間單幀就會前進 0.7，跟「真的跳回去」分不出來。
  改成量 `stats().expFrom`（這一段補間「從哪個 `t` 開始」）——
  它跟幀率無關，就是「接手的那一點」。斷言：展開到一半移開時 `0.03 < expFrom < 0.97`。
  rAF 的軌跡仍然留著，但只用來證明「上去 → 下來 → 再上去」真的發生過。
- 沒有 hover 的裝置用**真的 `is_mobile=True, has_touch=True` 的分頁**（`(hover: none)` 實測為真），
  不是把旗標改掉騙自己。

### 五、自己拍板的四件事（任務單沒寫）

1. **補間長度 820ms／500ms**（任務單給的是 700～900／約 500）。820 是因為機櫃的托盤位移最大（11 單位），
   700 會看起來像「彈開」而不是「被拆開」。
2. **補間長度照剩下的距離等比縮短**（`dur = 基準 × |to − t|`，最短 80ms）。
   不這樣做的話「拉開到一半就移開」要花整整 500ms 走完剩下的一半，手感會拖。
3. **`localStorage` 的 key 是 `tw.dg3d.exp`**（跟 `tw.dg3d`／`tw.dg3d.pal`／`tw.dg3d.drag` 同一族）。
4. **`canHover` 同時要 `(hover: none)` 為假**「而且」`(hover: hover)` 為真。
   只判一邊的話，`matchMedia` 不支援這兩個特徵的瀏覽器會兩邊都回 false，
   桌機就被誤判成手機（hover 永遠不生效，只能用點的）。讀不到就當成有 hover。

### 六、跟後續新增的 3D 場景（#247 半導體鏈四張、批次27 AI 伺服器鏈六張）的關係

這一批的行為寫在 `mount()` 裡，**不是逐場景寫的**，所以那十張合併進來之後
**自動吃到同一套收攏／hover 展開**，不必再改一次 ——
Andy 說的是「所有 3D 圖」，做法就要是「所有 3D 圖只有一份」。
它們的驗收（`批次24-半導體鏈3D`／`批次27-AI伺服器鏈3D`）用 `explode(0)`／`explode(1)`
程式驅動兩種狀態，跟 hover 不衝突（批次24 的註解已經先寫了「另一支 agent 正在把預設改成收攏」）。

### 七、⚠ 這一批對**整個驗收套件**的副作用，以及唯一正確的修法

`#246` 之後，游標停在 `#prod3d` 上時圖是展開的、而且補間期間每一幀都要重畫。
容器是 swiftshader 軟體渲染（3～8 fps），於是**「游標留在畫布上直接按工具列」會 6 秒點不到**，
`click(pg, "#dg3d")` 逾時 —— 而且會**連鎖**：3D 沒被關掉 → 後面驗 2D 剖析圖的段落
看到 `#prodDiagram` 被藏起來，整段變紅（實測 `批次21-CoWoS去重` 一次爆 10 條，
其中「圖上最小的字 0px」「文字 0 → 0 段」全部是這個原因，不是字真的變成 0px）。

修法不是把逾時調大，是**把測試改成真人做得到的順序**：工具列在 `#prod3d` 外面，
真人把手移過去按鈕的那一刻，圖本來就收攏了。所以新增 `_dg3d_toolbar_click()`：
先 `pg.mouse.move(4, 4)`、再 `_dg3d_settle()` 等補間停下來、才按。
套用在四個「剛用滑鼠點過 3D、游標還在畫布上」的呼叫點。
實測 `3D風格兩模式` 的第一支 `t_dg3d_style` 從 116 秒（含逾時重試）掉回 73 秒、紅燈歸零。

⚠ 另一件必須分清楚的事：**這個容器同時有好幾支 agent 在跑關卡**。
負載 4 以上時 `#dgPal`／`#dg3d` 一樣會 6 秒點不到，症狀跟上面一模一樣但原因是環境。
判法照 HANDOFF 那條：**單跑一次**。這一批每一段都單跑確認過 0 個問題。
## #247　半導體鏈四張補上 3D：推翻「不做真 3D」的是**理由的另一半**，不是那個理由（tech-illustrator 2026-09-23，分支 `claude/3d-semi`）

> Andy 2026-09-23：「確保這邊都有 3D 圖」。
> 半導體鏈的五張剖析圖裡只有「先進封裝」按得下「3D 立體」，另外四張
> （晶圓代工／矽晶圓／HBM／第三代半導體）的 `scene:` 都是 `null`。

### 一、為什麼原本寫「不做真 3D」，而現在做了

這四張的規格書 §0 各自寫過一句話當理由，**那幾句話到今天仍然成立** ——
這一批推翻的不是它們，是它們**漏掉的另一半**：

| 圖 | 原本的理由（仍然成立） | 漏掉的那一半（＝現在做 3D 的理由） |
|---|---|---|
| 晶圓代工 | 六個資訊點裡有五個是剖面／平面上的關係 | 第六個是「**閘極包得到幾個面**」—— 那本來就是三維的事，一個切面只看得到其中一面 |
| 矽晶圓 | 長晶爐是旋轉對稱體，轉一圈每一面都一樣 | 對**完整的**爐子成立，對**切開**的不成立：坩堝裡有液面、晶碇正從液面往上長，全在裡面 |
| HBM | TSV 是一根貫穿的柱子，只有剖面看得到 | 「只有剖面看得到」正是**切掉一角**的理由，不是不做 3D 的理由 |
| 第三代半導體 | 六個資訊點裡有五個是剖面上的層 | 第六個是「SiC 垂直 vs GaN 橫向」—— 兩顆並排轉一圈，電極在哪一面是一眼的事 |

所以四張走的都是**切開／並排**的立體，不是把 2D 拉厚。
⚠ 這一條要留著：下次有人想把某張圖的 `scene` 改回 `null`，判準是
**「轉一圈能不能多理解一件事」**，不是「2D 講不講得完」——
2D 永遠講得完，問題是它要花幾段文字講完。

### 二、兩種切法，各有各的場合（不准混用）

| 切法 | 給誰 | 為什麼 |
|---|---|---|
| **切角**（x > 0 且 z > 0 挖掉，跟 MLCC 完全一致，`cutSlab`／`cutFace`） | HBM | 東西是**厚**的，切開才看得到裡面那些柱子（TSV 與微凸塊上下對齊成一條導通柱） |
| **半剖**（z > 0 整個切掉，`halfSlab`／`halfFace`） | 第三代半導體 | 東西是**一疊薄層**。切角在薄片上只會變成「一疊缺了一角的板子」，那不是剖面 —— 半剖留下一個乾淨的 z = 0 剖面，內部構造（n⁺ 源極區、接觸窗）一律做到那個面上才看得到 |

第一版把兩顆功率元件也用切角畫，實際截圖出來就是一疊 L 形板子，**看不出是剖面**。
這是「規格沒錯、畫法錯了」的例子，記在這裡是為了下次不要再試一次。

### 三、量出來的數字（1440、科技模式、軟體渲染）

| 圖 | 零件 | 三角形 | draw call | 卡片上的台股晶片 |
|---|---|---|---|---|
| 晶圓代工 `foundry` | 7 | 1,948 | 45 | 17 |
| 矽晶圓 `silicon_wafer` | 9 | 3,680 | 22 | 12 |
| HBM `hbm` | 8 | 29,148 | 60 | 7 |
| 第三代半導體 `wide_bandgap` | 13 | 424 | 28 | 9 |

上限是 40,000 三角形／680 draw call，四張都在裡面。
最貴的是 HBM 的「對外凸塊」16,800 個三角形（`bump` 字彙固定 14×10 的銅柱＋錫帽，兩疊）——
留著沒減，因為整張還有 26% 餘裕，而減它會讓「凸塊成陣列」這個識別特徵掉下去。

⚠ 第三代半導體只有 424 個三角形，看起來低得誇張，但那是**對的**：
它畫的是一疊薄層的半剖，一層就是一塊板。那張圖的資訊量在「層的順序與厚薄關係」，不在多邊形數。
驗收的三角形下限因此各自訂（1,200／2,000／14,000／300），不是一個全站的數字。

### 四、台股對應：查不到就寫查不到（R5），一個都不編

- **晶圓代工**：GAA 奈米片節點只有 2330 台積電做得到，FinFET 先進節點同理 ——
  這兩顆的 `codes` 寫死成 `['2330']`，不走環節名單（走名單會把聯電與力積電一起列出來，那是錯誤宣稱）。
  基板與晶圓那兩顆列的是**矽晶圓五家**（6488／5483／6182／3532／8028），並在 `chipnote` 明寫
  「不在半導體鏈的環節名單上」。
- **矽晶圓**：半導體鏈 14 格裡**沒有一格是矽晶圓**，所以整張圖不掛真環節（`seg: 'silicon_wafer'` 是佔位）。
  長晶／切片／拋光那幾顆列 6488／3532／6182，信心：中（來源為媒體與公司網站整理）。
  爐體、加熱器、承座、坩堝**刻意留空**（顯示「台股無直接對應」）—— 那是矽晶圓廠自己的產線，不是外購零件。
- **HBM**：core die／TSV／微凸塊／對外凸塊四顆一律「台股無直接對應」（記憶體原廠自家製程）。
  base die 只列 2330（HBM4 base die 採台積電 12 奈米邏輯製程，來源：產業媒體 2026）——
  ⚠ 那是做**那顆邏輯晶粒**，不是做 HBM 顆粒。
- **第三代半導體**：同樣沒有對應環節。磊晶層列 3016 嘉晶、元件層列 3707 漢磊（兩家同屬漢民集團，信心：中）；
  **SiC 與 GaN 的基板兩顆留空** —— 2D 那張已經寫過「長晶、切片、研磨拋光三段查不到台股具名對應」，
  6488 環球晶有 SiC 基板業務但掛在「矽晶圓」族群、不在這一格，所以不把它畫進來。

### 五、驗收踩到的三個坑（都是「量錯東西」，不是功能壞掉）

1. **「不只是一顆方塊」這條不能一視同仁。** 坩堝是一圈車出來的（`LatheGeometry`）、
   TSV 是 `InstancedMesh`、電極是 merge 過的 —— 三個都只有**一顆 mesh**，而那正是效能要的樣子。
   判準改成三角形數（方塊＝12，門檻放兩倍）；三個「層」類的 kind 再另外放行，改驗「本體之外還有剖面」。
2. **「點背景＝全部歸零」在族群層級的網址上從一開始就是錯的期待。**
   `/dg/<族群>` 本來就會把那個族群的環節點亮，基準要用「點零件之前」而不是 0
   （`t_dg3d_parts` 2026-09-22 踩過同一個坑，這次照抄它的寫法）。
3. **等 300ms 讀引線端點會得到假紅。** 容器是軟體渲染、這幾張大約 3 fps，一幀就要 330ms ——
   等 300ms 有可能一幀都還沒畫，兩次取樣拿到同一批舊座標，看起來像「零件完全沒動」。
   改成 1200ms。⚠ 同一個原因也會讓「捲出畫面」變成假紅：`IntersectionObserver` 會把 rAF 停掉，
   所以量位移之前一定要先 `scroll_to('prod3d')`。

### 六、爆炸位移（`ex`）的寫法：兩種狀態都要成立

另一支 agent 正在把「進場自動爆開」改成「預設收攏、游標移過去才爆開」，所以這四張的 `ex` 是照
**「合攏時看起來是完整的成品、展開時看得出層次」**兩個條件一起訂的：

- 同一層的東西給**同一個方向**的位移，拆開之後仍看得出誰跟誰是一組；
- GAA 那顆刻意讓**閘極金屬**比奈米片多抬 17 個單位 —— 拆開才看得到金屬原本是
  「填進每一對片之間的縫」，那是 Gate-All-Around 這個名字的全部意義；
- HBM 的 TSV 抬得比 core die 還高，柱子會從整疊裡抽出來，「它真的是貫穿的」就不用靠文字講。

### 七、新增的零件字彙（23 個 kind，舊的一個都沒有動）

`fetp`／`fetf`／`nsheet`／`gaagate`（三種電晶體與 GAA 閘極）、`wafer`／`wstack`（晶圓與晶圓疊）、
`czshell`／`crucible`／`susceptor`／`melt`／`ingot`／`seedrod`／`heater`（長晶爐七件）、
`hbmcore`／`hbmbase`／`tsvcol`／`ubumprows`（HBM 四件）、
`wbglay`／`wbgbody`／`wbggate`／`wbgtop`／`wbgpgan`／`wbgelec`（功率元件六件）。
共用件：`_cutSlab`／`_cutFace`（切角）、`_halfSlab`／`_halfFace` 由 `wbg*` 自己用、
`_twoSided`（切開的殼要看得到內壁：複製一顆材質設 `DoubleSide` 再 `K.reg()` 登記進 `K.mats`，
不登記的話 highlight 的淡出與 applyPal 的換色都吃不到它）、`_hbmLayout`
（`hbmcore` 與 `ubumprows` **共用同一套層距**，各自算一套一定會錯開 ——
而「對不齊就電氣上接不起來」正是那張圖要講的事）。

## #249　不要先給我看、不要先問 —— 一次全部做完再找我（Andy 2026-09-23）

「等等不用給我看直接全部做完」「接下來 要將所有任務一次完成不要先問我 全部做完再找我」。

### 蓋掉了什麼
2026-09-21 的「功能做完就先部署讓 Andy 在線上確認」還在（那是**推**的節奏），
但「做好一個方案先截圖給他挑」這個**問的節奏**作廢。之前剖析圖四種風格、3D 的 v2／v3／v9
每一輪都停下來等他回一句 —— 他嫌那樣慢。

### 新的預設
1. **有選項就全部做**，做成畫面上可切換的，不要拿來當問他的理由。選擇權放進 UI，不是放進對話。
2. **整批做完才回報一次**；中途不截圖、不問、不確認。
3. 唯一例外：**會弄壞既有功能的高風險改動**，照舊先講一句（沿用 2026-09-21 那條）。
4. 回報時一次給：改了什麼、驗了哪幾段、網址、版號、**我自己拍板但他可能不同意的地方**（讓他一次否決）。
5. 不影響三道關卡、部署後確認、HANDOFF 記錄 —— 那些是我自己的安全網，不是拿去問他的問題。


## #250　AI 伺服器鏈六張補上 3D：同一條判準，逐張找「漏掉的另一半」（tech-illustrator 2026-09-23，分支 `claude/3d-aiserver`）

> Andy 2026-09-23：「確保這邊都有 3D 圖」。
> AI 伺服器鏈七張剖析圖裡只有「機櫃與運算托盤」按得下「3D 立體」，
> 另外六張（IC 載板／PCB 硬板／電源／液冷／氣冷／網通）的 `scene:` 都是 `null`。

### 一、為什麼六張的規格書都寫「不做真 3D」，而現在做了

判準跟 #247 完全一樣，一個字都沒改：**「轉一圈真的能多理解一件事嗎」**。
這六張規格書 §0 寫的理由**到今天仍然成立** —— 推翻的是它們**漏掉的另一半**：

| 圖 | 原本的理由（仍然成立） | 漏掉的那一半（＝現在做 3D 的理由） |
|---|---|---|
| IC 載板 | core ＋ 上下對稱增層、錐形微孔，全都只存在於**剖面** | 剖面講得出「有幾層」，講不出「**上下同時做、而且必須對稱**」。半剖立體把上下兩半同時放在一個畫面裡，對不對稱是一眼的事 —— 而不對稱就是翹曲，那正是這張圖右側在講的 |
| PCB 硬板 | 疊構與四種孔都是剖面上的關係 | 四種孔的差別是「**跨距**」。2D 並排畫四根，讀者要靠標註相信；立體上四根真的從板子裡抽出來，長度一目了然，殘端與鑽不乾淨的殘餘也才有地方放 |
| 電源 | 供電路徑是一條由左到右的單線圖 | 單線圖講得出「有四級」，講不出「**一顆 PSU 長什麼樣、四級各自是什麼形狀的零件**」。外罩掀起來才看得到 PFC 的大電容、LLC 的變壓器、同步整流那一排 —— 那是「為什麼 PSU 有貴賤」的答案 |
| 液冷 | 熱的路徑由內到外是一疊層 | 一疊層講得出順序，講不出「**冷板裡面是什麼**」。冷板剖開看得到微流道鰭片，而鰭片密度就是熱阻；板式熱交換器也只有立體上看得到「一片冷一片熱交錯」 |
| 氣冷 | 風扇剖面已經畫過由內到外五件 | 剖面只有切開的**那一刀**。沿轉軸拆開才看得到六個件是**怎麼套在同一根軸上**的 —— 馬達真的在輪轂裡、軸承真的夾在軸與輪轂之間 |
| 網通 | 板子疊構與訊號流向都是平面關係 | 前面板的**籠架陣列**與「一顆模組抽出來」本來就是三維的事；CPO 的「光引擎環繞在 ASIC 四周、共用同一片基板」更是 —— 那個「環繞」在平面上只能用箭頭講 |

⚠ 這條要留著：下次有人想把某張圖的 `scene` 改回 `null`，判準仍然是
**「轉一圈能不能多理解一件事」**，不是「2D 講不講得完」。

### 二、切法沿用 #247 的兩種，一種都沒有新增

| 切法 | 給誰 | 為什麼 |
|---|---|---|
| **半剖**（z > 0 整個切掉，`_halfSlab`／`_halfFace`） | IC 載板、PCB 硬板 | 東西是**一疊薄層**。#247 §二 已經記過：切角在薄片上只會變成「一疊缺了一角的板子」，那不是剖面 |
| **不切，靠爆炸位移拉開** | 電源、液冷、氣冷、網通 | 它們是**立體件**，本來就有厚度與方向；切開反而會把「一顆 PSU／一顆風扇」這個識別特徵切掉 |

新增的只有一個共用件：`_halfTube`。
半剖的剖面上，孔要畫成一截**內壁朝向鏡頭**的半管（`CylinderGeometry` 的 theta 0 在 +z，
`[π/2, π]` 這一段正好落在留下來的 z < 0 那一半），而且材質一定要 `_twoSided` ——
從 +z 看過去看到的是它的背面，不設雙面會被背面剔除整個吃掉。
**整根管畫進不透明的板子裡等於沒畫**，第一版就是這樣，剖面上完全看不到孔。

### 三、量出來的數字（1440、科技模式、軟體渲染）

| 圖 | 零件 | 三角形 | draw call | 主要的台股環節 |
|---|---|---|---|---|
| IC 載板 `ic_substrate` | 11 | 8,234 | 38 | `abf_pcb`（欣興／南電／景碩） |
| PCB 硬板 `pcb_rigid` | 13 | 4,896 | 20 | `hdi_pcb`、`ccl`、`ccl_material` |
| 電源 `server_psu` | 10 | 5,626 | 73 | `power`、`connector` |
| 液冷 `liquid_cooling` | 13 | 5,166 | 60 | `thermal` |
| 氣冷 `air_cooling` | 12 | 6,322 | 55 | `thermal`、`assembly` |
| 網通 `switch_wireless` | 13 | 13,532 | 102 | `optical`、`switch`、`hdi_pcb`、`thermal`、`power` |

上限是 40,000 三角形／680 draw call，六張都在裡面，而且**每一張都在餘裕的三分之一以內**。
最貴的是網通那張（籠架 24 格 ＋ ASIC 的 9×9 球柵陣列 ＋ 三顆風扇），留著沒減 ——
籠架成陣列與球柵成整片正是那兩顆零件的識別特徵。

### 四、`codes: []` ＝ 明說「台股無直接對應」（這一批新增的寫法）

以前 `three3d.js` 判斷的是 `p.codes && p.codes.length`：**空陣列會退回去列「這個環節的台股」**。
這一批四個零件（載板的晶粒剪影、電源的 GPU 核心、液冷的裸晶、網通的管理處理器）
在結構上必須掛在某個環節上（顏色、點選連動、2D 對位都靠 `seg`），
但它們本身**台股沒有人做** —— 退回去列的結果就是畫面上寫著
「散熱廠在做那顆晶片」「電源廠在做那顆 GPU」。**那比沒有圖還糟。**

改成判斷「有沒有給 `codes` 這個欄位」：給了空陣列就照它，卡片顯示「台股無直接對應」。
既有場景一個都沒受影響（它們的 `codes` 從來沒有空陣列）。
`_uitest.py` 的批次27 為這件事加了專門的斷言（`B27_NO_TW`）——
沒有斷言的話它會安靜退回去，而且沒有人會發現。

其餘查不到的地方一律靠**環節本身沒有台股**自然顯示「台股無直接對應」，一家都沒有編：
- **載板材料**（`substrate_material`）：味之素、三菱瓦斯化學，全是外商 → core 與 ABF 膜兩顆自動留白。
  這正是這張圖在產業鏈上的意義 —— **卡點在外商手上**。
- **先進封裝**（`adv_pkg`）：供應鏈資料裡這一格沒有公司（#247 §四 已經記過）→ 晶粒剪影留白。
- **交換晶片**：`switch` 環節只有智邦（做**整機**），所以那一顆的 `chipnote` 明寫
  「是做整機的，不是做這顆晶片 —— 交換晶片由美系原廠設計，台股沒有直接對應」。
- **快接頭**：2D 那張已經查證過「族群『液冷散熱』裡做快接頭的富世達 6805 不在環節名單上」，
  3D 的 QD 那一顆照抄同一句話當 `chipnote`。
- **風扇與熱管**：同理，2D 那張寫過的「尼得科超眾 6230、泰碩 3338、力致 3483、元山 6275
  不在環節名單上」也照抄到網通那張的系統風扇上。

### 五、結構上刻意寫死、違反就是畫錯的幾條（全部來自各自的規格書）

這幾條不是風格，是**判得出過／不過**的事實，所以直接寫進建造函式的註解裡：

1. **IC 載板**：core 只有一片、在正中間、明顯最厚，而且**全圖只有它看得出織紋**；
   上下增層層數與厚度相等；微孔**窄的那一端一律朝向 core**（上半朝下、下半朝上收窄）；
   core 貫孔**只穿 core**、孔內填塞、兩端蓋銅；bump pad 只在上表面、BGA 球墊只在下表面。
2. **PCB 硬板**：core 一定是「銅－介電－銅」三件一組，prepreg 一定**沒有銅**
   （兩者畫成同一種顏色是這張圖最致命的錯）；銅層總數偶數且上下對稱；
   背鑽**一定留一小段鑽不乾淨的殘餘**（工程上鑽不到零）；埋孔兩端都不准碰外層。
3. **電源**：降壓不跳級；匯流排是厚銅排不是圓線；板上 DC-DC 是**一排**等距電感（多相）；
   **BBU 在直流側、在機櫃裡**。★ UPS 與設施側**不在這條產業鏈上，3D 這張刻意不畫**
   （2D 那張有，而且 2D 有虛線框可以標「櫃內／櫃外」；3D 沒有那個框，畫進來只會讓人以為它也在櫃裡）。
4. **液冷**：每兩個固體之間都要有 TIM，而且 TIM 比上下任何金屬層都薄；冷板在蓋板之上；
   多個冷板**並聯**；板式熱交換器是**一疊交錯的板片**（兩個迴路只靠在一起、絕不相接）；
   VC 有支撐柱、熱管沒有（圓管靠管壁本身撐）。
5. **氣冷**：扇葉從輪轂長出來、馬達在輪轂裡、軸承在軸與輪轂之間；
   反轉雙轉子的第二組**攻角與轉向都相反**；滾珠軸承**看得到鋼珠**（四型不准互換）。
6. **網通**：VRM 緊鄰 ASIC；板上**沒有記憶體插槽、沒有 PCIe 插槽**
   —— 所以主板**不能沿用 `pcb` 那一支**（它畫了三條插槽，那是伺服器主機板），另寫了 `swboard`。

### 六、新增的零件字彙（41 個 kind，舊的一個都沒有動）

- 載板 7：`abfcore`／`abfbu`／`abftrace`／`abfvia`（`v: core|micro|stack`）／`abfsr`／`abfpad`／`abfbga`
- 硬板 5：`pcblay`（`lay: core|pp|foil|plane|weave`）／`pcbtrc`／`pcbmask`／`pcbenig`／`pcbvia`（`via: pth|bd|blind|buried`）
- 電源 8：`pshelf`／`pshell`／`pboard`／`pcardedge`／`pbusbar`／`pvrm`／`pbbu`／`pscap`
- 液冷 7：`timlay`／`ihslid`／`cplate`／`cpfin`／`cpport`／`lcmani`／`lcphe`
- 氣冷 8：`fframe`／`frotor`（`rev` ＝反轉那一組）／`fhub`／`fmotor`／`fbear`／`fwire`／`fwall`／`fshroud`
- 網通 6：`swboard`／`swasic`／`swcage`／`swmod`／`swgold`／`swcpo`

**用參數收斂 kind 數**（`abfvia` 一支管三種孔、`pcbvia` 一支管四種孔、`pcblay` 一支管五種層）
是刻意的：那幾種東西的差別只在**跨距與錐度**，寫成四支一定會各算各的、然後錯開，
而「孔停在哪一層銅上」對不準就是結構錯誤。厚度表也因此寫死成共用的
`ABF`／`pcbStack()`／`pcbCuY()`，跟 #247 的 `_hbmLayout` 同一個理由。

### 七、驗收（`_uitest.py` 的 `批次27-AI伺服器鏈3D`，新增 `t_b27_aiserver3d`）

寫法直接照抄 `t_b24_semi3d`，**連 #247 §五 記過的三個「量錯東西」的坑都照抄**：
三角形數當「不只是方塊」的判準（不是 mesh 數）、「點背景」的基準用**點零件之前**
（不是 0）、量位移前先 `scroll_to('prod3d')` 而且等 **1200ms**（軟體渲染約 3 fps，
300ms 會拿到同一批舊座標）。
多出來的一條是 §四 講的 `B27_NO_TW`：`codes: []` 的零件**一定要**顯示「台股無直接對應」。
★ 這一段跟批次24 一樣**一律 `--workers 1`** —— 多工時 CPU 吃滿，
工具列的「3D 立體」鈕六秒都點不到，整批會變成假紅。
