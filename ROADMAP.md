# ROADMAP.md — 中長期計畫與人力分配

> Andy 2026-09-12 下的題目：**可視化、人性化、資訊夠詳細、圖要貼近科技業事實、風格、
> 人力夠不夠、資料是不是最新、誰負責確認、指標怎麼搭配成策略、報酬率與回測怎麼記。**
> 這份是 CEO 的回答：做什麼、誰做、什麼時候做、做完誰驗。
> 每批工作結束前更新這裡與 `HANDOFF.md`。

---

## 一、現在的人力夠不夠 —— 不夠，補三個

現有六個 Agent（定義在 `.claude/agents/`）：
`finance-quant`（金融量化）、`frontend-ui`（前端）、`data-scraper`（資料源）、
`smc-technical`（技術分析）、`industry-analyst`（產業）、`reviewer`（審核）。

**缺口與補法：**

| 缺什麼 | 為什麼現在才缺 | 補誰 | 第一件事 |
|---|---|---|---|
| 沒有人負責「策略怎麼組、報酬怎麼算」 | 指標做完了，但沒人把它們串成可執行、可驗證的策略 | **`strategy-backtest`**（策略與回測） | 訂三套預設策略＋walk-forward 回測框架 |
| 沒有人負責「畫得對不對、夠不夠像真的」 | 剖析圖是工程示意圖，不是產業事實圖 | **`visual-explainer`**（圖解與教學） | 剖析圖改 3D（長寬高）＋名詞解釋＋外部知識連結 |
| 沒有人負責「資料是不是最新、壞了誰知道」 | 資料源會靜靜失敗，現在只有 log | **`data-steward`**（資料治理） | 資料新鮮度儀表＋失敗告警＋每週資料健檢 |

`reviewer` 的職責同步擴編：**每一批都要跑 `_uitest.py` 並簽名**（誰驗的、驗了什麼、哪幾項沒過）。

---

## 二、資料是不是最新的？壞了怎麼辦？

**現況（要如實講）**
- 價量／法人／融資券／集保：每個交易日盤後由 `daily.yml` 更新，append 進資料湖。
- 月營收、財報、股利：依法定公告期限入帳（`FINANCIAL_DEADLINES`），不會偷看未來。
- 新聞：鉅亨／TechNews／經濟日報，每日更新；資料湖目前只累積 2 天，每檔最多留 8 則。
- 分 K：Yahoo，每日盤後抓前 400 檔（族群成分股全給）。
- **公司重大訊息（公開資訊觀測站）目前沒有接。**

**要做的（data-steward）**
1. **資料新鮮度列**：每頁頂端顯示「各資料源最後更新到哪一天」，落後就變黃／紅。
   來源已經有了：`meta.json` 的 `table_summary` 每張表都帶 `latest`。
2. **落後自動告警**：`run_daily.py` 收尾時比對每張表的 `latest` 與交易日，落後就寫進
   `data/_state/last_run.json` 並在網站頂端掛橫幅，而不是只寫 log。
3. **每週資料健檢**：`scripts/_datacheck.py`（新）—— 缺天數、異常跳空、族群成分股健康度，
   跑不過就開 issue。
4. **接公開資訊觀測站重大訊息**：需要能實測端點與欄位；這個環境連不出去，
   要在 GitHub Actions 上先跑一支探針把回應存成 fixture，再照 fixture 寫 parser。

---

## 三、指標這麼多，怎麼搭配？怎麼變成策略？

**原則（寫進前端的「怎麼看」）：指標不是越多越好，是分層否決。**

```
M1 資金面   錢往哪裡跑    → 決定「看哪個族群」
M2 基本面   這裡貴不貴    → 決定「族群裡挑哪一檔」
M3 技術面   什麼時候進    → 決定「今天做不做」
M4 事件面   有沒有理由不做 → 一票否決
```

**三套預設策略（strategy-backtest 負責訂義、回測、寫進前端）**

| 策略 | 進場 | 出場 | 適合誰 |
|---|---|---|---|
| **A 輪動跟隨** | 族群從「改善」進「領先」＋個股站上 MA20＋法人連買 | 族群轉「轉弱」或跌破 MA20 | 波段、盯盤時間少 |
| **B 回檔承接** | 族群在「領先」＋個股回測 SMC 需求區＋KD 低檔轉折 | 跌破需求區下緣 | 想買便宜、能等 |
| **C 低估反轉** | 族群估值低於同業中位＋營收 YoY 轉正＋大戶比例回升 | 估值回到中位以上 | 中長線 |

每套都要有：**明確的進出場規則、停損、部位上限、以及回測績效**。

**回測與績效紀錄（新頁面「策略」）**
- walk-forward（不可用未來資料），逐年切；輸出年化報酬、最大回撤、勝率、賺賠比、交易次數。
- 與「買進持有 0050」對照 —— 贏不了大盤的策略要誠實標示。
- **紙上帳本**：每天把三套策略的當日訊號記進 `data/paper_trades`，累積真實的前進績效，
  和回測分開顯示（回測會過度樂觀，前進績效才是真的）。

---

## 四、工作排程（依序，每批做完就 push 讓 Andy 看得到）

### 第 1 批：眼前的破圖與死路（進行中，本批已完成）
- 熱力圖填滿卡片、放大可拖曳、還原不跳頁、剖析圖不加縮放
- 缺完整個股頁時給簡版頁（絕不出現「沒有資訊」）
- 籌碼頁資料不足時改列數字或整張不出現
- K 棒寬度可調、預設加寬

### 第 2 批：資金流動要像水（frontend-ui ＋ finance-quant）
- 資金去向改**粒子流動圖**：大盤 → 產業鏈 → 族群，線寬＝金額、粒子速度＝流速
- 河流圖改「近一個月資金趨勢」＋**所有圖都掛上期間切換**（現在只有排行與名次有）
- 驗收：切期間每張圖都要真的重畫

### 第 3 批：剖析圖 3D 化與知識化（visual-explainer ＋ industry-analyst）
- 現有等角圖改成**有長寬高的 3D 視角**（不是平面色塊），可旋轉、可點零件
- 每個零件掛：**這是什麼、為什麼重要、誰在做、延伸閱讀連結**
- 補齊所有科技族群的剖析圖；材料要標示它屬於哪個元件
- 驗收：每個零件都點得開說明，每條外部連結都能開

### 第 4 批：技術分析教學層（smc-technical ＋ visual-explainer）
- 每個指標一段白話：**是什麼、怎麼算、怎麼用、什麼時候會騙人**
- 名詞表（SMC／BOS／CHoCH／供需區／背離／黃金交叉…）
- K 線圖上直接掛「這根在講什麼」的即時解讀

### 第 5 批：策略與回測（strategy-backtest）
- 三套策略的規則、回測、績效頁、紙上帳本

### 第 6 批：資料治理（data-steward）
- 新鮮度列、告警、週健檢、重大訊息接入

---

## 五、誰做完、誰去確認（不可以自己驗自己）

| 工作 | 做的人 | 驗的人 | 驗什麼 |
|---|---|---|---|
| 前端版面與互動 | frontend-ui | reviewer | `_uitest.py` 每個新功能都要有一段真的操作它 |
| 指標與策略口徑 | finance-quant / smc-technical | reviewer ＋ strategy-backtest | 口徑與 Python／JS 兩邊一致、不偷看未來 |
| 產業圖與知識 | industry-analyst / visual-explainer | Andy | 產業事實對不對（這一項只有 Andy 能簽） |
| 資料源 | data-scraper | data-steward | 端點實測過、失敗有告警、沒有爬禁止的站 |
| 每批交付 | CEO | Andy | 「當自己是使用者，實際操作每個功能」 |

**硬性規則不變**：`pytest` → `_preview.py` → `_uitest.py` 三關全過才 push；
金鑰只在 Secrets；資料湖只 append；`www.twse.com.tw/rwd/...` 永遠不碰。

---

## 六、需要 Andy 拍板的三件事

1. **策略的風險參數**：單筆最大部位、單日最大回撤、停損用 ATR 幾倍？（沒有這個就沒辦法回測）
2. **剖析圖的 3D 程度**：純 CSS/SVG 等角圖（快、輕、可維護）vs Three.js 真 3D（重、可旋轉）。
3. **重大訊息要不要接**：需要在 Actions 上先跑探針取樣，會多一個工作流。

最後更新：2026-09-12

---

# 專家審查結果（2026-09-12，五份平行審查）

五位專家實際操作網站並讀過程式碼後的結論。完整清單見各段；**這裡只留「會讓數字錯掉」與「已修」的部分**。

## 已修（本批）

| 嚴重度 | 問題 | 根因 | 修法 |
|---|---|---|---|
| **P0 數字錯** | 1,035 檔的自算本益比系統性偏低約 20% | 證交所 `t187ap14_L` 給的是**年度累計數**（Q2＝上半年合計），FinMind 給單季，兩者混在同一張 `financial_q`；`ttm()` 直接把四列相加 | `fundamental._decumulate()`：算 TTM 前先把累計還原成單季（同 code 同年度 Qn 減 Q(n-1)）。用證交所官方 PE 對帳，比值從 0.80 回到 1.00 |
| **P0 靜默失敗** | 月線的 SMC 標記 **100% 失效**（2,022 個 BOS/CHoCH 全被丟掉），月線供需區一律從畫面最左緣開始畫 | 後端 `MS` 規則把月 K 標在**月初**，前端標**月底**，`setMarkers` 的時間集合對不上就整批過濾掉 | `mtf.resample_daily()` 月線比照週線改標「該月最後一個交易日」 |
| **P0 誤導** | 熱力圖「紅底寫 −0.7%」 | 顏色＝資金流向、文字＝漲跌，同一張圖兩種紅綠語意 | 方塊上兩個數字都寫（漲跌 ＋ 資金 ±pp） |
| **P0 視覺** | 季節性熱力圖 324 格幾乎沒有顏色 | `visualMap` 中點色 `#0f172b` **就是面板底色**，±5% 以內全部隱形 | 中點換 `#16203a`，兩端拉亮 |
| **P0 死按鈕** | 桌機「事件」與「×」按了完全沒反應，側欄關不掉 | `.layout.noside` 定義了但 `app.js` 從來沒加過這個 class | `setSide()` 同時 toggle `.open` 與 `.noside`，狀態存 localStorage；順便救回被 360px 側欄擠掉的候選表六個欄位 |
| **P0 手機破版** | 手機分頁列黏在**頂端**、底部留 120px 空白 | `.topbar` 的 `backdrop-filter` 讓 `position:fixed` 的 `.tabs` 以它為定位基準 | 手機寬關掉 `backdrop-filter` |
| 中 | 週／月線的 Order Block 用錯 lookback（21% 位置不同） | `compute_all` 沒把 `structure_lookback` 傳給 `order_blocks()` | 補上參數 |
| 低 | 連續 14 根平盤時 RSI 前後端不一致（Python nan / JS 100） | JS 少檢查 `g === 0` | 補上 |
| 低 | 候選理由寫「MA5 > MA20 > MA60」但實際判定含 MA120 | 文案漏字 | 補上 MA120 |

## 待辦（依序，已排進下面的批次）

**資料治理（data-steward）**
- `meta.json` 的 `table_summary` 與資料湖差 34 萬列 —— `backfill.yml` 回補完**沒有跑 `build_payload` 也沒有部署**，回補的十年歷史要等下一次 daily 才會上站
- 來源失敗不會變成錯誤：`http.get()` 失敗回 `None` → 空 DataFrame → `{"ok": false, "rows": 0}`，但 `errors` 是空的、Actions 全綠。**`twse.dividend`、`tdcc.shareholding`、`macro.fred` 現在就是掛的，沒有人知道**
- `macro` 表完全空的（FRED 從未成功）
- 網站只看 `generated_at` 不看 `data_date` —— 排程天天成功但證交所回舊資料時，banner 永遠不亮
- 上市價量落後 1 個交易日、集保落後 5 個交易日，首頁卻寫「T-1」

**技術分析教學（visual-explainer）**
- 30 個技術面名詞**完全沒有解釋**（MA、MACD、背離、RSI、KD、BOLL、ATR、量比、乖離、供需區、BOS、CHoCH、OB、FVG、掃蕩、風報比、技術分…）
- 個股頁一顆「怎麼看 ?」都沒有（總覽與資金流向頁都有）
- 專家已經把 30 則四段式說明（是什麼／怎麼算／怎麼用／什麼時候會騙人）寫好了，直接做成 `TERMS` 字典掛上去即可
- 站上兩個名字指同一件事：圖上「掃蕩」vs 燈號「假跌破」

**產業圖（industry-analyst ＋ visual-explainer）**
- `diagrams.js` 的兩張產業鏈圖**根本沒用到自己寫的 3D 工具箱**：`semiconductor()` 是純正面剖面、`aiServer()` 是 `skewX(-28)` 假斜視。18 張題材圖才是真等角
- 事實缺漏：CoWoS 圖沒有 **SoIC／混合鍵合**、interposer 畫成 CoWoS-S（2026 主力是 CoWoS-L）；AI 伺服器圖沒有 **NVLink 銅背板與 NVSwitch tray**（72 顆 GPU 靠的是它）；HBM 還停在 HBM3E 8 層（2026 是 HBM4 12/16 層，base die 改由邏輯代工＝台廠切入點）
- **YAML 本身有錯**（改圖沒用，要先改資料）：`osat` 的 2311 日月光已下市（應為 3711）；`panel` 的 3105 穩懋是砷化鎵代工不是面板；`defense` 的 5871 中租-KY 是租賃金融（疑似 2644 中信造船誤植）；`apple_chain` 的 2474 可成 2021 年已退出蘋果機殼鏈
- `themes3d.js` 的代號是硬寫的，與 `supply_chain.yaml` 不同步（同一個「散熱」環節兩頁列不同家數）

**視覺（dataviz）**
- 紅綠亮度不對等：綠比紅亮 **2.1 倍**，下跌日整頁在發綠光。建議 `--rise:#ff6b84` / `--fall:#19c489` 把比值壓到 1.20
- 河流圖一片淺藍色板打穿深色主題 → 改 100% 堆疊面積圖 + 單色系深淺
- `linkRow` 的彩色圓點長得像圖例但和圖上顏色無關 —— 全站最大宗的圖表垃圾
- 資金流向排行是長條圖（Andy 不要）→ 改 dumbbell：左＝20 日佔比、右＝5 日佔比，中間連線帶箭頭

**UX**
- 季節性頁「超額報酬」按鈕按了沒反應（312 格全 null，`hasExcess` 靜默改回絕對報酬）→ 至少要 disabled 並寫原因
- 「逐年明細」是 460×520 純黑空白卡；標題寫「超額報酬」但值是絕對報酬
- 總覽「今日候選 0 A / 0 B」看起來像壞了，那句寫得很好的解釋只在市場明細頁看得到
- 輪動階段「還有 10 個」是死路不能點
- 手機總覽要捲 11.3 個畫面，第一屏沒有任何結論

---

# Andy 的三個決定（2026-09-12）與執行方式

## 1. 策略風險參數 → CEO 訂（以下為提案，跑回測前可再調）

| 參數 | 值 | 理由 |
|---|---|---|
| 單筆風險上限 | **總資金 1%** | 連錯 10 次才虧 10%，帳戶還在。這是部位大小的唯一輸入 |
| 部位大小 | `張數 = 總資金 × 1% ÷ (進場價 − 停損價) ÷ 1000` | 停損決定買多少，不是先決定買多少再找停損 |
| 單一個股上限 | 總資金 **15%** | 再好的訊號都會錯 |
| 單一族群上限 | 總資金 **30%** | 輪動策略最大的風險是「整個族群一起倒」 |
| 同時持股檔數 | **5–8 檔** | 少於 5 檔等於押單一族群，多於 8 檔管不動也稀釋掉 alpha |
| 停損 | **區間下緣 − 0.5 ATR**（突破型：前高 − 0.75 ATR）；風險 < 2% 拉到 2%、> 8% 直接不做 | 沿用現行規則，不縮停損去湊風報比 |
| 移動停損 | 到 TP1 出一半並把停損移到成本；剩下用 **ATR 追蹤停損（收盤 − 2 ATR）** | 讓獲利跑，但不讓它變回虧損 |
| 最大回撤熔斷 | 帳戶從高點回落 **15%** → 部位砍半；**20%** → 全部出場、停手兩週 | 這條是保命的，回測也要照跑 |
| 大盤過濾 | 加權指數跌破 MA60 時，**只做 A（回檔承接）不做 B（突破追進）**，且單筆風險降到 0.5% | 突破在空頭裡的失敗率最高 |
| 集中度過濾 | 資金集中度（前 5 大佔比）5 日均**往上**時，停止新開 B 單 | 縮圈行情裡突破追進最容易套 |

## 2. 3D 剖析圖 → **先試 Three.js**（Andy 拍板）

執行方式（風險控管）：
1. **先做一張 POC**：AI 伺服器機櫃，Three.js + `OrbitControls`，`site/vendor/` 內建（公司網路擋 CDN，不可用 CDN）。
2. **硬性驗收**：(a) 可以繞著機櫃轉；(b) 點零件仍然亮起並帶出台股清單；(c) 文字標籤用 `CSS2DRenderer` 疊 DOM，`_preview.py` 的文字重疊檢查還能跑；(d) WebGL 不可用時自動退回現有 SVG。
3. **POC 過不了任何一條就回方案 A**（把現有等角圖做厚：單一光源、倒角亮邊、真陰影、AO 縫隙、爆炸拉桿、滑鼠視差）—— 這條路 400–600 行、18 張圖一起受惠、驗收機制不動。
4. 不論走哪條，**`supply_chain.yaml` 的環節定義與錯誤代號要先修**，否則圖畫了也對不到股票。

### POC 結果（2026-09-13）：**四條全過，Three.js 這條路留下**

| 驗收 | 結果 | 怎麼驗的 |
| --- | --- | --- |
| (a) 可以繞著機櫃轉 | ✅ | 真的用滑鼠從畫面中央拖 260px，比對 `Rack3D.current.cam()`：`[86,62,96] → [-102,110,-34]` |
| (b) 點零件亮起並帶出台股 | ✅ | 問場景「這個零件現在投影在螢幕哪裡」再真的點下去 → 該零件 `.sel`、其餘 14 個 `.dim`、說明框出現 3 個個股連結 |
| (c) 標籤是 DOM | ✅ | `CSS2DRenderer`，`.lbl3d` 是 `<div>` 且不在 `<canvas>` 裡，`_preview.py` 的文字重疊檢查照跑 |
| (d) 沒有 WebGL 退回 SVG | ✅ | 攔掉 `getContext('webgl')` → 3D 鈕隱藏、平面圖 24 個零件都在、說明寫明原因 |

POC 期間踩到、已修掉的三個坑（都寫進 `DECISIONS.md`）：
- 動態 `import()` 從傳統 `<script>` 載入，路徑一定要 `./vendor/…`，寫 `vendor/…` 會被當成 bare specifier 而解析失敗。
- `wire3D()` 放在模組層級卻用了 render 函式的區域變數（`segHi`／`syncHighlight`），每次回呼都 `ReferenceError`；改成把動作當參數傳進去。
- CSS2D 標籤蓋在畫布上，按在標籤上畫布收不到 `pointerdown`，整台機櫃就轉不動 —— 把 `pointerdown` 原樣轉給畫布，OrbitControls 才接得到。

下一步（第 3 批）：把 `semiconductor` 場景的零件補到跟 AI 伺服器一樣細，再評估 18 張題材等角圖要不要一起 3D 化
（**題材圖不急**：那 18 張本來就是真等角，優先序低於知識連結與名詞解釋）。

## 3. 重大訊息（公開資訊觀測站）→ **要接**

做法（這個環境連不到端點，所以分兩步）：
1. 先加一個 `.github/workflows/probe.yml`（手動觸發），在 Actions 上打端點、把回應存成 `docs/fixtures/mops_sample.json` 並 commit。
   **（2026-09-13 已完成）** `.github/workflows/probe.yml` ＋ `scripts/probe_sources.py` 已寫好，
   探測器本身有 7 個離線測試。候選端點包含證交所／櫃買的 OpenAPI 目錄（swagger.json，直接找帶「重大訊息」的 path）、
   `t187ap04_L` 系列的推測路徑、以及 FinMind 的 dataset 清單。
   **Andy 要做的只有一件事**：Actions → 「端點探測（寫 fixture）」→ Run workflow。
   跑完 fixture 會自己 commit 回 main，Claude 讀完才寫 parser。
2. 照 fixture 寫 `pipeline/sources/mops.py` 與 parser，註冊 `TABLES["material_news"] = ["code", "date", "seq"]`，接進個股頁的「新聞 / 券商」分頁與事件側欄。
3. 驗收：fixture 有測試、個股頁真的看得到公司自己發的重大訊息。

**不照 fixture 就硬寫 parser 是不行的** —— `config.py` 的端點都註明「已於 2026-09-10 逐一實測回應與欄位」，這條規矩不破。
